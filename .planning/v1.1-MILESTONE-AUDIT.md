---
milestone: v1.1
milestone_name: Wire What Was Built
audited: 2026-08-08T11:50:00Z
audited_at: fee26c2
status: gaps_found
scores:
  requirements: 9/10
  phases: 5/15 passed · 5 human_needed · 4 gaps_found · 1 ruled
  integration: 10 wired · 13 findings (2 blockers)
  flows: 3/5 entry points complete
gaps:
  requirements:
    - id: WIRE-02
      status: unsatisfied
      phase: Phase 22 — Reachability Guard
      claimed_by_plans: ['22-01', '22-02', '22-03', '22-04']
      completed_by_plans: ['22-01', '22-02', '22-03', '22-04']
      verification_status: gaps_found
      evidence: >-
        The guard is built, catches both named defects, and gates every commit — criteria 2 and 3
        MET. Criterion 1 asks that it "passes clean" and it does not: 67 of 217 callable barrel
        exports have no traced path from the five entry points, 47 with no production caller at
        all. Declined on the reading, not on the instrument.
  integration:
    - id: G1
      severity: blocker
      status: CLOSED
      closed_at: 8211d3c
      closed_on: 2026-08-08
      summary: >-
        A closed seed is unjoinable by the demo page, and the seed's own banner names two remedies
        that do not exist. SeedServerOptions has no `issuesCertificates` field, BootstrapInfo
        carries no provider address, and demo/main.ts's `autoStart` forwards only `relayAddrs` and
        `blockstoreName` — the `enrollment` block on `start` has no caller in the page.
      resolution: >-
        Closed by the SECOND of the two remedies, and the finding was PARTLY REFRAMED rather than
        simply fixed. Two halves of the summary above are correct decisions rather than defects,
        and were left alone: `autoStart` forwarding no `enrollment` is stated design in
        demo/main.ts — "a page that was found rather than configured must not be configurable by
        whatever found it" — and a closed seed refusing an anonymous visitor holding no
        certificate IS AUTH-02 working. What was genuinely broken is narrower: the
        `relayAdmission` docblock obliges a closed seed to "serve enrolment itself, or name a
        provider a joining peer can reach without a reservation", and no field, flag or
        BootstrapInfo member existed with which to name one. A stated requirement with no
        mechanism reads as though a mechanism exists. Added SeedServerOptions.enrollmentProvider,
        published through BootstrapInfo, `bin/seed.ts --enrollment-provider`, a banner line
        naming which of the two states the seed is in, and origin-side discovery in
        demo/main.ts. `issuesCertificates` was NOT added: serving enrolment from the seed folds
        two roles the repository separates on purpose. gated-seed.e2e.test.ts no longer injects
        providerAddr from Node — that injection is what hid the gap, because a visitor has no
        Playwright. M72/M73 ledgered, both watched red (e2e red on all three engines), both
        restored by surgical inverse with cmp exit 0.
      affects: [AUTH-01, AUTH-02, AUTH-04, DEMO-01]
    - id: G2
      severity: blocker
      summary: >-
        The AOT publish path terminates at a file on disk. tools/aot/cli.ts writes bytes and
        prints; nothing turns that into a signed NameRecord, which guardModuleProvenance requires
        before any node will run it. lift -> sign -> dispatch has no second step outside a test.
      affects: [AOT-01, AOT-02, AOT-03, AOT-04, DET-03]
  flows:
    - name: 'tools/aot/cli.ts — publish an AOT artifact'
      breaks_at: 'immediately after writeFile — see G2'
    - name: 'bin/seed.ts — closed-arm LAN demo join'
      breaks_at: 'the tab learns where to enrol — see G1'
    - name: 'demo/main.ts — tree-reduce'
      breaks_at: 'merges with answerOf (linear scan) rather than reduceJob — see G3'
tech_debt:
  - phase: ledger
    items:
      - 'WITHDRAWN 2026-08-10 (was: Six v1.1 requirements — SCHED-06, NET-08, NET-09, NET-10, DATA-10, BENCH-07 — are ticked [x] with NO row in the Traceability table, so the "[x] iff Done" rule never applies to them) — STALE SINCE 3a66549, 2026-08-08 12:30, which wrote all six rows from measurement. Re-derived 2026-08-10: 102 checkbox rows, 102 traceability rows, 102 distinct ids, BOTH orphan sets empty. This entry and item 6 of "what v1.1 still needs" both restated it as open on 2026-08-10 against a commit they descend from — the fourth instance in this file of the defect it keeps naming.'
      - 'STATE.md Session Continuity is STALE about bin/seed.ts and contradicts its own Current Position'
      - 'WITHDRAWN 2026-08-08 (was: Phase 13 verification reads 0/3 while STATE.md counts it closed) — FALSE. 13-VERIFICATION-2.md scores 3/3 on the amended criteria; the audit read one of two verification files.'
  - phase: 22
    items:
      - '47 callable barrel exports with no production caller, held at OPEN_FINDING_CEILING'
      - 'G4/G7/G8/G9/G10/G12 are the substantive members of that 47'
  - phase: 24
    items:
      - 'Relay-side refusal reasons have no wire surface — two distinct refusals reach a joiner as one PERMISSION_DENIED'
      - 'Gating the records/providers answers on the certificate is filed and unbuilt'
  - phase: 17
    items:
      - 'AUTH-02 browser-pinning leg, unresolved since 2026-08-05'
      - 'The graduated-cost reading of criterion 3 — escalated by a verifier, not settled'
  - phase: process
    items:
      - 'Ruling 2 (`**Mode:** mvp` on 25 roadmap blocks) never taken'
      - 'Six debug sessions open; two demonstrably stale (retake-measured-node-spans, relay-admission-intermittent-fail-open)'
      - 'AOT-06 closed as a NEGATIVE with a located cause: elfconv finds `main` by matching literal AArch64 encodings, so amd64 lifting aborts'
refresh:
  refreshed: 2026-08-10
  refreshed_at: b5f3623
  scope_note: >-
    Phases 25-28 are NOT members of the v1.1 milestone by the project's own count —
    STATE.md:109 reads "phases 11, 12, 13, 13.1, 14-24. **Fifteen**", and REQUIREMENTS.md
    says of Phase 27 that it is "neither v1 nor v1.1 scope". They are read in this refresh
    anyway, because out-of-scope phases produced in-scope evidence: Phase 27 falsified the
    rationale G13 was closed on, and Phases 25 and 28 moved the residue WIRE-02 is held
    open by. The 15-phase set in section 2 is NOT widened.
  measured_this_pass:
    callable_barrel_exports: 225
    unreachable: 73
    disposed: 26
    open_findings: 47
    method: >-
      barrelExports + buildCallGraph + unreachableExports called directly at b5f3623 with
      no ceiling touched and nothing planted; scratch spec removed, git status clean after.
  reopened:
    - id: G13
      status: RE_CLOSED
      reopened_on: 2026-08-10
      reclosed_on: 2026-08-11
      reclosed_by: EGR-01
      reclosed_at: the EGR-01 commit on feature/egr-01-sovereign-egress, which carries this file
      severity: major
      affects: [DATA-05, DATA-06, EGR-01]
      reason: >-
        The finding is closed; the RATIONALE is falsified. G13 closed on the claim that the
        egress panel "deliberately refuses the stronger reading". That safeguard held only
        while runJob had no caller in the page, and Phase 27 gave it one — so G4's runJob
        half closing is what broke G13's premise. Remedy is EGR-01, decider owner.
      resolution: >-
        RE-CLOSED on the terms the reopening set. EgressManifest.registeredSovereign is
        counted in submitJobWithEgress and carried through sliceManifest as a required
        argument; egressLines has three arms; the sovereign bring-your-own arm now prints
        "registered 6 sovereign shards … saw none of them leave" beside a sovereign-label
        region saying every shard was submitted owner-pinned, in one render pass. The green
        spec that required the false words is replaced by one that forbids them and was
        WATCHED RED under a planted two-arm branch, restored by surgical inverse with cmp
        exit 0. tsc 0 · node 180/2615 (1 skipped) · e2e 28/185, all exit 0. Not claimed: P7
        checks that A sentence is present, never that the right one is, and the guard's own
        whole-record tally is not job-scoped.
  closed_since_last_pass:
    - id: G12
      closed_at: 36c2800
      closed_on: 2026-08-08
      note: >-
        hasSeed DELETED, not merely uncalled. The 2026-08-08 re-audit re-stated G12 as
        "re-verified: zero non-test callers" three and a half hours AFTER the symbol was
        removed from the tree.
  disposed_since_last_pass:
    - ids: [G7, G8, G9, G10, G11]
      at: [36c2800, 97a127d]
      on: 2026-08-08
      note: >-
        All five moved from open findings into reachability-dispositions.ts under
        deferred-in-source. Disposed is NOT fixed — each carries the sentence in the module
        that declines to wire it. But none is an undiagnosed gap any more, and the re-audit
        of the same day said they "need the reachability guard's own reading, not a grep"
        when the guard had already given it.
  new_findings:
    - id: G14
      severity: minor
      status: CLOSED
      closed_at: 77b1e5e
      closed_on: 2026-08-10
      closed_note: >-
        Closed, and UNDER-COUNTED BY SIX. The finding named four pi symbols found by reading;
        the class was then derived by executing the register's own stated closing condition and
        26 symbols flip, of which 10 were undisposed. Residue 47 -> 37, not 43. See the
        2026-08-10 fourth-pass section.
      summary: >-
        Four of WIRE-02's 47 open findings (demo/buildPiInput, demo/estimatePi,
        demo/piErrorBound, demo/projectPiPartial) have real production callers in
        demo/main.ts behind the window.o2 hop that demo/answerOf is already disposed under.
        REQUIREMENTS.md's WIRE-02 row states the disposition as though it were made; the
        register has no such entry. The residue over-reports by 4.
      affects: [WIRE-02]
  still_open_unchanged: [G4-primes-half, G5, G7, WIRE-02]
nyquist:
  compliant_phases: 0
  partial_phases: 0
  missing_phases: 15
  overall: not_practised
---

# v1.1 "Wire What Was Built" — Milestone Audit

**Status: `gaps_found`.** One requirement is unsatisfied and two integration blockers stand. **Amended 2026-08-10 (third pass, `b5f3623`): `G13` is REOPENED IN PART —
it was closed on a safeguard whose precondition Phase 27 removed, and the sentence it
guaranteed now renders FALSE on sovereign runs. `G12` is closed, `G7`-`G11` are disposed,
`G14` is new, and `G4`'s primes half and `G5` are unchanged. See the refresh section.**
**Amended again 2026-08-11: `G13` is RE-CLOSED.** `EGR-01` landed the field, the third arm
and the four re-read specs; the reopening's own condition was *"closes when `EGR-01`
closes"*, and the assertion that held the false sentence in place is now one that forbids
it and was watched red. Nothing else in this file's verdict moves — one requirement is
still unsatisfied and both blockers still stand. The
milestone did most of what it set out to do — and the instrument it built in its final phase is
what makes the residue measurable rather than arguable.

## The headline, stated plainly

This milestone's premise was that v1.0 had built 36 mechanisms nothing called. **Phase 22 built the
guard that finds them, and the guard's first reading is that 67 of 217 callable barrel exports are
still unreachable — 47 with no production caller anywhere.** That is not a failure of the guard; it
is the guard working. The honest summary of v1.1 is: *the wiring that was done is real and is now
verified end to end, and a comparable amount of wiring was not done and is now counted.*

## 1. Requirements coverage — three sources cross-referenced

| REQ | Phase | `[x]` | Traceability row | Phase VERIFICATION | Final |
|---|---|:--:|---|---|---|
| WIRE-01 | 11 | ✔ | Done | passed 3/3 | **satisfied** |
| WIRE-02 | 22 | ✘ | **Partial** | **gaps_found 2/3** | **unsatisfied** |
| WIRE-03 | 19 | ✔ | Done | human_needed | satisfied |
| WIRE-04 | 20 | ✔ | Done | gaps_found 6/7 | satisfied |
| SCHED-06 | 13.1/14/16/18/23 | ✔ | **absent** | in 5 verifications | satisfied · **row missing** |
| NET-08 | 13.1/19/20 | ✔ | **absent** | in 3 verifications | satisfied · **row missing** |
| NET-09 | 13.1 | ✔ | **absent** | in 1 verification | **partial** (G9) · **row missing** |
| NET-10 | 13.1 | ✔ | **absent** | in 1 verification | satisfied · **row missing** |
| DATA-10 | 13.1/14 | ✔ | **absent** | in 2 verifications | satisfied · **row missing** |
| BENCH-07 | 23 | ✔ | **absent** | in 1 verification | satisfied · **row missing** |

**9/10 satisfied. WIRE-02 forces `gaps_found`** by this audit's own FAIL gate.

### A ledger finding that belongs to no phase

**Six of the ten v1.1 requirements have no row in the Traceability table.** Its preamble scopes
itself to *"All 72 v1 requirements … plus the 4 v1.1-only WIRE requirements"* — 76 rows, and that
is exactly what it holds. The consequence is not cosmetic: `acceptance-traceability.node.test.ts`
enforces *"`[x]` iff the traceability verdict begins Done"* **only over rows that exist**, so those
six checkboxes are unguarded. All six are genuinely satisfied — verified by grep against source and
present in phase verifications — but the ledger cannot say so, and nothing would notice if one
regressed.

## 2. Phase verdicts — 15 phases

| verdict | phases |
|---|---|
| passed | 11, 12, 13.1, 14, 18 |
| human_needed | 15, 17, 19, 23, 24 |
| gaps_found | **13 (0/3)**, 16 (3/4), 20 (6/7), 22 (2/3) |
| ruled | 21 (2/3) |

**~~Phase 13 disagrees with the project's own count.~~ WITHDRAWN 2026-08-08 — THIS FINDING (L3)
WAS FALSE, and the way it was false is worth more than the finding.** It read
`13-VERIFICATION.md`'s `0/3` against STATE.md's *closed and counted* and reported a contradiction.
**The directory was never globbed.** Phase 13 has **two** verification files, and the live one is
`13-VERIFICATION-2.md` — pass 2, same day, **`score: 3/3 amended criteria verified`**, 8
verifier-planted mutations, carrying an explicit `re_verification` block that reads
*"previous_score: 0/3 against the ORIGINAL criteria"* and *"this pass verifies the criteria as
amended in ROADMAP.md, not the wording 13-VERIFICATION.md quotes"*. Plans 13-04, 13-05 and 13-07
landed between the passes and closed the first pass's gaps.

**The cost was not zero.** The false finding was carried into a question put to the owner, the
owner ruled to reopen Phase 13 on it, the milestone count was moved 12 → 11, and all of it was
retracted the same day. **An audit that reads one file per phase produces findings shaped exactly
like this one** — plausible, specific, checkable, and wrong — and the fix is to enumerate a
phase's verifications rather than assume there is one.

## 3. Cross-phase integration

### Wired, and verified end to end

`serveAgent`'s full hook contract at both factories · `sovereignty.ownerId` → `EgressGuard` →
`withholdingFrom` consumed twice in both tiers · NET-08's ceiling on the receive path ·
`authorizeCapability` pinned to the same ownerId as placement · `enrolOverRpc` → `records` →
`relayAdmissionGate` → `verifyCertificate` with every loop exit a decision · `AbiExecutor` /
`WasiExecutor` composed under the provenance guards · `spawnAgent` → real `bin/agent.ts` handshake ·
the reachability guard armed as the seventh cheap guard · `--admit-issuer` threaded seed → server →
node · every dialable seed address printed, not the first.

### Findings

| id | Finding | Severity | Affects |
|---|---|---|---|
| **G1** ✅ | **CLOSED `8211d3c` 2026-08-08 — and partly reframed.** Two halves of the row below are correct decisions, not defects: `autoStart` forwarding no `enrollment` is stated design, and a closed seed refusing an anonymous visitor **is** AUTH-02 working. The real defect was that `relayAdmission`'s docblock obliges a closed seed to *name a provider* and no field, flag or `BootstrapInfo` member existed to name one. Fixed by `SeedServerOptions.enrollmentProvider` + `--enrollment-provider` + a banner line + origin-side discovery; `issuesCertificates` deliberately **not** added. The e2e no longer injects `providerAddr`. | — | — |
| ~~G1~~ *(original text)* | **A closed seed is unjoinable by the demo page**, and the seed's banner names two remedies that structurally do not exist: `SeedServerOptions` has no `issuesCertificates`, `BootstrapInfo` carries no provider address, and `autoStart` forwards only `relayAddrs`/`blockstoreName` — the `enrollment` block on `start` has no caller in the page. `gated-seed.e2e.test.ts` is green because the **harness** injects `providerAddr` from Node. | **blocker** | AUTH-01/02/04, DEMO-01 |
| **G2** ✅ | **CLOSED `0c43b14` 2026-08-08 — found by the re-audit, not reported by the fix.** The row below says *"nothing turns that into a signed NameRecord"*. `tools/aot/cli.ts` now imports `signName` and `encodeNameRecord` from `@o2/core` and takes `--publish-as` with `--signing-key`, refusing either alone as `half-a-publish` because *"the bare CID `guardModuleProvenance` already refuses"*. The second step exists outside a test. **The row stayed open in this table for hours after the code landed**, which is the finding recorded in the re-audit section below. | blocker | AOT-02, DET-03 |
| ~~G2~~ *(original text)* | **The AOT publish path terminates at a file on disk.** `tools/aot/cli.ts` writes bytes and prints; nothing signs a `NameRecord`, which `guardModuleProvenance` requires before any node runs it. `signName`'s only production callers are the committed demo kernel and `bin/bench.ts`'s own fixtures. | **blocker** | AOT-01…04, DET-03 |
| **G3** ✅ | **CLOSED `0d1fcb5` 2026-08-08 — by the feature the row asked for, not by a rewrite.** The row's own test was *"closing it means giving the visitor demo a verified tree-reduce"*, and that is what landed: `packages/browser/demo/main.ts:812`'s `runPi` calls `reduceJob` with `projectPiPartial`, so the requestor-side callers are now **two** — `demo/main.ts:812` and `bin/bench.ts:1979` — read off the tree rather than off a summary. The demo still merges the *colouring* job with `answerOf`, and that is correct rather than outstanding: a colouring is first-found-wins, so there is nothing to aggregate and a reduce over peers would be ceremony. What the row asked for was a demo workload the combiner exists for, and π is one. | major | MR-03…07, WIRE-02 |
| ~~G3~~ *(original text)* | `reduceJob` has one requestor-side caller (`bin/bench.ts`). The demo merges with `answerOf`, a linear scan. Confirms what MR-03…MR-07 already record. **RECLASSIFIED 2026-08-08 out of auto-fixable.** This row is accurate and was verified against the tree: `bin/bench.ts:1963` calls `reduceJob` in production and `bin/agent.ts` serves the other half of the pair, so nothing is unreachable. MR-03/04/05/06/07 each already read *"Partial — wired at `bin/agent.ts` and `bin/bench.ts` via `reduceJob`. The demo still merges with a linear scan: `answerOf`"*. It is a **ledgered Partial, not a hidden gap**, and closing it means giving the visitor demo a verified tree-reduce — which changes what the demo *computes*. A feature, not a wiring fix. | major | MR-03…07, WIRE-02 |
| G4 **split** | **AMENDED 2026-08-10 (Phase 27) — this finding has been described two different ways in two documents, and both halves are now settled separately.** The ROADMAP's Phase 27 entry names G4 as *"`runJob` has no caller in the page"*; the 2026-08-08 amendment below names the open half as the **primes entry point**. **The `runJob` half is CLOSED.** `#byo-form`'s submit handler in `packages/browser/demo/index.html` calls `window.o2.runJob` with a module CID, a complete signed `moduleRecord`, a peer set and an optional `sovereign` label — one call site, and the form refuses to enable its control until every field is present and the record's cid matches the module cid. `packages/node/src/demo-byo.e2e.test.ts` drives it on **four arms**: a record this tab pins, dispatched, six of six shards agreeing in 271 ms; a record signed by a key this tab does not pin, **refused in the fabric's own words** — *"module provenance refused on … is signed by a6d2…b869, which is not a pinned trust anchor"*, eighteen refusals rendered verbatim, in **142 ms against the acceptance's 271 ms in the same run**, so the refusal is read and not discovered as a timeout; and two sovereign arms, one unplaceable and one placed-then-refused-at-authorization. `packages/node/src/demo-liveness.e2e.test.ts`'s P5 drives the same control on every run and reports `exercised … colouring, pi, byo`. **The primes half is OPEN, and it is measured rather than asserted.** `packages/node/src/demo-primes.e2e.test.ts` re-runs the grep on every run and printed, 2026-08-10: **17 matches of the five symbols across 5 files, 11 of them on a code line in 2 files (`packages/demo/src/index.ts`, `packages/demo/src/primes.ts`), production callers 0.** Its header states that it is **expected to FAIL** the day somebody wires the workload — at which point the Primes surface stops being honest and must be replanned — and that was demonstrated: a throwaway file with a real import and a real call turned both arms red naming the file and the lines. `window.o2` has no `runPrimes`, held from three sides (this file's own inverted arm, and P4b's two arms in `packages/node/src/demo-regions.e2e.test.ts`); a stub `runPrimes` on `window.o2` turned **sixteen** assertions red. **The arithmetic of the row below is stale and its conclusion is not.** *"Two places only"* was true on 2026-08-08 and is now **five files** — Plan 27-03's Option B decision block in `packages/browser/src/demo-regions.ts` and Plan 27-06's surface header in `packages/browser/demo/surfaces/primes.ts` both name the symbols in prose while explaining why nothing calls them. *Zero production callers* holds exactly. The row below is left unedited on this file's own convention; the spec is re-measured on every run and the row is not, which is the argument for citing the spec rather than a figure. **Why it is open, and who decides.** Option B was planned and shipped (UI-SPEC section 10): twelve regions, no run control, the reason stated on screen. Option A — extend `scripts/sign-kernel.ts` to sign `primes.wasm`, regenerate `packages/demo/src/kernel-record.ts` with three records **under a new `KERNEL_TRUST_ANCHOR`**, because the script discards its private half on every run and the existing records cannot be extended, and add `TabApi.runPrimes` mirroring `runPi` — **changes what a stock `o2 agent` and a stock `o2 seed` will run**, because both default to that anchor. It touches the trust root. It is an **owner decision**, surfaced separately, and it **was not begun**: `git status --porcelain packages/demo/src/kernel-record.ts scripts/sign-kernel.ts` printed nothing before Plan 27-06's first commit and after every plant restore. | major | WIRE-02 |
| G4 **half** | **AMENDED 2026-08-08 — π is wired, primes is not, and the halves must not be reported together.** `runPi` (`demo/main.ts:765`) gives the π workload a runnable path, so that half is closed at `0d1fcb5`. **The primes half is untouched and still exactly as this row first described it**: `buildPrimesInput`, `primesKernelBytes`, `projectPrimeCount`, `readPrimeCount` and `PRIME_COUNT_KEY` appear outside `packages/demo/src/primes.ts` in **two places only — the barrel `packages/demo/src/index.ts`, and one doc comment in `pi.ts`** — which is zero production callers, measured by grep over `packages/` and `tools/` with tests excluded. So the independent published oracle **still has no runnable path**, and that was this row's whole point: π(x) is the check whose authority does not come from this repository. Closing G4 needs a primes entry point, not a second reading of the π one. | major | WIRE-02 |
| ~~G4~~ *(original text)* | The **π and primes workloads are wholly unwired** — ten symbols with zero references outside their own modules and the barrel. The independent published oracle they exist for has no runnable path to reach it. | major | WIRE-02 |
| G5 | `discoverCandidates` reaches production only behind `bin/bench.ts --discover`, off by default. | major | SCHED-01, SCHED-03 |
| **G6** ✅ | **CLOSED 2026-08-08 — NOT A DEFECT**, on three independent grounds. **(1) The requirement is mis-attributed.** NET-03 reads *"a backbone relay node auto-acquires a TLS certificate…"* — it is **AutoTLS**, unrelated to rendezvous, and `REQUIREMENTS.md` excludes it from v1.1 as *"a hosting decision, not a code one"*. **(2) The symbol is already disposed** as `global-object-hop`: it **is** reached in production through `demo/main.ts`, behind the `window.o2` assignment the tracer cannot follow — *"only `demo/main.ts` calls it"* is a statement about the tracer, not the fabric. **(3) `rendezvous.ts`'s header states the design:** a seed serves `/bootstrap.json` from the page's origin and *"on a LAN that is the better answer"*; the wire route exists because *"a static host has no origin to ask and DEMO-03 forbids adding a server-side process"*. A Node agent takes `--relay-addr` — configured, not discovered. Wiring it would add a second discovery path where a better one already runs. | — | — |
| ~~G6~~ *(original)* | **The Node tier never asks the rendezvous question it answers.** `findReservedPeers`' only production caller is `demo/main.ts`; `fabric-node.ts` only serves. | major | ~~NET-03~~ mis-attributed |
| G7 → **owner call** | `FallbackRecordIndex` / `MemoryRecordIndex` have no production caller — NET-06's fallback chain is built and never composed. **RECLASSIFIED 2026-08-08 out of auto-fixable**, on the constraint recorded when it was first triaged and never since refuted: **a real composition needs a genuine second source.** An empty `MemoryRecordIndex` in front of the RPC index would compose the types and demonstrate nothing — a fallback whose fallback is always empty is decoration, and this repository's own rule is that descoped is not satisfied. What the second source should be is a design decision. | major | NET-06 |
| G8 | `checkBlockstoreConformance` has no production caller; neither blockstore is checked at construction. | major | DATA-02 |
| G9 | `remoteDispatch` has no production caller, so NET-09's `'sender'` classification has no producer. | major | NET-09 |
| G10 | `sweepNodeCount` orphaned — two sweep implementations, the driver exercises the other. | minor | BENCH-07 |
| G11 | `EgressRefusal` is thrown and never caught by name. The refusal still reaches a requestor as a `reason` string, so NET-10 holds; the typed class has no consumer. | minor | NET-10 |
| G12 | `hasSeed` has no caller. | minor | — |
| **G13** ✅✅ *(re-closed)* | **RE-CLOSED 2026-08-11 by `EGR-01`, on the terms the reopening set — *"this row closes when `EGR-01` does, and not before"* — and the three rows below are left standing because each is true of the tree on the day it was written.** The reopening rested on four measurable facts and each is now false, measured rather than argued. **(1) The sentence.** `egressLines` (`packages/browser/demo/render.ts`) has **three** arms, split on `violations.length` and then on `EgressManifest.registeredSovereign`, a field added to `packages/net/src/egress.ts` and counted in `submitJobWithEgress` — the only code that knows a job's sovereign shard set — then carried through `sliceManifest` as a required argument, the way `totalBytes` is carried as a per-window figure. **(2) The contradiction in one render pass is gone, read off the same harness that first printed it**: the sovereign bring-your-own arm now prints `byo/sovereign-label = sovereign — every shard was submitted owner-pinned to public.` beside `byo/egress = … 0 withheld — and this run registered 6 sovereign shards, so the guard was watching for those bytes and saw none of them leave. That is a clean scan of what crossed, not a proof about what could not.` **(3) The green spec that held it is gone.** `demo-byo.e2e.test.ts`'s `expect(egress).toContain('registered no sovereign data')` is replaced by an assertion of both facts separately — the count must equal the shards the arm submitted, and the *no sovereign data* sentence is **forbidden** on a run that registered some. **Watched red, not reasoned about:** planting the two-arm branch back turned both sovereign cases red with *"the page told a visitor this run registered no sovereign data, on a dispatch that registered 6 — the two facts are conflated again"*, restored by surgical inverse with `cmp` exit 0. **(4) The stale comment** at `render.ts:245-249` saying `runJob` *"has no caller in this page"* is corrected, and `fabric.ts:58-70`'s inherited-limit paragraph now records the closure. Blast radius closes with it: the cross-cutting `fabric/egress-withheld` renders the corrected sentence **because it never held a copy** — the one-region-one-function rule doing exactly the job it was written for. Measured: `tsc --noEmit` exit 0, `--project node` 180 files / 2615 passed / 1 skipped exit 0, `--project e2e` 28 files / 185 passed exit 0. **What is NOT claimed:** P7 still cannot check *which* of the three sentences is the right one for a given run — it sees text and no manifest — and the factual half lives in `demo-byo.e2e.test.ts`; and `EgressGuard.manifest`'s own `registeredSovereign` is a whole-record tally that must never be rendered as a run's, which its docblock says at the point of definition. | major | DATA-05, DATA-06, EGR-01 |
| G13 **reopened in part** | **REOPENED 2026-08-10, and the two rows below are left standing because what they closed is still closed.** The original finding — *the demo produces an egress manifest and never shows it* — is closed and stays closed: the panel renders, from **one** template function (`egressLines`, `packages/browser/demo/render.ts:250`) across four importing surfaces (`colouring.ts`, `pi.ts`, `byo.ts`, `fabric.ts`) and five declared egress regions. **What is falsified is the closing rationale, which asserted something the finding never did**: that the panel *“deliberately refuses the stronger reading”*, and that failing to do so *“is the one way this surface could have been worse than absent”*. **That safeguard held only while `runJob` had no caller in the page — and Phase 27 gave it one.** G4's `runJob` half closing is precisely what removed G13's premise, and the two were recorded in this file as unrelated. Measured in this tree at `b5f3623`, not quoted: `packages/browser/demo/surfaces/byo.ts`'s `format()` writes `byo/sovereign-label` = *“sovereign — every shard was submitted owner-pinned to …”* (`:431-434`) and `byo/egress` = `egressLines(report.egress)` (`:379`, `:428`) **in one render pass over one report**; `egressLines` (`render.ts:257-261`) emits *“0 withheld — and this run registered no sovereign data”* on any manifest whose `violations` is empty, which is what a sovereign dispatch produces when its shards are unplaceable or refused at authorization before the canonical bytes cross. Phase 27's verifier printed both lines together off a run that submitted six owner-pinned shards (`27-VERIFICATION.md`, §*EGR-01, observed*). The sentence is true of the **guard** and false of the **run**, and the page gives a reader no way to tell those apart. **It is held in place by a green spec, not by inattention**: `packages/node/src/demo-byo.e2e.test.ts:774` *requires* those exact words in the sovereign arm whenever `violations` is empty. It reaches a second surface too — `fabric.ts:359-368`'s `fabric/egress-withheld` renders the last run's manifest through the same function. And `render.ts:245-249` still reads *“`runJob` … has no caller in this page. That is audit finding G4”*, a comment describing a tree that moved underneath it. **Remedy already specified and already owned:** `EGR-01`, `REQUIREMENTS.md:689` `[ ]`, with a five-file change and four specs written out verbatim at `:878`; decider **owner**. A visitor can reach the arm — `#byo-sovereign` and `#byo-owner-id` are real controls in `index.html`. **This row closes when `EGR-01` does, and not before.** | major | DATA-05, DATA-06, EGR-01 |
| **G13** ✅ | **CLOSED 2026-08-08 — also found by the re-audit rather than reported by the fix.** The row's own test was textual and is now false: `packages/browser/demo/index.html` contains **12** occurrences of `egress` / `withheld` / `sovereign`, and renders a panel that states frames sent and bytes total. The panel deliberately refuses the stronger reading — it prints "0 withheld" only together with the sentence saying the run registered no sovereign data, *"not a proof of sovereignty"* — which is the one way this surface could have been worse than absent. | major | DATA-05, DATA-06 |
| ~~G13~~ *(original text)* | **The demo produces an egress manifest and never shows it.** `index.html` contains no `egress`, `withheld` or `sovereign`. The sovereignty claim's operator-facing surface stops at `window.o2`. | major | DATA-05, DATA-06 |
| **G14** *(new 2026-08-10)* | **Four of WIRE-02's 47 open findings have production callers and are counted anyway — and the ledger already says they should not be.** Live census taken this pass at `b5f3623`, by calling `barrelExports` / `buildCallGraph` / `unreachableExports` directly with no ceiling touched and nothing planted: **225 callable barrel exports, 73 unreachable, 26 disposed, 47 open** (the audit's `67 / 217 / 20` triple is superseded; the 47 is unchanged and, per `reachability-dispositions.ts`, that agreement is coincidence). Among the 47: `demo/buildPiInput`, `demo/estimatePi`, `demo/piErrorBound`, `demo/projectPiPartial`. All four are called from `packages/browser/demo/main.ts` — `:778`, `:841`, `:842`, `:819` — inside `runPi`, a method of the object literal assigned to `window.o2`. That is the **`global-object-hop`** class, and `demo/answerOf` (`main.ts:946`, `runColouring`, same literal, same file) **is** disposed under it. The four π symbols are absent from `GLOBAL_OBJECT_HOP` and carry no disposition of any kind. **`REQUIREMENTS.md`'s own WIRE-02 row states the correction as though it had been made** — *“they move to a disposition under that stated mechanism rather than out of the count”* — and no such entry exists. So the milestone's headline residue is **43 symbols with no production caller plus 4 whose caller the tracer cannot follow**, reported as 47 of one kind. **The direction matters**: this over-reports the gap, which is the exact failure mode the 2026-08-08 re-audit named about itself. Two related stalenesses, recorded rather than edited (both outside this audit's file set): `packages/node/src/reachability.ts:594` reads *“`estimatePi` — which nothing anywhere calls”*, false since `0d1fcb5`; `packages/core/src/index.ts:402` quotes the pre-Phase-28 pair `75 → 87` / `49 → 61` where the re-derived figures are `73 → 85` / `47 → 59` (CRYPTO-03 already records this and left it). | minor | WIRE-02 |

**G3, G4, G7–G12 are members of Phase 22's 47** — the guard found them independently, which is a
cross-check on both instruments rather than a coincidence.

### One premise corrected — and it was mine

I briefed the integration check with STATE.md's account that `bin/seed.ts` has no `--admit-issuer`
and `seed-server.ts` hard-codes `'admits-any-peer'`. **That is stale and I verified the correction
myself**: `--admit-issuer` is at `bin/seed.ts` (repeatable, hex-validated in its own loop),
`SeedServerOptions.relayAdmission` is a **required** field, and `seed-server.ts` passes it straight
through. Landed at `afe8b0b`; criterion 8 re-scored MET at `580e461`.

**So STATE.md now contradicts itself**: Current Position says Phase 24 is closed 1/1, while Session
Continuity still says the seed cannot be told to close *and that this is why criterion 8 does not
close*. The second is false.

**What is genuinely asymmetric now is different and should not be closed casually.** `bin/bench.ts`
hard-codes `relayAdmission: 'admits-any-peer'` at three sites and has no flag mechanism at all.
`relayAdmissionGate`'s own header states the reason to leave it: 24-02's pre-gate baseline *"stays
comparable only while that remains true"*. Arming admission in the bench rig would invalidate the
comparative baseline this milestone's speedup numbers are read against — a deliberate trade with a
stated cost, not an unwired connection.

## 4. End-to-end flows — 3 of 5 at the first pass, **5 of 5 as re-measured 2026-08-08**

**Every break in the original table was a break at G1, G2, G3 or G13, and all four are now
closed.** That is not four coincidences: this table is *derived* from the gap list, so it went
stale the moment the gap list did. It is re-stated rather than edited in place, because a flow
table that silently agreed with whatever the gap list last said would never have shown the drift.

| Entry point | First pass | Re-measured 2026-08-08 |
|---|---|---|
| `bin/agent.ts` | complete as a serving entry point | **unchanged — complete.** Still cannot reach rendezvous discovery (G6, ruled not-a-defect) or `discoverCandidates` (G5, open) |
| `bin/seed.ts` | complete open / breaks closed at "the tab learns where to enrol" (G1) | **complete on both arms** — `--enrollment-provider` published through `BootstrapInfo` (G1 ✅ `8211d3c`) |
| `bin/bench.ts` | complete | **unchanged — complete** |
| `tools/aot/cli.ts` | breaks immediately after the write (G2) | **complete** — `--publish-as` + `--signing-key` sign a `NameRecord`, half a publish refused by name (G2 ✅ `0c43b14`) |
| `demo/main.ts` | complete for colouring; breaks at enrolment, tree-reduce, egress display | **complete for two workloads** — enrolment discoverable (G1), `runPi` merges through `reduceJob` (G3 ✅ `0d1fcb5`), egress panel rendered (G13 ✅). **Re-measured 2026-08-10 after Phase 27: three workloads have a control a person can press** — colouring, π (Plan 27-05 gave `runPi` a button and a spec that reads the tree off the screen), and bring-your-own (Plan 27-07 gave `runJob` a caller in `#byo-form`'s submit handler, which is G4's `runJob` half closed). Two further surfaces read rather than run: fabric state's twenty-one readings, and the cited Benchmarks transcription. **Primes is still unreachable from this page (G4's open half),** and the surface now says so in its own words with zero buttons of any class. |

**5 of 5 is a statement about reachability, not about coverage.** Every entry point can now complete
the flow it exists for. It does not say the demo shows everything the fabric runs — it does not,
which is G4's open half and Phase 27.

**Re-stated 2026-08-10, now that Phase 27 has run.** The demo shows more and still not everything,
and the gap has a name rather than a hand-wave: **the primes workload has no dispatch path from a
browser tab, and Phase 27 did not give it one.** What Phase 27 closed is the `runJob` half of G4;
what it left is the primes half, with Option A costed and unowned. See the `G4 **split**` row above
and `.planning/phases/phase-27-demo-ui-driven-by-real-fabric/27-OPEN-ITEMS.md`.

## 5. Nyquist coverage

No phase in this milestone carries a `VALIDATION.md` — 0 of 15. The practice is not in use here;
this is discovery, not a demand. `/gsd-validate-phase <N>` is available per phase if it is wanted.

## 6. What could not be checked, and why

From the integration pass, recorded rather than glossed: no flow was executed at runtime (the pass
was read-only and ran no vitest); `relayAdmissionGate`'s retry budget was read but not measured
against libp2p's 5 s reservation ceiling in the field; the 16 `global-object-hop` dispositions were
confirmed as a class but not individually; and whether `discoverCandidates`' absence from
`bin/agent.ts` is intended is a design question the code shape cannot answer.

## Re-audit, 2026-08-08 second pass — what changed, and the one thing that did not

Run against `0d1fcb5` plus this branch's two corrections. Every figure below was measured in this
pass; none is carried over.

### The headline: this document under-reported its own closures

The first pass's risk was over-claiming. **The actual failure was the opposite, and it is worse
than it sounds**: four gaps — G1, G2, G3, G13 — were closed in the tree while their rows here still
read open. Two of them (G2, G13) were closed hours before this pass and were found by *re-reading
the tree*, not by any record. The flow table in §4 then inherited the staleness wholesale, because
it is derived from the gap list: every one of its breaks cited a gap that no longer existed.

An audit that lags in the closing direction is not harmless. It makes the milestone look further
from done than it is, and it invites work that has already been done. This is the same defect class
STATE.md names about itself — *"three counts in one file, none of them right"* — and it has now
appeared in three documents in one day.

### Measured this pass

| Reading | Value | Method |
|---|---|---|
| Phase verifications present | **15 of 15** | every phase dir carries a `*-VERIFICATION*.md` |
| Phases at full criteria | **10 of 15** | 11, 12, 13(amended), 13.1, 14, 15, 17, 18, 23, 24 |
| Phases with a standing partial | **5** | 16 (3/4), 19 (4/5), 20 (6/7), 21 (2/3), 22 (2/3) |
| Requirement rows | **82** | 53 Done · 27 Partial · 1 Not started (VER-02) · 1 Built-not-wired (MR-02) |
| Of those, out of v1.1 scope | **4** | NET-03, BENCH-06, AOT-03, AOT-05 — excluded by prior decision |
| Nyquist `VALIDATION.md` | **0 of 15** | practice not in use; discovery only |
| `SECURITY.md` | **0 of 15** | `/gsd-secure-phase` never run for this milestone |

**The requirement parse is stated because it is easy to get wrong, and this pass got it wrong
first.** Bucketing on any occurrence of a status phrase reports 7 rows as *Built, not wired* — but
`CHURN-01` reads **Done** and merely *quotes* its own history (*"This row read Built, not wired"*).
Classifying on the leading marker only gives the table above, with zero unparsed rows. Every row in
this ledger narrates its own past, so any match past the first marker reports the past as present.

### The suite was red at the tip, and no document said so

**The first pass could not check this — it read documents. This one ran the suite**, which is
the difference that produced the only findings here that were not bookkeeping:

`npx vitest run --project node` at `0d1fcb5` → **12 failed, 2392 passed, exit 1.** Three were
real and all three trace to one commit; nine were environmental.

| Failure | Cause | Disposition |
|---|---|---|
| `serve-agent-hooks` — `demo/main.ts: both peer dispatches are public work` | `runPi` is a third `RemoteExecutor` site; the guard pins the count at 2 | **fixed** — pin moved to 3, with the reason each site is public work |
| `checkpoint-optout-scope` — `pins how many times each of them may say it` | `runPi` is a third `'checkpoints-nothing'` site; pinned at 2 | **fixed** — pin moved to 3, with why a π run has no resumable partial |
| `consent.test.ts` — `constructs without touching a browser global` | Node 25 binds a `localStorage` global with no methods; `backing()` trusted the binding | **fixed** — duck-typed on the three methods actually called |
| `elf-fixtures` × 9 | `tools/aot/fixtures/elf/` is gitignored build output, absent from a fresh worktree | **environmental, by design** — `.gitignore` says this spec *"fails, with those commands in the message, on any host"* lacking them; `tools/aot/build-elf-fixtures.mjs` rebuilds them in seconds |

**After the three fixes: 11 failed / 2393 passed — 9 environmental and 2 that are neither.**
`reachability.node.test.ts` and `late-combine.node.test.ts` failed in the post-fix run and had
**not** failed in the pre-fix one. Neither is a regression, and that is measured rather than
assumed — this repository's rule is that *"passes in isolation" is a claim to verify*:

| Spec | In the suite | Alone | `real` / `user` / `sys` | Reading |
|---|---|---|---|---|
| `reachability.node.test.ts` | 105.5 s, 1 failed | **9.4 s, 37 passed** | 10.36 / 13.82 / 3.21 | 11.2× slower under load; CPU ratio 1.64, so it got the CPU when run alone |
| `late-combine.node.test.ts` | 22.3 s, 1 failed | **8.0 s, 2 passed** | 9.11 / 9.88 / 1.78 | 2.8× slower under load; CPU ratio 1.28 |

The host carried a **1-minute load average of 354** across 26 users during the suite run. `0d1fcb5`
had already raised the graph timeouts *"FILE-WIDE to both reachability specs … because a contended
host starves every graph-building case equally"* — this is that same starvation, past the raised
bound. `late-combine` drives SIGSTOP/SIGCONT across real processes against `rpcTimeoutMs`, which is
the same sensitivity in a different mechanism.

**So the honest suite reading is: green on a quiet host with the fixtures built.**

**Confirmed 2026-08-09, and it is now a measurement rather than the prediction the paragraph
above was.** Run in the primary checkout — real `node_modules`, all six ELF fixtures present,
1-minute load average **5.0** rather than 354:

```
Test Files  172 passed (172)
Tests       2437 passed | 1 skipped (2438)
real 394.54   user 380.04   sys 54.50        exit 0
```

`(user+sys)/real` = **1.10**, so the run had the CPU rather than waiting for it — which is what
makes this comparable to the loaded runs rather than merely luckier than them.

Every earlier failure is now accounted for with nothing left over: **3 real** (fixed on this
branch), **9 environmental** (the gitignored fixtures, present here), **2 contention**
(`reachability` and `late-combine`, both green in this run). Neither of the last two is
dismissed — they are recorded as contention-sensitive, which is a property worth knowing before
either is used as evidence for anything.

**Two guards worked exactly as designed and nothing read them.** Both count pins exist precisely
to catch a newly-added call site, both went red the moment one arrived, and the commit landed
anyway. That is not a guard problem — it is the same gap this re-audit keeps finding in a
different medium: the instrument fired and the record did not move.

**The `consent.ts` failure is the one worth keeping.** It is not bookkeeping and not a stale
count: the browser tier's consent gate threw from the one function contracted never to throw,
on a host where `localStorage` exists and is hollow. The project targets Node 24 and this host
is Node 25, so it was invisible until something ran the suite on a newer runtime.

### Still open, and unchanged by this pass

`G5` (`discoverCandidates` off by default) · `G7` (fallback chain never composed — owner call) ·
`G8`, `G9`, `G10`, `G11` · `G12` (`hasSeed`, **re-verified: zero non-test callers**) · `G4`'s primes
half · `WIRE-02` (the guard does not pass clean; 47 unwired capabilities held at the ceiling).

**G5 and G8–G11 were deliberately not re-adjudicated.** A caller count that includes barrel
re-exports and doc comments is not evidence of a production caller, and re-closing a row on one
would be the over-claim this pass exists to catch. They need the reachability guard's own reading,
not a grep.

### Verdict of this pass

**`gaps_found` stands, and the reasons have changed.** Both original blockers (G1, G2) are closed
and every entry point completes its flow. What keeps the milestone open is narrower and more
honest: `WIRE-02` unsatisfied with 47 unwired capabilities, `G12` confirmed dead, `G4` half open, and
`G5`/`G7`–`G11` unmeasured since the first pass. **Corrected 2026-08-10: *"Phase 27 addresses G4's
remaining half"* was the sentence this row's whole defect consists of — G4 has two halves and
naming neither is how one gets reported as both.** Phase 27 addressed and **closed the `runJob`
half**, by giving `runJob` a caller in the page (Plan 27-07, `packages/node/src/demo-byo.e2e.test.ts`).
It did **not** address the **primes half**, which stays open with Option B shipped as the reason and
Option A costed and unowned (Plan 27-06, `packages/node/src/demo-primes.e2e.test.ts`). See the
`G4 **split**` row in section 3.


---

## Refresh, 2026-08-10 third pass — phases 23-28 reconciled against the tree at `b5f3623`

Run read-only against the working tree, one file-scoped vitest run and one direct call of the
reachability instrument. Nothing planted; the scratch spec was deleted and `git status
--porcelain` was clean before the commit. Every figure below was measured in this pass.

### The scope line this pass does not blur

**Phases 25, 26, 27 and 28 are not members of the v1.1 milestone.** `STATE.md:109` counts
*"phases 11, 12, 13, 13.1, 14-24 — **Fifteen**"*, and `REQUIREMENTS.md` says of Phase 27's
own requirement that it is *"neither v1 nor v1.1 scope … filed by owner instruction
2026-08-08, exactly as the Phase 25 and Phase 26 entries above were"*. Section 2's fifteen-phase
table is therefore **not widened**, and no phase verdict below is added to it.

**They are read anyway, because out-of-scope phases produced in-scope evidence.** That is the
whole finding of this pass and it is the thing a per-phase audit structurally cannot see:

- Phase 27 (out of scope) **falsified the rationale G13 (in scope) was closed on** — and did it
  by *succeeding*, not by failing.
- Phases 25 and 28 (out of scope) moved the residue **WIRE-02 (in scope)** is held open by, in
  both directions: `+2` at Plan 25-02, `-2` at Plan 28-01.

For the record, their own verdicts: 25 `passed` 7/7 · 26 `human_needed` 13/13 with a blocking
owner decision on spend · 27 **`gaps_found` 10/12** · 28 `human_needed` 5/6 + 1 partial.

### The headline: a gap closed in one phase was undermined by a defect found three phases later

**G13 is REOPENED IN PART.** The full row is in section 3; the ruling and its evidence:

| | |
|---|---|
| **What stays closed** | The finding as written — *"the demo produces an egress manifest and never shows it"*. The panel exists and renders through one template function across four surfaces. Nothing here re-opens that. |
| **What is falsified** | The **closing rationale**, which asserted more than the finding did: that the panel *"deliberately refuses the stronger reading"*, printing 0 withheld only beside the sentence saying the run registered no sovereign data — *"which is the one way this surface could have been worse than absent"*. |
| **Why it was true on 2026-08-08** | `render.ts:245-249`, in its own words: *"`runJob` — the path that registers owner rows and can produce a non-empty `violations` — **has no caller in this page**. That is audit finding G4."* With no sovereign dispatch reachable, the sentence could not be false. |
| **What made it false** | **Phase 27 gave `runJob` a caller** — `#byo-form`'s submit handler — which this file already records as G4's `runJob` half **closing**. The two events are the same event. |
| **Measured, this pass, in this tree** | `surfaces/byo.ts` `format()` writes `byo/sovereign-label` = *"sovereign — every shard was submitted owner-pinned to …"* (`:431-434`) and `byo/egress` = `egressLines(report.egress)` (`:379`, `:428`) **in one pass over one report**. `egressLines` (`render.ts:257-261`) emits the *"registered no sovereign data"* branch on any manifest with `violations.length === 0`. |
| **Observed live** | `27-VERIFICATION.md` §*"`EGR-01`, observed rather than cited"* prints both regions from one arm: `byo/sovereign-label = sovereign — every shard was submitted owner-pinned to public.` beside `byo/egress = … 0 withheld — and this run registered no sovereign data …`, off a run that submitted **six owner-pinned shards**. |
| **The aggravating fact** | It is **asserted green**. `packages/node/src/demo-byo.e2e.test.ts:774` *requires* those exact words in the sovereign arm whenever `violations` is empty. A passing spec holds the defect in place. |
| **Blast radius** | Two surfaces, not one: `fabric.ts:359-368`'s `fabric/egress-withheld` renders the **last run's** manifest through the same function, so a sovereign bring-your-own run puts the false sentence on the cross-cutting fabric card too. |
| **Reachable by a visitor** | Yes — `#byo-sovereign` and `#byo-owner-id` are real controls in `index.html`, defaulted off but one click away. |
| **Remedy** | `EGR-01`, `REQUIREMENTS.md:689` `[ ]`, five files named at `:878` and four specs to re-read. Decider **owner**. |
| **Ruling** | **Reopened in part, at `major`, scoped to the sovereign arm.** Closes when `EGR-01` closes. |
| **Outcome, 2026-08-11** | **`EGR-01` closed, so this row is RE-CLOSED** — see the `G13 ✅✅` row in section 3 for what was measured. Every line above is preserved as written: it was true of the tree between 2026-08-08 and 2026-08-11, and the *aggravating fact* row in particular is why the replacement assertion at `demo-byo.e2e.test.ts` is a `not.toContain` watched red rather than a `toContain` watched green. |

**Why this is the right disposition rather than "leave closed, note the caveat".** The rationale
did not merely over-explain; it named the *specific* way this surface could be worse than absent,
and the tree now does that thing. A panel that shows nothing is a missing feature. A panel that
tells a reader their sovereign run registered no sovereign data is the sovereignty claim's own
surface contradicting itself in one render pass. Leaving G13 closed would mean the milestone's
only egress finding reads `✅` while its own stated failure mode is live and green.

**The tree is honest about this in two of three places and stale in the third.** `byo.ts:12-20`
and `fabric.ts:58-70` both state the limit in full and say why they do not fork the copy.
`render.ts:245-249` still says `runJob` has no caller in this page. Recorded, not edited — this
audit does not write source.

> **2026-08-11, EGR-01.** All three now read the tree they are in: `render.ts`'s comment says
> Phase 27 gave `runJob` a caller and that the third arm is the direct consequence, and
> `fabric.ts`'s paragraph records the closure while keeping the argument for why that file did
> not fork the copy — which is the argument that made the fix cheap when it came.

### Every open row, re-derived rather than trusted

The instrument, run this pass at `b5f3623`: **225 callable barrel exports · 73 unreachable ·
26 disposed · 47 open.** The audit's `217 / 67 / 20 / 47` triple is superseded in three of four
figures — and `reachability-dispositions.ts` says of the fourth that its constancy is
**coincidence**, since Plan 25-02 raised the residue to 49 and Plan 28-01 measured it back to 47.

| Row | 2026-08-08 status | Status at `b5f3623` | What changed, and where it was read |
|---|---|---|---|
| **G4** primes half | open | **OPEN, unchanged, and re-derived** | `demo/buildPrimesInput`, `demo/projectPrimeCount`, `demo/readPrimeCount` are in the live open list. `#s-primes` carries **0 buttons** (counted in `index.html`); `window.o2` has no `runPrimes` — every `primes/*` region cites `TabApi.runPrimes()`, *"a method that does not exist"*, in the page's own words. Option A costed, unowned, touches the trust anchor. |
| **G5** `discoverCandidates` | open | **OPEN, unchanged — and structurally invisible to the guard** | `bin/bench.ts:162` still reads `const DISCOVER = process.argv.includes('--discover')`, and the sole production call is inside `if (DISCOVER)` at `:1346`. **The guard scores it REACHED**, because its own header says *"a capability called from a reachable but never-executed branch reads as reachable"*. So G5 is not among the 47 and never will be. **No later phase wired it.** This is the one open row the milestone's own instrument cannot measure, and it should not be closed on the guard's silence. |
| **G7** `FallbackRecordIndex` / `MemoryRecordIndex` | owner call | **OPEN — disposed `deferred-in-source` at `97a127d`** | Both are in the live disposed set. The register carries the argument verbatim: *"a first link that is always empty always falls through … composed on those terms the finding would go green and NET-06 would be no truer"*. Still an owner call; still not a fix. |
| **G8** `checkBlockstoreConformance` | open, "no production caller" | **DISPOSED at `36c2800` — and reclassified on a substantive reading** | The register's reason is new information and is not bookkeeping: *"it fails any store whose `size !== 0`, so it **cannot** run at construction on a node that already holds blocks … wiring it into a factory would refuse every restart."* That is a **not-a-defect** in the shape G6 already has. The audit's row asked for something the code cannot do. |
| **G9** `remoteDispatch` | open | **DISPOSED at `36c2800`** | Carries NET-09's `'sender'` classification — the same deferral as G11 seen from the other end. |
| **G10** `sweepNodeCount` | open, "two sweep implementations" | **DISPOSED at `36c2800`** | `packages/bench/src/index.ts` says of it in its own words that the question *"is not settled here"*; `bin/bench.ts` writes its own ladder because it varies redundancy per rung, **which this signature cannot express**. The audit's exemplar had drifted — WIRE-02's row was corrected for exactly this on 2026-08-10. |
| **G11** `EgressRefusal` | open | **DISPOSED at `36c2800`** | `packages/net/src/churn.ts` states the `'node'` classification deliberately and says catching it by name is the change it declines. |
| **G12** `hasSeed` | open, "re-verified: zero non-test callers" | **CLOSED at `36c2800` — the symbol was DELETED** | Absent from the corpus entirely. `reachability.node.test.ts:95` records the deletion and the floor move `16 → 15`. |
| **G13** | closed | **REOPENED IN PART on 2026-08-10 · RE-CLOSED 2026-08-11** | See above. The reopening stood for one day and was closed by `EGR-01` on the condition it named for itself; the row's whole history — original, closed, reopened, re-closed — is kept stacked in section 3 rather than overwritten. |
| **G14** | — | **NEW** | 4 of the 47 are `global-object-hop` artifacts with real callers; see section 3. |
| **WIRE-02** | unsatisfied | **UNSATISFIED, unchanged in verdict, corrected in every figure** | 73/225 unreachable, 26 disposed, 47 open. Corrected by Plan 28-04 against the tree. Of the 47, **43** genuinely have no production caller once G14's four are set aside. |

### The finding underneath the table: the instrument fired and the record did not move, again

`36c2800` — *"resolve G12, and dispose G8/G10/G11/G9 as the documented deferrals they are"* —
landed **2026-08-08 12:27**. `97a127d`, which added G7's disposition, landed **14:52**. The
re-audit in the section above was run against `0d1fcb5`, **15:50**, and is a descendant of both.
It wrote:

> `G8`, `G9`, `G10`, `G11` · `G12` (`hasSeed`, **re-verified: zero non-test callers**) …
> **G5 and G8–G11 were deliberately not re-adjudicated.** … They need the reachability guard's
> own reading, not a grep.

The guard's own reading had already been taken, three hours earlier, in the same branch, and it
had disposed four of the five and deleted the sixth. **This is the third instance in this file of
the defect it keeps naming about other documents** — G1/G2/G3/G13 closed while their rows read
open, the flow table inheriting it wholesale, and now the re-audit's own "still open" list stale
against a commit it descends from. It is recorded here rather than quietly corrected, because a
list of open findings that lags in the *closing* direction invites work already done, and this
one did so for two days across two documents.

### Requirement coverage — 64 of 102, and the three kinds of open are not the same kind

Counted this pass off `REQUIREMENTS.md`: **64 `[x]`, 38 `[ ]`.** The thin families, with the
distinction the audit is asked not to blur. Two of the figures in the brief were re-measured and
differ: **X509 is 1/7 and MR is 1/7**, not 1/6.

| Family | Met | Why the rest are open | Kind |
|---|---|---|---|
| **X509** | ~~1/7~~ **7/7 since 2026-08-11 — see the note below this table; the row is left standing rather than back-edited** | All six open rows read **Built, not wired**, each with two-sided bounds proved in `x509.test.ts`. Every one closes with the same sentence: *"`decodeX509Certificate` has no production caller — this phase does not wire the decoder into enrollment, issuance, or the demo (out of this phase's named scope)."* Phase 25 verified **`passed` 7/7** on its own criteria. Nothing is unbuilt and nothing is unmeasured. What is missing is a decision that the fabric's admission path adopts X.509 — which is a change to a trust path. | **Blocked on an owner ruling.** |
| **MR** | 1/7 | `MR-03…07` read **Partial** — wired at `bin/agent.ts` and `bin/bench.ts` through `reduceJob`, with the demo clause amended 2026-08-10 by Phase 27 (π now merges through `reduceJob`; the colouring merge stays a linear scan, correctly, because a colouring is first-found-wins). `MR-02` is **Built, not wired** *and* explicitly unmeasured: Phase 16 ran the aggregation over public shards only. | **Split, and it must be reported split.** `MR-03…07` are *partial and honestly ledgered* — real mechanism, incomplete surface. `MR-02` is *genuinely unmeasured*: the sovereign-input arm of the aggregation has never run. |
| **AOT** | 1/5 | `AOT-02` was **Partial** and both halves of its old text became false on 2026-08-08 (that is G2 closing). `AOT-04` **Partial**, both node factories construct a real port. `AOT-05` **Partial** — the demo loads its kernel from bundled base64, so the loader is not on the page's own path; a measured negative, reported unmet. `AOT-03` cross-machine is **descoped 2026-07-28 and unmeasured — not met**, with `CROSS_MACHINE_BLIND_SPOT` stamped on every artifact. | **Mostly measured negatives carried honestly** — and **one descope**, `AOT-03`. This repository's own rule is that *descoped is not satisfied; unmeasured is not met*, so `AOT-03` should never be read as a near-miss. |
| **EGR** | 0/1 | One row, `[ ]` **Not started**, minted by Phase 27 after being measured three times and deferred three times. The fix is specified to five files and four specs; the decider is **owner**, because it adds a field to a type in `@o2/net` and rewrites the one piece of fixed copy that carries the sovereignty claim. **The defect it names is live on screen.** | **Blocked on an owner ruling** — and the only one of the five families where the openness is currently *shipping a false sentence to a visitor*. |
| **VER** | 4/9 | `VER-02` is **Not started** and is the cleanest example in the ledger of a measured negative: the ceremony that shipped under the id *"was minted and compared by the requestor itself, so its check was unconditionally true and both failure branches unreachable; measured 2026-07-30 by making the mismatch branch throw and running the whole node project, 1171 tests, no reach"* — and it was **removed rather than relabelled**. `VER-03/04/09/10` are **Partial**, markers corrected 2026-08-03 by 19-12, reachable only through `submitJob`. | **Two kinds, and `VER-02` is the honest one.** `VER-02` = *measured negative carried honestly*, and it is genuinely unbuilt besides — the two-round ceremony needs a wire and a cross-node barrier and belongs to a phase that ships them. `VER-03/04/09/10` = *partial and honestly ledgered*. |

> **`X509` closed 2026-08-11. The ruling arrived, and it was the only thing missing.**
> The owner ruled **fail-closed**, on three stated grounds: this project's posture everywhere
> else is refusal-by-default; a fail-open X.509 path would be the only place a *malformed*
> certificate is treated more leniently than an *absent* one; and the decoder is bounded and
> hand-written precisely so a refusal is cheap and total. `verifyCertificate`
> (`packages/core/src/enrollment.ts`) is now the production caller this table said the family
> lacked, and all seven rows are `[x]` with **Done** traceability verdicts.
>
> **This paragraph's own classification held up and is worth keeping for that reason.** The
> audit said the family was blocked on a decision rather than on work, that nothing was
> unbuilt and nothing unmeasured. The wiring took one new module (`x509-encode.ts`, the
> issuing half), one gate, one optional field, and no change whatever to the six refusals
> themselves — which is what "blocked on a ruling" predicted and is the reason the rows above
> are left standing rather than re-written. What the audit did **not** anticipate is that the
> decision needed an *encoder* to be answerable at all: a gate wired fail-closed with nothing
> able to mint a form it accepts is a gate that refuses everything, which is not a profile.
> That is recorded here because "the decision is the only thing missing" was very nearly true
> and not quite.

**None of the five families is open because somebody forgot.** Three (`X509`, `EGR`, and the
X.509-adjacent half of the trust path) wait on an owner ruling. One (`AOT`) is a set of measured
negatives plus a descope. `MR` and `VER` are mixed and are reported mixed. The only genuinely
*unbuilt* work in the thin families is `VER-02`'s two-round ceremony and `MR-02`'s sovereign
aggregation arm — everything else exists, is tested, and is either unwired by decision or
unwired pending one.

### What v1.1 still needs before it could be declared complete

Stated as a list somebody could work, with who has to move first:

1. **`EGR-01` — owner ruling, then a five-file change.** This is the only item on the list where
   the current state is *actively misleading a visitor* rather than merely incomplete, and it is
   what keeps `G13` reopened. Nothing else on this list has that property.
2. ~~**`WIRE-02`'s residue — 43 real, not 47.** Add the four π symbols to `GLOBAL_OBJECT_HOP`
   (G14) so the number means one thing~~ — **DONE 2026-08-10 at `77b1e5e`, and the figure in
   this item was wrong: the residue is 37, not 43.** The class was *derived* rather than read
   and holds 26 symbols, ten of which were undisposed. Then decide whether to work the 37 down
   or hold them. The owner ruled 2026-08-08 to *hold*; that ruling governs the backlog, does not
   close the row, and is untouched by a reclassification.
3. **`G4`'s primes half — an owner decision that touches the trust anchor.** Option B shipped
   with the reason on screen. Option A regenerates `kernel-record.ts` under a new
   `KERNEL_TRUST_ANCHOR`, which changes what a stock `o2 agent` and a stock `o2 seed` will run.
   Not begun, and correctly not begun by a plan.
4. **`G5` — a design question, not a wiring one.** ~~Whether `discoverCandidates` should run by
   default at `bin/agent.ts`~~ cannot be settled by the guard, which scores it reached. It needs
   the same kind of decision `G7` needs.

   **The owner ruled on the criterion 2026-08-11: a capability that executes only behind an
   off-by-default flag is NOT wired.** Acting on that ruling produced a measured negative that is
   larger than the row, and it is recorded here because it is the reason the row cannot be closed
   by wiring:

   **There is no unflagged path in this repository on which `discoverCandidates` can qualify a
   single candidate.** Not one that is unwired — one that cannot exist yet. Three homes were
   checked against the code:

   | Candidate home | Why discovery cannot qualify anybody there |
   |---|---|
   | `bin/agent.ts` | **Serving-only.** Never calls `submitJob`; `REQUIREMENTS.md:267` says so in the ledger's own words. No job, no input CID, and zero peers by default. A call there reports `0 of 0` and **could not fail** — which this repo's own convention rules out: *a proof that cannot fail is not a proof.* **This is the framing the audit itself proposed, and it is not implementable.** |
   | the demo page | Has a real requestor and real certificate handling (`attestedNodes`, `main.ts:369`) — but **no visitor path enrols.** `main.ts:556-563`: *"Nothing on a visitor's path supplies this. `autoStart` passes no `enrollment` and grows no parameter for one"*, on the stated ground that *"a seed may publish where a joiner should knock; it may not publish who the joiner is."* No enrolment → `n.certificate === null` → empty `trustedIssuers` → `discoverExecutors` excludes every provider `invalid-certificate`. |
   | `bin/bench.ts --discover` | The **only** place every precondition is built: a provider node issuing certificates, N enrolled workers, pinned `trustedIssuers`, the requestor dialling each worker, the module block seeded. That is one extra process and N round trips — which is exactly why `bench.ts:128` keeps it off, citing 15-CONTEXT decision 2. |

   **So the work is not wiring, it is a role.** Closing this needs an enrolled requestor on a
   default path, and the two candidate homes each refuse for a documented reason rather than an
   accidental one. `attestedNodes` also only **decorates** a caller-supplied executor list with
   certificates; it never **selects**, which is precisely the leg NET-06 names.

   **Correction to this audit's own stale premise, found while checking the above.** The claim
   that *"every production submitter but `bin/bench.ts --discover` builds its descriptors through
   `publicNodes`, which states it carries none"* stopped being true around 2026-08-08:
   `attestedNodes` replaced `publicNodes` at all three demo submit sites (`main.ts:799`, `:918`,
   `:1206`) and verifies each peer's certificate against the issuer of the tab's own — on no flag
   at all. The blocker is *"no default path enrols"*, not *"no default path builds certificated
   descriptors"*.

   **Consequently `SCHED-01` is now at risk rather than safe.** It is marked Done, and its
   entry-point half is `bench.ts --discover` — the exact thing the ruling invalidates. No work
   here defends it; it needs the role above.
5. **`G7` — name the second source, or state that NET-06's chain stays composed of one.**
6. ~~**The six unguarded checkboxes.** `SCHED-06`, `NET-08`, `NET-09`, `NET-10`, `DATA-10`,
   `BENCH-07` still have no Traceability row, so `acceptance-traceability.node.test.ts` never
   applies the *"`[x]` iff Done"* rule to them. All six are genuinely satisfied; none is guarded.
   This is the cheapest item on the list and has been open since the first pass.~~
   **WITHDRAWN 2026-08-10 — THIS ITEM WAS FALSE WHEN IT WAS WRITTEN, and it is the same
   failure as the withdrawn `L3`.** All six rows were written at `3a66549` on **2026-08-08
   12:30**, from measurement, and this third pass ran on 2026-08-10 against a descendant of that
   commit and restated the item as open without re-reading the table. Re-derived 2026-08-10:
   **102 checkbox rows, 102 traceability rows, 102 distinct ids, and both orphan sets empty.**
   What *was* genuinely open is narrower and is now closed too — see the fourth-pass section.
7. **A decision about whether Phases 25-28 join the milestone.** They are excluded by
   `STATE.md`'s count and included in `ROADMAP.md`'s phase list, and one of them (27) is
   `gaps_found`. As long as the two disagree, "is v1.1 complete?" has two answers.

**What v1.1 does not need**: any of `G8`, `G9`, `G10`, `G11` to be wired. Each now carries a
mechanism-level reason in the tree for why it is not, and `G8`'s is a genuine refutation of the
audit's own row — `checkBlockstoreConformance` cannot run at construction, so the finding asked
for a change that would break every restart. `G12` needs nothing; the symbol is gone.

### Verdict of this pass

**`gaps_found` stands, and one row that read `✅` no longer does.** The milestone is closer to
done than the 2026-08-08 list said on six rows (G7-G12) and further from done on one (G13), and
the net of those is not the point. The point is the shape: **five rows were disposed or closed by
a commit this file descends from and never read, and one row was closed on a safeguard that a
later phase's success removed.** Both are the same defect — a document that measured once and
then reasoned — and this pass found it in both directions on the same day's work.

---

## Fourth pass, 2026-08-10 — three items closed, and two of the three were not what this file said they were

Run against `32df739` on `feature/v11-guard-blindspots`; closed at `f2f0aae` and `77b1e5e`.
Scope was exactly items 2 and 6 of *"what v1.1 still needs"* plus the `G14` row and its two
recorded stalenesses. `EGR-01`, Primes Option A, Phase 26's disposition, `G5` and `G7` were
out of scope and were not begun.

**Every figure below was measured in this pass. Two of the three items had already moved
underneath this document, which is the fourth and fifth instance of the defect it keeps
naming about other documents.**

### Item 6 — the six unguarded checkboxes were guarded two days before this file said they were not

**Re-derived rather than trusted**, with the same two patterns
`acceptance-traceability.node.test.ts` uses:

| Reading | Value |
|---|---|
| Checkbox rows (`- [x] **ID**` / `- [ ] **ID**`) | **102** — 64 `[x]`, 38 `[ ]` |
| Traceability rows | **102**, 102 distinct ids, 0 duplicates |
| **Checkboxes with no traceability row** | **0** |
| **Traceability rows with no checkbox** | **0** |

All six named ids carry rows: `SCHED-06` `Done`@855, `NET-08` `Done`@856, `NET-09`
**`Partial`**@857 against a `[ ]` box, `NET-10` `Done`@858, `DATA-10` `Done`@859, `BENCH-07`
`Done`@860. They were written at **`3a66549`, 2026-08-08 12:30:43** — *"six missing rows
written from measurement, not invented"* — and the third pass ran two days later, on a
descendant of that commit, and restated the item as open. **`NET-09` going in as `Partial`
with its box taken off is the evidence the six were measured rather than fitted**, and this
pass re-confirmed it: `remoteDispatch` still has no production caller and is disposed under
`deferred-in-source`.

**The five other ticks were re-derived against the tree, not accepted**, and one of them was
wrong — not the tick, the evidence:

| id | Claim checked | Verdict |
|---|---|---|
| `SCHED-06` | refusal at the slot limit | `[x]` **stands**; `LocalCapacity` → `serveAgent({capacity: admission})` at `fabric-node.ts:2430` and `browser-node.ts:1713` |
| `NET-08` | receive-path ceiling | `[x]` stands — `readMessage` (`libp2p-transport.ts:179`) charges a budget per chunk, throws `InboundTooLarge` naming sender/bound/reached, and calls `stream.abort` |
| `NET-10` | named refusal, not a timeout | `[x]` stands — `reason` on `AgentResponse`, `protocol.ts:182`/`:218` |
| `DATA-10` | withheld at the serving edge | `[x]` stands — `withholdingFrom(egressDisposition)` at `fabric-node.ts:2074` and `browser-node.ts:1229` |
| `BENCH-07` | N OS processes | `[x]` stands — `processRunnerFor` (`bin/bench.ts:2224`, called `:3351`), `assertIntegrity` from `gateRung` (`:2300`) |

**`SCHED-06`'s row overstated its own evidence and is corrected.** It read *"`CountingExecutor`
is composed **outermost** in both factories"*. It is outermost in `fabric-node.ts:2133`; in
`browser-node.ts:1327` it sits **inside** `GovernedExecutor`, and that file calls the deviation
*"deliberate on two counts"* — `this.executor` must stay exactly a `GovernedExecutor` for
BROW-04's surface, and a counter outside the governor would count tasks parked on its
serialization chain as in flight. **The tick is unaffected**: composition order is where the
*instrument* reads, while the refusal this requirement asserts comes from `LocalCapacity` on
the `offer` branch in both tiers. Nothing was un-ticked in this pass.

### What was genuinely open under item 6, and is now closed

The rule *"`[x]` iff `Done`"* filters on `row !== undefined`, so a rowless `[x]` is not judged
leniently — **it is not judged at all**. The case that catches that
(*"leaves exactly the recorded checkboxes without a traceability row"*) has existed since
2026-08-08 and works. **What nothing guarded is that case being relaxed.**
`CHECKBOXES_WITHOUT_A_ROW` is a mutable list with no required reason, evidence or date —
unlike `EXEMPT`, which the same file makes argue for itself three ways — and appending one id
exempts a row from *the headline rule of the file*, silently, with the suite green.

Two assertions added, neither with a hatch:

- **Coverage**: the number of `[x]` rows the join *evaluated* must equal the number the ledger
  holds. **Watched red with the hatch taken** — `CHECKBOXES_WITHOUT_A_ROW = ['SCHED-06']` plus
  the row deleted turns the pre-existing case **green** and leaves this one the only failure:

  > `× applies the [x]-iff-Done rule to every [x] row, with none exempt by having no row`
  > `+ "SCHED-06 — [x] at .planning/REQUIREMENTS.md:571 has no traceability row, so the [x]-iff-Done rule never sees it: the tick is unguarded rather than wrong. Write the row with an honest status, or clear the box — do NOT add the id to CHECKBOXES_WITHOUT_A_ROW, which exempts it from the rule rather than from the row"`
  > `Tests  1 failed | 46 passed (47)`

- **The reverse direction, unguarded until now**: a traceability row with no checkbox is a
  delivery claim nothing can retract. Watched red by renaming one row's id:
  > `+ "SCHED-99 at .planning/REQUIREMENTS.md:855 reads **Done** with no checkbox row anywhere in the ledger — a delivery claim nothing can retract"`

Both are pure functions driven by a synthetic ledger whose answer is known, and both are shown
to report nothing when the parse dies. Plants restored by surgical inverse, `cmp` exit 0.

### `G14` — closed, and under-counted by six

**The finding was right and its number was not, for the reason the finding itself is about.**
`G14` was found by *reading*: four π symbols called from `main.ts#runPi`, a `window.o2` method,
exactly as `answerOf` is from `runColouring`. `GLOBAL_OBJECT_HOP` had the same provenance —
sixteen symbols somebody noticed — so a class assembled by reading was corrected by reading,
and neither act could know what it missed.

**Derived this time.** The register states its own closing condition — *"teach the graph the
`window.o2` assignment"* — and that clause is executable. Rooting every declaration in
`packages/browser/demo/main.ts` and re-running `unreachableExports`:

| Reading | Value |
|---|---|
| Symbols that flip unreachable → reachable | **26** |
| Of those, already disposed `global-object-hop` | **16** (a strict subset) |
| **Of those, undisposed** | **10** — `G14` named 4 |
| Dispositions that do **not** flip | **10** — exactly the 4 `benchmark-driver-only` + 6 `deferred-in-source` |

The cross-check matters: if the plant merely reached more of the tree, the non-hop dispositions
would have flipped too. They did not. The six beyond `G14`'s four are `browser/GrantedConsent`,
`browser/browserLabel`, `browser/identifyBrowser`, `core/startReportFromCounts`,
`demo/colourOf`, `demo/readPiPartial`.

**Flipping is necessary but not sufficient, so every terminus was walked individually** — a
plant that roots all of `main.ts` would also rescue anything reached only from dead code there.
Each of the ten lands on a member of the `api` literal at `main.ts:449`, assigned to `window.o2`
at `:1279`: `runPi` (:774), `verifyAnswer` (:983), `startReport` (:680), `grantConsent` (:465),
`consentState` (:461), `revokeConsent` (:473), `start` (:483), `discoverRelays` (:592),
`autoStart` (:644).

**A rendering limit found on the way, and it is worth more than the count.** ~~Two of the ten —
`demo/colourOf` and `demo/readPiPartial` — read `callers=[]`…~~ **The count was wrong, and it was
wrong in this section's own characteristic way: it was reasoned rather than measured. FIXED and
re-measured 2026-08-11.**

`unreachableExports` filtered same-file callers out of what it reported, so `describeUnreachable`
rendered *"no production code calls it"* for any symbol whose callers all sat in its own file.
Running the repaired guard over the same ten gives **six**, not two — and they are exactly the six
this section already listed three paragraphs above as *"the six beyond `G14`'s four"*:

| Symbol | callers | of which same-file |
|---|---|---|
| `browser/browserLabel` | 1 | 1 |
| `browser/GrantedConsent` | 2 | 2 |
| `browser/identifyBrowser` | 1 | 1 |
| `core/startReportFromCounts` | 2 | 2 |
| `demo/colourOf` | 1 | 1 |
| `demo/readPiPartial` | 1 | 1 |

Zero cross-file callers in any row, which is why every one read empty. The section had the list
and did not count it.

**What this did NOT do, corrected against the carried-forward note that claimed otherwise.** The
note in `.continue-here.md` called this *"the instrument used to find unwired code can report a
symbol unwired because it declined to look in the file the symbol lives in"* and ranked it the
highest-value latent item. That is **false**. `reached` is computed over the whole graph at
`reachability.ts:890` and tested at `:904`, both **before** the filter ran, and `addEdge` keeps
same-file edges (`:732` drops only self-loops). **No wired symbol was ever reported unwired, and
no unreachability verdict is in doubt.** What was corrupted is the *reason*, which is what a
reader acts on.

**The residue that is worth acting on**: several open findings are one unwired caller wearing
several names — `browser/gatewayUrl`, `cidDefect`, `cacheVerdict` and `loadArtifact` all close by
wiring `measureRepeatLoad`/`loadArtifact`; `core/createNobleSyncVerifier` and
`Ed25519NotInitializedError` close together via `initEd25519`. Reading the open list as that many
independent uncalled symbols over-counts the work.

**No finding should be closed on the strength of an empty caller list** — the rule stands, but it
is now a discipline rather than a workaround: the list is correct as of `reachability.ts:916`, and
`reachability-guard.node.test.ts`'s one-edge plant holds it there.

**Ceilings, measured rather than inferred**, by the repository's own method — set to 0, read
what the guard prints, within one run:

| Ceiling | Was | Now | How read |
|---|---|---|---|
| `OPEN_FINDING_CEILING` | 47 | **37** | guard printed *"47 unreachable callable barrel exports carry no disposition"* before, *"37"* after |
| `DISPOSITION_CEILING` | 26 | **36** | register size, asserted exactly; dropping one entry reddens at 38 > 37 and adding one reddens at 37 > 36 |
| unreachable-of-callable | 73 | **73** | unchanged, correctly — disposing changes no edge |

**`47 − 10` is also `37`, and the agreement was refused as proof** — the trap this repository
already hit once and named in `reachability-dispositions.ts`'s `49 → 47` note.

**This is a reclassification, not wiring.** Nothing became reachable; ten symbols with real
production callers were being counted as having none, which **over-states** the gap — the
direction the 2026-08-08 re-audit named about itself. **The owner's 2026-08-08 ruling to hold
the residue rather than work it down is untouched**: holding a count still and correcting what
the count is *of* are different acts.

**The class is no longer trusted to be complete.** `reachability-guard.node.test.ts` re-derives
it every run and reddens in both directions, both watched:

> `× disposes every symbol the window.o2 hop hides, in both directions`
> `AssertionError: these become reachable the moment the window.o2 assignment is traced, so they have a real production caller and are being counted as unwired — add them to GLOBAL_OBJECT_HOP, or say why this one is different: expected [ 'demo/piErrorBound' ] to deeply equal []`

> `AssertionError: these carry a global-object-hop disposition but do NOT become reachable when the hop is traced, so whatever keeps them unreachable is not that mechanism — the entry names the wrong cause: expected [ 'core/checkLease' ] to deeply equal []`

### The two recorded stalenesses — and there were four, not two

| Where | Read | Now |
|---|---|---|
| `packages/node/src/reachability.ts:594` | *"`estimatePi` — which nothing anywhere calls"* | corrected; false since `0d1fcb5` (`main.ts:841`) |
| `packages/node/src/reachability-guard.node.test.ts` header | same claim, **defending the guard's `KNOWN_FALSE_ANCHOR`** | corrected |
| same file, the anchor case | *"called by nothing anywhere in the tree — checked by grep independently of this instrument"* | corrected; the grep it cites now returns a caller |
| `packages/core/src/index.ts:401-402` | `75 → 87` / `49 → 61` | **`73 → 85` / `37 → 49`** |

**The two extra copies are the ones that mattered**, because they sat in a *guard* and stated
the ground its most important anchor rests on. The anchor is sound and its stated reason was
not: what it needs is a symbol the **static tracer** cannot reach, and `estimatePi` qualifies
because its one caller is behind the `window.o2` hop — which is why the same symbol is now
disposed under `global-object-hop`. Disposing does not weaken it: dispositions are applied by
the *callers* of `unreachableExports`, which still reports the symbol.

**`index.ts`'s pair reflects the final figures, not an intermediate.** Phase 28's deferred item
3 stated the correction as `73 → 85` / `47 → 59` and **the second pair drifted again before
anything edited the comment** — `G14`'s closure moved the open ceiling to 37 the same day. That
item is now **closed**. The `+12` delta is Plan 28-01's measurement carried forward and is
marked in the source as *not* re-measured, because measuring it means barrel-exporting the
facades, which is the owner non-decision the comment exists to avoid taking by side effect.

### What this pass did not close

`EGR-01` (owner ruling; keeps `G13` reopened) · `G4`'s primes half (owner, touches the trust
anchor) · `G5` · `G7` · the Phase 25-28 scope question. **`WIRE-02` stays unsatisfied** — the
guard still does not pass clean, on a residue of 37 rather than 47. `gaps_found` stands.

**The shape of this pass, stated plainly:** of three items handed over as closeable, **one was
already closed and had been for two days**, one was **right in kind and wrong by six in
degree**, and one was **two comments that turned out to be four**. In all three the error was
the same and it is this file's own recurring finding: *a document that measured once and then
reasoned.* The remedy applied in all three cases was to make the instrument derive its own
population instead of being handed one.

## Verdict

> **Superseded in part by the 2026-08-08 re-audit and again by the 2026-08-10 refresh above.**
> The sentence below says two integration
> blockers break real operator flows; both are now closed and the flow table is 5 of 5. The
> `gaps_found` status is unchanged — for the reasons the re-audit gives, not these.

**`gaps_found`.** The milestone is not clean and should not be completed as if it were. Two
integration blockers (G1, G2) break real operator flows, one requirement is unsatisfied, and the
guard this milestone built is reporting 47 unwired capabilities.

**None of that is a reason to discard the milestone's work.** Fourteen phases landed with
verifications, the wiring that was done is verified end to end, and v1.1 ends holding something
v1.0 did not: an instrument that makes "built but not wired" a number rather than an argument.
