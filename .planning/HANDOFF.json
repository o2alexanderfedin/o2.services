{
  "version": "1.0",
  "timestamp": "2026-08-08T12:50:00.000Z",
  "context": "milestone",
  "milestone": "v1.1",
  "milestone_name": "Wire What Was Built",
  "work": "audit-fix over .planning/v1.1-MILESTONE-AUDIT.md",
  "phase": null,
  "phase_note": "NOT phase work. All 15 v1.1 phases have verifications. Phase 22 is COMPLETE at 2/3 (fee26c2) and its .continue-here.md was corrected from 'task 1 of 3, not_started' to COMPLETE in this same pause.",
  "status": "paused",
  "head": "1214d2b",
  "branch": "feature/phase-18-discovery-capacity-placement",
  "branch_state": "13 ahead of origin, unpushed; working tree clean; nothing WIP",
  "suite": "tsc exit 0; npm run test:unit exit 0, 111 files, 1702 passed",
  "progress": { "findings_total": 17, "closed": 9, "auto_fixable_remaining": 5, "manual_only": 4 },
  "reachability": { "unreachable": 66, "disposed": 24, "open": 42, "ceiling": 42 },
  "completed_tasks": [
    { "id": "G2", "name": "AOT publish path no longer stops at the bytes", "commit": "0c43b14", "status": "done" },
    { "id": "G12", "name": "hasSeed deleted — zero callers, zero tests", "commit": "36c2800", "status": "done" },
    { "id": "G8/G9/G10/G11", "name": "disposed under a new deferred-in-source cause, each quoting its own source", "commit": "36c2800", "status": "done" },
    { "id": "L1", "name": "six missing traceability rows, written from measurement", "commit": "3a66549", "status": "done" },
    { "id": "L2", "name": "STATE.md seed self-contradiction corrected in three places", "commit": "3a66549", "status": "done" },
    { "id": "L4", "name": "two stale debug sessions closed with evidence", "commit": "3a66549", "status": "done" },
    { "id": "phase-22", "name": "Reachability Guard built, armed and verified 2/3", "commit": "fee26c2", "status": "done" }
  ],
  "remaining_tasks": [
    {
      "id": "G1",
      "severity": "blocker",
      "name": "A closed seed is unjoinable by the demo page",
      "note": "The seed's own banner names two remedies and neither exists: SeedServerOptions has no issuesCertificates, BootstrapInfo carries no provider address, and demo/main.ts's autoStart forwards only relayAddrs and blockstoreName — the enrollment block on start has NO CALLER in the page. gated-seed.e2e.test.ts is green only because the HARNESS injects providerAddr from Node, which is what hides the gap.",
      "affects": ["AUTH-01", "AUTH-02", "AUTH-04", "DEMO-01"]
    },
    { "id": "G3", "severity": "major", "name": "The demo merges with answerOf (linear scan) rather than reduceJob", "affects": ["MR-03", "MR-04", "MR-05", "MR-06", "MR-07"] },
    { "id": "G6", "severity": "major", "name": "The Node tier never calls findReservedPeers; only demo/main.ts does", "affects": ["NET-03"] },
    { "id": "G7", "severity": "major", "name": "FallbackRecordIndex/MemoryRecordIndex built and never composed", "note": "A real composition needs a genuine second source. An empty memo in front of the RPC index would be decoration.", "affects": ["NET-06"] },
    { "id": "G13", "severity": "major", "name": "The demo returns an egress manifest and index.html never shows it", "affects": ["DATA-05", "DATA-06"] },
    { "id": "G4", "severity": "manual", "name": "π and primes workloads wholly unwired — wire a second workload into the demo, or retire them" },
    { "id": "G5", "severity": "manual", "name": "discoverCandidates reaches production only behind bin/bench.ts --discover; SCHED-01's row calls the intent unsettled" },
    { "id": "L3", "severity": "manual", "name": "Phase 13 reads 0/3 fully verified while STATE.md counts it closed — the counting convention is the owner's" },
    { "id": "residue", "severity": "manual", "name": "42 open unreachable exports, held still by OPEN_FINDING_CEILING. Lowering it is the work." }
  ],
  "blockers": [
    {
      "description": "A finding classified auto-fixable may be a documented decision. Four were this session — churn.ts defers the EgressRefusal classification and names the remedy; checkBlockstoreConformance FAILS any non-empty store so it cannot run at construction; @o2/bench's barrel says sweepNodeCount is not settled. Read the module that would do the wiring before wiring it.",
      "type": "technical"
    },
    {
      "description": "Do not write a ledger row to satisfy a checker. acceptance-traceability's CHECKBOXES_WITHOUT_A_ROW docblock names the failure: 'the only way to go green would be to invent rows'. The six written this session cite a symbol and a file each, and NET-09 went in as Partial with its checkbox taken OFF — that asymmetry is the evidence they were measured.",
      "type": "technical"
    }
  ],
  "decisions": [
    { "decision": "The entry-point set stays at five", "rationale": "tools/aot/bench-lifted.ts rescues four @o2/aot symbols and is a benchmark driver, not a way the fabric is entered. Owner ruling 2026-08-08." },
    { "decision": "Only symbols with a stated cause get a disposition", "rationale": "Owner ruling 2026-08-08. Everything else stays an open finding, which is why criterion 1 is DECLINED rather than closed — disposing the rest would make the guard decoration." },
    { "decision": "The window.o2 hop is disposed on the MECHANISM, not the tier", "rationale": "Extracting index.html's inline script would close it properly and is a change to the demo, not to a guard. The cause is a hop, so it does not violate the ban on tier-based dispositions." },
    { "decision": "bin/bench.ts staying 'admits-any-peer' is a deliberate trade — do not fix it", "rationale": "relayAdmissionGate's header records that 24-02's pre-gate baseline 'stays comparable only while that remains true'. Arming it would invalidate the milestone's speedup baseline." }
  ],
  "instrument_facts": [
    "gsd-sdk init.milestone-op reported all_phases_complete:true with Phase 22 at 1 of 4 summaries, and roadmap.analyze returned an EMPTY phases array. Both false. Derive phase state from disk.",
    "zsh does not word-split unquoted variables — a comparative measurement passed both arms through $GATE, vitest got one filter, and arm A 'passed' at 0.64s having run nothing. Caught by reading EXIT=$?.",
    "fromHex NEVER returns null; it parseInts each byte pair and turns garbage into 0. fromHex(x) === null is a check that cannot fail.",
    "program.getSourceFileNames() grows lazily — 907 then 963 in one session. Walk the corpus from disk.",
    "typescript/unstable/ast does not EXPORT forEachChild, but every node has it as a METHOD. That is what makes a precise AST walk possible instead of a text scan.",
    "The vocabulary guard refuses 'earn' and 'token'. Both caught me this session."
  ],
  "uncommitted_files": [],
  "next_action": "Start with G1. Read packages/node/src/seed-server.ts (SeedServerOptions, BootstrapInfo) and packages/browser/demo/main.ts (autoStart, and the enrollment block on start that has no caller). The shape: a seed that pins issuers must either serve enrolment itself or publish a provider address a joining tab can reach without a reservation — the seed's banner already says so, and neither field exists. Decide which remedy to build, then make gated-seed.e2e.test.ts green WITHOUT the harness injecting providerAddr, because that injection is what currently hides the gap."
}
