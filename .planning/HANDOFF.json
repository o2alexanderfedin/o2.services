{
  "version": "1.0",
  "timestamp": "2026-08-28T19:40:00.000Z",
  "phase": "29",
  "phase_name": "Hosted Tier Assembly & First Deploy",
  "phase_dir": ".planning/phases/phase-29-hosted-tier-assembly-and-first-deploy",
  "phase_dir_note": "Now holds 29-01-PLAN.md and 29-01-SUMMARY.md beside 29-REPORT.md. The earlier note here said a SUMMARY must not exist because no PLAN did; the plan landed 2026-08-27 and the summary on 2026-08-28, so the tooling reading a plan as executed is now correct rather than misleading. Note what that does NOT mean: the plan is executed and the PHASE is not counted — criterion 1 is refuted and criterion 2 holds on three legs of four.",
  "plan": "29-01",
  "task": 5,
  "total_tasks": 5,
  "status": "phase-closed-uncounted-on-criterion-1",
  "branch": "develop",
  "head": "develop at a5f7203 before this session's commits; main was two merges behind and is brought level in the same session",
  "tree": "clean",
  "lanes": {
    "node": "208 files. FULL LANE GREEN: 207 passed, 1 skipped, 2985 tests, 0 failures, LANE5_EXIT=0. Two earlier sweeps each carried exactly one failure, both repaired and both PRE-EXISTING: issuance-rate lost a teardown race (ENOTEMPTY on rmdir) and discovery-agents had no guard against the RPC-timeout stand-up its sibling admission-agents already names. A third sweep was DISCARDED because I edited a source file while it was still reading it — the same hazard CLAUDE.md names for `git add` during a run.",
    "browser": "RUN for the whole @o2/cloudflare package — 18 files x 3 engines, 363 tests, EXIT=0.",
    "aot_e2e_perf": "NOT RUN. Nothing in those lanes was touched.",
    "2026-08-28": "Ledger and reachability guards run directly rather than as a lane sweep, because this session's diff is two requirement rows and two guard files. acceptance-traceability + requirements-ledger: EXIT=0, 71 passed (71). reachability-guard: EXIT=0, 35 passed (35). Two plants watched red first — `Refuted` refused by RECOGNISED_STATUSES naming the row and its line, and HOST-01 refused by the unread-row check naming the row verbatim. Host quiet throughout: load/core 0.49-0.55 before and after every run, 8 cores, ceiling 4.00."
  },
  "completed_tasks": [
    {
      "id": 1,
      "name": "the straggler threshold was guarded in one direction only — a real production-guard gap, plus mutation-ledger D1b",
      "status": "done",
      "commit": "a312ea3"
    },
    {
      "id": 2,
      "name": "speculation-agents: a settledAt cast that crashed on the case its claims most clearly hold for",
      "status": "done",
      "commit": "f8ad0c8"
    },
    {
      "id": 3,
      "name": "speculation-agents: the drifted-reconstruction gate, keyed on identity not on the host",
      "status": "done",
      "commit": "4ab6fea"
    },
    {
      "id": 4,
      "name": "discovery-agents: a count replaced by an identity-keyed bounded re-query",
      "status": "done",
      "commit": "19ba8be"
    },
    {
      "id": 5,
      "name": "admission clause 3 now reads connection DIRECTION and ROUTE, not the connected set",
      "status": "done",
      "commit": "38384e8"
    },
    {
      "id": 6,
      "name": "package-lock.json stale in two ways; npm ci measured failing before and passing after",
      "status": "done",
      "commit": "0f3503a"
    },
    {
      "id": 7,
      "name": "MR-02's three-way disagreement settled as a WORDING problem with a dated note",
      "status": "done",
      "commit": "278611e"
    },
    {
      "id": 8,
      "name": "late-combine: the all-censored residue",
      "status": "done",
      "commit": "772fe8d"
    },
    {
      "id": 9,
      "name": "admission stand-up re-attempts one named host condition, once",
      "status": "done",
      "commit": "a3f7981"
    },
    {
      "id": 10,
      "name": "late-combine: the host gate was an AND where the file's own rule is an OR",
      "status": "done",
      "commit": "6806f9b"
    },
    {
      "id": 11,
      "name": "closed-fabric-agents: a SIXTH file in the class, absent from the survey",
      "status": "done",
      "commit": "8c3f04c"
    },
    {
      "id": 12,
      "name": "sovereign-arm: a SEVENTH, discriminated on the leg's own output with no host calibration",
      "status": "done",
      "commit": "55928fc"
    },
    {
      "id": 13,
      "name": "Phase 29: the assembly, the identity that survives an eviction, the closed name set",
      "status": "done",
      "commit": "127a5c3"
    },
    {
      "id": 14,
      "name": "Phase 29: the deploy configuration and three guards, each planted red",
      "status": "done",
      "commit": "34179f1"
    },
    {
      "id": 15,
      "name": "Phase 29: the report, DEMO-04 repurposed, NET-03 re-stated as a second route",
      "status": "done",
      "commit": "8f85b2f"
    },
    {
      "id": 16,
      "name": "RETRACTION — the listener was never blocked; corrected in five documents",
      "status": "done",
      "commit": "7e02aad"
    },
    {
      "id": 17,
      "name": "the minimal inbound listener, written in TypeScript over a PUBLIC base class",
      "status": "done",
      "commit": "389fd61"
    },
    {
      "id": 18,
      "name": "late-combine: a THIRD assertion, the map budget rather than the combine",
      "status": "done",
      "commit": "f7e8402"
    },
    {
      "id": 19.1,
      "name": "ARCHITECTURE step 1 — packages/libp2p/src/dht-record-sweep.ts, the DHT record sweep. Seven plants watched red; P7 retracted and redone. Found a latent [a-z]+ regex defect closing OPEN_FINDINGS to the libp2p barrel.",
      "status": "done",
      "commit": "d3d425d"
    },
    {
      "id": 19.2,
      "name": "Recorded what the sweep unblocks: DoDatastore REFUSES /dht/ and /o2/ and its own header names the missing sweep as the reason. Also that delete-during-iteration is safe on all three stores because each materialises its listing before the first yield.",
      "status": "done",
      "commit": "021fa30"
    },
    {
      "id": 19.3,
      "name": "ARCHITECTURE step 2 — packages/cloudflare/src/workerd-shims.ts. Six plants, two retracted and redone. A probe corrected a claim I had already written. node:process was patched and the patch REMOVED after a browser-lane run.",
      "status": "done",
      "commit": "c5ba478"
    },
    {
      "id": 19.4,
      "name": "ARCHITECTURE step 3 — relay-reservations.ts in @o2/libp2p, one definition for both tiers; fabric-node.ts deduplicated onto it. Three plants. Landed with O2_SKIP_GUARDS=1 and the reason recorded.",
      "status": "done",
      "commit": "f26b216"
    },
    {
      "id": 27,
      "name": "Phase 29 PLANNED, and planning turned up two false premises of my own. (a) I scoped the phase on 29-REPORT.md saying the inbound listener was unwritten. It was written, tested and WIRED — 895 lines in three files, 389fd61/2e8c13f/caa46c6 on 2026-08-26, and worker.ts routes Upgrade: websocket through acceptInboundSocket to the 101. The report was true when written and the listener landed 39 MINUTES LATER; I read a dated document as the state of the tree and never ran git log, while THIS FILE's own decisions[0] said \"The listener is WRITTEN, not borrowed\". A research subagent caught it. (b) On that false premise I put a real decision to the owner — vendor the upstream websocket-to-conn.ts — and he answered it; he reversed it once shown the history. Both are preserved as dated SUPERSEDED notes in 29-CONTEXT.md rather than rewritten. Two further claims I copied from the same report were stale: the open-question-1 guard had ALREADY flipped (noise reaches the bundle after all, 43 occurrences in 583.94 KiB, tree-shaking is per-symbol) and 74->80 had moved twice. Artifacts: 29-CONTEXT.md, 29-RESEARCH.md (945 lines, both branches answered), 29-01-PLAN.md, 29-EVIDENCE.md. Memory saved: report-date-is-not-tree-state.",
      "status": "complete",
      "commit": "f5d2932 (merge), 4a958c1 (evidence skeleton)"
    },
    {
      "id": 28,
      "name": "An independent plan check returned FAIL and its BLOCKER was a real hole in the evidence design. The four PeerId readings could not distinguish \"survived an eviction\" from \"was never evicted\" — two readings of a live object match trivially — and the DEFAULT made that the likely outcome, not an unlucky one: hibernatable-socket.ts:17-22 records yamux keepAliveInterval 30_000, so a held connection wakes the object every thirty seconds and \"hold a connection and wait\" was the one procedure GUARANTEED not to evict. The fact was in the tree; nobody had joined it to the runbook. Fixed by TDD in adc9336: GET /self returns a third field `instance`, fixed at construction, so a construction boundary is visible in the readings themselves — 4 cases, 3 watched red first. NOT the 1012 revived-socket path, because whether workerd accepts 1012 is UNVERIFIED in that file's own docblock. Timing mattered: adding it after the single deploy would have cost a second one. Three further findings closed in 4582932 — task_count 0 against two autonomous:false precedents (now 5 tasks, each with verify/done, verify.plan-structure 0 errors 0 warnings); files_modified [] was dishonest since closing flips requirement rows; and no precondition that the namespace holds zero objects, without which \"the FIRST object\" means nothing. The checker also refuted a candidate: wrangler evictDurableObject is Miniflare's local-dev simulator behind getRuntimeMiniflare, not a lever over a deployed edge object. Measured: node lane 2990/2990 EXIT=0 in 271.15s, tsc clean, dry-run EXIT=0 at 1870.20 KiB. Nothing deployed.",
      "status": "complete",
      "commit": "1f88968 (merge), adc9336, 4582932"
    }
  ],
  "remaining_tasks": [
    {
      "id": 23,
      "name": "CLOSED 2026-08-26 in 2e8c13f — record retaken at 206 files / 2948 tests and the cheap guards pass with no O2_SKIP_GUARDS. THE RUN WAS CONTENDED AND WAS USED ANYWAY, with the override and its evidence written into vitest.config.ts: the ratio band this task carried (5.2-5.4) was a PROXY for one property — that spans taken under load would mass-exclude fast specs — and that property is now measured directly. Excluded population 78 against 78; the slow SET diffed rather than counted, differing by four files all explained (two arrived with the commit; two left DOWNWARD, and contention does not deflate); hookShadowDisagreed 14 against 16, which was the stop condition; the two instruments close to 0.001%. THE CRITERION FOR THE NEXT RETAKE UNDER LOAD IS THE SET-DIFF, NOT THE RATIO. Also settled: the load figures are the LANE's own eight workers ramping 9 -> 106 monotonically, not a foreign spike, so waiting on 1-minute load was measuring the machine rather than the process. Two runs are recorded; the discarded one is kept because it caught a real vocabulary violation that would otherwise have reached the commit.",
      "status": "done",
      "commit": "2e8c13f"
    },
    {
      "id": 19,
      "name": "CLOSED 2026-08-26 in 2e8c13f — ARCHITECTURE steps 6+7 shipped together. The admitting DoDatastore is constructed FROM the sweep and the only producer of a sweep arms the alarm, so 'no safe intermediate state where 6 exists alone' is a value rather than a rule. Only /dht/ is lifted; /o2/ stays refused. The assembly's decisions are exported as data so a spec can read them outside kadDHT()'s closure. OPEN QUESTION 1 IS ANSWERED: noise reaches the bundle after all (43 occurrences in 583.94 KiB) because tree-shaking is per-symbol, so hosted-tier-deploy's diffieHellman case is a live green reading instead of a loud skip. createHostedFabric is deliberately uncalled until Phase 30's listener; ten reachability rows added, ceiling 87 -> 97. WHAT STAYS OPEN AND IS REPORTED, NOT SIMULATED: that an alarm survives eviction and fires on a fresh instance, and that this configuration dials and is dialled. Both are owner acts at the Cloudflare boundary. Nothing was deployed.",
      "status": "done",
      "commit": "2e8c13f"
    },
    {
      "id": 20,
      "name": "CLOSED 2026-08-26 in caa46c6 — Phase 30 requirement 4. A revived socket is CLOSED with 1012, not resumed: Noise has no session resumption and yamux no resync, so every alternative is a connection that lies about being live. The registry is keyed by socket identity, so the miss IS the detection. FIVE PLANTS WATCHED RED. The route is wired, so createHostedFabric has a production caller — bundle 583.94 -> 1867.80 KiB, kadDHT 0 -> 11, and diffieHellman/node:crypto STILL 0. §17's claim is deliberately narrowed in the prose: yamux defaults enableKeepAlive at 30 s, so an object under a live connection will not hibernate at all and the proven win is only the six-minute abort. TAMING THE KEEPALIVE IS A NAMED OPEN DECISION, NOT TAKEN — a peer that never sends cannot detect a dead path. Also open and reported: whether workerd accepts 1012 (1011 is known accepted), and whether binaryType may be assigned on a hibernatable socket, deliberately not asserted either way. Phase 30 criteria 1-3 need >5 distinct clients against a deployed node and stay owner acts.",
      "status": "done",
      "commit": "caa46c6"
    },
    {
      "id": 21,
      "name": "CLOSED 2026-08-26 in 2aa0a6b — the ninth mode has a CAUSE, proven by planting rather than by waiting. The solo owner’s shard has exactly one legal executor (sovereign, its owner, REDUNDANCY 1, nothing may be duplicated for it) so its only exit is the thaw; delay the thaw past RPC_TIMEOUT_MS and the single dispatch fails on that budget with no untried node behind it, ending no-untried-node and reading null at index 21. At 5 000 ms every shard still agreed; at 15 000 ms the frozen dispatches capped at 10001/10001/10000 ms and #21 came back \"rpc to ... timed out after 10000ms\". THAT IS THE FABRIC BEHAVING CORRECTLY — nothing in production to repair; what was defective is the fixture’s silence about resting on freeze-to-release staying inside the budget. NO NEW ASSERTION, and the reason is measured: the solo dispatch begins after freeze() resolves, so a bound on the span would redden a green run in a ~200 ms band. The span is printed instead. THE PLANT CAUGHT A DEFECT IN MY OWN INSTRUMENT — the first version read thawedAt, the instant the thaw is DECIDED, and printed 764 ms under a 15 000 ms delay; now taken at releasedAt, reading 1381 ms clean and 16 244 ms planted. NOT REPRODUCED NATURALLY in ten runs under eight CPU burners at load 16-50, and no frequency is claimed.",
      "status": "done",
      "commit": "2aa0a6b"
    },
    {
      "id": 22,
      "name": "CLOSED 2026-08-26 in 6868139 — ShardResult.judgedAt is published by the scheduler from its own `woke`, threaded exactly parallel to `speculated`, and the fixture READS it instead of reconstructing it. THE FINDING IS THAT THE BIAS IS SMALLER THAN THE CLAIM THAT ASKED FOR THE FIELD: the handoff said the window admits \"exactly the dispatches running alongside the duplicates, the slowest in the job\", which was reasoning; six runs (one quiet, five under eight CPU burners at load 162-184) put it at 0-2 ms holding 0 dispatches EVERY TIME. So the field is not a repair of an observed defect — it is a number read where it was inferred, and both are printed so the day the window holds something the run says so. THE CLOCK BASIS IS THE TRAP AND IT IS REAL: JobClock defaults to Date.now() (epoch) while the wrapper log is performance.now(); the fixture converts once by performance.timeOrigin. Plant P3 printed \"expected 1787789701143 to be null\", which is that basis confirming itself. Three plants watched red — merge dropped, woke+10_000, published at the eligibility gate — and the invariant is closed on BOTH sides in one run. No codec: no .proto in the tree, ShardResult is requestor-local. Blast radius 4 fixture literals, enumerated by tsc rather than grep.",
      "status": "done",
      "commit": "6868139"
    },
    {
      "id": 24,
      "name": "A TENTH speculation-agents mode. INSTRUMENT LANDED 2026-08-26 in 4909617, CAUSE STILL UNVERIFIED and deliberately so. The symptom: `on.shards.flatMap(s => s.rejections)` came back with one `unreachable: no answer in 2000ms`, seen once in ten runs under eight CPU burners at load 29. It could not be attributed because the fixture printed no frozen ids, and the two readings it could not separate are OPPOSITE — the fixture’s own freeze reaching the admission-offer path (a fixture defect) versus a genuine reading about that path under load. Each refusal now prints the ROLE of the node that gave it: FROZEN(public-victim), FROZEN(paired-owner), FROZEN(solo-owner), FROZEN(unclassified) or healthy. Two plants watched red, one per branch. NOT REPRODUCED SINCE: twelve further runs under eight burners at load up to 235 stayed green, so across both hunts it is ONE redness in TWENTY-TWO runs, and LOAD IS NOT ESTABLISHED AS THE VARIABLE and must not be written down as the cause. CORRECTED 2026-08-26, same day: 4909617’s message says \"the single hit came at load 29 while eighteen runs above it were green\" and THAT FIGURE IS NOT DEFENSIBLE — per-run load readings exist only for the first hunt (15.95, 29.00 RED, 29.93, 36.01, 41.25, 49.91, 49.37, 49.88, 50.69, 48.90, each taken AFTER its run), while the second hunt recorded only a final 235.70 and began on fresh burners, so an unknown number of its twelve runs were below 29. What survives is weaker and is the whole point: eight green runs in hunt 1 read above the red run’s own reading, twelve more ran green ending at 235, and no per-run figure supports load as the variable. The \"load 29\" attributed to the red run is itself an after-the-fact reading of a ramp, not a measurement during it. NEXT: keep the instrument, and let the next natural occurrence name the role before any repair is attempted. The 2000 ms is an absolute on the admission-offer path and is where to look once the role is known.",
      "status": "in_progress",
      "commit": "4909617"
    },
    {
      "id": 25,
      "name": "CLOSED 2026-08-26 — git-flow is ENFORCED now, and three holes were measured before being closed. (1) The branch-name rule was a WARNING that printed \"Proceeding with commit (not enforced for other branches)\": 44 of 108 branches carried names outside its list, so it became a refusal AND the list widened to what the work produces (fix/, chore/, docs/, test/ beside git-flow's own, plus the exact shape worktree-agent-<hex> which is the harness's). (2) NO MERGE WAS EVER CHECKED — git runs pre-merge-commit for an automatic merge and that hook did not exist. (3) The same merge got opposite verdicts depending on whether it conflicted, because a conflicted one is finalised by `git commit` and hit the flat refusal. THE FIRST FIX WAS GREEN ON 26 SPEC CASES AND COMPLETELY INERT, caught only by planting: both forbidden merges went through and both branches moved. Measured with a probe hook — on git 2.33.0 MERGE_HEAD does NOT exist yet at pre-merge-commit and GITHEAD_<sha> does; MERGE_HEAD belongs to the conflicted path, a separate process no GITHEAD survives into. Rules now live in .githooks/git-flow-rules.sh, exercised by packages/node/src/git-flow-rules.node.test.ts (27 cases). ONE ARM CANNOT BE PLANTED AND SAYS SO: develop taking main is a fast-forward and git invokes no merge hook for one; reference-transaction could catch it and is deliberately refused as disproportionate. A FRESH CLONE ALSO HAD ZERO ENFORCEMENT — core.hooksPath lives in .git/config, not in the repository — closed by a `prepare` script.",
      "status": "done",
      "commit": "0edfc12"
    },
    {
      "id": 26,
      "name": "THE REPOSITORY HAS THREE GIT TREES, NOT ONE, AND AN EARLIER ANSWER IN THIS SESSION SAID ONE. `git worktree list` reports one worktree and that is true of the MAIN repository only; two nested repositories sit inside it and neither is a worktree. (a) third_party/elfconv — a declared submodule, measured clean: 0 dirty, 0 stashes, all five branches level with their upstreams, and the parent's recorded pointer matches. (b) packages/browser/dist — the gh-pages publish checkout of the SAME origin, ignored by the parent at .gitignore:37, which is why it never appears in the parent's status. Its committed state was already level with origin/gh-pages (191f497), but its working tree held an unpublished rebuild from 2026-08-24 with `.nojekyll` DELETED — a file GitHub Pages needs. Nothing in the repository reads that directory (the two grep hits are a forbidden-command string list in disclosure-gate and a docblock in x509-bundle explaining that it deliberately does not). The stale build was discarded to the published state after a verified backup; the live site was never touched and answers HTTP 200. THE PUBLISHED SITE IS FROM 2026-08-06 AND 776 develop COMMITS OLD — republishing is an owner act and is NOT done. NEXT TIME: enumerate nested repositories, not only worktrees.",
      "status": "done",
      "commit": "n/a — measurement, no code change"
    }
  ],
  "blockers": [
    {
      "description": "RESOLVED 2026-08-27/28, and not by both criteria passing. The owner performed the acts. Criterion 1 (billing alert before the first object) is REFUTED and permanently unsatisfiable — he was shown the ordering and chose to deploy without the alert; the object exists and its creation time is fixed. Criterion 2 holds on three legs of four: dials, identify completes, PeerId survives a redeploy. The fourth — the identity surviving an EVICTION — is carried only by an agent-taken reading of 2026-08-28 and is the one thing still outstanding.",
      "type": "resolved",
      "workaround": "n/a"
    },
    {
      "description": "RESOLVED 2026-08-28. The owner was asked whether he had run scripts/deploy-hosted.sh --live by hand between ~09:30Z and 18:21:13Z and answered NO, which is the one thing the run list cannot show and the whole difference between an eviction and a second redeploy. Evidence row 2 is filled: two /self reads ~8h50m apart, one PeerId, markers f57ced1f... -> 8ac6c674..., a three-read control holding one marker, and an outside libp2p dial completing identify on the post-boundary instance. Criterion 2 is MET IN FULL and HOST-01 is Done. THE EVICTION WAS OBSERVED, NOT FORCED — the forcing lever is still unverified with its one candidate refuted, so this reading is not repeatable on demand.",
      "type": "resolved",
      "workaround": "n/a"
    },
    {
      "description": "The distinct-machine benchmark (Phase 39 criterion 4) is blocked on there being a build to hand the Telegram tester cohort",
      "type": "human_action",
      "workaround": "none; it is a calendar decision"
    },
    {
      "description": "NOTHING ELSE IS BLOCKED. The listener blocker reported earlier this session was RETRACTED the same day — it was Node's resolver measured where esbuild's mattered, and three research documents already covered the ground.",
      "type": "retracted",
      "workaround": "n/a"
    }
  ],
  "human_actions_pending": [
    {
      "action": "Optional, and no longer able to satisfy HOST-10: configure the $15/month budget alert anyway",
      "context": "The row is Refuted permanently — the first object exists and its creation time is fixed, so no later alert makes the ordering true. The alert is still worth having for its own sake. Measured cost is ~$5/month: 331,776 GB-s against 400,000 included. Cloudflare has no hard ceiling; its own wording for budget alerts is \"informational only. It does not cap your usage.\"",
      "blocking": false
    },
    {
      "action": "Cut a build for the Telegram tester cohort",
      "context": "The scaling claim has never been measured off one laptop. Access is the only gate, and access is the disclosure event.",
      "blocking": false
    }
  ],
  "decisions": [
    {
      "decision": "The listener is WRITTEN, not borrowed",
      "rationale": "webSocketToMaConn is not on @libp2p/websockets' public surface; reaching it means a path into dist/, a build artefact with no semver signal. The base class comes through @libp2p/utils' public barrel and only the Cloudflare half is ours. The owner's 'we write TypeScript' is what settled it.",
      "phase": "29"
    },
    {
      "decision": "Six new cloudflare symbols to OPEN_FINDINGS, not DISPOSITIONS",
      "rationale": "A disposition says a symbol has a real caller behind a hop the tracer cannot follow; global-object-hop's symbols have callers that RUN. These have a caller in a Worker nothing deploys. Dispositioning would have made the number look like wiring.",
      "phase": "29"
    },
    {
      "decision": "No budget was raised in any of the seven load-sensitive files",
      "rationale": "Owner ruling: slow under load is normal. Implemented as discrimination rules moved from failure-message prose into conditions.",
      "phase": "20-29"
    },
    {
      "decision": "GET /self carries a per-instance marker, and it shipped BEFORE the deploy",
      "rationale": "Criterion 2 needs one PeerId across a construction boundary. Without a marker the owner's four readings cannot show a boundary happened, and yamux's 30s keepalive makes \"no boundary\" the likely case. A construction-time value rests on nothing platform-specific, unlike the 1012 close-code path whose acceptance by workerd is UNVERIFIED. Added after the deploy it would cost a second deploy, so it had to precede the first.",
      "phase": 29
    }
  ],
  "uncommitted_files": [],
  "next_action": "PHASE 29 IS CLOSED AND STILL UNCOUNTED, AND ONLY CRITERION 1 HOLDS IT THERE. Criteria 2-7 are all met — criterion 2 IN FULL as of 2026-08-28, when the owner confirmed no manual deploy ran in the idle window and evidence row 2 could be filled. Criterion 1 (billing alert preceding the first Durable Object) is REFUTED permanently: the object exists and its creation time is fixed, so no later work repairs an ordering. The ROADMAP checkbox stays OFF for that reason alone — the 20/21/22 precedent, verified but uncounted. Do not read the phase as complete because 29-01-SUMMARY.md exists.\n\nTHE HOSTED TIER IS LIVE: four tagged releases (v2.0.0-rc.1..rc.4), the node answering at o2-bootstrap.af-4a0.workers.dev, naming its own build in GET /self, dialable from outside with identify completing, and holding one PeerId across both a redeploy and an eviction. The browser client is published to GitHub Pages and can actually join since rc.4. Both deployments run from one script each on a release:published trigger, identically from a laptop and from CI.\n\nTWO LIMITS TRAVEL WITH HOST-01 AND ARE IN THE ROW, NOT ABSORBED: the eviction was OBSERVED, NOT FORCED (the forcing lever stays unverified, one candidate refuted), and its ledger-side witness (packages/cloudflare/src/hosted-identity.test.ts, which now names HOST-01 in a describe title) carries only the MECHANISM — no test in this repository deploys or dials.\n\nQ6 WAS REFUTED A SECOND TIME: the deploy happened and the unreachable count did not move (measured 103, 29 of them cloudflare). A deploy adds no in-repo caller, which is the only thing a static walk counts. Stop writing \"a deploy\" as those rows' closing condition; what would close them is an in-repo driver constructing the object and calling its platform entry points directly, and that is scoped by neither Phase 29 nor Phase 30.\n\nNEXT SUBSTANTIVE WORK, named by the owner and measured absent: limit simultaneously executing tasks by CPU core count and run them in WebWorkers. hardwareConcurrency appears ONCE in the tree, in browser-id.ts, about fingerprinting; the workers exist (worker-executor.ts, task-executor.worker.ts, worker-factory.ts) and nothing bounds how many tasks run at once.\n\nALSO OPEN AND MINE, deliberately kept out of the two-row diffs above: HOST-05, HOST-08, HOST-11 and HOST-12 all read \"Not started\" and are criteria 3-6, met; two clauses of NET-03's row are falsified by the deploy (\"nothing is deployed\", \"the INBOUND half is not built\"). Each wants its own dated amendment. And late-combine failed once on 2026-08-28 and did not reproduce, with load NOT discriminating — red run 3.69 per core, green run 3.99.",
  "context_notes": "The session's redirect is the finding: the recorded next action was task 19, and task 19's OWN first instruction — read ARCHITECTURE.md:470-506 — reorders it. Steps 1-3 are Group A, provable in the node lane today; step 6 is Group B and needs the owner's deploy. Doing the ordered work surfaced three things nothing had asked for: a latent [a-z]+ regex that closed the reachability register to the libp2p barrel, DoDatastore's refusal of the exact prefixes the sweep walks, and mortice's browser build being the WRONG remedy for gap 2. Five plants across three modules were retracted for proving nothing — one ReferenceError, two aimed at a mechanism they left in place — and each was redone and recorded rather than quietly replaced. The pause itself produced one more instance of the family: writing a bracketed section name into a replacement body made the NEXT replacement match that mention instead of the real tag, deleting four sections between them. Caught by the line count falling 263 -> 206 rather than by any check, restored from the last commit, and redone with every span computed against the original string before any substitution."
}
