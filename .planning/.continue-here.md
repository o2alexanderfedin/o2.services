---
context: default
milestone: v1.1
phase: 13.1-node-side-admission-and-transport-bounds
task: null
status: ready_to_gather_context
last_updated: 2026-07-28T20:15:00.000Z
---

# START HERE — Phase 13.1, and it has no context file yet

Everything before it is closed and verified. Nothing is half-done, nothing is stranded, and
for the first time in three sessions the ledger contains no claim I know to be false.

```
Skill(gsd-discuss-phase)  →  .planning/phases/phase-13.1-.../13-1-CONTEXT.md
```

Phase 13.1 is **five defects, every one measured against the real stack** — not inferred, not
reasoned about. The measurements are in `ROADMAP.md`'s Phase 13.1 entry and in
`REQUIREMENTS.md` under *Node-side admission & transport bounds*.

| ID | What is wrong today |
|---|---|
| SCHED-06 | 4 peers × 200 concurrent `exec` requests → **800 simultaneous `execute()` calls, zero refused**. `serveAgent` consults `capacity` only in the `offer` branch |
| NET-08 | A single **64 MiB** frame was sent over the real transport and accepted — `readMessage` has no cap |
| NET-09 | N=8 completes, **N=12 fails entirely** on `MaxEarlyStreamsError: 11/10`, which aborts the whole connection. The requestor blames the *receiving* node for the *sender's* overrun. `bin/bench.ts` ships `SHARDS = 8`, one below the cliff |
| NET-10 | A sovereignty refusal arrives as `rpc … timed out after 4000ms` — no label, no attribution, against a 30,000 ms default |
| DATA-10 | Only the **executing** node registers its input, so a submitting node that never ran the task serves the raw bytes on request — measured, 95 raw bytes inside a 138-byte frame |

**`LocalCapacity` gets wired or deleted.** It has been built, tested and unreachable since
Phase 6, and it is the only thing that can emit the refusal SCHED-06 needs. Leaving a third
option open is how it survived this long.

**Phase 18 needs an added criterion, and this is the trap.** Its criterion 2 exercises the
*`offer`* branch, so Phase 18 can pass in full while SCHED-06 stays wide open. The amendment
is already written into Phase 13.1's roadmap entry — carry it forward.

---

# BLOCKING CONSTRAINTS — read before proposing anything

- [ ] **ALL NODES HAVE EQUAL FUNCTIONALITY. The only difference is discovery.** Stated by the
      owner four times; a class was deleted over it. If a decision keys on *what kind of node*
      something is, it is wrong. Grep for the mechanism, never the vocabulary.

- [ ] **DEMO-04: no deploy workflow file may exist in the repository at all** — absent, not
      disabled. `disclosure-gate.node.test.ts` checks by content, so relocating does not evade it.

- [ ] **Vocabulary discipline** is enforced repo-wide by `vocabulary.node.test.ts`, which scans
      `.planning/` too. **Read the `BANNED` array from that file; never enumerate it in prose.**
      A document quoting the prohibition violates it.

- [ ] **Do not reintroduce static determinism analysis.** Built, hardened, fuzzed, deleted
      (1,214 lines). Divergence is *detected* by byte comparison, never predicted.

- [ ] **Do not write an allow-list for host imports.** `WebAssembly.instantiate` is the sandbox
      and names the offending import.

- [ ] **The kernel must never require `crypto.subtle`.** A LAN origin is not a secure context.

- [ ] **Never trust elfconv's exit code.** It is `0` on binaries it could not fully translate.

- [ ] **Background agents share ONE working tree**, not worktrees, and can outlive their
      session. Never `git checkout --` a file you did not personally write; never `git add -A`;
      `git commit` without `-a` still commits whatever another agent has staged.

- [ ] **Do not run `gsd-sdk query state.begin-phase`.** It rewrites `STATE.md`'s progress block
      from a plan count that includes nine unarchived v1.0 phase directories — observed turning
      25% into 62% — and mangles the Current focus paragraph. Same for `progress.bar`, which
      reports "17/9 plans (100%)". **Maintain that frontmatter by hand.**

<current_state>

```
milestone   v1.1 — Wire What Was Built · 3 of 14 phases (21%)
tests       124 files · 1798 passing · tsc --noEmit clean
branch      develop d2f446f == origin/develop · tree clean · no stashes · 1 worktree
main        4e76c27 — deliberately at v1.0, ~60 commits behind
```

| Phase | State |
|---|---|
| 11 — Explicit serveAgent Hook Contract | done, 3/3 |
| 12 — Sovereignty-Pinned Placement | done, 4/4 |
| 13 — Egress Manifest Completeness | **done, 3/3 on amended criteria** (7 plans, 2 verification reports) |
| 13.1 — Node-Side Admission & Transport Bounds | **next** — no context file |
| 14–23 | not started |

`main` is a release boundary, not a review one. Promoting a milestone that is 3 of 14 complete
would put unfinished work under a production tag. The owner is solo and does not want PRs, so
nothing waits on review — this is a deliberate hold, not a queue.
</current_state>

<what_happened_to_phase_13_and_why_it_matters>

**It was marked done, scored 0/3, and closed at 3/3 — all in one session.** This is the
clearest worked example in the project of the gap between code being right and claims being
right, and the next phase should be run the same way.

Round one shipped real, falsifiable wiring. An independent pass then measured three things:

- the leaking job completed as **`agreed`**, and `manifest.violations` had **zero production readers**
- a probe sending the raw sovereign characters alone **crossed the tap with no violation** —
  matching needs the whole registered block, contiguous and byte-identical
- `manifest.totalBytes` read **130** where the raw input was **95** and the aggregate **8** —
  the manifest reported *more* than the data it was meant to prove had stayed home

None of that was a coding error. Every one was a criterion promising more than the mechanism
delivered. The criteria were amended to what is true — and *up*, where the owner's ruling made
a stronger claim available — then four more plans closed the distance, and a second
independent pass scored 3/3 with eight mutations it planted itself.

**The amended criteria are in `ROADMAP.md` with an HTML comment explaining every change.**
`13-VERIFICATION.md` quotes the *old* wording throughout. Read it for its findings; never
verify against its quoted criteria.
</what_happened_to_phase_13_and_why_it_matters>

<owner_decisions_this_session>

- **A sovereignty violation refuses the send.** The guard throws instead of forwarding, so the
  bytes never leave. Failing a job after the data crossed cannot un-send it. This made
  criterion 1's "the job fails" clause true as a *consequence* rather than needing someone to
  remember to read `violations`.
- **Raw sovereign data does not move between nodes at all** — not even between two machines one
  owner controls. Recorded in `PROJECT.md`, and **narrowed the same day** when the second
  verifier measured that the code delivers *"does not leave a node holding a registration for
  it"*, and only the executing node registers. DATA-10 closes the distance.
- **The manifest promises the submitting node's tap**, not every owner's. Cross-process
  retrieval needs a wire message kind that does not exist; it is a named future item now
  instead of an implied promise.
- **Same machine, different browsers/contexts/processes is the testing standard everywhere.**
  "A second machine" is no longer a blocker on any of this project's criteria. **BENCH-06 and
  AOT-03 were descoped, not satisfied** — the record says so in those words, and
  `CROSS_MACHINE_BLIND_SPOT` stays attached to every artifact with its guard test intact.
- **Phase 22's reachability guard (WIRE-02) runs early**, not last. Its exemption mechanism
  matters more than the guard: copy `vocabulary.node.test.ts`'s `EXEMPT_LINES` — each exemption
  is data, carries a written reason, and a dead-exemption test fails when one stops being needed.
</owner_decisions_this_session>

<remaining_work>

1. **Phase 13.1** — gather context, plan, execute. Five measured defects, above.
2. **Phase 22 / WIRE-02** — the reachability guard, pulled forward by owner decision.
3. **Phase 23 / BENCH-07** — the multi-process benchmark driver. The scaling claim is currently
   **unmeasured**, not unsupported: every published curve runs on one event loop, so no
   parallel speedup is measurable at any N.
4. **Phases 14–21.**

## Four browser gaps, now unblocked

`WIRE-03` in Phase 19 can finally run: Playwright already ships chromium + firefox + webkit
with isolated contexts, which is three genuine peers over real WebRTC on one machine. Scope
that phase to **discharge** these, not inherit them:

- `BrowserNode.start()` has no dedicated runtime test anywhere (Phase 11)
- its `guardSovereignty` wiring has zero runtime proof (Phase 12)
- `BrowserNode.egress` unproven at runtime (Phase 13)
- the `EgressGuard` refusal added this session inherits into the browser untested (13-VERIFICATION-2)

Common cause, recorded four times: `BrowserNode.start()` needs real IndexedDB *and* a relay to
dial, so it runs in neither vitest project.

## Recorded, deliberately not fixed

`EgressGuard.#entries` grows one object per outbound frame for the node's lifetime and is
peer-driven — an 800-request probe added 800 entries. Bounding it was considered and declined
**because `submit-with-egress.ts` delta-slices `entries`**, so a ring buffer would silently
drop history a reader may be mid-slice on. If it is ever fixed, follow `wasi-executor.ts`'s
house style and **count what was dropped**, surfacing it in the manifest.
</remaining_work>

<blockers>

**Nothing blocks the code.** Three items need the owner — down from four:

1. **US provisional patent — the only hard deadline, 2027-07-26.** EPO and China are already
   permanently forfeit.
2. **A hosted relay with AutoTLS** — closes NET-03, is what a Pages deployment needs to function
   at all, and is the sole reason Phase 3 is still unchecked.
3. **GitHub Pages serves the pre-Phase-9 bundle** (`gh-pages` at `db8ddf3`). Republishing is a
   deliberate human act under DEMO-04.

**Residual from the same-machine ruling:** cross-machine reproducibility (AOT-03) and
distinct-machine benchmarking (BENCH-06) are unverified *by choice*. Closing either needs
hardware the project does not have. Unmeasured is not met, and the record says so.
</blockers>

## Environment notes

- **Node 23.11.0, not LTS** — every install prints `EBADENGINE`. Everything passes.
- Test suffixes are load-bearing: `*.browser.test.ts`, `*.node.test.ts`, `*.e2e.test.ts`.
- A full `vitest run` is **~5 minutes** — the AOT tests really do drive Docker. Not hanging.
- Real-process tests spawn via `spawn(process.execPath, [AGENT, ...])`;
  `sovereignty-placement.node.test.ts`, `two-process.node.test.ts` and
  `egress-refusal.node.test.ts` are the precedents.
- `bin/agent.ts` takes `--owner-id` and `--can-execute-sovereign`; omitting them keeps the safe
  default (cleared for nobody).

## Calibration — the most useful thing in this file

**Every real defect this session came from checking a claim against the repository. None came
from reading source carefully, and none came from trusting a report.**

I was wrong twice, both times the same way: I read a well-built mechanism and let its *intent*
stand as its *behaviour*.

- I cited `over-committed: N of M slots in use` as proof the project had chosen refusal
  deliberately. **That string cannot be produced by any running node** — the only thing that
  emits it is constructed nowhere outside tests, and both production factories opt out.
- I wrote a decision row saying *"the fabric will not move raw sovereign data."* The code
  delivers something narrower, and a submitting node was measured shipping the raw bytes.

Both were caught by agents I had explicitly told to **refute me**, not to check my work. That
framing is the difference. An agent asked to verify tends to confirm; an agent asked to break a
named claim, with a reproduction required, finds things. Budget for it as a first-class step.

Two more from the same session: an executor reported a green suite while it was red at five
failures, and a SUMMARY cited a plan that never ran. The agents do good work and their
self-reports overstate it — **re-run the suite yourself before believing a count.**

## Resume

```
/gsd-resume-work
```
Then gather context for Phase 13.1.
