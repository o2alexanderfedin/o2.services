# Continue Here

**Paused:** 2026-08-11 10:30 PDT
**HEAD:** `898bc4c` on `main`, level with `origin/main`
**Tree:** clean · 0 stashes · 1 worktree · 0 unmerged branches · `develop` synced
**Session:** 128 commits since `60d7758`

---

## Position

**All 28 roadmap phases are planned, executed and merged. Phases 9–28 carry a VERIFICATION.md** (1–8 predate the plan format and never had one).

**Requirements ledger: 79 of 102 met. 23 open.**

**Suites, last measured at `898bc4c`:**

| Project | Result |
|---|---|
| `node` | 184 files / 2739 passed / 1 skipped · exit 0 |
| `e2e` | 28 files / 185 passed · exit 0 |
| `browser` | 270 files / 4746 passed · exit 0 |
| `tsc --noEmit` | exit 0 |

The 1 skip is pre-existing in `transport-bounds.node.test.ts`.

---

## ⚠️ STOP HERE FIRST — one item was flagged and never answered

**Primes Option A was about to start when the pause came. It has NOT been started.** `git status --porcelain packages/demo/src/kernel-record.ts scripts/sign-kernel.ts` is clean.

It regenerates the trust root:

- `scripts/sign-kernel.ts` **discards its private half on every run**, so a third record cannot be appended — all three must be re-signed together under a **new `KERNEL_TRUST_ANCHOR`**
- `bin/agent.ts` and `bin/seed.ts` both default to that anchor, so it **changes what a stock `o2 agent` and a stock `o2 seed` will run**
- Every currently-signed artifact becomes unverifiable against the new anchor

Precedent exists (`f42e985`, *"one key signs both kernels — the trust root regenerated, by owner ruling"*) but that was an explicit owner call.

**Expected side effect, which is the design working rather than a regression:** the moment `runPrimes` lands, Phase 27's `P4b` guard reddens with *"the dispatch path this surface was told it does not have now exists — the surface must be replanned."* That is intentional.

**Do not start this without a decision.**

---

## The other parked decision

**Phase 26 disposition.** The gate returned a measured **NO-GO** (glog has no `__wasi__` branch; 21 of 27 TUs fail; threads move the error one line). Four options, none begun:

| Option | Cost |
|---|---|
| `close-negative` | Zero. The negative *is* the deliverable, on AOT-05's precedent |
| **`replan`** ← my suggestion | One planning pass. Front-loads three cheap unmeasured questions that would turn the cross-build estimate from a guess into a number |
| `narrow` | Stage 2 only. Glog is not on its path — but shares the unpaid LLVM cross-build, and roadmap item 8 is itself inferred |
| `cross-build` | Hours. Starts with glog, not LLVM. Can fail late in code never type-checked |

---

## Completed this session

**Phases 26, 27, 28** — planned, executed, verified, merged. Then closed by requirement:

| Item | Result |
|---|---|
| `EGR-01` | The egress sentence now distinguishes two facts it conflated. **G13 re-closed** |
| `VER-02` | Commit-reveal ceremony, 14 failure branches each planted red, wired through `bench.ts` |
| `X509-01…07` | All seven wired **fail-closed** into `verifyCertificate` |
| `CRYPTO-03` | Closed by *counting* the module, not by inventing a caller |
| `MR-02` | Built, 15 refusals planted. `[ ]` — blocked on a two-owner rig |
| Residue sweep | 6 rows closed; 4 had been stale **pessimistically** |
| Re-read register | `WITHOUT_A_CHECKABLE_CLAIM` membership now costs a reason, a date and measured witnesses |
| v1.1 audit, 3rd pass | G13 reopened→re-closed, G7–G12 disposed/closed, G14 filed and closed |
| Span re-measurement | `slow-specs` drift 0. **The JSON reporter cannot see 44%** of node span time |

---

## Rulings I took (confirm or reverse)

1. **X.509 gate is fail-closed.** Grounds recorded at the wiring site: this project refuses by default everywhere else, so fail-open would be the only place a *malformed* certificate is treated more leniently than an *absent* one.
2. **Ed25519 port: fail-closed** — found already satisfied by construction; all nine call sites sit inside a `try` whose `catch` refuses. **Wiring the port was measured to be strictly worse** and was not done.
3. **Primes ships as Option B** (named absences), recorded **open, not satisfied**.

---

## Open rows (23)

`VER-03 VER-04 VER-10 · AUTH-02 AUTH-03 AUTH-04 · NET-03 NET-06 · MR-02 MR-03 MR-04 MR-05 MR-06 MR-07 · CHURN-03 CHURN-04 · BENCH-06 · AOT-02 AOT-03 AOT-04 AOT-05 · WIRE-02 · AOTW-06`

**Four turn on one untaken ruling:** does a capability reached only behind `--discover --sovereign` count as entry-point reachable? (`VER-03`, `VER-04`, `AUTH-03`, G5.) Not taken deliberately — the audit's instruction is not to close it on the guard's silence.

**Measured negatives / descopes, correctly `[ ]`:** `AOT-03` (cross-machine, descoped — **never read as a near-miss**), `AOT-05`, `BENCH-06`.

**`AOTW-06`** is only meaningful after a Phase 26 GO.

---

## Known issues carried forward

- **`unreachableExports` filters same-file callers out of its own report** (`reachability.ts:898`). The instrument used to find unwired code can report a symbol unwired *because it declined to look in the file the symbol lives in*. Two were caught; **nobody has swept the rest.** Highest-value latent item.
- **`churn-agents`** — not contention (16 solo runs, 8 under load, never below 4 holders). It is **placement concentration**: `participants 8, held [1,7]`, identical signature twice. Bound not relaxed; assertion now prints the census.
- **`late-combine`** — `RPC_TIMEOUT_MS = 1500` is a real correlation budget, not a threshold; cannot be re-sited comparatively because the left side *is* the budget.
- **The re-read register's countdown reddens `npm test` on a calendar.** Scoped under the pre-commit hook to agents touching the ledger; a bare suite run blocks everyone. Deliberate, flagged, reversible.
- **`packages/browser/dist/`** still carries the pre-`EGR-01` sentence — committed build output, regenerated on next build.

---

## Resume

```
/gsd-resume-work
```

Then either answer the two parked decisions above, or continue the open rows. **Do not run any `gsd-sdk query state.*` or `roadmap.update-plan-progress` verb** — seven are measured to corrupt this directory while reporting success; edit `.planning/` by hand.
