# Continue Here

**Paused:** 2026-08-17 11:58 PDT
**Branch:** `main` — everything merged and pushed. `main 4915b47`, `develop 774594a`, both match origin.
**Working tree:** clean. No worktrees. `tsc --noEmit` clean.

---

## Where the milestone stands

v1.1 "Wire What Was Built", executing. 20 of 29 phases; every one of the 125 planned plans has a
summary. **No phase is in flight** — STATE.md is right that the open work is the audit's residue,
not a phase.

**The residue is two owner decisions and nothing else:**

- **G4** — the primes workload has no entry point. Closing it means Option A, which regenerates
  the kernel records under a **new trust anchor** and therefore changes what a stock `o2 agent`
  and a stock `o2 seed` will run. It touches the trust root. Not a wiring job.
- **G5** — `discoverCandidates` reaches production only behind `bin/bench.ts --discover`, off by
  default. The reading of whether that counts as reachable was itself an owner ruling.

**Do not read the audit table by scanning rows for ✅.** It gives `G1 G2 G3 G6 G13` and invites
the conclusion that nine findings are open. `G7`–`G12` were disposed and closed by `36c2800` and
`97a127d`, recorded in the file's *amendments*; `G14` was closed 2026-08-17. The rows are left
standing on that file's own convention. This misreading has now happened twice and the file
carries a warning about it.

---

## The finding worth carrying forward: the debug ledger lies by default

Seven debug sessions were reported open. **All seven were closed defects whose `status:` field was
never moved** — five found on 2026-08-17 morning, the last two that afternoon. Each carried a
complete `Resolution` block — root cause, fix, plants, verification — under a frontmatter still
reading `investigating` or `awaiting_human_verify`, for twelve days.

**The hit rate is the finding.** Seven for seven. Not one of the sessions reported open was
actually open, and two of them carried a `next_action` that had gone stale in a way that would
have wasted the reader's time: one asked for a measurement that had already been taken and
recorded two days earlier, the other pointed at a function that had been deleted.

| session | actually fixed at |
|---|---|
| `49-repick-erases-refusal` | `0e045f5`, 2026-08-05 |
| `63-mutual-block-fetch-silence` | `cb842e7`, 2026-08-05 |
| `late-combine-arrival-15s` | `adbd17e` + three refinements |
| `relay-admission-intermittent-fail-open` | already `status: resolved` |
| `retake-measured-node-spans` | already `status: resolved` |
| `three-tracked-defects-69-65-19` | fix at 2026-08-05; its one hand-over item done `2026-08-15` |
| `three-tracked-defects-78-79-59` | `4f8a8cf`, 2026-08-05; #78 superseded `2026-08-12` |

Two compounding causes, both now fixed for these five:

1. Nothing in the process closes a session when its fix lands.
2. The progress workflow counts by **filename** — `ls .planning/debug/*.md | grep -v resolved` —
   and nothing had ever renamed one. All five are now `*.resolved.md` and the counter reads **2**.

**If you add a debug session, rename it on resolution.** Otherwise it is counted as open forever.

---

## What landed today

| | |
|---|---|
| `d58288b` | **NET-03** — a relay acquires a real TLS certificate against a local ACME authority. No public host, no real DNS name. 7/7. |
| `72d5c14` `55a50ec` | **AOTW-06** — elflift compiles, links, runs and lifts as one `wasm32-wasi` module. All four steps. |
| `5dd17fd` | The wasm lift repeats byte for byte; native elfconv's **bitcode stage** does not. |
| `a1ff33b` | **The module did not rebuild byte-identically.** `--strip-all`, not `--strip-debug`. |
| `ef99c54` | Four engines, one answer — Node, Chromium, Firefox, WebKit. |
| `84c9913` | The 40 LLVM archives reproduce, and the module through them. |
| `a024f52` | The extension-sequencing guard cried wolf at X.509. |
| `9ea5585` | G14 closed; the audit's residue named. |
| three agent branches | five debug sessions closed. |

### The two determinism claims, stated at the right width

- **Build reproducibility.** Three links of the same objects gave three different modules, all
  13 565 617 bytes with every section the same size; the first differing byte, 8 087 429, is the
  first byte of the `name` section. `CODE` and `DATA` reproduce exactly. `--strip-all` gives one
  hash, `33b6ba7f…`, at 8 087 408 bytes, and the same bitcode out. **This removes the symptom, not
  the cause** — wasm-ld still emits those names in an unstable order.
- **Run determinism.** The lift is byte-identical across runs and across four engines. The
  *intermediate bitcode* from native `elflift` is not — four runs, four hashes.
  **AOT-03 already records the FINAL `.wasm` from the full elfconv driver as byte-identical on one
  host, and that case runs and passes.** Both are true; they are different stages. Do not repeat
  the unqualified claim "native elfconv does not equal itself".

---

## Open, and honestly labelled

**~~Two debug sessions~~ — both closed 2026-08-17, and neither needed work.** The debug ledger
lied twice more, in the same way it lied five times above. `.planning/debug/` now holds **zero**
open sessions.

- `three-tracked-defects-69-65-19` wanted a whole-table timing retake on a quiet host. **The
  retake happened on 2026-08-15** and nobody closed the record: `vitest.config.ts` carries
  `NODE_MEASUREMENT.date: '2026-08-15'`, `files: 191`, taken at load 5.92 with `(user+sys)/real`
  of 1.45 — and a note that a 2026-08-14 attempt was *discarded* at a ratio of 0.48 because an
  LLVM build held the machine. `slow-specs.node.test.ts` is EXIT=0, 9 passed, including the
  drift case that was red when the session was written.
- `three-tracked-defects-78-79-59` said to read `imageIsPresent()`. **That function no longer
  exists.** It was superseded on 2026-08-12 by `tools/aot/docker-gate.ts`, which asks the daemon
  first and the image second — a generalisation of the very fix the session had already landed
  at `4f8a8cf`, now shared by the five call sites that each carried a copy. #79 and #59 verified
  present in the tree by symbol.

**~~One coverage gap~~ — closed 2026-08-17, and the plants were watched failing.** Three cases at
the foot of `packages/net/src/named-refusal.test.ts` now pin the coupling that was a reading of
the call graph:

- A combine over a **mutually-wired pair** — a topology that existed nowhere in the tree; every
  fixture in `combine.test.ts` points its workers at one origin — refuses an unobtainable input
  by name at the cost of a chain arm run in the same call.
- The hop count is read **off the wire, not off the clock**: the serving node emitted exactly one
  request frame while the combine was in its hands and it was a `block`; its peer emitted none.
- A positive control on the same pair completes a combine whose inputs the peer holds, so none of
  the above is bought by a branch that refuses everything.

Both halves fire. Removing the `has` gate reads `expected 2002 to be less than 500`, beside the
pre-existing block case's 2004 — the same 2 000 ms inner-budget signature on both branches. And
the half that used to redden nothing: a plant that leaves the fetch working and merely adds a
combine frame per peer reads `expected [ 'combine', 'block', 'combine', 'block' ] to deeply equal
[ 'block', 'block' ]`. `agent.ts` restored by surgical inverse and `cmp`'d against a snapshot taken
immediately before each of the three plants — EXIT=0 every time.

**Unfinished measurement:** `tools/aot/lift.node.test.ts` has never been given a genuinely quiet
run today. Six failures at one load, eleven at a higher one, all of them the spec's own
`docker was reached but did not answer within 20000 ms`. Monotonic with load, so the attribution
is sound, but **there is no clean green on record.** Also unresolved: which single test the full
`--project node` run reported as skipped — it is not AOT-03's byte-identity case, which runs.

**Not covered by the reproducibility work:** the 27 elfconv objects in `obj/`. They come from the
containerised gate, not from `build-wasi-llvm.sh`, so their reproducibility is a separate and
currently unmeasured question.

---

## How to rebuild the wasm translator

```bash
bash tools/aot/build-wasi-llvm.sh      # ~10 min from scratch, skips what exists
bash tools/aot/build-wasi-deps.sh      # glog + gflags
bash tools/aot/link-elflift-wasm.sh    # -> $ROOT/elflift.wasm, 8 087 408 bytes
node tools/aot/run-elflift-wasm.mjs --elf <aarch64-elf> --semantics <aarch64.bc> --out out.bc
npx vitest run --project node tools/aot/elflift-wasm-determinism.node.test.ts
npx vitest run --project browser packages/aot/src/elflift-cross-engine.browser.test.ts
```

State outside the repo, in `/Volumes/ProjectsSSD/o2-wasi-llvm/`: `lib/` (40 archives), `obj/`
(27 objects), patched `wasi-sdk/`, `llvm-16/`, `elflift.wasm`, and `fixtures/` — `hello_static`
and `aarch64.bc`, extracted from `ghcr.io/yomaytk/elfconv:arm64`, which is present locally. The
browser spec needs those three staged into `packages/aot/artifacts/` (gitignored).

**`--bitcode_path` is a DIRECTORY, not a file**, despite the name: remill searches it for
`<arch>.bc`. That cost one run.

---

## Do not

- Do not re-add `set -e` to the build scripts. It cost a silent failure and an hour.
- Do not make `rm -rf build-wasi` unconditional. It threw away every compiled object per run.
- Do not read a bare `MODE` in a script. vitest exports `MODE=test` from Vite; the link script
  died on `MODE must be command or reactor, got test`. It is `LINK_MODE` now.
- Do not declare `sigaltstack`, `getrlimit`, `setrlimit` or `posix_spawn` in
  `tools/aot/wasi-compat`. cmake probes them by compiling AND linking, so declaring them flips
  `HAVE_*` on and compiles code back in. It makes MORE work.
- Do not trust a shell probe with no sanity arm. One inventory reported every identifier missing
  because this shell is **zsh** and an unquoted `$FLAGS` reached clang as a single argument. The
  same trap silently emptied a `grep` list later the same day via an unmatched glob. Control arms
  caught both.
- Do not gate a commit on vitest alone. A commit went in on a red `tsc --noEmit`.
- **Do not infer contention from load average.** It predicted nothing here: at load 9.58 the
  measured CPU share was 0.774, *above* both regimes in which a load-sensitive defect reproduces.
  Use `/usr/bin/time -p` and the derived `(user+sys)/real`. Measure the process, not the machine.
