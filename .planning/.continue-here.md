# Continue here — paused 2026-08-25 02:17 PDT

**Milestone v2.0 "Open the Doors" is open and scoped. Research is done. Requirements and
roadmap are not started.** Nothing is in flight; no agent is running; the tree is clean.

```
branch   feature/milestone-v2.0-open-the-doors   b3704d2   NOT merged
develop  ddefce3 == origin/develop        main  d5f6f01 == origin/main
worktrees: one, the main checkout
```

---

## Where the GSD workflow stands

`/gsd-new-milestone` ran through **step 8 of 11**. Completed: context loaded, goals gathered
from conversation (there was no `MILESTONE-CONTEXT.md` and no seeds), version confirmed as
**v2.0** rather than v1.2 by owner choice, `PROJECT.md` and `STATE.md` written, artifacts
committed, and four parallel researchers run.

**Not done: step 9 (requirements with REQ-IDs), step 10 (roadmap), step 11 (commit).**

The immediate next action is the **research synthesizer** — the workflow's step-8 tail — and
the owner was asked whether to run it and had not answered when work paused. The four
dimension files exist; `SUMMARY.md` does not.

```
Agent(subagent_type="gsd-research-synthesizer", model="sonnet")
  reads  .planning/research/v2.0/{STACK,FEATURES,ARCHITECTURE,PITFALLS}.md
  writes .planning/research/v2.0/SUMMARY.md     ← note the v2.0/ path
```

---

## BLOCKING — read before touching anything

### 1. The research files are under `.planning/research/v2.0/`, not `.planning/research/`

The workflow's own template says to write `.planning/research/STACK.md`. **Do not.** Those
five files are the **v1** research, they are 52–70 KB each, and `CLAUDE.md` cites
`STACK.md:113` by line. Overwriting them breaks a citation and destroys the v1 record. Every
v2.0 research artefact goes in the `v2.0/` subdirectory, `SUMMARY.md` included.

### 2. `phases.clear` was deliberately skipped — do not "fix" this

Step 6 of the workflow says to run `gsd-sdk query phases.clear --confirm`. It would delete
**28 phase directories** — every plan, summary and verification of v1.1 — with **no archive**,
because v1.1 was never closed through `/gsd-complete-milestone` (`latest_completed_milestone`
is `null`, `phase_archive_path` is `null`). Two rows of `REQUIREMENTS.md` cite those paths and
the milestone audit reads them.

The step exists only to stop new `01-*` directories colliding with old ones **when numbering
restarts**, and numbering is *not* restarting. **v2.0 phases continue from 29.**

### 3. STATE.md's reset content was preserved on purpose

`state.milestone-switch` correctly rewrote `## Current Position` and cleared `stopped_at`.
Sampling then showed passages in both that exist **nowhere else** in `.planning/` or `docs/` —
one naming three pending owner rulings. Both are preserved verbatim inside
`## Accumulated Context`, under two `<details>` blocks dated 2026-08-25. Re-sampled after:
**0 of 60** long lines missing.

Do not tidy those blocks away. They are the only copy.

### 4. Do not produce a fifth wrong answer about provider-record expiry

`RFC-0003-RESPONSE-04` §8 records **three** wrong answers in a row about the same number —
48 hours, then 24, then "never" — and corrects them. This session produced a **fourth**, in
`PROJECT.md` and in the owner-ruling consult, and it is corrected in place at `b3704d2`.

What is true, and is checkable:

- The provider **store** ignores `providers.provideValidity` and `cleanupInterval` — declared
  in the public options type, spread into the constructor, read by nothing. *This part of the
  measurement stands.*
- `reprovide.validity` **is** honoured, and **this project already sets it**.
  `providerRecordPolicy` (`packages/libp2p/src/constants.ts:316`) derives sweep interval and
  republish threshold from one validity of **one hour**; both tiers pass it
  (`fabric-node.ts:2154`, `browser-node.ts:1539`); `packages/node/src/provider-expiry.node.test.ts`
  reads the result across two processes (NET-06).

So the v2.0 requirement is **port, not build**: drive the same policy from a Durable Object
alarm, because `setInterval` cannot fire reliably on workerd; and add the sweep the
`/o2/<nodeKey>` value records still lack. The read-time half is already satisfied by
`verifyCapabilityRecord`'s `expiresAt` check through `DhtRecordIndex`.

---

## Decisions taken this session — settled, do not re-open

| Decision | Where recorded |
|---|---|
| **Open source, monetization for commercial use added later** | `PROJECT.md` Key Decisions |
| **No CLA** — "rely on the civilized world"; the no-contributions row stands as written | `PROJECT.md` Constraints + Key Decisions |
| **Milestone is v2.0**, not v1.2; `v1.2-CARRIED.md` renamed with its four references | `v2.0-CARRIED.md` |
| **Public run is inside the milestone**, with **no disclosure gate** — the owner decides timing | `PROJECT.md` milestone text |
| **Multi-region relay from the start** | Key Decisions |
| **TURN and a relayed connection, two fallback rungs** | Key Decisions |
| **All four carried items ride along** — BENCH-06, NET-03, AOT-03, AOTW-06 | `v2.0-CARRIED.md` |

**The disclosure question is closed twice over** and must not be raised again: the repository
has been public since **2026-07-26** (recorded in `PROJECT.md` Constraints — it was raised in
this session without reading that, which was wrong), and the open-source ruling settles it
outright. `DEMO-04`'s guard still passes but its stated rationale is spent; whether it is
retired, repurposed or kept is an **open decision**, not a rule.

---

## Open questions the requirements author must resolve

These came out of the research and each one changes a requirement:

1. **Can wrangler's `alias` redirect one deep file inside `@chainsafe/libp2p-noise`**, or only
   whole packages? This is the fix for the `diffieHellman` gap. Needs a real build test —
   it is the difference between a config line and a patched dependency.
2. **Is Workers KV's ~60 s global propagation acceptable for the kill switch**, or must the
   push-over-open-socket path be built in the same phase rather than later?
3. **Telemetry: consent or legitimate interest.** Contested *across the sources the researcher
   checked*, not merely unresolved. Flagged for legal review. The engineering recommendation
   is to piggyback on the CPU-use consent gate the milestone already requires, not a second
   banner.
4. **Where TURN is hosted** — unmeasured in both consults. Cloudflare Realtime TURN is the
   research's recommendation ($0.05/GB, 1000 GB/month free) on the grounds that the project
   already committed to that edge.
5. **Telegram's in-app WebView** — likely to kill sessions on backgrounding and to diverge on
   IndexedDB and WebRTC. **Verify on real iOS and Android Telegram links before recruiting.**
   Recruitment is *from Telegram*, so this is a precondition of the run, not a detail.

## Two things the research says are structural, not nice-to-have

- **Runaway Durable Object cost, and the qualifier matters.** The often-cited figure —
  $34,895 over 3–11 April 2026 — comes from **one self-report on Hacker News** (`thewillmoss`,
  31 points, 4 comments, no billing response from Cloudflare at time of writing). Read at the
  source: the bill was for **row reads at ~930 billion/day**, not for alarm invocations, and
  the cause was an unconditional `setAlarm()` **times 60+ preview deployments** — a CI hazard
  as much as an alarm one.
  **The part that is independently corroborated and matters more than the number: Cloudflare
  has no spending limits.** A second, unrelated report ($8,846, two looping objects) says the
  same. So: an interval floor, a billing alert before launch, and note from Cloudflare's own
  docs that `setAlarm` *overrides* rather than stacks, while a throwing `alarm()` handler
  retries **six** times with backoff.
- **The hosted tier becoming load-bearing is the median outcome**, with IPFS's own measured
  cloud reliance and Matrix's homeserver dominance as precedents. The prevention is two
  counters — P2P-carried bytes versus relay-carried bytes — and a scheduled relay-kill drill.

## Conditions of entry for the public run

From `FEATURES.md`, and stated there as a literal gate on the first Telegram invite: consent
gate, always-on indicator, hard stop control, plain-language disclosure, kill switch, minimal
status page, data-cost disclosure. Everything else can follow after invites go out.

Rejected as anti-features **with reasons**, so nobody re-proposes them: leaderboards, any
payment or token framing, and adaptive intensity that ramps with user inattention.

---

## What is measured and does not need re-measuring

`.planning/consults/2026-08-24-cloudflare-as-a-fabric-node-measured.md` — 20 sections, every
figure taken against a **deployed** worker rather than `wrangler dev`, which diverges on the
two questions that mattered most. Highlights a planner will need:

- A Durable Object is a **dialable libp2p peer** over WSS and a working **Circuit Relay v2
  server**; peer B reached peer A only through it and pinged in 54 ms.
- One object held **599 simultaneous connections**; the ceiling found was the test machine.
- **50 subrequests per invocation, cumulative** — but traffic on an already-open socket is
  free, so a warm pool is not bound by it.
- The relay enforces its data limit **bidirectionally**: **64 KiB each way, not 128**.
- Runtime WASM compilation is refused; `instantiateStreaming` does not exist there at all.
- `Date.now()` does not advance without I/O. Alarms fire 1 ms late and survive eviction.
- Two listener fields are required and silent when omitted: `direction: 'inbound'`, and a
  remote address from `CF-Connecting-IP` — without the second, libp2p rate-limits the whole
  internet as one host to five connections per second.

---

## Resume

```
/gsd-new-milestone            → do NOT re-run; it would redo steps 1–8
```

Continue from step 9 instead. In order:

1. Run the synthesizer into `.planning/research/v2.0/SUMMARY.md`.
2. Define requirements with REQ-IDs, continuing existing category numbering. Read
   `FEATURES.md`'s conditions-of-entry list as a gate, and `PITFALLS.md`'s phase mapping.
3. Roadmap, **phases numbered from 29**.
4. Merge `feature/milestone-v2.0-open-the-doors` → `develop` → `main` and push. It is two
   commits and is not merged yet.
