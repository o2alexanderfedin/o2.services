---
context: default
milestone: v1.1
phase: 24 CLOSED 1/1 · 16 CLOSED 4/4 · 19 CLOSED 5/5 · 17 DECLINED 2/3 · 22 planned, RUNS LAST
task: five owner rulings; then AOT-06 (one step from a verdict), Phase 22, milestone audit
status: CLEAN — nothing running, tree clean, all branches pushed
last_updated: 2026-08-07T07:50:00.000Z
---

# Continue here — 2026-08-07 ~00:48 PDT

```
feature/phase-18-discovery-capacity-placement  344eaec   IDENTICAL to origin
dirty 0 · nothing running · subagent budget EXHAUSTED (200/200) for that session
```

**This file was rewritten rather than appended to.** Almost everything in the previous
version is superseded — Phase 24 closed, the count moved twice, two of its "open" items
turned out not to be defects. The old text is in git history; nothing was lost.

**`main` and `develop` are ~24 commits behind and were deliberately not merged.** Merging
was not requested and is outward-facing on a public repo.

---

## THE COUNT IS 11 OF 15

| moved | how |
|---|---|
| **16 → 4/4** | carried criterion closed against a destination that landed MET |
| **24 → 1/1** | scored **0/1 in the morning** and **1/1 the same day** — see below |
| **19 → 5/5** | criterion 5 closed on 24's criterion 8 |
| **17 stays 2/3** | **declined**, and the decline is the best result of the day |

Uncounted, each on one PARTIAL criterion: **17 (2/3), 20 (6/7), 21 (2/3)**.
Unexecuted: **22**, four plans, runs last.

---

## PHASE 24 CLOSED, AND THE BOUND IS PART OF THE VERDICT

The 0-of-1 pass refused it on one thing and **wrote down exactly what would change its
mind**: *"'pass-with-a-stated-bound' would be the right disposition if the bound were a
deployment posture an operator can remove. It is not: for the seed there is no knob."*
Four gap-closure plans built the knob. Nothing was rewritten.

**MET carries a stated bound — do not let it be quoted without one.** The default posture
of `bin/agent.ts`, `bin/seed.ts` and `bin/bench.ts` is **open and must be** (19 + 3 argv
sites; `reservation-exhaustion` arm A is a live behavioural guard on it). *"The fabric"*
was read as **a fabric this repository can be deployed and operated as, with a posture
stated on every relay-capable door** — not the default argv. The load-bearing ground:
**both option types make `relayAdmission` required with no default, so the API cannot
express silence.** Only argv has a default.

**The bootstrap paradox is FALSE and was measured.** Enrolment runs over a plain dial with
no reservation in its path. A joiner against a provider pinning a key nobody holds still
gets a valid certificate from it, `relays === []`, provider's `reservations` empty. And
the `reader` reservation 24-04 could not explain is caused by **dialling a peer that
admits everybody** — never on the enrolment path.

---

## PHASE 17 DECLINED, AND THAT IS THE FINDING TO READ FIRST

First time RULING A held against a destination that **actually landed MET**. Criterion 3
is about **mass** creation, and bounded issuance plus *"an unissued identity buys
nothing"* only yields a per-identity price **if one certificate admits exactly one
identity**. That binding is in `fabric-node.ts` — and **no test observed it**. Neutralising
it stayed green across 8 files and 5 runs while the control plant reddened, so the silence
was a fact about the corpus.

**Now guarded** (`8719029`): a borrowed certificate is refused *at the door*, with the
proof running both ways — planting `fabric-node.ts` reddens the new case; planting
`peer-verifier.ts` leaves it green **while reddening peer-verifier's own row**, which is
what stops the green being vacuous. Ledgered as `M68`.

**17 is therefore re-verifiable and may now close at 3/3.** Nobody has re-run it.

---

## FIVE OWNER RULINGS OWED

1. **The wider reading of "the fabric."** If it means *any* fabric this repo can deploy —
   including default argv — no per-relay mechanism can satisfy criterion 8, and the route is
   `24-CONTEXT.md` open ruling 1: make both binaries **refuse to start** unless a posture is
   stated. Cost measured: 19 + 3 argv sites. **If you take the wider reading, 24 returns to
   PARTIAL and 19 returns with it.**
2. **`**Mode:** mvp` is wrong on 25 roadmap blocks.**
3. **X.509 — ruled ADOPT on 2026-08-06, against the standing recommendation.** Standing;
   what it obliges is that the crypto profile becomes load-bearing (algorithms, SHA-1/weak-curve
   bans, DER canonicalisation, parsing limits, extension sizes, duplicate extensions).
4. **`vitest.config.ts:786` records 923 ms for a spec that measures 20.8–21.2 s.** Re-siting
   pulls the admission fixtures out of the pre-commit fast loop — gate coverage against loop
   speed. The number is no longer silently wrong; the decision is open. (The trade is
   *smaller* than first stated: two of the three files are already above the cutoff.)
5. **`bin/bench.ts`'s orphan-leash exemption** now says it has **no valid ground**. Deleting
   it needs one line — `armOrphanLeash` — in a file no agent owned.

---

## AOT-06 — x86-64. ONE STEP FROM A VERDICT.

Owner: *"We must support x86-64 if possible!"* **The recorded constraint was stale.**

- elfconv's README still says *"Only AArch64 ELF binaries are currently supported"* — **stale**
- `scripts/build.sh` takes `ELFCONV_X86=1` → `CMAKE_ELFCONV_X86_BUILD`, mutually exclusive
  with the AArch64 build
- `tests.yml` carries `arch: AMD64` / `ECV_X86=1`; `build-image.yml` **publishes `:amd64`**
- six x86 PRs merged Feb 2026, incl. *"Activate all x86 semantics files"*
- Remill has supported amd64 for years — **instruction semantics were never the gap**

**BOTH IMAGES ARE CACHED LOCALLY** — `ghcr.io/yomaytk/elfconv:amd64` (5.67 GB) and `:arm64`
(6.08 GB). No re-download.

**Measured:** the image carries `/root/elfconv/bin/elflift`, **emsdk AND wasi-sdk-24.0**, and
a `gcc` that builds a genuine static x86-64 ELF (`Machine: Advanced Micro Devices X86-64`,
`Type: EXEC`, 900,344 bytes). **Use `--entrypoint /bin/bash`** — the default entrypoint is an
emsdk wrapper that swallows stdout.

**NOT established: whether the x86 path emits wasm.** My invocation returned `LIFT_EXIT=0`,
empty output, no artifact. **Do not read that as a limitation** — it is indistinguishable
from a wrong invocation, and it is *also* the exact signature CLAUDE.md records for this
tool. Next: read `scripts/elfconv.sh`'s entry contract (its `setting()` needs PWD's parent
basename to be `elfconv`; it takes `$1` elf and `$2` outdir and defines functions that may
need an explicit call), also try `bin/exe.sh` and `elflift` directly — then **measure the
module, never the exit code**.

If a module appears: widen `packages/aot/src/elf.ts`'s hard-coded `EM_AARCH64` screen to the
machine the **selected target** accepts, carry the machine in the cache key so artifacts
cannot collide (`elf.real.node.test.ts` and `elf-fixtures.node.test.ts` gate on
`arch() === 'arm64'`), and **re-measure both floors** — 5.40 MiB and 43 ms are properties of
elfconv's emitted runtime and machine-state init, and the x86-64 `State` struct is a
different size.

**Owner's larger idea, worth its own phase: build elfconv ITSELF to wasm.** Removes Docker
from the AOT tier and makes translation a *fabric workload* rather than a build-machine
privilege — closing the asymmetry where the fabric can RUN lifted artifacts anywhere but
only PRODUCE them on a Docker host. Favourable: the image already ships emsdk and wasi-sdk,
so it is a new **target**, not a new dependency. Measure, do not assume: module size
(LLVM+Remill), and wasm32's 4 GB ceiling for LLVM passes — Memory64 is reportedly stable in
browsers, **verify against wasi-sdk, not a blog**.

---

## WHAT ELSE IS OPEN

- **Phase 22** (4 plans, 0 summaries) — the reachability guard, runs last. Its requirement
  **WIRE-02 is the milestone's last unticked ID**.
- **#72** milestone audit → complete → cleanup.
- **#99 `SECURITY-PROPERTIES.md`** — Praxis approved RFC-0003's *direction*, explicitly not
  *"security is proven"*, conditional on a six-section implementable contract. **Items 2 and
  6 are already satisfied** by the `Math.min` fold and by this system's existing shape.
- **#50** a tab cannot pin an issuer · **#58** no `--withholds-start-report` flag — feature
  gaps no phase owns. **#61** is a bug in `gsd-sdk` itself. **#84/#85** gap register.
- **#93/#95/#96/#97/#98/#100/#101/#102/#103/#104/#105/#106** all closed this session.

---

## TWO CONVENTIONS BOUGHT THIS SESSION — both now in CLAUDE.md

- **Restore a plant by SURGICAL INVERSE, not `cp`.** `cp` restores a whole file to a
  snapshot; safe only if you were the sole writer for the whole window, which was false
  twice. **And the hunk count is a ONE-WAY test** — more hunks proves another writer,
  **equal hunks proves nothing** (a plant dropped inside a hunk you already had open leaves
  the count unmoved). `cmp` against a pre-plant snapshot is the check.
- **Agents that plant must not run in parallel on shared source.** Disjoint `files_modified`
  is not sufficient: a plan says what will be *edited*, not what will be *mutated and
  restored*. The cost was not a lost edit but a **false security finding** — one agent read
  another's live plant as an intermittent fail-open in the admission gate; refuting it took
  111 executions, a `node_modules` patch, and a reading of the library's call ordering.

---

## STANDING CONSTRAINTS

- **Never trust an exit code you did not read directly.** `EXIT=$?` on the very next line,
  no pipes. **A background task's "exit code 0" is the SCRIPT's, not the command's** — this
  produced a false green twice in one session.
- **Run vitest by project**, never a bare path.
- **A proof that cannot fail is not a proof.** Plant, watch it go red, restore, record the
  observed text.
- **Count a plant's own effect before running it** — an unapplied plant and a green plant are
  identical in a log.
- **Do not create or delete files while a vitest run is in flight** — `bench-attestation` and
  `discover-arm` snapshot `git status --porcelain` around themselves. **Creating an untracked
  file counts, not just `git add`.**
- **Commit with explicit paths**, never bare; verify with `git show --stat`.
- **Never `git commit -F -`**; never `git stash`; never `git checkout --` a file you did not
  write.
- **Do not kill or signal any process you did not start.**
- **Do not run `gsd-sdk query state.*`.** STATE.md's frontmatter now parses (folded block
  scalar) and a guard holds it — **`stopped_at` can no longer be edited by string
  replacement**; the fold rewraps at 96 columns. Parse → edit → re-fold → verify.
- **ALL NODES HAVE EQUAL FUNCTIONALITY. Only discovery differs.**
- **Repo is PUBLIC; disclosure accepted 2026-08-04.** US provisional deadline **2027-07-26**.

---

## LAST FULL SWEEP — 2026-08-06 23:37, quiet tree, every exit read directly

```
tsc      EXIT 0
node     EXIT 0   167 files · 2340 passed | 2 skipped
browser  EXIT 0   249 files · 4134 passed
e2e      EXIT 0    17 files ·   80 passed
perf     EXIT 0     1 file  ·    2 passed
```

Commits after that sweep (`d024d99`, `344eaec`) are `packages/aot/src/elf.{ts,test.ts}` and
docs only, verified by targeted run — `elf.test.ts` 46 passed exit 0, `tsc` exit 0.
