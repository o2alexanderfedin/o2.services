---
context: default
phase: 29-hosted-tier-assembly-and-first-deploy
task: null
total_tasks: null
status: in_progress
last_updated: 2026-08-26T03:47:02.934Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each was discovered through failure **in this session**, not predicted.

- [ ] **A green run is not a proof, and a silent one is worse.** Three errors this
      session were caught by a COUNT moving the wrong way, never by a red test: the
      skip count going `1 → 2` with nothing printed beside it; the perf-suffix literal
      count going `2 → 3` because a COMMENT quoted the literal it was explaining; the
      project file count `209 → 198` that `slow-specs` did not notice. Before trusting
      green, name what would have gone red.
- [ ] **On a SKIPPED vitest test, `console.log` and `ctx.skip(note)` are both swallowed
      by the default reporter.** Only `process.stdout.write` reaches the terminal.
      Measured on vitest 4.1.10. A skip nobody can read is a fail-open.
- [ ] **Never write a measured value carried out of a context summary.** Two premises in
      a subagent brief were false for exactly this reason and would have written a
      number beside an option that has no number in the code. Re-derive with one
      command, or mark it in the brief as unverified.
- [ ] **A column-0 comment inside YAML frontmatter breaks gsd-sdk's parser nesting.**
      It silently lifted a nested key to top level. Indent comments into the mapping,
      and verify with a differential parse before/after rather than by eye.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Guard models structure, not truth | `state-frontmatter` passed 6/6 over `status: completed` on an unstarted milestone and `total_phases: 42` against 13. It checked that keys were PRESENT. Same shape: `slow-specs` reimplemented the config's globs and never read the config back, so it stayed green while modelling a project that no longer existed. | blocking | When a guard checks a field exists, ask what a FALSE value in it would look like and add the cheapest closed-vocabulary or cross-file count check. `opt-in-only-sources` reads the config text back and caught the same change instantly — copy that. |
| Discrimination rule in the message, not the condition | `late-combine` printed "ALL THREE inflated together is a starved host, not this code" and then failed anyway. It could tell the two apart in prose and not in the assertion. | blocking | If a failure message explains which of two causes it is seeing, that explanation belongs in the condition. Move it; do not raise a budget. |
| Skipping on a count instead of on an identity | A count-only skip in `discovery-agents` would hide an issuer-gate regression: a genuine defect drops the same one executor a starved host does. | blocking | Key the discriminator on WHICH item is missing, never on how many. If the print that would say which does not exist, writing it is part of the work. |
| Raising a budget to close a red | Raising the 900000ms hook budget would have addressed 2 of 5 container failures and said nothing to the 3 killed by a signal. | advisory | Measure `(user+sys)/real` first. Below ~0.1 the process is WAITING, so a wall-clock budget over it measures the host and never the code — remove the contention instead. |
| Editing the tree while another agent's specs run | `coverage-agents`, `discover-arm` and `bench-attestation` snapshot `git status --porcelain` around themselves. Editing `.planning/BENCHMARK-RESULTS.md` mid-run reddened one of them. | advisory | Hold edits and the index while any sweep is in flight. Four `tools/aot` specs also read git status. |

<current_state>
Branch `feature/phase-29-hosted-tier`, HEAD `60782c9`, tree CLEAN, ahead of `main` and
not merged. Thirteen commits this session; every one carries its own verification in
its message.

**Phase 29 has ONE of its criteria.** Criterion 3 landed earlier — `packages/cloudflare`,
a hand-written `DoDatastore` binding Durable Object storage to `interface-datastore`.
There is no `.planning/phases/phase-29-*` directory and no PLAN.md; the work so far
landed outside the plan-phase workflow, which is why this handoff is at the default path.

Most of this session was NOT Phase 29. It was an audit-fix run that the owner turned
into test-infrastructure work.
</current_state>

<completed_work>
- Audit findings F-44, F-45, F-46 closed (`8bf7720`, `6c629f5`, `ce0f70c`).
- **The `aot` test lane** (`d251771`): `tools/**` carved out of the `node` project at
  `fileParallelism: false`. Closed Phase 21's W1 by removing contention, not by raising
  a number. Measured — aot alone 11/11 files and 240/240 tests at EXIT=0 in 1181.61s;
  the node lane went 1224.93s → 161.70s, a 7.6x speedup, because its twenty minutes were
  never the 198 package specs but the eleven container specs holding the machine.
- **W1 closed** (`74b4ec7`, `1e8fdba`) with the comparative reading it had lacked.
- **BENCH-06 re-read by hand and re-recorded** (`2434eab`) — three of four clauses
  carried and re-measured green; the hardware premise retired by name in two documents.
- **38 phase goals restated as user stories** (`b37797c`) by owner ruling; Mode markers
  untouched, originals preserved verbatim, five lost clauses named in place.
- **Three test specs given starved-host discriminators** (`ce34171`, `60782c9`) —
  `late-combine`, `coverage-agents`, `dht-registration`. No budget raised; the
  `dht-registration` fix is a single character (`>=` → `>`) and is fail-closed.
- **STATE.md corrected and its guard strengthened** (`8c2703c`) — see anti-patterns.
</completed_work>

<remaining_work>
**The load-sensitive class, ranked by (value)/(risk of hiding a real defect).** A survey
found seven distinct failing assertions across five files; three are fixed. The full
evidence is at `/private/tmp/.../scratchpad/flaky-survey/SURVEY.md` — if that scratch
path is gone, the ranking and reasoning survive in `60782c9`'s commit message.

1. `speculation-agents` — the reconstruction of the scheduler's own judgement sample
   drifts under load, so the reconstructed threshold sits above every elapsed while the
   scheduler is on record having duplicated. The rule is already in the file's prose
   twice. **Needs one plant watched red before implementing.**
2. `discovery-agents` — see the anti-pattern above; the identity print does not exist yet.
3. `admission-agents` clause 3 — `transport.peers` is direction-blind (connected set)
   while the claim is about dialling, and the assertion that carries the clause passed.
   **Cause unverified — measure connection direction before changing anything.**
4. `admission-agents` stand-up — a 30s enrolment RPC budget inside a CHILD process on a
   host that spent 262s on one `it`. Hardest: no in-process observable.
5. `late-combine` third mode — all six cold samples hit the 1500ms timeout UPSTREAM of
   where `ce34171`'s discriminator computes its baseline, so it is never reached.

**Phase 29 proper** — the libp2p node assembly (criteria 2 and 7) and three guards
(criteria 4 one-creation-call-site, 5 no-preview-deployments, 6 closed `idFromName()`
name set). Wiring the assembly also closes the three `OPEN_FINDINGS` rows registered for
`packages/cloudflare`, whose own note says: if Phase 29 closes with those three still
present, that is a finding about the phase.

**`package-lock.json` is stale** — no `packages/cloudflare` entry, so `npm ci` fails.
</remaining_work>

<decisions_made>
- **Vertical slices are mandatory** (owner, 2026-08-25). The 38 goals were restated, not
  the markers removed. I had argued restating was "hours and arguably worse"; the owner's
  one-line counter-example showed the mapping is close to mechanical.
- **W1 closed by serialising, not by a bigger budget** (owner: "do it"). A larger budget
  answers 2 of 5 failures.
- **Slow under load is normal** (owner). Implemented as discriminators moved into
  conditions — never as relaxed thresholds.
- **BENCH-06 re-recorded, not ticked.** Ticking on the half that was never in question is
  the overclaim the register entry exists to prevent.
- **Autonomous execution stops at the Cloudflare boundary** (owner, earlier). Criteria 1
  and 2 stay open and must be REPORTED open.
</decisions_made>

<blockers>
- Phase 29 criteria 1 and 2 — owner acts, open by ruling. Not a defect.
- The distinct-machine run (Phase 39 criterion 4) — the only clause holding BENCH-06
  open. **Not blocked on hardware**; the owner has a few hundred testers across countries
  and continents. Blocked on there being a build to give them. Owner: "будет тебе прогон
  на живых машинах. как только будет что прогонять — так сразу и будет."
</blockers>

## Required Reading (in order)

1. `vitest.config.ts` — the `aot` project's docblock carries the measured case for the
   lane split and the two probe results that settled it (projects do NOT run
   sequentially; `fileParallelism:false` does serialise its own project).
2. `git show ce34171` — the worked example for every remaining item in the flaky class.
3. `.planning/phases/phase-21-.../21-VERIFICATION.md` — W1's `observed_again` and
   `closed` entries: what was measured, and why raising the number was refused.
4. `.planning/STATE.md` frontmatter `stopped_at` — the four false values a writer put
   there and what the guard now checks.
5. `.planning/METHODOLOGY.md` (if it exists) — project analytical lenses.

## Carried forward from the previous handoff — I overwrote it and these are the live parts

**This section exists because of a mistake made writing this file.** The pause workflow
writes `.continue-here.md`, and doing so replaced a 244-line handoff whose content was
still live — including a line reading *"Do not tidy those blocks away. They are the only
copy."* Nothing was actually destroyed: the blocks it protects are in `.planning/STATE.md`
and were verified present after the overwrite. What was destroyed was the POINTER to them,
and the warnings beside it. They are restored here. The previous file remains readable at
`git show 577f72f~1:.planning/.continue-here.md`.

- **Cloudflare, and this is the safety one.** The account carries **zero Durable Object
  namespaces**, so no object created by this work can wake or bill. Only the owner's three
  `ocr-checks-worker*` scripts exist and they are untouched throughout — **do not touch
  them, they are production**. Nothing is ever deployed without explicit approval; there is
  no hard spending ceiling on Cloudflare, and the owner has said so in plain terms.
- **Do NOT write `.planning/research/STACK.md`.** The new-milestone workflow's template says
  to. Those five files are the **v1** research at 52–70 KB each, and `CLAUDE.md` cites
  `STACK.md:113` by line. Overwriting them breaks a citation and destroys the v1 record.
  Every v2.0 research artefact goes under `research/v2.0/`, `SUMMARY.md` included.
- **`phases.clear` was deliberately SKIPPED — do not "fix" it.** The 28 directories under
  `.planning/phases/` are v1.1's. The workflow offers to clear them only to stop new `01-*`
  directories colliding when numbering restarts, which is not happening here — v2.0
  continues from 29. Clearing would delete every plan, summary and verification v1.1 has.
- **The two `<details>` blocks in `.planning/STATE.md` under `## Accumulated Context` are
  the only copy** of v1.1's `stopped_at` and `Current Position`, preserved at the v2.0
  switch. One of them names three pending owner rulings. Verified still present after this
  session's STATE.md edits. Do not tidy them away.
- **Do not produce a fifth wrong answer about provider-record expiry.**
  `RFC-0003-RESPONSE-04` §8 records THREE wrong answers in a row about the same number — 48
  hours, then 24, then "never" — and a fourth was produced and corrected at `b3704d2`. Read
  what is recorded there before stating the number again.
- **Fifteen v1.1 phase checkboxes read `[ ]`** while their directories hold plans, summaries
  and verifications. Deliberately not ticked, twice, and the distinction is recorded: a
  phase checkbox records a verdict owned by a VERIFICATION file's score; a plan line records
  that a plan landed, evidenced by its SUMMARY. Ticking the first to tidy would widen what
  counts as passing.

## Infrastructure State

- Docker daemon: answering. The eleven `tools/aot` specs run against it and are green
  when run as their own lane; five of them fail together if run inside the node project.
- Test lanes: `npm run test:node` (198 files, ~2.7 min), `npm run test:aot` (11 files,
  ~20 min), `npm run test:node-lanes` (both, in series). **`test:node` no longer reaches
  the container toolchain** — that is why `test:aot` exists.
- No servers or watchers left running.

<context>
The single recurring shape this session, worth carrying forward: **a guard that models
structure and not truth.** A citation that parses and points nowhere. A frontmatter key
that is present and false. A discrimination rule written in a failure message but absent
from the condition. A reimplementation of a config that was never read back. Every one
of them passed its own checks while being wrong.

Two working habits that paid for themselves. **Rival subagents** — one briefed to refute
the other — caught three errors in my own briefs, none of which reached a file; a
cooperative pair would have written all three. And **planting the defect and watching it
go red**, rather than trusting green: it is how the `dht-registration` fix was shown to
stay fail-closed, and it is how both new STATE.md checks were shown to be able to fail
at all.
</context>

<next_action>
Start with: `git show ce34171`, then `packages/node/src/speculation-agents.node.test.ts`.
Plant a broken `stragglers()` predicate FIRST and watch what reddens — the argument that
the upstream assertions catch it before the reconstruction does is reasoned, not
measured, and implementing the discriminator without that plant would be exactly the
fail-open this session spent the day finding.

If instead resuming Phase 29 itself: the libp2p node assembly (criteria 2 and 7), which
also closes the three `OPEN_FINDINGS` rows for `packages/cloudflare`.
</next_action>
