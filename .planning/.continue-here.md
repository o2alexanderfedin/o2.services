---
context: default
milestone: v1.1
phase: 13-egress-manifest-completeness
task: null
status: phase_13_unverified
last_updated: 2026-07-27T18:00:00.000Z
---

# START HERE — Phase 13 is marked done and was never verified

**Do this first.** `ROADMAP.md` shows `[x] Phase 13`, and `DATA-05`/`DATA-06` are `[x]` in
`REQUIREMENTS.md`. There is **no `13-VERIFICATION.md`**. No independent pass has re-run
its three criteria; the checkboxes rest on the executors' own SUMMARYs.

That is precisely the defect this milestone exists to remove, and it is now sitting in the
milestone's own ledger. Either verify Phase 13 or un-check it. Do not build Phase 14 on top
of it.

```
Skill(gsd-verifier)  →  .planning/phases/phase-13-egress-manifest-completeness/13-VERIFICATION.md
```
Model the prompt on the Phase 12 verifier brief: tell it to **find the next defect, not
confirm the last two were fixed**, and require it to plant the mutations itself. That
framing is what caught the dropped plan in Phase 12.

---

# BLOCKING CONSTRAINTS — read before proposing anything

Each was learned through a failure or an explicit owner correction.

- [ ] **ALL NODES HAVE EQUAL FUNCTIONALITY. The only difference is discovery.**
      Stated by the owner **four times**; `RelayNode` was deleted over it. If a decision
      keys on *what kind of node* something is, it is wrong. Grep for the mechanism, never
      the vocabulary. `canExecuteSovereign` is a per-node clearance for one owner's data —
      **not** a node class, and it must not become one.

- [ ] **DEMO-04: no deploy workflow file may exist in the repository at all** — absent, not
      disabled. Enforced by `disclosure-gate.node.test.ts`, which checks by content so
      relocating a file does not evade it.

- [ ] **Vocabulary discipline**, enforced repo-wide by `vocabulary.node.test.ts`, which
      scans `.planning/` too. **Read the `BANNED` array from that file; never enumerate it
      in prose.** A plan that quotes the prohibition violates it — Phase 11 lost a suite
      run to exactly that, and the fix was to reword, not to exempt.

- [ ] **Do not reintroduce static determinism analysis.** Built, hardened, fuzzed, deleted
      (1,214 lines). Divergence is *detected* by byte comparison, never predicted.

- [ ] **Do not write an allow-list for host imports.** `WebAssembly.instantiate` is the
      sandbox and names the offending import.

- [ ] **The kernel must never require `crypto.subtle`.** A LAN origin is not a secure
      context. Hashing is `@noble/hashes`, pure JS.

- [ ] **Never trust elfconv's exit code.** It is `0` on binaries it could not fully
      translate.

- [ ] **Background agents share ONE working tree, not worktrees**, despite
      `use_worktrees: true`, and they can outlive the session that spawned them by hours.
      **Never `git checkout --` a file you did not write** — a planted mutation and an
      abandoned regression look identical in `git diff`. I reverted a live agent's
      in-flight mutation this session; it survived on timing alone. Also: `git commit`
      without `-a` still commits whatever is **staged**, and another agent's staged
      deletion rode into one of my commits unannounced.

<current_state>

```
milestone   v1.1 — Wire What Was Built · 3 of 12 phases
tests       122 files · 1775 passing   (v1.0 closed at 112 / 1673)
tsc         --noEmit clean, now including packages/browser/demo/**
branch      develop afe4484 == origin/develop · tree clean · no stashes
main        4e76c27 — deliberately left at v1.0, 50 commits behind
```

| Phase | Verified | State |
|---|---|---|
| 11 — Explicit serveAgent Hook Contract | **3/3** | done |
| 12 — Sovereignty-Pinned Placement | **4/4** | done |
| 13 — Egress Manifest Completeness | **none** | ⚠ marked done, unverified |
| 14 — Signed Artifact Resolution | — | `14-CONTEXT.md` written, ready to plan |
| 15–22 | — | not started |

**Requirements closed (7 of 44):** WIRE-01, DATA-03, DATA-04, DATA-05, DATA-06, DATA-07,
DATA-09. The last two are the ones resting on Phase 13's unverified state.
</current_state>

<the_finding_that_should_shape_the_rest_of_this_milestone>

**Three consecutive phases shipped a mechanism with zero callers.** Not bad luck — a
structural property of how the plans are written. Each ends at "mechanism built and
tested"; "reach it from an entry point" keeps falling outside the task list.

| Phase | Symbol | Callers when reported done |
|---|---|---|
| 12 | `guardSovereignty` | 0 |
| 13 | `submitJobWithEgress` | 0 |
| 13 | `EgressGuard.guard()` | 0 (pre-existing, found by scouting) |

All three were caught by hand. **The structural fix is Phase 22's reachability guard
(WIRE-02), and it was scheduled last** — it would confirm at the end what I kept finding
throughout.

**Decision taken, not yet executed: pull WIRE-02 forward and run it next**, in Phase 14's
slot, leaving numbering alone so requirement IDs stay stable. Eight phases remain; a guard
that fails the build the moment an exported capability has no entry-point path is worth
far more before them than after.

**Exemption design matters more than the guard.** Some exports are legitimately unreached.
A guard with no exemption mechanism gets disabled the first time it is inconvenient; one
with unaudited exemptions becomes the Phase 10 NUL-byte hole. Copy `vocabulary.node.test.ts`'s
`EXEMPT_LINES`: each exemption is data, carries a written reason, and **a dead-exemption
test fails when one stops being needed**.
</the_finding_that_should_shape_the_rest_of_this_milestone>

<what_landed_this_session>

**Phase 11 — serveAgent's six hooks became required.** Each is now
`T | '<named-absence>'`, so a node serving without an authorizer writes
`authorize: 'serves-unauthenticated'` rather than omitting an argument. Follows Phase 9's
recorded pattern (*consent is a value, not a check*). Two guards, both mutation-proved:
`@ts-expect-error` per hook (weakening the contract yields `TS2578: Unused directive`), and
a sentinel-count guard asserting **both** that each sentinel appears *and* that it is absent
where a real hook is supplied — so swapping `fabric-node`'s real `reservations` thunk for
the sentinel fails.

**Phase 12 — sovereignty on the live job path.** `submitJob` places through
`planPlacement`/`eligibleNodes`; `executorsFor` and `not-enough-executors` are gone; 33 call
sites swept. Two corrections I had to force mid-phase: `guardSovereignty` had no production
caller, and it **failed open** on an absent label (fixed at `parseRequest`, since the wire is
the trust boundary). Default clearance is cleared-for-nobody.

**Phase 13 — the egress tap now watches something.** `EgressGuard.guard()` gained its first
production caller (`registerSovereignInputs`), and all three job-submitting entry points
emit a manifest.

**Two fixes outside any phase:**
- `packages/browser/demo/main.ts` — one of the five runnable entry points — was **outside
  the root tsconfig include and had never been type-checked**. Zero errors surfaced; the
  point is the next one gets caught.
- `code-cache.e2e.test.ts` stated its control measurement three incompatible ways
  ("over a megabyte" / "464 KB" / an assertion floor of 500,000). AOT-05 is published as a
  measured negative and its whole weight rests on that control. Reconciled to the recorded
  2,078,297 bytes.
</what_landed_this_session>

<remaining_work>

1. **Verify Phase 13** (above). Blocking.
2. **Run the reachability guard** (WIRE-02, pulled forward from Phase 22).
3. **Phase 14 — Signed Artifact Resolution.** `14-CONTEXT.md` is written and carries two
   findings worth acting on:
   - The resolver must check **`record.cid === task.moduleCid`**, not merely that the
     signature verifies. Otherwise a validly-signed record for a *different* module
     satisfies resolution and the signature buys nothing. `ResolveFailure` has no variant
     for that mismatch yet.
   - DET-03 has no public-only exemption, so the demo's own colouring kernel is in scope and
     currently ships unsigned.
   - Phase 14 and Phase 21 are **different, composing** concepts: `translationCid` is an
     unsigned build-time reproducibility key whose output CID later becomes eligible for a
     signed name. Phase 21 must not invent a second trust mechanism.
4. **Phases 15–22.**

## Browser-tier debt — three gaps, all routed to Phase 19's WIRE-03

Scope that phase to **discharge** these, not inherit them:
- `BrowserNode.start()` has no dedicated runtime test anywhere (found in Phase 11)
- `BrowserNode`'s `guardSovereignty` wiring has zero runtime proof — `grep` for "sovereign"
  across `packages/browser` and every `*.e2e.test.ts` returns nothing (Phase 12)
- `BrowserNode.egress` likewise unproven at runtime (Phase 13)

Common cause: `BrowserNode.start()` needs real IndexedDB and a relay dial, so it runs in
neither vitest project.
</remaining_work>

<decisions_made>

- **The absence must be a value, not an omission.** `hook: T | 'named-absence'` beats
  required-`| undefined`, which records nothing. Extends Phase 9's `GrantedConsent`.
- **The wire is the trust boundary.** `Task.label` stays optional at the port type (~65 raw
  literals in unrelated tests) and is enforced at `parseRequest`, where untrusted input
  enters. Verified as genuinely closing the hole, not relocating it.
- **Do not change the `Executor` port.** Sovereignty correlates by `nodeId`; `JobSpec` gained
  `nodes: NodeDescriptor[]`. Phase 2's rule: if it can be an adapter behind an existing port,
  it must be.
- **`@o2/core` must not import `@o2/net`.** Registration lives in `@o2/net`, which already
  owns `EgressGuard` and imports core.
- **Register sovereign bytes from the LOCAL-ONLY blockstore.** Fetching bytes over the
  network in order to declare them un-sendable is self-defeating.
- **An empty manifest is not a clean run — it is an absent instrument.** Every cleanliness
  assertion pairs `violations == []` with `entries.length > 0`.
- **Separate the implementer from the falsifier.** Phase 13's proofs went to a different
  agent than its code, against the recorded defect *fixtures written from the same
  understanding as the code*. That agent found the criterion-2 gap.
- **`main` stays at v1.0** until the v1.1 audit. Promotion is a release decision, not a
  preservation one.
</decisions_made>

<blockers>

**Nothing blocks the code.** Three items need the owner:

1. **US provisional patent — the only hard deadline, 2027-07-26.** EPO and China are already
   permanently forfeit.
2. **A hosted relay with AutoTLS** — closes NET-03 and is what a Pages deployment needs to
   function at all.
3. **GitHub Pages serves the pre-Phase-9 bundle** (`gh-pages` at `db8ddf3`). Republishing is
   a deliberate human act under DEMO-04.

**Struck 2026-07-28: "a second machine" was a fourth item here and is not a blocker any
more.** The owner ruled that same machine — different browsers and/or different browser
contexts, and different OS processes — is the project's testing standard everywhere, so no
criterion of the project's own waits on hardware. BENCH-06 and AOT-03 were rewritten to
what one host genuinely establishes. **The risk it named is not closed: cross-machine
reproducibility and distinct-machine benchmarking are unverified by choice and remain
unmeasured — descoped is not satisfied, and neither may be reported as demonstrated.**
`CROSS_MACHINE_BLIND_SPOT` stays attached to every lifted artifact, and the same-machine
benchmark label stays required and derived.
</blockers>

## Environment notes

- **Node 23.11.0, not LTS** — every install prints `EBADENGINE`. Everything passes.
- Test suffixes are load-bearing: `*.browser.test.ts`, `*.node.test.ts`, `*.e2e.test.ts`.
- A full `vitest run` is **~5 minutes**; the AOT tests really do drive two Docker
  containers. Not hanging.
- Real-process tests spawn via `spawn(process.execPath, [AGENT, ...])` —
  `two-process.node.test.ts` and `sovereignty-placement.node.test.ts` are the precedents.
- `bin/agent.ts` now takes `--owner-id` and `--can-execute-sovereign`; omitting them keeps
  the safe default (cleared for nobody).
- A reported flake in `colouring-demo.e2e.test.ts` under Playwright contention **did not
  reproduce** in four full-suite runs today.

## Calibration

Every real defect this session came from **checking a claim against the repository**, never
from reading source or trusting a report. An executor reported the suite green while it was
red at 5 failures. A plan cited evidence that did not exist. Three phases shipped unwired
capabilities. The agents do good work and their self-reports overstate it — budget for
verification as a first-class step, not a formality.

## Resume

```
/gsd-resume-work
```
Then verify Phase 13 before anything else.
