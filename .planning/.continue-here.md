# Continue Here

**Paused:** 2026-08-16 01:45 PDT
**Branch:** `feature/flag-ruling-and-open-rows` — 7 commits ahead of `main`, pushed, tree clean
**`main`:** `ee64f24` · **`develop`:** `449a63f` — both level with origin, identical trees
**Worktrees:** **1 STILL OPEN AND STILL WORKING** — see the warning below

---

## ⚠️ READ FIRST — there is a live worktree with uncommitted work

```
.claude/worktrees/agent-aeafd2b44e50f8345   [worktree-agent-aeafd2b44e50f8345]  locked
```

Its HEAD is still `ee64f24`, i.e. **the agent had committed nothing when the session paused**, and
its branch is **not pushed**. Do not `git worktree remove` it and do not delete the branch — that
would discard whatever it has in its working tree.

**What it was doing:** fixing a **pre-existing** failure in
`packages/node/src/seed-discovery.e2e.test.ts:188`
(*"discovers the relay from a non-secure LAN origin and reserves on it"*):

```
TypeError: Cannot read properties of undefined (reading 'digest')
```

**The hypothesis it was given, which is probably right but was NOT yet confirmed:** on a non-secure
origin — `http://` on a LAN IP, exactly what that test drives — `globalThis.crypto.subtle` is
`undefined` while `crypto` itself exists. So `crypto.subtle.digest(...)`, or a default parameter
like `subtleCryptoBackend(subtle = globalThis.crypto.subtle)` followed by a `.digest` call, throws
precisely this. The repository already designs against this hazard elsewhere —
`packages/aot/src/wasi-executor.ts:278` says its hashing is *"pure JS, no `crypto.subtle`, so a LAN
origin that is not a secure context still"* works — so a surviving `subtle` call on a
browser-reachable path is a gap rather than a quirk.

**It was told not to fix it blind:** reproduce first, and if it does not reproduce, say so and stop.

To resume it, send it a message rather than starting over — it still has its context and its tree.

---

## The headline: the work list was over half stale

A 5-agent read-only sweep verified all eighteen items against the code before anything was built.
**Nine were already done and merged.** Building them again would have been pure waste, and this is
the second session running in which a stale list nearly caused rework.

| item | reality |
|---|---|
| MR-03, MR-05, MR-07 | **done** 2026-08-14 in `4b211b1` |
| the five false "entry-point-not-driven" reason codes | **done** — same commit |
| WIRE-02 "owner decision" | **already decided and shipped** 2026-08-14 — not an open decision |
| node timing table | **done** in `ee64f24` |
| NET-06 "install a DHT — there is none" | **done** in `ee64f24`; real remainder is narrower |
| AOT-03's first half | **already done and recorded** — verified, not rebuilt |
| AOTW-06's "treat 27/27 as unreproduced" | **already done** |

---

## Landed this session (7 commits, pushed)

| commit | what |
|---|---|
| `473b6d0` | the flag ruling recorded, and MR-04's publish-vs-derive argument |
| `6f16c58` | **AOT-04** — a real Chromium tab executed a real 5.6 MB elfconv-lifted artifact |
| `c01a996` | **BENCH-06** process ladder + **NET-03** seed-join — measured, *no row closed* |
| `45e991d` | **CHURN-03 + CHURN-04** closed on the default path, no flag |

### AOT-04 — the router plant was proved BOTH ways

Forcing the native arm reddened the lifted case (`Import #0 "wasi_snapshot_preview1"`); forcing the
WASI arm turned the lifted case **green** and reddened the source-compiled case (`Import #0 "o2"`).
The second plant is the one that separates *"the router chose"* from *"one arm answers everything"*.
Fixtures were present, so the cases **ran** rather than skipping.

**Its box is still `[ ]` on purpose.** The row now says the open conditions are about the *evidence*
— the artifact is gitignored, and it was one engine rather than the three-engine browser matrix —
not about a missing measurement. Ticking is the phase owner's call.

### CHURN — two judgements better than the instructions given

`checkpointsInto` was put in **`core`, not `browser`**, because its whole act is a read-back through
the `Blockstore` port; in the browser package the only durable sink would be unreachable to every
other tier. And its `publish` **reports rather than throws**, because it is awaited inside a single
serialised promise chain — a throwing sink would turn one evicted IndexedDB block into a dead job,
and eviction is the *expected* case in a tab.

Verified by hand at the merge, because a ceiling was raised and I had refused to raise one earlier:

```
packages/browser/demo/main.ts:1032    checkpoints: checkpointsInto(node.store),
packages/browser/demo/main.ts:1000    admit: rpcAdmission(node.rpc),
```

Both are real production call sites with **no flag**. The reachability ceiling raise (67→68, 43→44)
is therefore a **disposition** — the tracer cannot follow the `window.o2` assignment, the same blind
spot 27 existing `GLOBAL_OBJECT_HOP` entries carry — not a threshold widened to admit an unwired
symbol. That distinction is the whole reason the raise is acceptable here and was not earlier.

### BENCH-06 — it measured its own unreliability and refused to report a curve

Three rungs of 1/2/4 real spawned `bin/agent.ts` processes:

| run | load | 1w | 2w | 4w | ratios | (user+sys)/real |
|---|---:|---:|---:|---:|---|---:|
| A | ~39 | 19.4 | 22.4 | 17.5 ms | 1.000 / 1.156 / 0.903 | 1.86 |
| B | ~40 | 18.4 | 15.8 | 24.2 ms | 1.000 / 0.856 / 1.314 | 1.85 |
| C | ~126 | 54.4 | 64.8 | 82.9 ms | 1.000 / 1.191 / 1.525 | 0.81 |

**Between-run variation exceeds between-rung variation, so it asserts no trend.** Run C was taken
under an unrelated LLVM build and every absolute figure roughly tripled — the ratio-over-milliseconds
argument, made by measurement rather than assertion.

Labelled exactly: `SAME-MACHINE: N nodes on 1 host`, separate processes and address spaces, real TCP
**on the loopback interface** (`bin/agent.ts` binds `127.0.0.1`, never `0.0.0.0`) — not separate
machines, clocks, OS builds or a physical network path.

---

## Owner decisions waiting on you

1. **MR-04** — publish the contribution set on a frame, or let both sides derive it? A reword is
   already drafted in the tree. `.planning/consults/2026-08-16-mr-04-publish-or-derive-the-contribution-set.md`
   argues both sides. The finding that reframes it: a published shard→node manifest **is** a map of
   which node holds which owner's data, in a fabric whose core claim is that raw data never leaves
   its owner's device. Meanwhile the strongest generic argument *for* publishing — dispute
   resolution — is largely moot on the sovereign path under the 2026-08-14 threat-model ruling.
   Every clause of MR-04's code claim was verified: `combineId` crosses the wire, is validated for
   shape, then discarded at `capability-authorizer.ts:100`. It is not the slot key, not signed, not
   echoed. And its requirement text is **false twice** — the sort is by contributor string, and the
   projection closure never crosses a wire.
2. **AOT-02** — needs Docker's storage driver switched from containerd to the classic image store.
   That is an operator action on your daemon affecting your existing images. Not started deliberately.
3. **AOT-04's box** — tick it or not, given the two evidence caveats above.

---

## Not started, and honestly so

- **AUTH-02** — verified **PARTIAL**, and the task list's claim about it is **false**. Re-read the
  verification before building.
- **AUTH-04** — a threat model and measurements exist; **no sizing**. That is the actual gap.
- **AOTW-06** — an LLVM-from-source build. Long, not impossible. It was wrongly called "blocked".

**A correction worth carrying:** five rows were classified "blocked on hardware" and the owner
rejected that — *"You can run your privately-reachable host here… Use as many processes and browser
instances as you need."* He was right. Only two things are genuinely unobtainable on one private
host: a real Let's Encrypt certificate (needs public DNS + inbound reachability), and a *label*
saying cross-machine on a one-machine result (`vitest.config.ts:675-685`). **And use Node processes,
never Docker containers — containers are too heavy for this machine.**

---

## Carried defects, none of them diagnosed

- **`tools/aot/fixtures/elf/` is gitignored** (`.gitignore:63`), so four AOT specs
  (`elf-fixtures`, `elfconv-differential`, `elflift-wasi-gate`, `lift`) fail in any **fresh**
  worktree while passing in the main checkout. Anyone cloning this repo cannot run them.
- **Two flakes, reported as observations rather than diagnoses**: `speculation-agents.node.test.ts`
  (passes alone 6/6; spawns real processes) and `colouring-surface.node.test.ts`'s eighteen-regions
  case (failed twice, then passed nine consecutively).
- **`demo-pi.e2e.test.ts`** remains a known flake from earlier sessions, cause not found.

---

## Owed before merging to `main`

A **full sweep has not been run since `45e991d`**. The individual agents ran their own specs and one
ran the full node project (2832 passed), but nothing has verified the merged tip across all four
projects. Do this first:

```
npx vitest run --project node
npx vitest run --project browser
npx vitest run --project e2e
npx tsc --noEmit
```

Check the host is quiet first (`uptime`, `ps -Ao pcpu,comm -r | head`) — an LLVM/clang build farm
runs on this machine for hours at a time and starves runs to a `(user+sys)/real` around 0.5, which
produces reds that are contention rather than code. **Poll until it exits; never signal it.**

---

## Resume

```
/gsd-resume-work
```

**Order:** finish the live worktree agent first (message it, do not restart it) → full sweep → merge
to `main` and `develop` → then AUTH-02 / AUTH-04 / AOTW-06.

**Do not** rebuild anything in the stale table above without reading the code first. Nine of
eighteen items were already done, and that was only discovered because the sweep was run *before*
any building started.
