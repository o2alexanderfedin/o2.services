---
context: default
milestone: v1.1
phase: 13.1-node-side-admission-and-transport-bounds
task: null
status: blocked_on_usage_limit
last_updated: 2026-07-29T08:30:00.000Z
---

# START HERE — hard block until 2026-07-30 16:00 America/Los_Angeles

**The weekly usage limit is exhausted.** Seven repair agents died on it mid-run. Nothing else
blocks this project — no failing test, no unmerged work, no corrupted file. Do not try to work
around it by running more agents; they will fail the same way and burn context reporting it.

```
develop   0263789    tree clean    124 files / 1801 tests passing    tsc clean
main      c62bae5    == origin     carries v1.1 phases 11-13
```

## The one thing to do first when the limit resets

Re-run the repair. All seven of its agents errored, so **nothing is cached** and
`resumeFromRunId` would re-run from scratch anyway — just launch it fresh:

```
Workflow({scriptPath: "<session>/workflows/scripts/v11-plan-strip-repair-wf_58026ea1-212.js"})
```

Its dossiers are still on disk at `<scratchpad>/repair/phase-{13.1,14,16,17,21,23}-REPAIR.md`.
**If the scratchpad was cleaned, regenerate them from
`<session>/tasks/w0jwofkhk.output`** — that file holds the strip verification with every
`lostContent` and `survivingContradiction` entry verbatim.

---

# WHERE THE PLANS ACTUALLY STAND

Seven dependency-ready phases were planned, adversarially reviewed twice, then stripped.

| Phase | Plans | State |
|---|---|---|
| 14 — Signed Artifact Resolution | 5 | **stripped clean, ready to execute** |
| 15 — Capability-Chained Dispatch | 4 | **stripped clean, ready to execute** |
| 13.1 — Node-Side Admission | 5 | needs repair: 4 restores, 5 dangling refs |
| 16 — Tree-Reduce Wiring | 4 | needs repair: 5 restores, 5 dangling refs |
| 17 — Node Identity & Enrollment | 5 | needs repair: 6 restores, 8 dangling refs |
| 21 — AOT Signing & Runtime | 5 | needs repair: 0 restores, 5 dangling refs |
| 23 — Multi-Process Benchmark | 5 | needs repair: 1 restore, 2 dangling refs |

**Phases 14 and 15 can be executed without waiting for the repair.** They stripped clean with
zero lost content and zero surviving contradictions. If budget is tight, start there.

## What the repair must do

1. **Restore 16 over-deleted facts** from `git show 2bcfb0d:<path>`. The strip was told to
   remove derived quantities and took qualitative facts with them. The distinction it missed:

   - *stays deleted* — "the worst case is SHARDS × redundancy = 32" (arithmetic producing a
     claim about runtime behaviour)
   - *must come back* — "`submitJob` runs every shard through one `Promise.all`
     (`submit.ts:206`) and `executeVerified` runs every replica through another
     (`verify.ts:149`)" (a structural fact read from source, and the entire justification for
     why an admission bound is needed)

   Also lost: a dated owner ruling, a disclosure record naming what a design does *not*
   protect, rejected alternatives with their reasons, and an admission that something was
   unverified when written.

2. **Check the remaining CONTEXT files** for derivations. This was a scoping error — the strip
   only touched `*-PLAN.md`, and all seven `*-CONTEXT.md` are `read_first`. But note the
   strip-verifier's citations for 13.1 were **stale**: it named derivations that pass 2 had
   already removed. Verify before editing; do not delete on a report's say-so.

3. **Resolve 27 dangling references**, all of the form "X points at deleted Y". Includes a
   fenced ` ```ts ` block in 13.1-05 holding `export const X   // value: see Plan Y` — a const
   with no initializer, which does not compile.

---

# EXECUTION ORDER, once plans are ready

Computed from real file-conflict analysis, not guessed. It is mostly serial **because the
collisions are real**: six of seven phases modify `bin/bench.ts`, five modify `fabric-node.ts`
and `browser-node.ts`.

```
group 1   13.1                alone
group 2   14                  alone
group 3   15                  alone
group 4   16 + 21          together   the only file-disjoint pair
group 5   17                  alone
group 6   23                  alone
```

Then 18 → 19 → 20 → 22 unlock. **22 runs last** — it verifies what every other phase claims to
have wired.

**Phase 18 has criterion 2b now** (added 2026-07-28). Its original criterion 2 exercised only
`serveAgent`'s `offer` branch, so Phase 18 could have passed in full while the `exec`-branch
defect stayed open. Do not drop it.

---

# BLOCKING CONSTRAINTS — read before proposing anything

- [ ] **ALL NODES HAVE EQUAL FUNCTIONALITY. The only difference is discovery.** Stated by the
      owner four times; a class was deleted over it. Grep for the mechanism, never the
      vocabulary. A relay-only or storage-only node budget reintroduces the deleted class —
      `fabric-node.ts:394-396` records that no construction path yields a node that will not
      compute, and the comment notes the mechanism *"survived three rounds of renaming."*

- [ ] **DEMO-04: no deploy workflow file may exist in the repository at all** — absent, not
      disabled. `disclosure-gate.node.test.ts` checks by content.

- [ ] **Vocabulary discipline** is enforced repo-wide by `vocabulary.node.test.ts`, which scans
      `.planning/`. **Read the `BANNED` array from that file; never enumerate it in prose.**

- [ ] **Do not reintroduce static determinism analysis.** Built, hardened, fuzzed, deleted
      (1,214 lines). Divergence is *detected* by byte comparison, never predicted.

- [ ] **Do not write an allow-list for host imports.** `WebAssembly.instantiate` is the sandbox.

- [ ] **The kernel must never require `crypto.subtle`.** A LAN origin is not a secure context.

- [ ] **Never trust elfconv's exit code.** It is `0` on binaries it could not fully translate.

- [ ] **Background agents share ONE working tree.** Never `git checkout --` a file you did not
      personally write; never `git add -A`; `git commit` without `-a` still commits whatever
      another agent staged. Verify a dirty file is yours before touching it.

- [ ] **Do not run `gsd-sdk query state.begin-phase`** or trust `progress.bar`. Also
      **`roadmap.analyze` returns `phases: []`** — the parser cannot see this roadmap's 23
      phases, and a workflow trusting it would announce "all phases complete" and exit.
      Extract phases with `grep -n "^### Phase" .planning/ROADMAP.md`.

---

# THE FINDING WORTH KEEPING FROM THIS SESSION

**Three independent passes produced three different false derivations of the same quantity** —
the worst-case concurrent `exec` count on one node:

| | claim | why false |
|---|---|---|
| original | "8 concurrent streams, so the early-stream ceiling is unreachable" | `earlyStreams` is cumulative, never spliced on close |
| pass 1 | "8 × R requestors bounds concurrent exec" | `Transport.send` is fire-and-forget; all 200 arrive at once |
| pass 2 | "SHARDS × redundancy is the per-node worst case" | `planPlacement` slices **distinct** nodes (`sovereignty.ts:185`) |

Each was confidently reasoned from real code by a capable agent. Each was instructed into a
**production source comment as fact**.

The lesson is not that the reviewers were careless. **A quantitative property of emergent
runtime behaviour cannot be reliably derived by reading, and must be measured.** Every plan now
carries that rule as a blockquote. Hold the line on it — the instinct to derive one more number
is exactly what produced all three.

## Calibration

Plan revision **diverged**: pass 1 fixed 65 of 70 and introduced 28; pass 2 fixed 30 of 32 and
introduced 52. The defects concentrated in metadata layers the plans had grown — threat tables
with IDs that must stay unique, proof tables whose header prose counts their own rows,
front-matter grep patterns for unwritten code. Those are gone now. The **task instructions held
up across both passes** and are what the strip preserved.

Task counts were the instrument that made the strip safe: 7 of 7 phases matched the checkpoint
exactly, which is how we know no task was quietly summarised away. **Commit before a
destructive mechanical pass** — the 16 restores are recoverable only because `2bcfb0d` exists.

---

# ALSO LANDED THIS SESSION

- **Disclosure gate fixed.** The `wrangler` pattern missed `wrangler pages deploy`, and nothing
  noticed because every test asserted an *absence* — a pattern matching nothing reads exactly
  like a clean repository. Patterns now assert what they must catch and must ignore. 36 → 39
  guard tests.
- **README rewritten.** It claimed 274 tests and phases 1-2.
- **The DHT claim was false and is corrected** in `CLAUDE.md` and both research docs.
  `@libp2p/kad-dht` is not installed, and `browser-node.ts:197` listens on
  `['/p2p-circuit','/webrtc']`, so a reserved browser *is* dialable.
- **Cloudflare cannot host the relay** — no inbound TCP, no UDP. Containers are not
  transport-blocked but fail on restart cadence against a 2 h reservation TTL. Recorded in
  STATE.md with the cost corrections.

## Two live gaps found in existing code, not yet in the ledger

Both verified against source. Neither is a Cloudflare issue — they surfaced while researching one.

1. **`sovereignFor` is a unilateral claim.** `signCapabilityRecord` signs with the node's own
   key and `verifyCapabilityRecord` verifies against that same key. There is **no
   counter-signature from the `userKey` it names**, so any enrolled node can advertise
   clearance for any owner's sovereign data, and `not-cleared-for-owner` (`discovery.ts:288`)
   filters honest nodes rather than attackers.
2. **Two unlinked sovereignty models.** Nothing converts `DiscoveredExecutor` →
   `NodeDescriptor`; the latter's `canExecuteSovereign` comes from a CLI flag and
   `publicNodes()` hardcodes `true`. `sovereignty.ts:62` admits it: *"placeholders here."*
   Whatever glue Phase 18 writes **is** the trust boundary.

These look like genuine ledger additions rather than notes. The owner was asked and has not
answered.

# BLOCKERS NEEDING THE OWNER

1. **US provisional patent — 2027-07-26.** EPO and China already permanently forfeit.
2. **A host for the seed** — a small always-on box with a public IP and arbitrary port binding.
   Cloudflare is ruled out. Budget for a **full node**, not a relay daemon.
3. **GitHub Pages serves the pre-Phase-9 bundle.** Republishing is a deliberate human act.
