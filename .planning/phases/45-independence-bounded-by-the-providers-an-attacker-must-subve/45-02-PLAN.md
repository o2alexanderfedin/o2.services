---
phase: 45-independence-bounded-by-the-providers-an-attacker-must-subve
plan: 02
type: execute
wave: 2
depends_on: ["45-01"]
autonomous: true
requirements: [VER-12]
criteria: [5]
files_modified:
  - packages/node/src/bin/agent.ts
  - packages/node/src/bin/bench.ts
  - packages/browser/demo/surfaces/fabric.ts
  - packages/browser/demo/render.ts
  - packages/browser/src/demo-regions.ts
  - packages/browser/demo/index.html
  - .planning/phases/phase-27-demo-ui-driven-by-real-fabric/UI-SPEC.md
  - packages/core/src/job/submit.test.ts
  - packages/net/src/reduce-job.test.ts
  - packages/core/src/enrollment.test.ts
  - packages/node/src/quorum-agents.node.test.ts
  - packages/node/src/result-signature.node.test.ts
  - packages/node/src/owner-domain-agents.node.test.ts
  - packages/node/src/bench-attestation.node.test.ts
  - packages/node/src/sovereign-arm.node.test.ts
  - packages/node/src/mutation-guard.node.test.ts
  - packages/node/src/demo-fabric.e2e.test.ts
  - packages/node/src/attestation-ui.e2e.test.ts
  - packages/node/src/owner-domain-tabs.e2e.test.ts
  - packages/node/src/sovereign-agent.e2e.test.ts

must_haves:
  truths:
    - "Every surface that prints a strength also prints how many providers vouched for it"
    - "A reader who sees `single-issuer` on any surface can tell that the operators were distinct and the provider was one"
    - "No spec in the tree asserts `'independent'` over a fixture whose certificates carry one issuer"
    - "`bin/bench.ts`'s real rungs report `owner-attested` or `owner-domain` and never `single-issuer`, verified rather than assumed"
    - "Every consumer of the `(replicas N, operators M)` format string moved with it, in all three projects it is read from — node, e2e and the demo page"
  artifacts:
    - path: packages/node/src/bin/agent.ts
      provides: "the CLI strength line carrying the issuer count"
      contains: "issuers"
    - path: packages/node/src/bin/bench.ts
      provides: "both the map and the aggregate strength lines carrying the issuer count"
      contains: "issuers"
    - path: packages/browser/demo/render.ts
      provides: "the page's `Established over …` line carrying the issuer count"
      contains: "issuer"
    - path: packages/browser/demo/surfaces/fabric.ts
      provides: "the F8 counts region carrying the issuer count"
      contains: "issuer"
  key_links:
    - from: "packages/browser/src/demo-regions.ts F8"
      to: "packages/browser/demo/index.html data-source attribute"
      via: "the two strings must stay identical or `demo-regions.e2e.test.ts` reddens"
      pattern: "attestation-counts"
---

<objective>
Close criterion 5 where a human actually reads it, and finish the census the mechanism started.

Purpose: `describeAttestation` is one sentence in one place, and every surface copies it — so
the *words* were fixed in 45-01. What no surface yet shows is the **count** the new strength
turns on. Five surfaces print `strength (replicas N, operators M)`; without an issuer count a
reader who sees `single-issuer` is left guessing which of the two dimensions failed, which is
exactly what criterion 5 forbids.

Output: the issuer count on every surface, the remaining census with a written finding per
fixture, and a measured answer to the one thing `45-CONTEXT.md` says must be verified rather
than assumed — whether `bin/bench.ts`'s real rungs are below the rule's reach.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-independence-bounded-by-the-providers-an-attacker-must-subve/45-CONTEXT.md
@.planning/phases/45-independence-bounded-by-the-providers-an-attacker-must-subve/45-01-SUMMARY.md
@./CLAUDE.md

45-01 left a named list of red specs in its SUMMARY. **Read that list first** — it is the
measured starting state of this plan, and the observed failure text tells you what each
assertion was really reading.

The dividing line for the census is `45-CONTEXT.md` §4 and it is this repository's own: a test
fixture may be given a second issuer, because the number of parties is what the rule checks; a
driver that publishes a reading of a real rig may not, because its labels must describe the
rig. **An across-process fixture is a rig**: `quorum-agents`, `result-signature`,
`owner-domain-agents` and the e2e tabs all enrol against one real provider, so their labels
move to describe that rig rather than being given a second authority that does not exist.
</context>

<interfaces>
<!-- Every surface that prints the pair today. Measured 2026-09-16. All five copy `description`
     from `describeAttestation` rather than composing a sentence — several say so in their own
     comments — so the WORDS are already fixed. Only the parenthetical counts are local. -->

- `packages/node/src/bin/agent.ts:2435-2446` — `strengthReading`, returns
  `` `${strength} (replicas N, operators M) — ${description}` ``
- `packages/node/src/bin/bench.ts:2127-2138` — `strengthReading`, the **map** line, same shape
- `packages/node/src/bin/bench.ts:2165-2184` — `aggregateReading`, the **aggregate** line, same
  shape. Its docblock forbids the two lines from losing the word that names which claim they
  are about; adding a count must not disturb that
- `packages/browser/demo/surfaces/fabric.ts:353-357` — region `fabric/attestation-counts`,
  `` `${plural(replicas,'replica')} from ${plural(operators.length,'operator')} · shared relay: …` ``
  (rendered into the text view at `:472-475`)
- `packages/browser/demo/render.ts:267-281` — `attestationLines`, second line
  `` `Established over ${plural(replicas,'replica')} from ${plural(operators.length,'operator')}.` ``

`AttestationReceipt.issuers: readonly PublicKeyHex[]` already exists (Phase 44) and is already
carried onto `ShardAttestation`, so no plumbing is needed — only the format strings.

**THE SECOND POPULATION, ENUMERATED — this plan has two blast radii and they are not the same
set.** Task 2's census is the **strength-literal** population: 34 occurrences across 13 files,
measured 2026-09-16. Task 1's is the **format-string** population — every consumer of
`(replicas N, operators M)` — and it is a different set that nothing else in this phase
enumerates. Derived from the tree with
`grep -rn "replicas 1, operators\|replicas (\\d+), operators" packages --include='*.ts'`, it is
exactly three test files, and **two of them are in neither the strength-literal list nor any
`read_first` before this revision**:

| Consumer | Site | Reads | Project | What Task 1 breaks |
|---|---|---|---|---|
| `packages/node/src/sovereign-agent.e2e.test.ts` | `:524` | `agent.ts:2639`'s `strengthReading` | **e2e** | a deliberate `toBe` equality on `` `owner-attested (replicas 1, operators 1) — ${describeAttestation('owner-attested')}` `` — its comment at `:519-522` explains why a substring match is unsafe there |
| `packages/node/src/sovereign-arm.node.test.ts` | `:200` `EXPECTED_RECEIPT`, asserted at `:519` | `bench.ts`'s `strengthReading` | node | the same equality shape, confirmed by its own `:370` comment |
| `packages/node/src/bench-attestation.node.test.ts` | `:206` `const STRENGTH` | `bench.ts`'s `strengthReading` | node | a regex pinning **both** the alternation and the parenthetical |

Checked and **unaffected**, named so nobody re-checks it: `demo-fabric.e2e.test.ts:273`'s
`toContain('2 replicas agreed')` is in the *absence* arm, which Task 1 does not touch; and no
`mutation-ledger.ts` entry carries a `signature` in this format, so no ledger entry goes stale
from Task 1.

**The F8 region has a registry entry and a matching HTML attribute that must move together:**
`packages/browser/src/demo-regions.ts:1313-1322` holds
`source: 'TabApi.runColouring().attestation.replicas with .operators and .sharedRelay'`, and
`packages/browser/demo/index.html:1590` repeats that exact string in its `data-source`
attribute. `demo-regions.e2e.test.ts` reads both. `packages/browser/dist/` is untracked build
output — do not edit it, the e2e project rebuilds.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: every surface that names a strength names how many providers stood behind it</name>

  <read_first>
    - `packages/node/src/bin/agent.ts` `:2430-2450`
    - `packages/node/src/bin/bench.ts` `:2110-2190` — both `strengthReading` and `aggregateReading`, including the docblock that forbids either line from losing its naming word
    - `packages/browser/demo/surfaces/fabric.ts` `:335-360` and `:465-480`
    - `packages/browser/demo/render.ts` `:238-285`
    - `packages/browser/src/demo-regions.ts` `:1300-1325`
    - `packages/browser/demo/index.html` `:1583-1596`
    - `.planning/phases/phase-27-demo-ui-driven-by-real-fabric/UI-SPEC.md` — the F8 row at `:654`
    - `packages/core/src/quorum.ts` as 45-01 left it, for the exact `describeAttestation('single-issuer')` sentence
  </read_first>

  <files>packages/node/src/bin/agent.ts, packages/node/src/bin/bench.ts, packages/browser/demo/surfaces/fabric.ts, packages/browser/demo/render.ts, packages/browser/src/demo-regions.ts, packages/browser/demo/index.html, .planning/phases/phase-27-demo-ui-driven-by-real-fabric/UI-SPEC.md</files>

  <action>
Change all five format strings **the same way**, so the CLI and the page cannot come to report
one result differently — the property every one of those call sites already states about
`description` and which now has to hold for the counts too.

- `agent.ts` `strengthReading` and `bench.ts` `strengthReading`: the parenthetical becomes
  `(replicas N, operators M, issuers K)` where `K` is `attestation.issuers.length`. The
  named-absence arm is untouched — it reports `agreeing`/`verified` and has no receipt to read
  issuers from.
- `bench.ts` `aggregateReading`: the same third count, in the same order, keeping the line's
  naming word intact.
- `render.ts` `attestationLines`: the second line becomes
  `Established over <N replicas> from <M operators>, vouched for by <K providers>.` using the
  existing `plural` helper for all three, so `1 provider` and `2 providers` both read.
- `fabric.ts`'s F8 composition: the same third clause, sited before the ` · shared relay: …`
  suffix so the relay clause stays last.

Then keep the region registry honest, in one edit each, because a guard reads both:
`packages/browser/src/demo-regions.ts`'s F8 `source` becomes
`TabApi.runColouring().attestation.replicas with .operators, .issuers and .sharedRelay`, and
`packages/browser/demo/index.html:1590`'s `data-source` attribute is set to that **identical**
string. Update the F8 row of
`.planning/phases/phase-27-demo-ui-driven-by-real-fabric/UI-SPEC.md` to list `.issuers` beside
`.operators`.

Add one short comment at each of the five sites saying why the third count is there: without
it a reader who sees `single-issuer` cannot tell which of the two dimensions fell short, which
is the whole of ROADMAP criterion 5. **Those comments must not quote either strength sentence
verbatim** — describe the reason in your own words. This repository has been bitten four times
by a guard's own prose tripping the guard, and one of this task's acceptance criteria is a grep
for those sentences. Do **not** compose a sentence at any of these sites — `description` remains
`describeAttestation`'s verbatim, and every one of these files already says so about itself.

**This task breaks three test files and repairs none of them.** They are Task 2's, and they are
the format-string table in `<interfaces>`: `sovereign-agent.e2e.test.ts:524`,
`sovereign-arm.node.test.ts:200` and `bench-attestation.node.test.ts:206`. Record their observed
failure text in the SUMMARY so the handover between the two tasks is a measurement rather than a
promise.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npx vitest run --project e2e packages/node/src/demo-regions.e2e.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "issuers" packages/node/src/bin/agent.ts` is at least 1 and the match is inside `strengthReading`'s template literal
    - `grep -c "issuers" packages/node/src/bin/bench.ts` is at least 2 — one in `strengthReading`, one in `aggregateReading`
    - `packages/browser/demo/render.ts` emits a third count using `plural`, so a one-provider run reads `1 provider` and not `1 providers`
    - `packages/browser/demo/surfaces/fabric.ts`'s F8 string names an issuer/provider count before the ` · shared relay: ` suffix
    - The F8 `source` string in `packages/browser/src/demo-regions.ts` and the `data-source` attribute at `packages/browser/demo/index.html:1590` are byte-identical — verify with `grep -o` on both and `diff <(…) <(…)`
    - `npx vitest run --project e2e packages/node/src/demo-regions.e2e.test.ts` exits 0
    - `npx tsc --noEmit` exits 0, with `EXIT=$?` read on the line immediately after
    - No surface composes a strength sentence of its own, **and the check is not comment-blind** — the same task adds a comment at each of five sites, and a comment quoting the new sentence would trip a naive grep. Either strip comments before the scan (`stripComments` from `packages/node/src/strip-comments.ts`) or confirm by inspection that none of the five comments quotes either sentence; then `grep -n "independently verified\|single-issuer agreement" packages/node/src/bin/agent.ts packages/node/src/bin/bench.ts packages/browser/demo/render.ts packages/browser/demo/surfaces/fabric.ts` returns nothing
  </acceptance_criteria>

  <done>
All five reader-facing surfaces print the issuer count beside the replica and operator counts,
the F8 registry and its HTML attribute still agree, and no surface has grown a sentence of its
own.
  </done>
</task>

<task type="auto">
  <name>Task 2: the census — every spec that was reading `independent` over one provider</name>

  <read_first>
    - `.planning/phases/45-*/45-01-SUMMARY.md` — the named red list and its observed failure text
    - `.planning/phases/44-*/44-02-SUMMARY.md` — the standard a written census finding is held to; Phase 44 recorded five and this phase must match that form
    - `packages/core/src/job/submit.test.ts` `:3160-3210`, `:3300-3310`, `:3390-3440`, `:3530-3540`
    - `packages/net/src/reduce-job.test.ts` `:760-830`
    - `packages/node/src/quorum-agents.node.test.ts` `:595-610`, `:715-730`, `:1130-1145`
    - `packages/node/src/result-signature.node.test.ts` `:475-495`
    - `packages/node/src/owner-domain-agents.node.test.ts` `:590-655`
    - `packages/node/src/bench-attestation.node.test.ts` `:110-135`, **`:195-210` (the four module-level regexes, of which `:206` `const STRENGTH` is the one Task 1 breaks)**, **`:470-490` (`strengthOf`, which throws on a parse miss)**, `:530-560`
    - `packages/node/src/demo-fabric.e2e.test.ts` `:235-280`
    - `packages/node/src/attestation-ui.e2e.test.ts` `:200-215`, `:450-480`, `:650-670`, `:890-905`, `:960-975`
    - `packages/node/src/owner-domain-tabs.e2e.test.ts` `:150-160`, `:760-775`
    - `packages/node/src/sovereign-arm.node.test.ts` **`:170-205` (the docblock and `const EXPECTED_RECEIPT` at `:200` — a format-coupled equality, NOT a comment)**, `:365-375`, `:495-525`
    - `packages/node/src/sovereign-agent.e2e.test.ts` `:515-530` — `expectedReading` at `:524`, asserted by equality; its comment at `:519-522` says why a substring match is unsafe there. Reads `agent.ts:2639`
    - `packages/node/src/mutation-guard.node.test.ts` `:455-470`
    - `packages/core/src/enrollment.test.ts` `:975-990`
    - `packages/node/src/mutation-ledger.ts` — grep for each file above before renaming ANY `it(...)` title; 125 entries carry `signatureSource: 'test-title'` and a renamed title reddens `mutation-guard.node.test.ts`
  </read_first>

  <files>packages/core/src/job/submit.test.ts, packages/net/src/reduce-job.test.ts, packages/core/src/enrollment.test.ts, packages/node/src/quorum-agents.node.test.ts, packages/node/src/result-signature.node.test.ts, packages/node/src/owner-domain-agents.node.test.ts, packages/node/src/bench-attestation.node.test.ts, packages/node/src/sovereign-arm.node.test.ts, packages/node/src/mutation-guard.node.test.ts, packages/node/src/demo-fabric.e2e.test.ts, packages/node/src/attestation-ui.e2e.test.ts, packages/node/src/owner-domain-tabs.e2e.test.ts, packages/node/src/sovereign-agent.e2e.test.ts</files>

  <action>
34 occurrences of `'independent'` were measured across 13 test files on 2026-09-16, and not all
of them are assertions on a strength. **Classify before touching anything** — a blanket replace
is the failure this task exists to avoid.

Work the three populations separately and write a finding for each file, in the SUMMARY, in the
form *what was this relying on, and was it true?*

**(a) Reddened by the mechanism — these were asserting `'independent'` over one issuer.**

- `packages/core/src/job/submit.test.ts` — six sites. The live path waives the refusal, so
  composition still succeeds; what moved is the receipt's strength. Each `toMatchObject({
  strength: 'independent' })` over a fixture whose certificates carry one issuer becomes
  `'single-issuer'`. The `not.toMatchObject({ strength: 'independent' })` at `:3206` stays but
  is now weaker than it reads — there are two labels above `owner-domain` now, not one.
  Strengthen it, and **name the form**: either two explicit assertions
  (`not.toMatchObject({ strength: 'independent' })` beside
  `not.toMatchObject({ strength: 'single-issuer' })`) or a rank comparison
  (`attestationRank(shard.attestation.strength)` below `attestationRank('single-issuer')`).
  **Do not reach for `expect.stringMatching`** — a regex over the strength field is the string
  coupling this whole phase removes from source, and a test file is outside 45-03's guard
  corpus, so nothing would catch it being reintroduced by hand. If a site should not be
  strengthened, say in the finding why.
- `packages/net/src/reduce-job.test.ts:828` — the three-label case. Its stated reasoning is that
  one reading alone is satisfied by a constant, so the **set** is the assertion. Extend it to
  four readings rather than moving three: give the fourth fixture worker a second issuer so the
  expected array reads `['owner-attested', 'owner-domain', 'single-issuer', 'independent']`. If
  the fixture cannot carry a second issuer, move the third reading to `'single-issuer'` and say
  so in the finding — but then the case no longer proves `'independent'` is reachable, and the
  finding must name which case does.
- `packages/node/src/quorum-agents.node.test.ts` `:603`, `:604`, `:1140` — an across-process rig
  with one real provider. Move to `'single-issuer'` and
  `describeAttestation('single-issuer')`. `:724`'s `not.toBe('independent')` gains
  `not.toBe('single-issuer')` for the same reason as `owner-domain-agents` below.
- `packages/node/src/result-signature.node.test.ts:484` — two agents under two owner seeds and
  one provider. Moves to `'single-issuer'`. The comment beside it already explains that two
  operators is two user keys since VER-11; add the sentence that the two are still **one
  party's reach**, which is the whole of VER-12.
- `packages/node/src/demo-fabric.e2e.test.ts` `:242`/`:256` — a hand-built receipt fixture
  passed to the formatter to prove verbatim passthrough. It can legitimately keep
  `'independent'` (its `issuers: []` is a fixture, not a fabric reading), but it must gain an
  arm for `'single-issuer'` so the passthrough is proved for the new label too. `:273`'s
  `toContain('2 replicas agreed')` is in the absence arm and does not move; the F8 counts
  assertions do move, because Task 1 added a count to that region.

**(a2) Broken by Task 1's format change, not by the strength rule — a different population, and
two of these three appear nowhere in the strength-literal census.** The table in `<interfaces>`
is the enumeration; repair all three here.

- `packages/node/src/sovereign-agent.e2e.test.ts:524` — `expectedReading` is a deliberate `toBe`
  equality and Task 1 added a third count to the line it reproduces. Add the issuer count in the
  **same order Task 1 emits it**, keeping the equality: an equality is what makes this a reading
  of the fabric's own words rather than of the driver's formatting, and its comment at `:519-522`
  records why a substring match is unsafe here. Do not weaken it to `toContain`.
- `packages/node/src/sovereign-arm.node.test.ts:200` — `EXPECTED_RECEIPT` is the same shape
  against `bench.ts`'s `strengthReading`, confirmed by its own comment at `:370`. Same repair,
  same prohibition on weakening it. **This file is NOT comment-only**, which an earlier reading
  of this plan had it as; its `:510` comment is a separate, third occurrence handled in (b).
- `packages/node/src/bench-attestation.node.test.ts:206` — `const STRENGTH`. Task 1 breaks
  **both** halves of it and both must move in the same edit:
  1. the alternation becomes four-way, gaining `single-issuer`;
  2. the parenthetical gains the third count group, `\(replicas (\d+), operators (\d+), issuers (\d+)\)`,
     and `strengthOf` (`:470-490`) gains the fourth capture so its returned record carries it —
     the description capture's index moves from `hit[4]` to `hit[5]`.

  **Widening the alternation and forgetting the count group is the dangerous half, and it is
  silent.** `:542` is `expect(STRENGTH.test(reading)).toBe(false)` — an absence check whose only
  instrument is this regex. A regex that no longer matches any real line makes that assertion
  pass **vacuously** on every memory rung, which is exactly the shape this file's own note at
  `:198-203` warns about. The loud half is merely loud: `strengthOf` throws on a parse miss, so
  `:555`/`:559` would fail with a thrown error attributed to the wrong cause.

**(b) Green but stale — these still pass and have stopped meaning what they say.**

- `packages/node/src/bench-attestation.node.test.ts:543` — a loop over
  `['owner-attested', 'owner-domain', 'independent']` asserting a memory rung's line contains
  none of the sentences. **It must gain `'single-issuer'` or the absence check stops being
  exhaustive** — a driver appending the new label would now slip past. `:124`'s prose describes
  a past plant and stays, but say in a bracketed note that a fourth label exists now.
- `packages/node/src/owner-domain-agents.node.test.ts` `:600`/`:650` — `not.toBe('independent')`
  guarding against an implementation that returns the stronger label unconditionally. There are
  now two labels stronger than `owner-domain`; add `not.toBe('single-issuer')` at both sites so
  the guard keeps its reach.
- `packages/node/src/attestation-ui.e2e.test.ts` and
  `packages/node/src/owner-domain-tabs.e2e.test.ts` — both hold
  `const INDEPENDENT = describeAttestation('independent')` and use it in
  `not.toContain(INDEPENDENT)` (five sites and one site respectively) plus one
  `[OWNER_ATTESTED, OWNER_DOMAIN, INDEPENDENT].some(...)` membership test at
  `attestation-ui.e2e.test.ts:474`. Add a `SINGLE_ISSUER` constant beside `INDEPENDENT`,
  include it in every `not.toContain` group and in the `.some(...)` list, so "the page showed
  no strength at all" and "the page showed a strength" both keep meaning what they meant.
- `packages/node/src/sovereign-arm.node.test.ts:510`,
  `packages/node/src/mutation-guard.node.test.ts:464`,
  `packages/core/src/enrollment.test.ts:982` — comment-only occurrences **in these three
  positions**. (`sovereign-arm.node.test.ts` also carries a live format coupling at `:200`,
  which is population (a2), not this one — the file appears in both and the two must not be
  conflated.) Read each, and amend only where the sentence has become false. `sovereign-arm`'s
  argument at `:510` about why an equality is used rather than `not.toContain('independent')` is
  **strengthened** by a fourth label, not invalidated; say so in one clause rather than
  rewriting the paragraph.

**(c) Verify the belief `45-CONTEXT.md` §4 says must not be assumed.** `bin/bench.ts`'s real
rungs were re-labelled `owner-domain` in Phase 44 because every worker enrols under
`BENCH_USER_SEED` unless `--sovereign` is passed, so they hold one `operatorId`. Add a case to
`bench-attestation.node.test.ts` that **measures** rather than asserts the belief: over the real
rungs already read by that file, no reading's strength is `'single-issuer'` and no reading
carries the refusal kind `single-issuer-quorum`. Give it a **positive control in the same case**
— apply the same parser to a synthetic line carrying `single-issuer` and require it to match —
because an absence claim is worth exactly what its positive control is worth, and this file's
own docblock already argues that.

**Build the synthetic line from `bench.ts`'s own template, never by hand beside the regex.**
A control typed next to the pattern it exercises is written to match whatever the executor just
wrote, so it cannot detect the mismatch it exists to catch — and the mismatch this task is most
likely to produce is the vacuous one in (a2), a widened alternation with no count group.
Construct it the way the driver does: interpolate `describeAttestation('single-issuer')` and a
count triple into the same `strength (replicas N, operators M, issuers K) — description` shape
`bench.ts` emits, so the control moves if and only if the driver's template moves. Assert it
matches **and** that the parsed record carries all three counts, not merely that `.test()` is
true — a `.test()` alone is satisfied by a regex whose groups are wrong.

**Do not rename an `it(...)` title** in any of these files without first grepping
`packages/node/src/mutation-ledger.ts` for it. If a rename is unavoidable, correct the ledger
signature in the same edit.
  </action>

  <verify>
    <automated>npx vitest run --project node packages/core/src/job/submit.test.ts packages/net/src/reduce-job.test.ts packages/core/src/enrollment.test.ts</automated>
    <automated>npx vitest run --project node packages/node/src/quorum-agents.node.test.ts packages/node/src/result-signature.node.test.ts packages/node/src/owner-domain-agents.node.test.ts packages/node/src/bench-attestation.node.test.ts packages/node/src/sovereign-arm.node.test.ts packages/node/src/mutation-guard.node.test.ts</automated>
    <automated>npx vitest run --project e2e packages/node/src/demo-fabric.e2e.test.ts packages/node/src/attestation-ui.e2e.test.ts packages/node/src/owner-domain-tabs.e2e.test.ts packages/node/src/sovereign-agent.e2e.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - Each of the three verify commands exits 0, with `EXIT=$?` read on the line immediately after each — no pipe, no trailing `echo`, no `tail`
    - `grep -rn "'independent'" packages --include='*.test.ts'` returns no site that asserts a strength over a fixture carrying a single issuer; every remaining occurrence is either a comment, a two-issuer fixture, or a hand-built formatter fixture
    - `packages/node/src/bench-attestation.node.test.ts` contains a four-element strength list including `single-issuer`
    - **Both halves of `:206`'s `STRENGTH` regex moved**: `grep -n "const STRENGTH" packages/node/src/bench-attestation.node.test.ts` shows a four-way alternation **and** a third count group matching `issuers (\\d+)`. A widened alternation without the count group is the vacuous failure and is a blocking defect, not a nit
    - `strengthOf` returns the issuer count and its description capture index moved with the added group — the parsed record carries `replicas`, `operators` and `issuers`
    - The (c) positive control's synthetic line is **interpolated from `describeAttestation('single-issuer')` and a count triple in `bench.ts`'s own emitted shape**, not hand-typed beside the regex, and the case asserts the parsed record's three counts rather than only that `.test()` is true
    - `packages/node/src/sovereign-agent.e2e.test.ts:524` and `packages/node/src/sovereign-arm.node.test.ts:200` both still use an **equality** (`toBe`), now carrying the issuer count — neither was weakened to `toContain`
    - `grep -rn "replicas 1, operators 1)" packages --include='*.ts'` returns nothing: no consumer of the old two-count format survives
    - `packages/node/src/owner-domain-agents.node.test.ts` contains `not.toBe('single-issuer')` at both sites that already carry `not.toBe('independent')`
    - `packages/node/src/attestation-ui.e2e.test.ts` and `packages/node/src/owner-domain-tabs.e2e.test.ts` each define a `SINGLE_ISSUER` constant from `describeAttestation('single-issuer')` and use it everywhere `INDEPENDENT` is used
    - `bench-attestation.node.test.ts` carries a case whose positive control matches a synthetic `single-issuer` line and whose real-rung arm finds none
    - `npx vitest run --project node packages/node/src/mutation-guard.node.test.ts` exits 0 — no title moved out from under a ledger signature
    - The SUMMARY carries one written finding per file touched, in Phase 44's `44-02-SUMMARY.md` form
  </acceptance_criteria>

  <done>
Every spec that was asserting `'independent'` over one provider now asserts what the fixture
actually is, every absence check covers the fourth label, `bench.ts`'s real rungs are measured
to be below the rule's reach rather than believed to be, and each repair has a written finding.
  </done>
</task>

</tasks>

<verification>
Run by project, never by bare path — a bare path fans out across all five projects. Read
`EXIT=$?` on the line immediately after each command.

    npx tsc --noEmit
    npx vitest run --project node packages/core/src/job/submit.test.ts packages/net/src/reduce-job.test.ts packages/core/src/enrollment.test.ts packages/core/src/quorum.test.ts
    npx vitest run --project node packages/node/src/quorum-agents.node.test.ts packages/node/src/result-signature.node.test.ts packages/node/src/owner-domain-agents.node.test.ts packages/node/src/bench-attestation.node.test.ts packages/node/src/sovereign-arm.node.test.ts packages/node/src/mutation-guard.node.test.ts
    npx vitest run --project e2e packages/node/src/demo-fabric.e2e.test.ts packages/node/src/attestation-ui.e2e.test.ts packages/node/src/owner-domain-tabs.e2e.test.ts packages/node/src/sovereign-agent.e2e.test.ts packages/node/src/demo-regions.e2e.test.ts

`bench-attestation.node.test.ts` and `quorum-agents.node.test.ts` spawn real processes. Record
`/usr/bin/time -p` and the `[host conditions]` banner beside any duration quoted in the SUMMARY,
and never diagnose a failure in them before reading that banner.

Stage with `git add` **between** runs and never during one:
`discover-arm.node.test.ts` and `bench-attestation.node.test.ts` snapshot
`git status --porcelain` around themselves.
</verification>

<success_criteria>
- ROADMAP criterion 5 is met **where a human reads it**: every surface that reports a strength
  also reports the issuer count, and the sentence beside it names which dimension fell short
- No spec in the tree asserts `'independent'` over a one-issuer fixture
- Every absence check that enumerated three strengths now enumerates four
- `bin/bench.ts`'s real rungs are **measured** to sit below the issuer rule's reach, with a
  positive control proving the measurement could have found otherwise
- Each repaired fixture has a written finding, not a silent edit
</success_criteria>

<output>
After completion, create
`.planning/phases/45-independence-bounded-by-the-providers-an-attacker-must-subve/45-02-SUMMARY.md`,
carrying the per-file census findings and the bench measurement with its positive control.
</output>
