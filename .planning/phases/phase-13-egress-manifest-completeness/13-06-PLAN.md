---
phase: phase-13-egress-manifest-completeness
plan: 06
type: execute
wave: 4
depends_on: ["13-04", "13-05", "13-07"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - packages/node/src/bench-egress.node.test.ts
autonomous: true
requirements: [DATA-05, DATA-06]
gap_closure: true

must_haves:
  truths:
    - "The DATA-05 and DATA-06 ledger rows state what the code does after this phase -- refusal semantics, whole-registered-payload granularity, and the submitting node as the scope -- with no clause that overstates the mechanism"
    - "Removing the egress leg from bin/bench.ts fails a test rather than only the type-checker, and the scan behind that test is proven able to report an unmet requirement"
    - "The scan cannot be satisfied by a doc comment that merely names the identifier"
    - "The rule that the fabric will not move raw sovereign data between any two nodes, including two the same owner controls, is recorded where Phase 19 will read it -- not left in a phase summary"
    - "The browser tier inheriting the refusal with no runtime coverage is recorded against the phase that closes it, rather than only in a summary"
  artifacts:
    - path: "packages/node/src/bench-egress.node.test.ts"
      provides: "The automated coverage 13-VERIFICATION.md found missing for bin/bench.ts's egress leg"
      min_lines: 60
    - path: ".planning/REQUIREMENTS.md"
      provides: "DATA-05 and DATA-06 rows and checklist entries restated against the amended criteria"
    - path: ".planning/PROJECT.md"
      provides: "The Key Decisions row recording that raw sovereign data does not move, unconditionally"
  key_links:
    - from: "packages/node/src/bench-egress.node.test.ts"
      to: "packages/node/src/bin/bench.ts"
      via: "reads the source, strips comments, and requires the call-site shape"
      pattern: "bin/bench\\.ts"
---

<objective>
Close the two remaining `missing:` entries from 13-VERIFICATION.md that Plans 13-04 and
13-05 do not touch, and bring the requirement ledger back in line with what is true.

13-VERIFICATION.md's closing section is blunt about the ledger: `REQUIREMENTS.md`'s
DATA-05 row claims the test "fails a running job between real `FabricNode`s when a raw
sovereign byte crosses", and both halves of that clause were wrong — the test asserted
the job reached `agreed`, and an eleven-byte raw field crossed the tap unremarked in a
direct probe. The ROADMAP criteria were amended on 2026-07-28 and Plans 13-04/13-05 make
the refusal real, so the rows can now be rewritten to something true rather than
something aspirational. They must be rewritten in the same pass, because a row that
overstates the mechanism is exactly the defect this milestone exists to stop.

The second item is `bin/bench.ts`. 13-VERIFICATION.md's mutation 4 showed that removing
its egress leg is caught by the type-checker only, and only because the manifest read
was left in place — an edit removing both together would pass the whole suite. That is a
guard nobody is watching.

Purpose: leave the ledger honest and the last uncovered call site covered.
It also records the two owner rulings that came out of planning this phase, in the
places that constrain future work rather than in a summary nobody re-reads: raw
sovereign data does not move between nodes at all, which bounds how Phase 19 can do
owner-domain redundancy; and the browser tier now inherits a refusal path that no test
exercises.

Output: rewritten DATA-05/DATA-06 entries, two recorded decisions, and one new test with
its own planted-failure proof. The ROADMAP Phase 13 plan list already names all seven
plans and this plan does not touch that block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/phase-13-egress-manifest-completeness/13-VERIFICATION.md
@.planning/phases/phase-13-egress-manifest-completeness/13-04-PLAN.md
@.planning/phases/phase-13-egress-manifest-completeness/13-05-PLAN.md
</context>

<interfaces>
<!-- Everything this plan builds against. No exploration needed. -->

The amended ROADMAP criteria (`.planning/ROADMAP.md`, Phase 13), which the ledger rows
must be restated against — and the HTML comment beneath them explaining each change is
the rationale to draw on:

1. A stream tap installed on the wire between two nodes started via `bin/agent.ts`
   **refuses the send** when a registered sovereign block would cross it, so the bytes
   never leave the node, and the running cross-owner job fails rather than completing as
   `agreed`
2. Every job run through the browser demo emits an egress manifest recording exactly
   what left the **submitting** node, with byte counts, retrievable from the job's own
   result metadata after completion — not only inside a test harness
3. A job with zero sovereign data crossing the network records no violation over a
   non-empty manifest, and a job that legitimately moves an aggregate carries its raw
   input nowhere on the wire — with the pushdown size claim evidenced by
   `encodeCanonical(output)` against `encodeCanonical(rawInput)`, not by
   `manifest.totalBytes`

The four places in `.planning/REQUIREMENTS.md` this plan edits:

- line ~106: the DATA-05 checklist entry and the HTML comment beneath it
- line ~114: the DATA-06 checklist entry (currently un-checked) and its HTML comment
- line ~398: the DATA-05 traceability-table row
- line ~399: the DATA-06 traceability-table row

The egress leg in `packages/node/src/bin/bench.ts`, as it stands (the shape the new test
requires — the identifiers are what the assertions key on):

```ts
// realFabric(), the real-libp2p path:
guard: requestor.egress,

// memoryFabric(), the in-process path:
const callerRpc = new RpcEndpoint(requestorGuard, { timeoutMs: 30_000 })
guard: requestorGuard,

// runnerFor(), the measured job path:
const result = await submitJobWithEgress(spec, fabric.blockstore, [fabric.guard])
const manifest = result.manifests[0]
if (manifest !== undefined) {
  egressEntries += manifest.entries.length
  egressBytes += manifest.totalBytes
}
```

`packages/node/src/vocabulary.node.test.ts` — the house idiom for a source-scanning
guard, and the shape to follow. Its matcher (`rawMatches`) is a plain function over
`(file, content)`, separated from the tree walk so synthetic content can prove the
matcher fires; its "the checker can fail — proved by mutation, not assumed" describe
block plants each case in synthetic content and requires the scanner to catch it. Note
also that `packages/node/src/vocabulary.node.test.ts` scans every git-tracked file for a
banned list of words, and `.planning/phases/phase-13-*/` is **not** an exempt path — so
this plan's own prose is in its jurisdiction, and so is any new text added to
`REQUIREMENTS.md` and `ROADMAP.md`.

`.planning/ROADMAP.md` carries two `EXEMPT_LINES` entries keyed on exact phrases in its
own text about that word list, which is why this plan reads that file and never writes
it: disturbing either phrase turns `vocabulary.node.test.ts`'s "carries no line exemption
that no longer matches anything" check red.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: the ledger says what the code does</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/REQUIREMENTS.md lines 100-125 (the DATA-05 and DATA-06 checklist entries and their HTML comments — both comments currently describe the pre-refusal behavior and the DATA-06 one records the un-check)
    .planning/REQUIREMENTS.md lines 395-402 (the DATA-05 and DATA-06 traceability rows — the DATA-05 row is the one 13-VERIFICATION.md calls out for two errors in one clause)
    .planning/ROADMAP.md lines 274-327 (read only — the amended criteria and the HTML comment explaining each amendment are the source this task restates the ledger against. The plan count and the plan list were already updated when these gap-closure plans were written, and the milestone checkbox at line 51 stays un-checked: whether the phase is met is the next verification pass's call. Do not edit this file)
    packages/node/src/vocabulary.node.test.ts:137-188 (EXEMPT_LINES — two entries keyed on exact phrases inside .planning/ROADMAP.md, which is one more reason not to touch that file here)
  </read_first>
  <action>
    Rewrite the DATA-05 entries in `.planning/REQUIREMENTS.md`. The checklist entry stays
    checked, and its wording moves from a stream-tap *test* failing to the mechanism that
    now exists: the send is refused, so the bytes do not leave, and the running job fails
    as a consequence. Replace its HTML comment wholesale. The replacement must state
    three things and no more than it can support. First, the refusal: `EgressGuard.send`
    records the attempt and rejects instead of forwarding, the entry with its violation
    label is still pushed, and `manifest.totalBytes` excludes a frame that never left.
    Second, the granularity, in the honest form 13-VERIFICATION.md measured: detection
    matches the **whole registered payload, contiguous and byte-identical**. A re-encoded
    or partial copy is not matched, and a probe sending the raw field characters alone
    crossed with no violation. Phase 4 drew the same line and the phrase to reuse is
    "a detector, not a prover". Third, what the caller observes, and the asymmetry: on a
    node's own outbound request the refusal arrives immediately as a named send failure;
    on a reply the failure is swallowed by `rpc.ts`'s documented response-leg behavior and
    the dispatcher observes a timeout instead, so it learns the dispatch failed but not
    why. Name that as an accepted cost with the evidence for the cause living in the
    owner's own manifest, not as an open defect. Add a fourth, short: a registration
    lives only until its reply frame has settled (Plan 13-07), so the scan the refusal
    depends on is bounded by a node's in-flight sovereign tasks rather than by its
    uptime.

    Re-check the DATA-06 entry and rewrite its comment. Under the amended criterion the
    promise is the **submitting** node's own tap, retrievable from the job's own result
    metadata — which `submitJobWithEgress` delivers and which the browser demo's two real
    Chromium e2e tests read back from the API's own return value, independently
    mutation-verified by 13-VERIFICATION.md. State plainly what the amendment gave up and
    where it went: retrieving a *remote* node's manifest needs a wire message kind that
    does not exist, `13-CONTEXT.md` deferred it, and it is now a named future item rather
    than an implied promise. Say that `bin/agent.ts` is out of this requirement's scope
    because it is serving-only and never submits a job. Do not claim "each owner's node";
    that clause is gone.

    Rewrite both traceability rows to match, and keep them the length the surrounding
    rows are. The DATA-05 row must lose the two clauses 13-VERIFICATION.md names as
    errors — that the test fails a running job (it now genuinely does, but say so in the
    new form: the *send* is refused and the job fails as a consequence) and "a raw
    sovereign byte" (replaced by the registered-payload granularity above). Both rows
    should cite the plans that make each claim true (13-04 for the refusal mechanism,
    13-05 for the in-process and cross-process proofs, 13-07 for the registration
    lifetime, 13-06 for the benchmark call-site coverage) so a later reader can check
    rather than trust.

    Do not touch `.planning/ROADMAP.md` in this task. Its Phase 13 plan count and plan
    list already name all seven plans, and its criteria are the amended text this task
    restates the ledger against. Task 2 edits that file, and only its Phase 19 block.

    Before finishing, re-read every sentence added in this task against
    `vocabulary.node.test.ts`'s banned list — this plan's own file and the edited
    requirement document are inside the scan's jurisdiction.
  </action>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/o2.services && npx vitest run packages/node/src/vocabulary.node.test.ts</automated>
  </verify>
  <done>
    DATA-05 and DATA-06 read, in both the checklist and the traceability table, as
    statements a verifier could check against the code and find true. Neither claims
    per-byte detection, neither claims "each owner's node", and the response-leg cost is
    written down rather than left to be rediscovered. `.planning/ROADMAP.md` is
    unmodified by this task. `vocabulary.node.test.ts` is green, including its
    dead-exemption check.
  </done>
</task>

<task type="auto">
  <name>Task 2: record the two rulings where the phase that inherits them will read them</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md</files>
  <read_first>
    .planning/PROJECT.md lines 203-225 (the Key Decisions table — its columns are Decision / Rationale / Outcome, and the rows around line 216 "Owner-domain replication promoted to v1" and line 209 "All nodes have equal functionality" are the two this ruling qualifies; read both before writing, because the new row narrows 216 and must say so rather than contradict it silently)
    .planning/PROJECT.md lines 26-31 (the integrity-mechanism table — the "Sovereign, owner has 2 or more live nodes" row promises redundant execution within the owner's node set, which is exactly the row this ruling constrains)
    .planning/ROADMAP.md lines 393-404 (the Phase 19 entry: its requirement list including VER-08, VER-09 and WIRE-03, and success criterion 2, which reads "a sovereignty-pinned task with two or more of that owner's nodes live executes on two of them" — the criterion that presumes the data is already on both)
    .planning/phases/phase-13-egress-manifest-completeness/13-03-PLAN.md:245-275 (the existing routing of the browser-tier gap to Phase 19's WIRE-03, and the wording used for it — the note added here extends that routing rather than opening a second one)
    packages/node/src/vocabulary.node.test.ts:137-188 (EXEMPT_LINES — two entries keyed on exact phrases inside .planning/ROADMAP.md; neither is in the Phase 19 block, and both must still be present verbatim afterwards)
  </read_first>
  <action>
    Two records, both of them constraints on Phase 19 rather than statements about
    Phase 13, which is why they go in the two documents Phase 19's planner will read and
    not in a phase summary.

    First, add a row to `.planning/PROJECT.md`'s Key Decisions table for the owner's
    ruling of 2026-07-28. Its Decision cell states the rule; its Rationale cell carries
    this text, which is the owner's own and must not be paraphrased into something
    weaker:

    Redundant sovereign execution within an owner's node set requires the owner to have
    placed the data on each of their nodes already. The fabric will not move raw
    sovereign data, even between nodes the same owner controls. VER-08/09's "redundantly
    within the owner's own node set" therefore presumes prior out-of-band replication,
    not fabric-mediated fetch.

    Do not weaken it to "usually", "by default", or "in the current design" -- it is
    unconditional under `EgressGuard`'s refusal, and the whole reason for recording it is
    that a later phase will otherwise assume the fabric can fetch. Name the mechanism
    that makes it true (`EgressGuard.send` refuses any frame containing a registered
    payload, Plan 13-04) so the row is checkable. Set the Outcome cell in the style the
    surrounding rows use. In the same edit, add a short clause to the Key Decisions row
    that promoted owner-domain replication, pointing at the new row -- that row says an
    owner's own devices "share data without leaving the trust domain", which is still
    true and now has a stated mechanism, namely the owner placing it there rather than
    the fabric moving it. Leave the integrity-mechanism table near line 26 alone: it
    describes what is verified, not how the data arrives, and adding a transport caveat
    to it would blur a table that is deliberately about integrity.

    Second, edit **only** the Phase 19 block of `.planning/ROADMAP.md`. Add a Constraints
    line, or extend the Research line in that block's existing style, recording two
    things a Phase 19 planner must know before writing criterion 2's plan. One: the same
    ruling, in one sentence with a pointer to the PROJECT.md row -- a task pinned to an
    owner with two live nodes can only execute on both if the owner has already put the
    input on both, because the fabric will refuse to move it. Two: the browser tier now
    inherits the refusal path added in Plan 13-04 with no runtime coverage anywhere --
    `BrowserNode` composes the identical guard, but no sovereign job has ever run in a
    real tab, so the browser's refusal branch is compiled and never executed. That is the
    same structural gap `12-VERIFICATION.md` recorded and `13-03-PLAN.md` already routed
    to WIRE-03; this note names the refusal specifically so the WIRE-03 planner knows
    there is now a behavior to exercise and not only a composition to inspect.

    Touch nothing else in `ROADMAP.md`. The Phase 13 block, the amended criteria, the
    plan list, the milestone checklist and both `EXEMPT_LINES` phrases must be
    byte-identical afterwards -- confirm with `git diff` that the only changed hunk is
    inside the Phase 19 block before finishing.

    Check every sentence written in this task against `vocabulary.node.test.ts`'s banned
    list; both edited documents are inside its jurisdiction.
  </action>
  <verify>
    <automated>npx vitest run packages/node/src/vocabulary.node.test.ts && git diff --stat .planning/PROJECT.md .planning/ROADMAP.md</automated>
  </verify>
  <done>
    `.planning/PROJECT.md`'s Key Decisions table carries the ruling verbatim and
    unconditionally, cross-referenced from the owner-domain replication row.
    `.planning/ROADMAP.md`'s Phase 19 block records both the replication constraint and
    the browser tier's untested refusal path, and `git diff` shows no hunk outside that
    block. `vocabulary.node.test.ts` is green, including its dead-exemption check.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the benchmark's egress leg gets a guard, and the guard is watched failing</name>
  <files>packages/node/src/bench-egress.node.test.ts</files>
  <read_first>
    packages/node/src/bin/bench.ts (the three sites the scan keys on: realFabric's `guard: requestor.egress`, memoryFabric's `requestorGuard` construction and `guard: requestorGuard`, and runnerFor's `submitJobWithEgress` call plus its `manifests[0]` / `entries.length` / `totalBytes` reads. Note the file also *mentions* these identifiers in doc comments, which is exactly why the scan must strip comments before matching)
    packages/node/src/vocabulary.node.test.ts:285-333 and :518-590 (the house idiom: a matcher that is a plain function over content, separated from the file walk, plus a describe block that plants each case in synthetic content and requires the matcher to report it — copy this structure, it is what makes a scanning test honest)
    packages/node/src/disclosure-gate.node.test.ts:200-227 (the other source-scanning guard in this package, for its failure-message style: each reported item names the file, the thing that is missing, and why it matters)
    .planning/phases/phase-13-egress-manifest-completeness/13-VERIFICATION.md lines 386-400 (mutation 4 — the exact defect this test exists to catch, and the reason the type-checker alone is not coverage)
  </read_first>
  <behavior>
    - Against the real bin/bench.ts source, the scan reports no unmet requirements.
    - Against synthetic sources with each requirement individually removed, the scan
      reports exactly that requirement by name -- four planted cases, four catches. A
      scan that reports nothing against the real file proves nothing until it has been
      shown reporting something.
    - Against a synthetic source where the only occurrence of each identifier is inside a
      line comment or a block comment, the scan still reports the requirement unmet. A
      doc comment naming submitJobWithEgress must not be able to satisfy a check that
      the benchmark calls it.
    - The requirement names in the report are readable enough that someone who broke one
      knows which call site to look at without opening the test.
  </behavior>
  <action>
    Create `packages/node/src/bench-egress.node.test.ts`. Structure it the way
    `vocabulary.node.test.ts` structures its scan: a `stripComments(source: string):
    string` helper that removes `//` line comments and `/* */` block comments, and a
    `unmetRequirements(source: string): string[]` matcher that runs over the stripped
    text and returns the names of the requirements the source does not satisfy. Neither
    function touches the filesystem; the file is read once at module scope and the real
    result is computed from it, exactly as `vocabulary.node.test.ts` computes `REPO` once.

    Define four requirements, each with a name, a pattern, and a stated reason, held in a
    data array so the reasons are auditable rather than buried in assertions:

    - the real-libp2p fabric hands the submitting node's own tap to the job path -- the
      `guard: requestor.egress` shape. Its reason names the amended criterion 2: the
      manifest promised is the submitting node's, and this is the site that supplies it.
    - the in-process fabric constructs a tap for the submitting endpoint and supplies it
      the same way -- the `requestorGuard` construction and its use as `guard`. Its
      reason should record, without treating it as a defect, that this fabric's worker
      endpoints are deliberately built over a raw transport: under the amended criterion
      the submitting node's tap is what is promised, so an untapped worker here is the
      scope of the criterion rather than a hole in it.
    - the measured job path calls `submitJobWithEgress` with a guard array rather than
      bare `submitJob`.
    - the returned manifest is actually read -- the `manifests` index together with an
      `entries.length` and a `totalBytes` read. This is the half that mutation 4 showed
      the type-checker only catches incidentally.

    Write the tests: one asserting `unmetRequirements` over the real source is empty,
    with a failure message that lists any unmet name and its reason; one per requirement
    asserting that removing that requirement's text from a synthetic source makes the
    matcher report exactly that name; and one asserting that a synthetic source in which
    every one of the four appears only inside comments reports all four as unmet. Give
    the file a header comment stating what this guard is and, honestly, what it is not:
    it is a call-site shape guard in the idiom this package already uses for
    `purity.node.test.ts` and `disclosure-gate.node.test.ts`, not a behavioural one.
    Record why a behavioural test was rejected -- `bin/bench.ts` runs its ladders and
    real-libp2p sweeps on import through a top-level `main()`, so exercising it means
    running the benchmark, which does not belong in a suite this size -- so the choice
    reads as a decision rather than an oversight.

    Then plant the real mutation. Delete the `guard: requestor.egress` line from
    `packages/node/src/bin/bench.ts`, run this new test file, and confirm it fails naming
    that requirement. Paste the verbatim output into 13-06-SUMMARY.md alongside the
    `npx tsc --noEmit` result for the same mutated tree, so the summary shows what the
    type-checker catches and what only this test catches. Revert with
    `git checkout -- packages/node/src/bin/bench.ts`. Do not run `git add -A`,
    `git commit -a`, or `git checkout` against any other path -- other agents may hold
    uncommitted work in this shared working tree.
  </action>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/o2.services && npx tsc --noEmit && npx vitest run packages/node/src/bench-egress.node.test.ts && git status --short packages/node/src/bin/bench.ts</automated>
  </verify>
  <done>
    `bench-egress.node.test.ts` reports no unmet requirements against the real source and
    is proven able to report each of the four, including when the only occurrence is
    inside a comment. The planted deletion of the real fabric's guard was watched failing
    and reverted, with verbatim output in 13-06-SUMMARY.md. `bin/bench.ts` is clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the requirement ledger → a future verifier | Rows are read as statements of fact by the next independent pass. A row that overstates the mechanism spends that pass's budget on rediscovering a known limit |
| a source scan → the property it claims to guard | A scan matching an identifier that also appears in prose can be satisfied by a doc comment while the call site is gone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-13-12 | Repudiation | A ledger row that claims more than the code does, then gets marked done, hides the limit until an independent pass measures it — the exact failure this milestone was created to catch | mitigate | Task 1 rewrites both rows against the amended criteria and names the two residual limits (payload granularity, response-leg opacity) inside the rows themselves, where a verifier reads them |
| T-13-13 | Tampering | The new scan is satisfied by `bin/bench.ts`'s own doc comments, which name every identifier it keys on, so the call sites could be deleted with the test still green | mitigate | `stripComments` runs before every match, and one planted case proves that a source carrying all four identifiers in comments alone reports all four unmet |
| T-13-14 | Spoofing (of evidence) | A scan asserting "nothing unmet" passes identically when the matcher is broken, the pattern is mistyped, or the file was never read | mitigate | Four planted single-requirement removals, each required to be caught by name, plus one real planted deletion in `bin/bench.ts` watched failing and reverted |
| T-13-15 | Denial of Service | An edit to `.planning/ROADMAP.md` that disturbs either exempted phrase, or any line outside the Phase 19 block, turns `vocabulary.node.test.ts`'s dead-exemption check red for unrelated work in a shared tree | mitigate | Task 1 does not open that file; Task 2 edits only its Phase 19 block and is required to confirm with `git diff` that no other hunk changed. Neither exempted phrase is in that block |
| T-13-16 | Repudiation | The ruling that raw sovereign data never moves is recorded only in a phase summary, so Phase 19 plans owner-domain redundancy assuming the fabric can fetch the input onto a second node | mitigate | Task 2 records it in `.planning/PROJECT.md`'s Key Decisions table and in the Phase 19 roadmap entry — the two documents a phase planner reads — cross-referenced from the row that promoted owner-domain replication, and stated unconditionally with the mechanism named so it is checkable |
</threat_model>

<verification>
`npx tsc --noEmit` exits 0. `npx vitest run packages/node` passes, including
`vocabulary.node.test.ts` with its dead-exemption check and the new
`bench-egress.node.test.ts` with its four planted cases and its comments-only case. The
DATA-05 and DATA-06 entries in `.planning/REQUIREMENTS.md` contain no clause claiming
per-byte detection and none claiming a manifest is retrieved from a node other than the
submitting one. `git diff .planning/ROADMAP.md` shows one hunk, inside the Phase 19
block. `.planning/PROJECT.md`'s Key Decisions table carries the sovereign-data-does-not-
move ruling with no hedging word in it.
</verification>

<success_criteria>
Every `missing:` entry in 13-VERIFICATION.md is either closed by 13-04, 13-05, 13-07 or
this plan, or explicitly superseded by the 2026-07-28 amendment and recorded as such in
the ledger. The last call site the verifier found unguarded is guarded, by a test that
has been watched failing. Nothing in the requirement ledger claims a property the code
does not have. The two rulings this phase produced that constrain later work — sovereign
data never moves, and the browser tier's refusal path is untested — are recorded where
Phase 19 reads, not where a summary is filed.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-13-egress-manifest-completeness/13-06-SUMMARY.md`
</output>
