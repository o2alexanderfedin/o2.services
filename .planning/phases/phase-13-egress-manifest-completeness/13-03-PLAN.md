---
phase: phase-13-egress-manifest-completeness
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - packages/node/src/egress-manifest.node.test.ts
autonomous: true
requirements: [DATA-05, DATA-06]

must_haves:
  truths:
    - "A stream tap installed via FabricNode.start (the identical factory bin/agent.ts calls) fails a running cross-owner job -- names a violation -- when a map step forgets to aggregate and its raw sovereign row crosses the wire, with no test-side call to guard.guard()"
    - "Every job dispatched through submitJobWithEgress against real FabricNode instances carries its egress manifest in the function's own return value -- retrievable from the job's result, not only inside a hand-built fabric"
    - "A job with zero sovereign shards reports a manifest with violations equal to [] and entries.length greater than zero, so a clean run is never indistinguishable from a guard nobody wired"
    - "A pushdown job's manifest total reflects only the derived aggregate that crossed the wire, smaller than the raw sovereign input it was computed from, with zero violations"
    - "Removing registerSovereignInputs's production call in fabric-node.ts makes the DATA-05 falsification test fail; removing the EgressGuard transport wrap makes the DATA-06 clean-manifest test fail -- both mutations are planted, observed failing, and reverted, leaving the tree clean"
  artifacts:
    - path: "packages/node/src/egress-manifest.node.test.ts"
      provides: "the production-wiring proof for DATA-05/DATA-06 over real TCP, via the same FabricNode.start() factory bin/agent.ts calls, plus the two required mutation-and-revert cycles"
      min_lines: 80
  key_links:
    - from: "packages/node/src/egress-manifest.node.test.ts"
      to: "packages/net/src/submit-with-egress.ts"
      via: "submitJobWithEgress dispatched against real FabricNode.egress instances over real RPC"
      pattern: "submitJobWithEgress\\("
    - from: "packages/node/src/egress-manifest.node.test.ts"
      to: "packages/node/src/fabric-node.ts"
      via: "FabricNode.start(...) -- the same factory bin/agent.ts calls, no test-only bypass"
      pattern: "FabricNode\\.start\\("
---

<objective>
Prove ROADMAP Phase 13's three success criteria on real, running `FabricNode` instances
-- not a hand-built fabric, and not by trusting that Plans 13-01/13-02 compile. Per
13-CONTEXT.md's `<specifics>` section, a same-process test spinning up multiple real
`FabricNode.start()` instances over real TCP sockets is accepted evidence in this repo
(the precedent `fabric-node.node.test.ts`'s DATA-09 test already set, and which
12-VERIFICATION.md's addendum independently confirmed satisfies a roadmap criterion
worded "through `bin/agent.ts`" -- `startNode` in that file is the identical factory call
the binary makes). This plan follows that exact pattern.

This plan also performs the two mutations 13-CONTEXT.md's finding demands: removing
Plan 13-02's `registerSovereignInputs` wiring and watching the DATA-05 falsification
test fail, and removing its `EgressGuard` transport wrap and watching the DATA-06
clean-manifest test fail. A guard nobody has watched fail is not a guard -- this repo's
own recorded lesson, twice already (the vocabulary scanner's NUL-byte blind spot, and
`PINNED_WASI_FUNCTIONS`'s identity-only check in Phase 10).

Purpose: turn Plans 13-01/13-02's wiring into demonstrated, falsifiable evidence.
Output: one new node-tier test file, and a clean working tree with both mutations proven
and reverted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-13-egress-manifest-completeness/13-CONTEXT.md
@packages/node/src/fabric-node.node.test.ts
@packages/net/src/sovereign-execution.test.ts
</context>

<interfaces>
<!-- What Plans 13-01/13-02 built, exactly as this plan must call it. -->

From `packages/node/src/fabric-node.ts` (Plan 13-02):
```
export interface FabricNodeOptions {
  readonly blockstoreDir?: string
  readonly sovereignty?: NodeSovereignty   // { ownerId, canExecuteSovereign }
  readonly listen?: readonly string[]
  readonly rpcTimeoutMs?: number
  // ...other fields unchanged
}
export class FabricNode {
  readonly egress: EgressGuard            // new in Plan 13-02
  readonly rpc: RpcEndpoint
  readonly store: Blockstore
  readonly peerId: string
  readonly multiaddrs: readonly string[]
  static async start(options?: FabricNodeOptions): Promise<FabricNode>
  async dial(address: string): Promise<string>
  async stop(): Promise<void>
}
```

From `packages/net/src/submit-with-egress.ts` (Plan 13-01):
```
export type SubmitWithEgressResult =
  | { ok: true; job: JobResult; manifests: readonly EgressManifest[] }
  | { ok: false; error: SubmitError }
export function submitJobWithEgress(
  spec: JobSpec, blockstore: Blockstore, guards: readonly EgressGuard[],
): Promise<SubmitWithEgressResult>
```

From `packages/node/src/fabric-node.node.test.ts:26-36` (the `startNode` helper pattern
this plan's own helper must match -- same `workdir`/`running` bookkeeping, same
`beforeEach`/`afterEach` cleanup):
```
async function startNode(name: string, extra: Partial<FabricNodeOptions> = {}): Promise<FabricNode> {
  const node = await FabricNode.start({
    blockstoreDir: join(workdir, name),
    listen: ['/ip4/127.0.0.1/tcp/0'],
    rpcTimeoutMs: 20_000,
    ...extra,
  })
  running.push(node)
  return node
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The production-wiring proof -- real FabricNode instances, real RPC, all three criteria</name>
  <files>packages/node/src/egress-manifest.node.test.ts</files>
  <read_first>
    packages/node/src/fabric-node.node.test.ts (the whole file -- startNode helper, beforeEach/afterEach workdir bookkeeping, and the DATA-09 describe block at the end, which is this new file's closest sibling in both shape and evidentiary standard)
    packages/net/src/sovereign-execution.test.ts:317-370 (the clean-manifest and falsification test pair -- the assertions this file reproduces, but through real FabricNode/submitJobWithEgress instead of the hand-built ownerFabric and a manually-called guard.guard())
    packages/core/src/executor/fixtures.ts (MODULE_ECHOES_INPUT, MODULE_WRITES_PARTITION -- exact byte fixtures, no build step)
    packages/core/src/job/submit.ts (ShardSpec, JobSpec, NodeDescriptor shapes submitJobWithEgress's spec argument needs)
  </read_first>
  <behavior>
    - DATA-05, falsification: a sovereign shard whose module is MODULE_ECHOES_INPUT, dispatched via submitJobWithEgress against a real FabricNode started with sovereignty {ownerId: 'alice', canExecuteSovereign: true}, produces a manifest whose violations array contains the shard's input CID (computed independently in the test via canonicalCid, not read back off the result) -- and the test file contains no call to guard.guard() anywhere.
    - DATA-06, clean job: the identical setup but with MODULE_WRITES_PARTITION (which ignores its input and never leaks it) produces a manifest with violations equal to [] and entries.length greater than zero and totalBytes greater than zero -- proving a clean pass is never indistinguishable from an unwired tap.
    - DATA-06, public job: a job with a single 'public'-labelled shard against a node with default (unopted-in) sovereignty produces a manifest with violations equal to [] and entries.length greater than zero.
    - Criterion 3, pushdown: a sovereign shard whose value is a multi-field object larger than MODULE_WRITES_PARTITION's fixed output, dispatched the same way, produces a manifest with violations equal to [] and a totalBytes/output smaller than the raw shard value's own canonical encoding -- the aggregate crossed, not the raw input.
  </behavior>
  <action>
    Create egress-manifest.node.test.ts following fabric-node.node.test.ts's exact
    scaffolding: the same workdir mkdtemp/rm in beforeEach/afterEach, a running: FabricNode[]
    array cleaned up the same way, and a startNode helper identical in shape to the one
    quoted in this plan's interfaces block. Import submitJobWithEgress from '@o2/net'
    alongside RemoteExecutor; import canonicalCid, encodeCanonical from '@o2/core' for
    independent CID/size computation; import MODULE_ECHOES_INPUT, MODULE_WRITES_PARTITION
    from '../../core/src/executor/fixtures.ts' (the same test-only relative import
    fabric-node.node.test.ts already uses, with the same comment explaining why).

    Structure four tests across two describe blocks -- 'DATA-05 -- production
    registration lets the tap catch a real leak, through the same factory bin/agent.ts
    uses' and 'DATA-06 -- every job's manifest is reachable from its own result metadata'.
    Each test: start one node named 'alice' with sovereignty {ownerId: 'alice',
    canExecuteSovereign: true} and one node named 'requestor' with no sovereignty option;
    dial requestor to alice; put the test's module onto requestor.store; build the
    JobSpec with shards: [{value: <the row>, label: 'sovereign', ownerId: 'alice'}] (or
    'public' for the public-job test, with no ownerId), executors: [new
    RemoteExecutor(alice.peerId, requestor.rpc)], nodes: [{nodeId: alice.peerId, ownerId:
    'alice', canExecuteSovereign: true, load: 0}] (or the public equivalent), redundancy:
    1; call submitJobWithEgress(spec, requestor.store, [alice.egress]); assert
    result.ok, result.job.shards[0].verification.status, and result.manifests[0]'s
    fields per the behavior list above. For the falsification test's violation-label
    assertion, compute `const inputCid = await canonicalCid(<the row>)` independently in
    the test and assert `result.manifests[0].violations).toContain(inputCid.ok ?
    inputCid.cid.toString() : '<unreachable>')` (fail loudly rather than silently if
    encoding fails) -- do not read the label back from the result to avoid a test that
    passes by construction. For the pushdown test, additionally compute
    encodeCanonical of the raw row and of result.job.shards[0].verification.output (when
    status is 'agreed') and assert the output's byte length is smaller, mirroring
    sovereign-execution.test.ts's criterion-3 test.

    Add a doc comment at the top of the file naming this file's evidentiary standard
    explicitly: same-process, multiple real FabricNode.start() calls over real TCP,
    accepted per 13-CONTEXT.md's `<specifics>` section and fabric-node.node.test.ts's own
    precedent -- and stating plainly that no test in this file calls guard.guard()
    directly, which is the entire point.
  </action>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/o2.services && npx tsc --noEmit && npx vitest run packages/node/src/egress-manifest.node.test.ts</automated>
  </verify>
  <done>
    All four tests pass against real FabricNode instances over real TCP. Grep confirms
    zero occurrences of a bare `.guard(` call in the new test file. `tsc --noEmit` clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prove both guards can fail -- plant, watch fail, revert</name>
  <files>packages/node/src/fabric-node.ts</files>
  <read_first>
    packages/node/src/fabric-node.ts (post-Plan-13-02 state -- grep for `registerSovereignInputs(` and `new EgressGuard(transport` to find the exact current lines; 13-CONTEXT.md itself warns line numbers drift between planning and execution, so locate them fresh rather than trusting a cited number)
    packages/node/src/egress-manifest.node.test.ts (this plan's Task 1 output -- know which specific test each mutation must break before planting anything)
  </read_first>
  <action>
    Mutation 1 (DATA-05): edit fabric-node.ts so the served executor reverts to its
    pre-Phase-13 shape -- `guardSovereignty(new WasmExecutor({nodeId:
    libp2p.peerId.toString(), blockstore}), sovereignty)` with no
    `registerSovereignInputs(...)` wrap. Run `npx tsc --noEmit` (expect clean -- removing
    a wrap does not break types) and `npx vitest run
    packages/node/src/egress-manifest.node.test.ts`. Confirm the DATA-05 falsification
    test fails, and capture why: the echo module still runs and still returns the raw
    input as output (the task is not refused -- it is cleared), but
    result.manifests[0].violations no longer contains the input CID, because nothing
    told the guard to watch for it. This is a strictly different failure signature from
    "the task was refused" -- confirm the captured assertion failure names an empty or
    missing violations array, not an outcome.ok mismatch. Revert with `git checkout --
    packages/node/src/fabric-node.ts`. Confirm `git status --short` is clean and `npx
    tsc --noEmit` is clean again.

    Mutation 2 (DATA-06): edit fabric-node.ts so `const rpc = new RpcEndpoint(...)`'s
    first argument reverts from `egress` to `transport` (leave the `egress` field's
    construction and class wiring otherwise intact, so this isolates exactly the
    transport-wrap half of the fix). Run `npx tsc --noEmit` (expect clean) and `npx
    vitest run packages/node/src/egress-manifest.node.test.ts`. Confirm the DATA-06
    clean-job test fails: result.manifests[0].entries.length stays zero forever, because
    `egress` is constructed but nothing ever sends through it -- the RPC layer sends
    through the raw `transport` instead. Confirm the captured failure is the
    `entries.length > 0` assertion, not the `violations` assertion (this is the
    empty-manifest trap made concrete: violations would still read `[]`, which is why
    this phase's tests always pair the two assertions). Revert with `git checkout --
    packages/node/src/fabric-node.ts`. Confirm `git status --short` is clean and `npx tsc
    --noEmit` is clean again.

    After both mutation-and-revert cycles, run the full regression suite once more and
    confirm the baseline test count is unchanged from the end of Plan 13-02 (no test was
    added or removed by this task -- it only exercised existing ones under mutation).
  </action>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/o2.services && git status --short && npx tsc --noEmit && npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <done>
    Both mutations were planted, each broke exactly the test named for it (and no other),
    with a failure signature distinct from "the task was refused" or a type error. Both
    were reverted. `git status --short` is empty. `npx tsc --noEmit` is clean. The full
    suite passes at the same count as the end of Plan 13-02.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| this plan's test file → the claim it proves | A test that has never been watched to fail is not evidence; this is the boundary the whole plan exists to cross |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-13-07 | Repudiation | `egress-manifest.node.test.ts` could pass vacuously if a test's assertion is satisfied by an empty or default value rather than genuine tap behavior | mitigate | Task 2's two planted mutations are the direct falsification proof; each must break exactly one named test with a specific, documented failure signature, not merely "some assertion failed somewhere" |
| T-13-08 | Information Disclosure | The browser tier (`BrowserNode.egress`, wired identically in Plan 13-02) has no equivalent real-process proof in this plan -- `BrowserNode.start()` requires a real `indexedDB` and a real relay, neither available in the node vitest project, mirroring the exact gap `12-VERIFICATION.md` recorded and routed to human verification for Phase 12's DATA-09 | accept | Structurally identical wiring to `fabric-node.ts` (same functions, same composition order, `tsc`-clean against the same types), verified by code review rather than a runtime test in this phase. Routed to human verification below, exactly as 12-VERIFICATION.md did for the same underlying constraint; real browser-tier coverage is Phase 19's WIRE-03 |
</threat_model>

<verification>
`npx vitest run packages/node/src/egress-manifest.node.test.ts` green with 4 tests,
none skipped. Both planted mutations, run individually, produce the specific failure
each is documented to produce, then are reverted with a clean `git status --short`. Full
suite green at the post-Plan-13-02 count. `npx tsc --noEmit` clean.
</verification>

<success_criteria>
All three ROADMAP Phase 13 success criteria are demonstrated against real `FabricNode`
instances dispatched over real RPC, through the same factory `bin/agent.ts` calls --
without any test in this phase calling `EgressGuard.guard()` by hand. Both of this
phase's new guards (the auto-registration wrap, and the transport wrap itself) are proven
capable of failing, and reverted clean.
</success_criteria>

<human_verification>
  - test: "Dispatch a sovereign-labelled Task at a running BrowserNode (via a real IndexedDB + relay, e.g. a fake-indexeddb-backed unit test or a window.o2 API extension plus a new e2e scenario) and confirm its `.egress.manifest` behaves identically to FabricNode's -- entries recorded, violations empty for a clean pushdown job, non-empty for an echoing one."
    expected: "BrowserNode.egress records real entries for a real dispatched job, with the same clean/violating manifest shape this plan proves for FabricNode."
    why_human: "BrowserNode.start() requires a real indexedDB and dials a real relay; it cannot run in the node vitest project, and the browser-mode vitest project has no Node-side relay to dial against -- the identical constraint 12-VERIFICATION.md recorded for Phase 12's DATA-09 browser-tier gap. The wiring is structurally identical to fabric-node.ts's (same registerSovereignInputs/EgressGuard composition, tsc-clean against the same types, verified in Plan 13-02), but runtime composition-correctness for the browser tier is unproven, exactly as it was left after Phase 12. Phase 19's WIRE-03 is where the browser tier gets real coverage."
</human_verification>

<output>
After completion, create `.planning/phases/phase-13-egress-manifest-completeness/13-03-SUMMARY.md`
</output>
