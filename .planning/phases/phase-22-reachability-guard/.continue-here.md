---
context: phase
phase: phase-22-reachability-guard
plan: 22-01
task: 1
total_tasks: 3
status: not_started
last_updated: 2026-08-08T10:09:27.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Both were discovered by measurement this session, not predicted. `execute-phase.md` parses the
> table below and will require you to answer three questions on each `blocking` row before you
> may proceed.

- [ ] **CONSTRAINT: a count-identical wrong answer.** A green build with the *right* count and the
      *wrong* output is the failure mode this entire phase exists to prevent, and it happened
      here on 2026-08-07 in the elfconv work — see the table. Structural mitigation required:
      **22-01 Task 3's two-granularity reading, scored in ONE run, with `arm A ≡ arm B` as RED.**
      Do not defer Task 3; without it Task 2's anchors are five-of-six satisfiable by a tracer
      that returns "reachable" for everything.
- [ ] **CONSTRAINT: 22-01's own corpus numbers are stale, measured 2026-08-08.** The plan says
      209 callable / 581 total / 869 source files. The tree says **217 / 604 / 907**. Pin the
      measured numbers as **floors**, and never assert the plan's. The plan itself calls its
      interface block "a starting point, not an authority" and demands floors over equalities —
      this is that instruction paying out, not a defect in the plan.

**Do not proceed until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| Count-identical wrong answer | The elfconv loader's first build was green, linked, and emitted the correct **900** functions — with wrong bitcode (`ec2dc6f9` → `1b89e054`). Cause: `STT_GNU_IFUNC` symbols admitted from memory of bfd's source; the bfd in the image does not flag them. Ten extra symbols shifted every later index and, through an **unstable sort over tied addresses**, changed which alias named each lifted function. The count never moved. | **blocking** | Never accept a count as evidence of correctness. Score the corpus at two granularities in one run and assert the **gap**; `arm A ≡ arm B` is red. This is 22-01 Task 3 and it is not optional. |
| Stale plan interface numbers | 22-01's `<interfaces>` block was measured 2026-08-04. Phases 20, 21 and 23 landed after it. Its corpus counts are all low by 2–8%. | **blocking** | Re-measure before pinning; assert floors, never equalities. Task 2 says the same about its anchors: an anchor that changed sides is **a finding to report**, not an assertion to quietly adjust. |
| A handoff that lies | `.planning/HANDOFF.json` claimed *"Phase 26, task 2 of 7, paused"* for ~13 hours after that stopped being true, and `/gsd-resume-work` would have resumed straight into it. Found by `/gsd-progress`, not by the resume path. | advisory | `pause-work` must regenerate **both** artifacts every time. This handoff does; the root `.planning/.continue-here.md` now carries a pointer here rather than a competing story. |
| Editing a table with an open debug session on it | `retake-measured-node-spans.md` is open (*investigating*) against the `vitest.config.ts` span ledger — the exact table **22-04** edits. | advisory | Read that debug file **before** wave 4. The ledger was also updated 2026-08-07 for the elfconv differential; re-measure rather than inherit. |

<current_state>
Phase 22 is the **last unexecuted v1.1 phase** — 4 plans, 0 summaries, 0 verification — and its
requirement **WIRE-02 is the milestone's last unticked ID**. The count is **12 of 15**.

Nothing in the phase has been built. What exists is the pre-task verification 22-01 Task 1
demands: the TypeScript unstable-API contract the whole plan rests on has been checked claim by
claim, and it holds. The working tree is clean and every branch is level with origin.
</current_state>

<completed_work>
- Pre-task: verified every claim in 22-01's `<interfaces>` block. **All structural claims hold**,
  including the one the plan says to stop on — `updateSnapshot({openProjects: ['<abs>/tsconfig.json']})`
  with a **bare path string** returns exactly 1 project. `project.program` and `project.checker`
  are fields; `project.getProgram` is `undefined`. 8 barrels found on disk: aot, bench, browser,
  core, demo, libp2p, net, node.
- Measured the corpus fresh (see blocking constraint): **604 exports — 217 callable, 111 other
  value, 276 type-only** over 907 source files. Per-barrel: core 252 (88 callable), net 78 (32),
  browser 75 (25), bench 49 (18), node 42 (16), demo 40 (16), aot 39 (13), libp2p 29 (9).
</completed_work>

<remaining_work>
- **22-01 Task 1** — the corpus. Barrels from disk; aliases resolved before classifying; type-only
  exclusion stated at the line; counts as floors. Four plants, each watched red.
- **22-01 Task 2** — the call graph. Declaration nodes, four edge classes, `FabricNode.start` as a
  member-expression entry. **One plant per edge class.** Re-measure every anchor.
- **22-01 Task 3** — resolving power. Two granularities, one run, assert the gap.
- **22-02** — the guard. Red both ways; empty corpus / entries / edges each redden.
- **22-03** — dispositions. `autonomous: false`, **stops for the owner**.
- **22-04** — wire into the gate.
</remaining_work>

<decisions_made>
- **Phase 22 over the alternatives** (closing 20/21's PARTIAL criteria, settling the four open
  rulings, or running the milestone audit) — it is the last unexecuted phase and it certifies what
  every other phase wired.
- **Inline sequential execution, not spawned executor agents.** `execute-phase.md` sanctions this
  fallback explicitly. It also avoids that workflow's `gsd-sdk` state writers, which CLAUDE.md bans
  after three separate corruptions of STATE.md.
- **Floors, not equalities, using freshly measured numbers.** Following the plan's own instruction
  rather than its stale figures.
</decisions_made>

<blockers>
- **22-03 will stop for an owner decision.** Its criteria forbid granting a disposition on the
  basis of which tier or factory a symbol belongs to — that is a judgement reserved to the owner.
- **`retake-measured-node-spans` debug session is open** on the table 22-04 edits.
</blockers>

## Required Reading (in order)

1. `.planning/phases/phase-22-reachability-guard/22-CONTEXT.md` — §§ *THE FINDING THAT DECIDES
   THIS PHASE* (the 54/3/102 table), *Four edge classes*, *A proof that cannot fail is not a
   proof*. The plan's objective says to read these three before starting.
2. `22-01-PLAN.md` — all of it. Its `<proof>` blocks are the specification, not commentary.
3. `packages/node/src/purity.node.test.ts` — the shape this file follows, especially why
   `violationsIn(label, source, rules)` is split out so a planted violation can be handed to it.
4. `packages/node/src/requirements-ledger.node.test.ts` — establishes that a barrel is not a
   caller, and states the limit this phase removes: *"It reads call syntax, not reachability."*
5. `.planning/debug/retake-measured-node-spans.md` — **before wave 4 only.**

## Infrastructure State

- Working tree **clean**; `main` / `develop` / `feature/phase-18-…` all **0 ahead, 0 behind**.
- Submodule `third_party/elfconv` clean at `9655e33`, matching the gitlink on all three branches.
- Nothing running. No background processes. No worktrees beyond the main checkout.
- Three upstream PRs open (`yomaytk/elfconv` #263/#264/#265) with **no CI reported** — waiting on
  the maintainer, nothing actionable.

<context>
The habit that paid for itself all session: treat every document's claim about the code as a
hypothesis and measure it. Six claims turned out false — elfconv's README on x86 support, the
praxis review on `verifyChain`'s depth bound, two of my own comments written before I had run the
check, a commit message asserting Phase 26 items were deleted when they are demonstrably live,
and 22-01's own corpus counts.

Phase 22 is *about* this failure mode. Three cheap approximations of criterion 1 scored the same
209 exports at 54, 3 and 102 — the answer was dominated by the instrument, not the codebase. So
the phase's first job is not to take a reading; it is to build an instrument whose resolving power
is itself measured. Task 3 is what makes the rest non-vacuous.
</context>

<next_action>
Start with: 22-01 Task 1. Create `packages/node/src/reachability.ts` exporting `barrelExports`,
`reachableFrom` and `ENTRY_POINTS`, keeping every function pure over its arguments except one thin
loader — the test must be able to hand a planted **string** to the predicate, never a path. Derive
the eight barrels from disk. Resolve aliases with `getAliasedSymbol` before classifying. Pin the
measured floors (217 callable, 604 total), assert the TypeScript version from
`node_modules/typescript/package.json`, and **do not edit `package.json`** — it is contended.
Then run all four of Task 1's plants and record each observed red.
</next_action>
