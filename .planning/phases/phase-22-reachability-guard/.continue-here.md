---
context: phase
phase: phase-22-reachability-guard
status: COMPLETE
score: 2/3
verified_at: fee26c2
last_updated: 2026-08-08T12:50:00.000Z
supersedes: the 2026-08-08T10:09 handoff, which said "task 1 of 3, not_started"
---

# Phase 22 is DONE — this file is a record, not a resume point

4 plans, 4 summaries, `22-VERIFICATION.md` at **2/3**. Do not resume into it.

**This file said `status: not_started, task 1 of 3` until 2026-08-08T12:50.** That was true when
written and stopped being true six commits later. A handoff that outlives its truth is the
anti-pattern this very file recorded on arrival — *"a handoff that lies"* — and it caught itself.

## What landed

| Plan | Commit | |
|---|---|---|
| 22-01 | `fa9820c`, `9f30271`, `7f8a74b` | the tracer, the call graph, both granularity arms |
| 22-02 | `a5fa2bd` | the guard, its message contract, its liveness |
| 22-03 | `7e03562` | the disposition register |
| 22-04 | `d7beefe` | armed in `.githooks/pre-commit` |
| verification | `fee26c2` | 2/3 |

## The score, and why criterion 1 is declined rather than closed

- **Criterion 2 — MET.** Both named defects planted and watched red.
- **Criterion 3 — MET.** A real commit adding an unwired export was attempted and **the hook
  refused it at exit 1**, naming the symbol. The CI→hook substitution is stated, not assumed:
  DEMO-04 forbids `.github/` existing at all.
- **Criterion 1 — DECLINED.** It asks the guard to *"pass clean"*. It does not: **66 unreachable,
  24 disposed, 42 open**. Disposing the rest would have made it pass and made the guard
  decoration. RULING A forbids rewriting the criterion, and it was not rewritten.

## What the instrument is, for whoever reads it next

`packages/node/src/reachability.ts` — a declaration-level call graph over **five** edge classes
(static import, barrel re-export, Vite `?worker`, `new URL` worker entry, and function
reference), with callees resolved **through the TypeScript checker** rather than by name
matching. No regex and no comment stripper anywhere: the walk uses the AST's own `isX` type
guards and `node.forEachChild`.

**Two defects in it were found by watching a plant, and neither by reading.** The first graph had
zero edges (trailing-slash root; the API lowercases paths). Then the `FabricNode.start` plant
changed *nothing*, which exposed type annotations being counted as call paths — the silent
over-connection failure, worth nine findings, and it would have shipped as a clean run.
