---
phase: phase-14-signed-artifact-resolution
plan: 05
type: execute
wave: 4
depends_on: ["14-03"]
files_modified:
  - packages/node/src/signed-artifact.node.test.ts
autonomous: true
requirements: [DET-03, DATA-08]

must_haves:
  truths:
    - "A job submitted against a real bin/agent.ts process, carrying a record signed by the key that process was started with, resolves and executes end to end — the agent's peer id is the agreeing node"
    - "The same job with the record omitted fails, and the reason that travels back names the module CID and the missing signed record"
    - "The same job with a record signed by a key the agent was not started with fails, naming the untrusted signer"
    - "A record that verifies against the agent's pinned key but names a different artifact fails, naming both CIDs — a valid signature for one module does not authorise another"
    - "In every refusal above the agent's blockstore directory never acquires the module block, while in the accepted case it does — the module's bytes were never fetched, so nothing could have been instantiated from them"
    - "An agent started with no --trust-anchor prints exactly [KERNEL_TRUST_ANCHOR] in its handshake and refuses a record signed by any other key; an agent started with --trust-anchor <pub> prints exactly [pub] — so the flag is read as replacing the default, not extending it, across a real process boundary"
  artifacts:
    - path: "packages/node/src/signed-artifact.node.test.ts"
      provides: "ROADMAP criterion 3 in the form it is written — bin/agent.ts against a real signed artifact — plus criteria 1 and 2 across a real operating-system process boundary"
      min_lines: 150
  key_links:
    - from: "packages/node/src/signed-artifact.node.test.ts"
      to: "packages/node/src/bin/agent.ts"
      via: "spawn(process.execPath, [AGENT, '--dir', dir, '--trust-anchor', pub]) then RemoteExecutor dispatch over the resulting RPC connection"
      pattern: "spawn\\("
    - from: "packages/node/src/signed-artifact.node.test.ts"
      to: "packages/node/src/fs-blockstore.ts"
      via: "readdir of the agent's own blockstore directory, where FsBlockstore writes one file per block named by its CID"
      pattern: "readdir\\("
---

# Phase 14 — Signed Artifact Resolution · Plan 05

**Closes:** DET-03, DATA-08 — the criterion-3 proof
**Wave:** 4 · **Depends on:** 14-03 · **Autonomous:** yes

No new mechanism. Plan 14-01 built the guard and watched it fail under three planted
defects; Plan 14-03 composed it into `FabricNode` and gave `bin/agent.ts` its
`--trust-anchor` flag. What is still missing is the thing ROADMAP criterion 3 asks for
in as many words: *"Running `bin/agent.ts` against a real signed artifact resolves and
executes it end to end, proving `signName`/`SignedNameResolver` sit on the production
dispatch path rather than only in their own spec."*

This plan is that proof, in the evidentiary form this repository has settled on for
that phrase. "Started via `bin/agent.ts`" means what `sovereignty-placement.node.test.ts`
and `egress-refusal.node.test.ts` established it means: the node under test is a genuine
operating-system process spawned from that binary, sharing nothing with the test process
but a socket. The submitter is a `FabricNode` in the test process, because the binary is
serving-only and never calls `submitJob` — there is no second process it could be.

<objective>
Prove all three ROADMAP criteria across a real process boundary, and prove the
"before `WebAssembly.instantiate`" half of criterion 2 with an instrument that exists on
the far side of that boundary rather than with an argument about layering.

The instrument: the agent's own blockstore directory. `FsBlockstore` writes one file
per block, named by its CID string (`fs-blockstore.ts:101-103`). `WasmExecutor`'s first
act is `blockstore.get(task.moduleCid)`, and the agent's blockstore is a
`FetchingBlockstore` that pulls a missing block from its peers and writes it to the
local store on the way through (`block.ts:120`). So the module block appearing in the
agent's directory is a direct reading of "this process went and got the module's bytes",
and its absence is a direct reading of "it did not". A guard that refuses before
`inner.execute` produces the second; a guard that refuses after would produce the first.

Purpose: everything before this plan is a claim about wiring. This is the measurement.
Output: one new `.node.test.ts` spawning real agent processes, with six readings and no
new production code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/phase-14-signed-artifact-resolution/14-CONTEXT.md
@packages/node/src/two-process.node.test.ts
@packages/node/src/sovereignty-placement.node.test.ts
</context>

<interfaces>
<!-- The exact scaffolding to reuse, and the contracts under test. -->

From `packages/node/src/egress-refusal.node.test.ts` (the most recent instance of the
pattern, and the one that already takes extra argv — copy this shape, do not invent a
new one):

```ts
const AGENT = fileURLToPath(new URL('./bin/agent.ts', import.meta.url))
type AgentProcess = ChildProcessByStdio<null, Readable, Readable>
interface Agent { readonly peerId: string; readonly multiaddrs: readonly string[]; readonly dir: string; readonly child: AgentProcess }
async function spawnAgent(name: string, extraArgs: readonly string[] = []): Promise<Agent>
// spawn(process.execPath, [AGENT, '--dir', dir, ...extraArgs], {stdio: ['ignore','pipe','pipe']})
// then await the one-line JSON handshake on stdout, 30s budget, stderr captured into the failure message
async function stopAgent(agent: Agent): Promise<void>   // SIGTERM, SIGKILL fallback after 10s
// beforeEach: workdir = await mkdtemp(...); afterEach: stop every node/agent, rm workdir
```

From `packages/node/src/bin/agent.ts` (as amended by Plan 14-03):

```
usage: agent.ts --dir <blockstore-dir> [--port <n>] [--owner-id <id> [--can-execute-sovereign]] [--trust-anchor <hex> ...]
```
`--trust-anchor` is repeatable and **replaces** the default rather than extending it.
Omitting it yields `[KERNEL_TRUST_ANCHOR]` — the same expression `bin/seed.ts` writes, so
the two binaries answer identical input identically and the only difference between two
running nodes remains discovery. The handshake line the binary prints gains a
`trustAnchors` field carrying the array it actually passed to `FabricNode.start`; that is
the reading this plan uses, and it is why "the default is what the source says" does not
have to be taken on trust here.

From `@o2/core`:

```ts
export function signName(privateKey: Uint8Array, fields: Omit<NameRecord, 'signature' | 'signer'>): NameRecord
export function submitJob(spec: JobSpec, blockstore: Blockstore): Promise<SubmitResult>
export interface JobSpec { moduleCid; shards; executors; nodes; redundancy; moduleRecord? }
export interface NodeDescriptor { nodeId: string; ownerId: OwnerId; canExecuteSovereign: boolean; load: number }
```

From `packages/core/src/job/verify.ts` — how a refusal travels home. `executeVerified`
runs each executor exactly once; an `{ok: false}` outcome becomes a `failures` entry, and
when every receipt fails the shard is
`{status: 'insufficient', reason: 'every executor failed', failures}` where each failure
is `{nodeId: string, reason: string}`. With `redundancy: 1` and one executor, the
refusal text is `failures[0].reason` — that is where every assertion below reads from.

From `packages/node/src/fs-blockstore.ts`:

```ts
#pathFor(cid: CID): string { return join(this.#dir, cid.toString()) }
```
One file per block, named by the CID string. `FsBlockstore.open` also filters `.tmp-`
prefixed entries, so a plain `readdir` of the directory is a list of block CIDs plus
possibly a stray temp file — filter the same way rather than asserting a bare length.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: bin/agent.ts against a real signed artifact, and against four things that are not one</name>
  <files>packages/node/src/signed-artifact.node.test.ts</files>
  <read_first>
    packages/node/src/egress-refusal.node.test.ts (the whole file — the spawn/handshake/teardown scaffolding with `extraArgs`, the per-file header that states what the file proves and what it deliberately does not, and the discipline of putting the second half of a proof in the file where the instrument actually lives)
    packages/node/src/sovereignty-placement.node.test.ts (the shorter instance of the same pattern, and its 120_000 ms test timeout)
    packages/core/src/executor/fixtures.ts (MODULE_WRITES_PARTITION and MODULE_ECHOES_INPUT — the two fixture modules; do not add a third, the substitution case needs exactly two distinct CIDs and these are they)
    packages/core/src/naming.test.ts:15-18 (the `keypair(seed)` fixture)
    packages/node/src/fs-blockstore.ts (the directory layout and the `.tmp-` filter, so the blockstore-directory reading is written against what the store actually does)
  </read_first>
  <behavior>
    - **Criterion 3, the accepted case.** Spawn one agent with `--trust-anchor <publisher.pub>`. Start a submitter `FabricNode` with `trustAnchors: [publisher.pub]`. Put MODULE_WRITES_PARTITION in the submitter's store, sign a record over its CID, and submit a one-shard public job at `redundancy: 1` with that record and one `RemoteExecutor` aimed at the agent. `result.ok` is true, the shard's `verification.status` is `'agreed'`, and `verification.agreeing` is exactly `[agent.peerId]`. The agent's blockstore directory then contains a file named for the module CID — the positive reading of the instrument every refusal below relies on.
    - **Criterion 1, the bare CID.** The same job with `moduleRecord` omitted. The shard is `insufficient`; `failures[0].nodeId` is the agent's peer id; `failures[0].reason` contains the module CID string and the phrase naming the missing signed name record. The agent's blockstore directory contains no file named for the module CID.
    - **Criterion 2, the unpinned key.** The same job with a record signed by `impostor.priv` over the correct CID. Refused; the reason contains the impostor's public key and the resolver's "not a pinned trust anchor" wording. No module block in the agent's directory.
    - **The substitution attack.** A record signed by `publisher.priv` that is entirely valid — correct key, unexpired, version 1 — but whose `cid` is MODULE_ECHOES_INPUT's, dispatched on a task whose `moduleCid` is MODULE_WRITES_PARTITION's. Refused; the reason contains **both** CID strings. No module block in the agent's directory. This is the case a signature check alone does not catch and the one 14-CONTEXT.md calls the entire point of the check.
    - **The default, read rather than assumed.** A second agent spawned with **no** `--trust-anchor` at all. Two readings from it, in one test: its handshake line's `trustAnchors` array equals `[KERNEL_TRUST_ANCHOR]` imported from `@o2/demo` — the binary's default, observed in the process rather than inferred from the source — and the criterion-3 job, correct key, correct CID, valid signature, is refused against it with the untrusted-signer wording, because `publisher.pub` is not that anchor. No module block in its directory.
    - **The flag replaces the default.** The criterion-3 agent — spawned *with* `--trust-anchor publisher.pub` — prints `trustAnchors` equal to `[publisher.pub]` and nothing else. Read against the reading above, this is one field shown taking both values, and it is what makes "replaces rather than extends" a measurement. It also fixes the two binaries' symmetry in place: `bin/seed.ts` writes the identical default expression, which `trust-anchors.node.test.ts` compares textually, but no test spawns that binary, so **the seed's runtime no-flag behaviour is unmeasured**; say so in the file header and name what would measure it (spawning `bin/seed.ts --port 0 --ws-port 0 --dir <tmp>` and reading its printed anchors line, which is not done here because that binary boots a Vite dev server).
    - Every one of the cases uses a **freshly spawned agent with its own directory**, so a block fetched by an earlier case cannot make a later case's directory reading pass or fail for the wrong reason. State that in a comment where the fixture is set up.
  </behavior>
  <action>
    Create `packages/node/src/signed-artifact.node.test.ts`.

    Copy `egress-refusal.node.test.ts`'s `spawnAgent(name, extraArgs)`, `stopAgent`, and
    `beforeEach`/`afterEach` scaffolding into this file. Copying rather than sharing is
    the established convention for spawn helpers in this package — every `.node.test.ts`
    that spawns already carries its own — and following it keeps this file readable
    standalone. Use the same `120_000` ms test timeout.

    Write the file header in the register `egress-refusal.node.test.ts` uses. It must
    say, in order: which ROADMAP criteria this file carries and in whose words; what
    "started via `bin/agent.ts`" means here and why the submitter is in-process; what the
    blockstore-directory reading is an instrument for and why it is a real reading rather
    than an inference (the module block appears in that directory only if the agent's
    `FetchingBlockstore` went and got it, which `WasmExecutor` does as its first act);
    and — explicitly — what this file does **not** prove. It does not observe
    `WebAssembly.instantiate`; no test in any process can. It observes that the bytes
    that would have been instantiated were never fetched, which is a stronger reading
    than a layering argument and a weaker one than watching the call, and it should be
    described as exactly that.

    Mint `publisher` and `impostor` with `keypair(seed)`, using seeds distinct from
    every other fixture key in the repository.

    Write a small local helper `blockNames(dir: string): Promise<string[]>` that
    `readdir`s an agent's blockstore directory and filters `.tmp-` prefixed entries the
    way `FsBlockstore.open` does. Assert **containment by CID string**, never a count —
    a count would couple every assertion to how many input blocks a job happens to
    persist, which is not what is under test.

    Write the cases above as separate `it`s. Keep the fixture setup for each inside its
    own test rather than in a shared `beforeEach` beyond the workdir, so each spawns its
    own agent into its own directory and the readings cannot contaminate one another.

    Extend `spawnAgent`'s handshake parsing to keep the whole parsed object, not only
    `peerId` and `multiaddrs`, so the `trustAnchors` field is readable from the returned
    `Agent`. Assert it with `toEqual` against the expected array rather than a length or a
    `toContain` — the claim under test is *exactly this set and no other*, and a
    containment check would pass against a merged set, which is precisely the defect the
    reading exists to catch.

    Assert failure text with `toContain` against substrings that come from the
    production code's own wording — the module CID string, the impostor's hex key, the
    phrase `signed name record`, the phrase `not a pinned trust anchor` — and not against
    whole sentences. A whole-sentence assertion turns every wording improvement into a
    test failure, which is how a test stops being maintained.

    Add no production code. If any assertion here cannot be made to pass without changing
    a file outside this one, stop and report it: that means Plan 14-03's wiring is
    incomplete, and discovering it here is the point of the plan, not an obstacle to it.
  </action>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/o2.services && npx vitest run packages/node/src/signed-artifact.node.test.ts</automated>
  </verify>
  <done>
    Five cases pass against real `bin/agent.ts` child processes: one signed artifact
    executed end to end, and four things that are not one refused with named reasons. The
    module block is present in the agent's directory in the accepted case and absent in
    every refused one. No file outside this test was modified.
  </done>
</task>

<task type="auto">
  <name>Task 2: confirm the production call site is load-bearing — plant, watch, capture, revert</name>
  <files>packages/node/src/fabric-node.ts (planted and reverted, not left modified)</files>
  <read_first>
    .planning/phases/phase-13-egress-manifest-completeness/13-04-PLAN.md (Task 3 — the plant/run/capture/revert discipline and the capture format: the exact command, verbatim failure output including assertion text and file:line, and confirmation of a clean revert)
    packages/node/src/fabric-node.ts (as amended by Plan 14-03 — the `provenance` wrapper and the executor composition it is applied in)
  </read_first>
  <action>
    One mutation, planted in `packages/node/src/fabric-node.ts` only, run, captured
    verbatim in 14-05-SUMMARY.md, and reverted **by file copy, never by git**.

    Take the baseline before planting:

        cp packages/node/src/fabric-node.ts "$TMPDIR/o2-14-05-fabric-node.baseline.ts"

    Revert with

        cp "$TMPDIR/o2-14-05-fabric-node.baseline.ts" packages/node/src/fabric-node.ts
        cmp "$TMPDIR/o2-14-05-fabric-node.baseline.ts" packages/node/src/fabric-node.ts

    and do not continue until `cmp` is silent. **`git checkout`, `git restore`,
    `git stash`, `git reset`, `git add` and `git commit` are forbidden in this task, by
    name.** This working tree is shared with other agents holding uncommitted work, and
    `fabric-node.ts` is the worst hotspot in the repository — five of seven phases modify
    it. `git checkout -- <file>` reverts to HEAD, which would discard Plan 14-03's own
    uncommitted changes to this very file along with anything another agent had in it.
    `cp` from a scratch copy restores exactly what was there. Read-only git
    (`git status --short`, `git diff --stat`) is fine for reporting. Phases 16, 21 and 23
    already use this pattern; this is the same one.

    The mutation: delete the `provenance(...)` application from the executor
    composition, so `FabricNode.start` builds exactly the executor it built before this
    phase while still accepting a `trustAnchors` option that now does nothing. This is
    the precise defect this whole phase exists to make impossible — a mechanism present,
    configured, documented, and called by nobody — and it is the defect the v1.0 audit
    found repeatedly. A phase that closes it without watching the closure fail has not
    established anything.

    Run `npx vitest run packages/node/src/signed-artifact.node.test.ts
    packages/node/src/fabric-node.node.test.ts`. Expect four of the five cases in this
    plan's file to fail (every refusal; the accepted case still passes, because a node
    with no guard runs everything), and the refusal behaviours added to
    `fabric-node.node.test.ts` by Plan 14-03 to fail alongside them. Capture the count
    and at least two verbatim assertions, one from each file.

    Say in the summary what the surviving green test means: the accepted case passing
    under the mutation is exactly why a wiring proof cannot consist of a success. Four
    refusals and one success is the shape that distinguishes a wired guard from an absent
    one; a success on its own does not.

    After reverting, confirm `cmp` reports no difference against the baseline copy, and
    re-run both files plus `npx tsc --noEmit`. If `git status --short` (read-only) shows
    changes outside this plan's own files, report them and leave them alone — they belong
    to another agent.

    **State in the summary what the capture is and is not, in these words.** It is a
    one-time reading taken on the day it was run; no command in this plan or any other
    re-derives it, so it is not a standing guarantee that the call site is load-bearing and
    must not be cited as one. Three things do stand on every later run, and the summary
    should name all three: `signed-artifact.node.test.ts`'s four refusals and
    `fabric-node.node.test.ts`'s refusal behaviours, which go red if the call is deleted;
    and `trust-anchors.node.test.ts`'s comment-stripped assertion that
    `guardModuleProvenance(` is present in `fabric-node.ts` at all, which is the cheap
    check that fires in seconds rather than in spawned processes.
  </action>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/o2.services && npx tsc --noEmit && npx vitest run packages/node/src/signed-artifact.node.test.ts packages/node/src/fabric-node.node.test.ts packages/node/src/trust-anchors.node.test.ts && cmp "$TMPDIR/o2-14-05-fabric-node.baseline.ts" packages/node/src/fabric-node.ts</automated>
  </verify>
  <done>
    The mutation was planted, the failures captured verbatim in 14-05-SUMMARY.md with
    file and assertion named and labelled a one-time reading, and `fabric-node.ts`
    restored by `cp` from the scratch baseline with `cmp` silent. All three test files are
    green and `npx tsc --noEmit` exits 0.
  </done>
</task>

</tasks>

## How this is proved

| Claim (ROADMAP criterion) | Test file | The assertion that fails if it regresses |
|---------------------------|-----------|------------------------------------------|
| Criterion 3 — a real signed artifact resolves and executes through `bin/agent.ts` | `packages/node/src/signed-artifact.node.test.ts` | `expect(shard.verification.agreeing).toEqual([agent.peerId])` for the signed job — the agreeing node is the spawned process, not the submitter |
| Criterion 1 — a bare CID is refused, the refusal naming the missing signature | same | `expect(shard.verification.failures[0].reason).toContain('signed name record')` together with `expect(shard.verification.failures[0].reason).toContain(moduleCid.toString())` |
| Criterion 2 — a key outside the pinned anchors is refused | same | `expect(failures[0].reason).toContain(impostor.pub)` and `toContain('not a pinned trust anchor')` |
| Criterion 2 — refused *before* the module could be instantiated | same | `expect(await blockNames(agent.dir)).not.toContain(moduleCid.toString())` in each refusal, read against `expect(await blockNames(agent.dir)).toContain(moduleCid.toString())` in the accepted case — the same instrument, shown reading both values in the same file |
| A valid signature for one artifact does not authorise another | same | the substitution case's `expect(failures[0].reason).toContain(echoCid.toString())` **and** `toContain(writesCid.toString())` — a reason naming only one of the two would mean the guard compared nothing |
| Omitting `--trust-anchor` keeps the binary's default, and supplying it replaces that default | same | `expect(noFlagAgent.trustAnchors).toEqual([KERNEL_TRUST_ANCHOR])` read against `expect(flaggedAgent.trustAnchors).toEqual([publisher.pub])` — one handshake field shown taking both values, across a real process boundary — together with the no-flag agent's `expect(shard.verification.status).toBe('insufficient')` for a correctly signed job |
| `bin/seed.ts`'s runtime behaviour with no flag | *nothing* | **Unmeasured.** No test spawns that binary. What stands instead: `trust-anchors.node.test.ts` compares the two binaries' default expressions textually, so they cannot drift apart in source. What would measure it: spawning `bin/seed.ts --port 0 --ws-port 0 --dir <tmp>` and reading its printed anchors line — declined here because that binary boots a Vite dev server |
| The production call site in `fabric-node.ts` is load-bearing | Task 2's capture in `14-05-SUMMARY.md` | deleting `provenance(...)` from the executor composition turns four of five cases in this file red, plus the refusal behaviours in `fabric-node.node.test.ts` — verbatim output captured. **A one-time reading, not a standing check**: nothing re-runs it. The standing checks are the rows above, plus `trust-anchors.node.test.ts`'s assertion that `guardModuleProvenance(` is present in `fabric-node.ts` with comments stripped |

## Files touched

- `packages/node/src/signed-artifact.node.test.ts`

(Task 2 plants and reverts `packages/node/src/fabric-node.ts` by `cp` from a scratch
baseline; it is not left modified and is therefore not a file this plan changes. That file
is edited by five phases, so the restore must be exact rather than "back to HEAD" — see
Task 2's forbidden-command list.)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → spawned `bin/agent.ts` process | A genuine operating-system and socket boundary, the same one NET-01 established. The agent shares no heap, no event loop and no module registry with the test that dispatches to it |
| the agent's pinned anchors → everything it will run | Fixed by argv at spawn time, and printed in the handshake so the fixing is observable rather than asserted. Nothing over the wire can add one |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-21 | Denial of Service | A spawned agent that hangs on shutdown leaks across test runs | accept | The existing `stopAgent` SIGTERM-then-SIGKILL-after-10s pattern, already proven in `two-process.node.test.ts` and `egress-refusal.node.test.ts`. Not a new risk this plan introduces |
| T-14-22 | Repudiation | The blockstore-directory reading is claimed as proof that instantiation did not happen, when it proves that the bytes were never fetched | accept | Named in the file header in exactly those words. The two are not the same statement, and the file says which one it makes. The complementary reading — the executor was never called — is `module-provenance.test.ts`'s call counter, in a different process and a different plan |
| T-14-23 | Tampering | A block left in an agent's directory by an earlier case makes a later case's absence reading pass or fail for the wrong reason | mitigate | Each case spawns its own agent into its own directory, stated in a comment at the fixture |
</threat_model>

<verification>
`npx vitest run packages/node/src/signed-artifact.node.test.ts` passes, spawning real
child processes. `npx vitest run packages/node --project node` shows no regression
against the end-of-14-03 baseline, plus this one new file. `npx tsc --noEmit` exits 0.
One mutation capture is present in 14-05-SUMMARY.md with verbatim output naming both
affected files.
</verification>

<success_criteria>
All three of ROADMAP Phase 14's success criteria are proved across a real
operating-system process boundary, dispatched to through `bin/agent.ts` exactly as any
other job is: a signed artifact runs, four things that are not a signed artifact are
refused by name, and in every refusal the module's bytes were never fetched. The binary's
default anchor set and the effect of `--trust-anchor` on it are read out of the process's
own handshake rather than inferred from its source, one field shown taking both values.
Deleting the production call site that makes this true has been watched turning four of
the five readings red — a one-time reading, recorded as one, with the standing checks
named beside it.
</success_criteria>

## Out of scope

- Observing `WebAssembly.instantiate` itself. No test in any process can, and this plan
  does not pretend to — see the file header and T-14-22.
- A second machine. Same machine, different operating-system processes is this project's
  testing standard, and it is the standard this file meets.
- The browser tier's equivalent, which is Plan 14-04's `two-tabs.e2e.test.ts` refusal
  case between two real browser contexts.
- `packages/net`'s in-process fabrics. Their subject is the protocol; they compose their
  own executors and are unaffected by how a `FabricNode` composes its.
- Spawning `bin/seed.ts`. Its no-flag runtime behaviour is unmeasured here, and the reason
  and the recipe are both written down above rather than left as a gap to be found.

<output>
After completion, create `.planning/phases/phase-14-signed-artifact-resolution/14-05-SUMMARY.md`
</output>
