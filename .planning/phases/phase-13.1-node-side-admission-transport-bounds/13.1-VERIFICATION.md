---
phase: 13.1-node-side-admission-transport-bounds
verified: 2026-07-31T20:30:35Z
status: passed
score: 7/7 success criteria verified (6/7 on 2026-07-31; criterion 7 closed 2026-08-02 — see the amendment)
overrides_applied: 0
type_check: "npx tsc --noEmit — exit 0"
host_load_at_verification: "load averages 65.96 / 47.96 (8 cores) — recorded because two criteria are timing-sensitive; no timing bound was set or changed by this verification, and no test failed on time"
gaps:
  - truth: "Criterion 7 — A node asked for a raw sovereign block refuses, whether or not it executed a task over that block"
    status: partial
    reason: >-
      What ships is a property of one *function* over one *job's duration*, not a
      property of a node. `submitJobWithEgress` registers each sovereign shard's
      canonical bytes on the submitter's own guard and releases them in a `finally`.
      Two of the three routes by which a node holds raw sovereign bytes are left
      open, and both are unmeasured. The source says so itself:
      `sovereign-block-refusal.node.test.ts:52` — "Criterion 7 is a property of a
      *node*; what ships here is a property of one *function*."
    artifacts:
      - path: "packages/net/src/submit-with-egress.ts"
        issue: >-
          Registration is job-scoped (taken at :155, released in a `finally`), so a
          peer asking for the same block after the job has ended still receives the
          raw bytes. Stated at :68-75 and unmeasured.
      - path: "packages/net/src/submit-with-egress.ts"
        issue: >-
          The registration fires only when a caller reaches the fabric through this
          function. Bare `submitJob` puts every shard input — sovereign included —
          into the submitter's blockstore and registers nothing. Stated at :77-88.
          `packages/node/src/sovereignty-placement.node.test.ts` drives a real
          spawned-agent sovereign scenario that way and passes *because* the gap is
          real.
    missing:
      - "Registration at a boundary the node owns rather than at one entry point — the submitting node's blockstore-put of a shard labelled sovereign, or a helper both `submitJob` callers share"
      - "A persistent per-node set of sovereign CIDs that survives the job and the process, to close the at-rest case"
deferred:
  - truth: "Criterion 1, second clause — the requestor re-picks on the production job path"
    addressed_in: "Phase 20"
    evidence: >-
      Phase 20 SC1: "`submitJob` is the only function a caller uses to run a job
      through `bin/agent.ts` … `runResilient` no longer exists as a separate,
      uncalled entry point, either merged in or removed". The re-pick is proved over
      the real wire on the coordinator path today; `submitJob` has no re-pick loop.
      13.1-CONTEXT.md names this deferral explicitly (WIRE-04, Phase 20).
  - truth: "Criterion 5 — sender-attribution observed on the production job path"
    addressed_in: "Phase 20"
    evidence: >-
      13.1-CONTEXT.md deferred ideas: "Merging `runResilient` and `submitJob`.
      WIRE-04, Phase 20. Criterion 1's 'requestor re-picks' and criterion 5's
      production-path attribution both become measurable there and are not fully
      measurable before it."
  - truth: "Criterion 7, browser tier — BrowserNode's submitter path"
    addressed_in: "Phase 19"
    evidence: >-
      Phase 19 Requirements include WIRE-03; ROADMAP.md:494-495 records the
      multi-browser Playwright standard that unblocks four items deferred for want
      of a browser environment, `BrowserNode.start` needing a real `indexedDB` and a
      relay being the shared root cause.
---

# Phase 13.1: Node-Side Admission & Transport Bounds — Verification Report

**Phase Goal:** A node refuses work it cannot run, with a stated reason, and neither side of
the wire can be driven past a bound by a peer — closing three defects measured against the
real stack rather than inferred

**Verified:** 2026-07-31T20:30:35Z
**Status:** passed — criterion 7 closed 2026-08-02, see the amendment at the end
**Re-verification:** No — initial verification. Phase executed 5/5 plans on 2026-07-29 with
5/5 summaries and had never been verified.

## Method

Every criterion was scored from production source located by grepping for the **mechanism**,
never the vocabulary, and from tests run in this session. No criterion was scored from a
SUMMARY.md claim. Criteria 1 and 4 were held to the real stack — real `FabricNode`s over
tcp + noise + yamux, started through the same `FabricNode.start` call `bin/agent.ts` makes —
because a fake dispatch that is faster than the real thing cannot see a timing bug.

**Type check:** `npx tsc --noEmit` — **exit 0**.

**Host load:** 65.96 at start, 47.96 at finish, on 8 cores, from an unrelated build. Recorded
because criteria 1 and 4 are timing-sensitive. No test failed on time, so no load-attributed
excuse is being made anywhere in this report, and no timing bound was set or changed.

## Goal Achievement

### Success Criteria

| # | Criterion (abridged) | Verdict | Evidence |
|---|---|---|---|
| 1 | Node at slot limit refuses `exec` with a stated reason naming the limit; requestor re-picks — by concurrency against the real stack | **MET** | `agent.ts:352,373`; `admission.node.test.ts` 6/6 |
| 2 | `LocalCapacity` constructed by both production node factories, or deleted | **MET** | `fabric-node.ts:525,655`; `browser-node.ts:429,476`; `serve-agent-hooks.node.test.ts:27-55` |
| 3 | `readMessage` enforces a declared max message size and calls `stream.abort()` past it | **MET** | `libp2p-transport.ts:197-205`; `transport-bounds.node.test.ts` 16/16 |
| 4 | N shards immediately after dial succeeds for N well above 12, or fails sender-attributed | **MET** | `constants.ts:196`; `transport-bounds.node.test.ts:586-612`; `bench.ts:77` |
| 5 | A connection torn down by the **sender** is not classified as a **node** failure of the receiver | **MET** | `churn.ts:72-83`; `coordinator.ts:487-492`; `transport-bounds.node.test.ts:697-745` |
| 6 | A sovereignty refusal reaches the requestor as a named outcome, not an RPC timeout | **MET** | `agent.ts:217-228,257,448`; `named-refusal.node.test.ts:169-175` |
| 7 | A node asked for a raw sovereign block refuses, whether or not it executed a task over it | **MET** — 2026-08-02 | `submit-with-egress.ts:155` for the job; `SovereignCids` + `submit.ts`'s put for at rest; `sovereign-at-rest.node.test.ts` |

**Score: 6/7**

---

### Criterion 1 — MET

> A node at its execution slot limit refuses an `exec` request with a **stated reason** naming
> the limit, and the requestor re-picks — verified by concurrency against the real stack, not
> by a single-request unit test. Today: 4 peers × 200 concurrent `exec` requests produced 800
> simultaneous `executor.execute()` calls and **0 refusals**

**Mechanism.** The slot is taken on the `exec` branch — the one that costs a
`WebAssembly.compile` plus an `instantiate` plus a linear memory:

- `packages/net/src/agent.ts:350` — slot key derived as `${inputCid}:${partitionIndex}`,
  because an `exec` frame carries no shard id
- `packages/net/src/agent.ts:352` — `capacity.offer(...)`, the *taking* form
- `packages/net/src/agent.ts:373` — refused **before** the `try`, returning
  `{kind:'error', reason: admission.reason}`, so a refused request never reaches the executor
- `packages/net/src/agent.ts:438` — `capacity?.release(slotKey)` in a `finally` covering
  success, a failed outcome, a throw, and the `authorize` refusal that never called the
  executor
- `packages/core/src/placement.ts:389` — the stated reason names the limit:
  `over-committed: ${inFlight} of ${slots} slots in use`
- `packages/net/src/agent.ts:319-326` — the `offer` branch answers from the same counters via
  `LocalCapacity.would` and reserves nothing

**Test run.**

```
npx vitest run packages/node/src/admission.node.test.ts
→ Test Files  1 passed (1)   Tests  6 passed (6)   Duration 8.98s
```

Real stack, not a unit fake: `packages/node/src/admission.node.test.ts:67-77` starts real
`FabricNode`s on `/ip4/127.0.0.1/tcp/0` through the production factory. The concurrency case
at `:141-221` dials two real requestors into a node with `maxConcurrentTasks: 2` and fires
**64 concurrent `exec` requests** (32 from each peer, distinct `inputCid`s so the keys do not
collide), then asserts:

- `executorPeakInFlight <= 2` — the falsifiable half; the `CountingExecutor` can read any
  number at all
- `refusals.length > 0`, every refusal `startsWith('over-committed: ')`, and at least one
  contains `2 of 2 slots in use`
- zero `no-reply` entries — under the pre-fix behaviour half the requestors' RPCs timed out
- `admission.peakInFlight === 2` (the limit was reached, not merely never approached) and
  `admission.inFlight === 0` (every slot given back)

Two independent senders are load-bearing: with one sender, "at most 2" is equally consistent
with a per-sender bound nobody declared.

**Second clause, the re-pick.** `admission.node.test.ts:253-320` proves it over the real wire —
`runResilient` + `remoteDispatch`, refusal composed by the node, classified by
`remoteDispatch`, `shard.attempted === [busy, free]`, one failure of kind `'node'` against the
busy peer. The saturation is *declared* (a slot held directly on the node's own
`LocalCapacity`) rather than raced, and the test says so.

**Recorded residual, deferred not missing.** `runResilient`/`remoteDispatch` have no
production caller — confirmed by grep; `bin/bench.ts:389` says so in its own comment — so the
re-pick is proved as a mechanism, not as production behaviour. The test states this in those
words at `:255-262`, and 13.1-CONTEXT.md routes it to WIRE-04, Phase 20. Not scored against
this phase.

---

### Criterion 2 — MET

> `LocalCapacity` is either constructed by both production node factories, or deleted. It may
> not remain built-and-unwired: today it appears only in two test files while `fabric-node.ts`
> and `browser-node.ts` both pass the opt-out sentinel

**Both factories construct it and pass it:**

| Factory | Constructs | Passes to `serveAgent` |
|---|---|---|
| `packages/node/src/fabric-node.ts` | `:525` `new LocalCapacity({...})` | `:655` `capacity: admission` |
| `packages/browser/src/browser-node.ts` | `:429` `new LocalCapacity({...})` | `:476` `capacity: admission` |

The opt-out sentinel is gone from both. The hook is exposed as a public field named
`admission` — `fabric-node.ts:320`, `browser-node.ts:175` — because `capacity` on `FabricNode`
already means relay reservation capacity since NET-05.

**Test run.**

```
npx vitest run packages/node/src/serve-agent-hooks.node.test.ts \
  packages/node/src/bench-egress.node.test.ts packages/node/src/constants.node.test.ts
→ Test Files  3 passed (3)   Tests  18 passed (18)
```

`serve-agent-hooks.node.test.ts:39` and `:54` assert `occurrences(FILE,
"'accepts-every-offer'") === 0` for both factories — a count of zero is the burn-down, and
`mutation-guard.node.test.ts` (passed, in the 890-test batch below) independently confirms the
ledger's `find` text `      capacity: admission,` is present exactly once in each file
(entries M2a, M2b at `mutation-ledger.ts:119-151`). The benchmark driver was burned down too:
`bench.ts` shows `'accepts-every-offer'` 0 times and `new LocalCapacity(` exactly twice.

**Noted, not scored against this criterion:** the browser factory's wiring is structurally
pinned, not behaviourally run — `BrowserNode.start` needs a real `indexedDB` and a relay, so
it runs in neither vitest project. `serve-agent-hooks.node.test.ts:50-53` records this as
"unmeasured, not met" and refuses to let the count stand in for running it. Criterion 2 asks
for *construction*, which is met and pinned; the behavioural half is WIRE-03, Phase 19.

---

### Criterion 3 — MET

> A peer cannot make a node allocate an unbounded buffer: `readMessage` enforces a declared
> maximum message size and calls `stream.abort()` past it. Today a single **64 MiB** frame was
> sent over the real transport and accepted

**Mechanism.** `packages/libp2p/src/libp2p-transport.ts:188-206` — the check sits **inside**
the `for await`, immediately after the byte count grows, not after the loop:

```
      total += flat.byteLength
      if (total > max) {
        const error = new InboundTooLarge(
          `inbound message from ${from} exceeds ${max} bytes (reached ${total}) — stream aborted`,
        )
        stream.abort(error)
        throw error
      }
```

That placement is the whole content of NET-08 — a cap applied after the loop has already paid
for the allocation it exists to prevent. The declared bound is
`MAX_INBOUND_MESSAGE_BYTES = 8_388_608` (`packages/libp2p/src/constants.ts:118`), overridable
per node via `maxMessageBytes`.

A second bound closes what the per-message cap cannot: a per-peer accumulation budget
(`libp2p-transport.ts:152-163, 256`), derived as `maxMessageBytes ×
MAX_INBOUND_MESSAGES_IN_FLIGHT_PER_PEER` (`constants.ts:157`, value 4) so a budget below one
legal message cannot be expressed. A peer that never closes write is bounded across
concurrent reads, and `InboundOverBudget` also aborts the stream (`:218`).

**Test run.**

```
npx vitest run packages/node/src/transport-bounds.node.test.ts
→ Test Files  1 passed (1)   Tests  16 passed (16)   Duration 13.29s
```

Real transport. `transport-bounds.node.test.ts:231-422` covers: refuses a frame one byte over
an overridden bound and keeps serving; **resets the stream** so a sender still holding a yamux
window is told (`:307`); records the abort reason in the receiver's own log naming peer and
counts (`:361`); delivers a frame of exactly the bound and counts no refusal (`:389`);
enforces the shipped default with no override anywhere (`:401`). `:431-531` covers the
per-peer budget, including that an in-limit message from the same peer is delivered once the
budget frees up.

---

### Criterion 4 — MET

> Dispatching N shards immediately after dial succeeds for N well above 12, or fails with a
> **stated, sender-attributed** reason. Today N=8 completes and N=12 fails entirely with
> `MaxEarlyStreamsError: Too many early streams were opened - 11/10` … which calls
> `muxer.abort()` and tears down the whole connection

**Mechanism.** A per-peer FIFO send gate with a bounded queue in
`packages/libp2p/src/libp2p-transport.ts` (`#gates`, `:235`), with two declared constants:

- `MAX_CONCURRENT_STREAMS_PER_PEER = 8` (`constants.ts:196`) — chosen to sit **below**
  libp2p's un-exported `maxEarlyStreams` default of 10
- `MAX_QUEUED_SENDS_PER_PEER = 256` (`constants.ts:211`) — past which the gate refuses
  immediately with a `SendRefused` (`libp2p-transport.ts:489`) rather than waiting

**Test run** (same 16/16 run as criterion 3).

`transport-bounds.node.test.ts:586-612` — **32 sends immediately after dial**, which is 2.7×
the old cliff:

- all 32 delivered
- zero `MaxEarlyStreamsError` on the receiver's capture
- connection intact afterwards, and a 33rd send still works
- `peakStreamsTo(to) <= MAX_CONCURRENT_STREAMS_PER_PEER` **and** `>= 2` — the lower bound
  matters as much as the upper, since `<= 8` alone would pass if the burst never happened

The negative reading is admissible only because the paired positive runs first: `:543-585`
widens the gate to 32, reproduces the tear-down, and asserts the captured error's own
denominator is 10 — the only runtime read of libp2p's operative default available anywhere in
this repository.

**Sender-attributed refusal** where the criterion allows one: `:631` (refuses past the queue
bound at once, naming this node, rather than waiting) and `:697-724` (over the real transport,
`RpcFailure{send-refused}` with `detail.by === sender.transport.localId` and `detail.to ===
receiver`).

**Production path.** `packages/node/src/bin/bench.ts:77` now ships `SHARDS = 16`, past the
measured cliff at 12, and `:111` declares `DECLARED_ADMISSION_LIMIT = 64` fed to every node
construction in both rigs and printed in the report (`:588`). This is the criterion's own
stated urgency — the benchmark no longer ships one below a connection-killing bound, and the
two published curves are no longer measured under different node behaviour.

Corroborating, at a higher layer: `admission.node.test.ts` fires 64 `exec` requests
immediately after dial across two peers with **zero** no-replies.

---

### Criterion 5 — MET

> A connection torn down by the **sender** exceeding a stream limit is not classified by the
> coordinator as a **node** failure of the receiver

**Mechanism.** A third `DispatchOutcome.kind`:

- `packages/core/src/ports.ts:138` — `SendRefused`, the marker a `Transport` raises to say
  *this node did not send, by its own decision*
- `packages/net/src/rpc.ts:208-213` — `send-refused` produced **only** when the caught cause
  is a `SendRefused`; every other rejection stays `send-failed`
- `packages/net/src/churn.ts:72-83` — `RpcFailure{send-refused}` → `kind: 'sender'`, with the
  reason naming the refusing node: `dispatch to ${nodeId} refused by ${cause.detail.by}`
- `packages/core/src/coordinator.ts:122,186,238,291,355` — `'sender'` carried through the
  outcome, attempt, failure and record types
- `packages/core/src/coordinator.ts:487-492` — the single policy read, with a comment stating
  that `'sender'` falling through to the same retry treatment as `'node'` is a decision, not
  an omission: the value of the third kind is that the **record** names it

**The contrast case is preserved, which is the half most easily lost.** A failed dial to an
unreachable peer arrives as `send-failed` and keeps its `'node'` classification
(`churn.ts:29-44`) — classifying that as `'sender'` would record a dead peer as a failure of
this node, the same misattribution pointed the other way.

**Test run** (same 16/16 run). `transport-bounds.node.test.ts:697-724` and `:726-745` produce
both sides of the distinction over a real transport, not from a hand-constructed
`RpcFailure`. The unit-level cases passed in the 890-test batch (`packages/net/src/rpc.test.ts`,
`packages/net/src/churn.test.ts`).

**Recorded residual, deferred:** production-path attribution is not observable until
`runResilient` and `submitJob` merge — WIRE-04, Phase 20, named in 13.1-CONTEXT.md's deferred
ideas. The shipped gate at 8 sits below libp2p's 10, so the tear-down the criterion describes
is prevented rather than reclassified; when the sender's own bound refuses, the attribution is
correct and named.

---

### Criterion 6 — MET

> A sovereignty refusal reaches the requestor as a **named outcome** carrying its reason, not
> as an RPC timeout. Measured today: `rpc … timed out after 4000ms`, no label, no attribution,
> against a 30,000 ms default

**Mechanism.** A pre-send scan, on both branches that can carry a payload:

- `packages/net/src/egress.ts:287` — `violationIn(frame)`, a pure query that records nothing
- `packages/net/src/egress.ts:315` — `refuse(to, frame)`, which records on a hit only
- `packages/net/src/agent.ts:217-228` — `refusedReason(...)` encodes the candidate reply once
  and asks about it before handing it to the exit, returning
  `egress refused: ${violated} on ${nodeId}` — the label **and** the refusing node
- `packages/net/src/agent.ts:257` — the `block` branch
- `packages/net/src/agent.ts:448` — the `exec` branch, replacing the candidate with a frame
  that by construction cannot carry the payload it refuses

`rpc.ts` is untouched: its responding leg still swallows a send failure by design and
`EgressGuard.send` still refuses on its own. This is the fast path, not the guarantee.

**Test run.**

```
npx vitest run packages/node/src/sovereign-block-refusal.node.test.ts \
  packages/node/src/named-refusal.node.test.ts packages/net/src/named-refusal.test.ts
→ Test Files  6 passed (6)   Tests  30 passed (30)
```

The measurement is what the test **omits**: `packages/node/src/named-refusal.node.test.ts:48`
deliberately passes no `rpcTimeoutMs`, so the live budget is the production
`DEFAULT_RPC_TIMEOUT_MS` of 30,000 ms, unshortened — every other test on this path shortens
it, which is exactly how a 30 s cost stayed invisible. Assertions at `:169-175`:

- `failure.reason.startsWith('egress refused: ')` — named
- `failure.reason` contains the input CID (the label) and `alice.peerId` (the attribution)
- `elapsed < NOT_AN_EXPIRY_MS` where `NOT_AN_EXPIRY_MS = LIVE_RPC_BUDGET_MS / 3` (`:99`) — a
  whole job completing inside a third of the live budget cannot be the budget expiring

The refusal is also confirmed still recorded on the owner's own manifest: it moved earlier, it
did not stop being recorded.

---

### Criterion 7 — PARTIAL

> A node asked for a raw sovereign block refuses, **whether or not it executed a task over
> that block**. Measured today: a submitting node that never ran the task holds no
> registration and serves the raw 95 bytes inside a 138-byte frame

**What is genuinely closed, and it is real.** `packages/net/src/submit-with-egress.ts:142-175`
registers every sovereign shard's canonical bytes on every supplied `EgressGuard` before
`submitJob` runs — `:155`, `held.push(guard.guard(label, encoded.bytes))` — under the label
`canonicalCid(shard.value).cid.toString()`, matching `takeSovereignHold`'s label exactly so
the two registration paths cannot produce two labels for one payload. Released in a `finally`
on every exit. The `block` branch then refuses via `agent.ts:257`.

`packages/node/src/sovereign-block-refusal.node.test.ts` proves it against two real
`FabricNode`s over TCP, with the production 30,000 ms RPC budget live and unshortened. The
claim is checked against **every frame that reached the owner**, not inferred from an outcome,
with the wire tap proved to be capturing before it is allowed to report an absence (`:228-250`)
and the scanner proved able to read as a separate assertion. Passed, in the 30-test run above.
A source scan at `:389-443` pins the set of production files that may call bare `submitJob` at
exactly three, so a **new** production submit path fails loudly rather than quietly inheriting
the gap.

**Why this is PARTIAL and not MET.** The criterion is worded about a *node*. What ships is a
property of one *function* over one *job's duration*. The codebase states this itself, at
`packages/node/src/sovereign-block-refusal.node.test.ts:52`:

> Criterion 7 is a property of a *node*; what ships here is a property of one *function*.

`packages/net/src/submit-with-egress.ts:77-88` says the same. Of the three routes by which a
node comes to hold raw sovereign bytes (13.1-CONTEXT.md's own framing), two are left open:

1. **At rest.** The registration is job-scoped and given back in a `finally`, so a peer asking
   for the same block after the job has finished still receives the raw bytes.
   `submit-with-egress.ts:68-75` — **unmeasured**. Closing it needs a persistent per-node set
   of sovereign CIDs surviving the job and the process. Recorded in 13.1-CONTEXT.md's deferred
   ideas but **scheduled into no later phase**.
2. **Bare `submitJob`.** It content-addresses every shard input and `put`s it into the store it
   was handed, sovereign ones included, and registers nothing — so such a submitter holds the
   row exactly as the guarded one does, unguarded.
   `packages/node/src/sovereignty-placement.node.test.ts` drives a real spawned-agent sovereign
   scenario that way and **still passes, because the gap is real**.
   `submit-with-egress.ts:79-81` — **unmeasured**. Scheduled into no later phase.
3. **The browser tier's submitter path** — unmeasured, and this one *is* scheduled: WIRE-03,
   Phase 19. Deferred, not counted as a gap.

The honest scoping is visible in the executor's own words too: `13.1-04-SUMMARY.md:7` claims
DATA-10 "**for a job in flight submitted through `submitJobWithEgress`**", not the criterion's
full wording. That is a summary that did not overclaim; the shortfall is against the ROADMAP
criterion, not against the summary.

---

### Deferred Items

Not scored against this phase — each is addressed by a later milestone phase.

| # | Item | Addressed In | Evidence |
|---|---|---|---|
| 1 | Criterion 1's re-pick on the **production** job path | Phase 20 | SC1: `submitJob` becomes the only job path; `runResilient` merged in or removed. Named as WIRE-04/Phase 20 in 13.1-CONTEXT.md |
| 2 | Criterion 5's attribution observed on the production job path | Phase 20 | 13.1-CONTEXT.md: "criterion 5's production-path attribution … not fully measurable before it" |
| 3 | Criterion 7 / criterion 2 behaviour on the **browser** tier | Phase 19 | WIRE-03 in Phase 19's Requirements; ROADMAP.md:494-495 |

### Recorded Consequence — NOT a defect

`packages/net/src/discovery.test.ts:261-285` pins four shards landing on one 1-slot node with
zero refusals. This is a **deliberate, recorded consequence**, not a regression: Phase 13.1
moved the slot reservation out of `LocalCapacity.offer` (where it had no release path and
would have leaked one slot per peer per liveness probe) into the `exec` branch, which does
have one. That removed cross-shard over-commit protection, and `agent.ts:300-318` carries both
candidate mechanisms for rebuilding it, both protocol changes. **It is Phase 18's criterion 2c
to rebuild, and that test is expected to turn red when Phase 18 closes it.** Verified as
recorded, not reported as a failure.

### Behavioural Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Type check clean | `npx tsc --noEmit` | exit 0 | PASS |
| Criterion 1 on the real stack | `npx vitest run packages/node/src/admission.node.test.ts` | 1 file, 6 tests passed, 8.98s | PASS |
| Criteria 3–5 on the real stack | `npx vitest run packages/node/src/transport-bounds.node.test.ts` | 1 file, 16 tests passed, 13.29s | PASS |
| Criteria 6–7 on the real stack | `npx vitest run …/sovereign-block-refusal.node.test.ts …/named-refusal.node.test.ts …/net/src/named-refusal.test.ts` | 6 files, 30 tests passed | PASS |
| Criterion 2 structural pins | `npx vitest run …/serve-agent-hooks.node.test.ts …/bench-egress.node.test.ts …/constants.node.test.ts` | 3 files, 18 tests passed | PASS |
| No regression across `@o2/net`, mutation guard, coordinator, placement | `npx vitest run packages/net/src …/mutation-guard.node.test.ts …/vocabulary.node.test.ts …/coordinator.test.ts …/placement.test.ts` | 66 files, 890 tests passed, 27.75s | PASS |

### Anti-Patterns Found

None. `TBD`, `FIXME`, `XXX`, `HACK`, `PLACEHOLDER` and `TODO` all scan clean across the
thirteen production files this phase touched (`agent.ts`, `churn.ts`, `egress.ts`, `rpc.ts`,
`submit-with-egress.ts`, `counting-executor.ts`, `placement.ts`, `coordinator.ts`,
`libp2p-transport.ts`, `constants.ts`, `fabric-node.ts`, `browser-node.ts`, `bin/bench.ts`).

Every limitation this verification found was already written down in the source next to the
code that has it, in prose that names what is unmeasured. That is the opposite of the failure
mode this milestone exists to remove, and it is why criterion 7 could be scored honestly at
all.

### Requirements Coverage

| Requirement | Criteria | Status | Evidence |
|---|---|---|---|
| **SCHED-06** | 1, 2 | SATISFIED — ticked | `agent.ts:352,373,438`; `placement.ts:389`; both factories at `fabric-node.ts:525,655` / `browser-node.ts:429,476`; `admission.node.test.ts` 6/6 |
| **NET-08** | 3 | SATISFIED — ticked | `libp2p-transport.ts:197-205`; `constants.ts:118,157`; `transport-bounds.node.test.ts` 16/16 |
| **NET-09** | 4, 5 | SATISFIED — ticked | `constants.ts:196,211`; `churn.ts:72-83`; `coordinator.ts:487-492`; `transport-bounds.node.test.ts:586,697,726`; `bench.ts:77` |
| **NET-10** | 6 | SATISFIED — ticked | `agent.ts:217-228,257,448`; `egress.ts:287,315`; `named-refusal.node.test.ts:169-175` against the live 30 s budget |
| **DATA-10** | 7 | **SATISFIED — ticked 2026-08-02** | Both remaining routes closed by a durable per-node CID set: `sovereign-cids.ts` (Node), `idb-sovereign-cids.ts` (browser), registered at `submit.ts`'s blockstore-put and consulted by `agent.ts`'s `block` branch. `sovereign-at-rest.node.test.ts` 3/3, reddened three ways |

No orphaned requirements: REQUIREMENTS.md maps exactly these five IDs to Phase 13.1, and all
five were claimed by plans.

### Gaps Summary

One gap, and it is a scope gap rather than a broken mechanism. Six of seven criteria are met
with runnable evidence against the real stack, and the three defects the phase was inserted to
close — no admission control on `exec`, an unbounded inbound accumulation, and an unbounded
per-peer send path — are all closed and measured: 64 concurrent `exec` requests bounded to 2
with named refusals, a frame one byte over an 8 MiB declared cap aborted mid-accumulation, and
32 shards dispatched immediately after dial with the connection intact where 12 used to kill
it.

Criterion 7 is the exception. `submitJobWithEgress` closes the submitter route the roadmap
measured — 95 raw bytes inside a 138-byte frame — for the duration of the job, proved against
every frame that crossed the wire. But the criterion is about a **node** and the shipped
guarantee is about a **function**: a peer asking after the job ends still gets the raw bytes,
and a submitter reaching the fabric through bare `submitJob` is unguarded. Both are unmeasured
and neither is scheduled into a later phase, which is what keeps them gaps rather than
deferrals. The fix both share is one move: register at a boundary the node owns — the
blockstore-put of a shard labelled sovereign, or a helper both `submitJob` callers share —
rather than at one entry point.

### Note on phase mode

ROADMAP.md marks this phase `Mode: mvp`, but its goal is not in User Story form (`As a …, I
want to …, so that …`), and neither are the goals of the neighbouring phases carrying the same
marker. Verification therefore proceeded against the seven Success Criteria, which are the
authoritative contract and are substantially more specific than a user story would be. Flagged
for the owner, not treated as a defect of this phase.

---

_Verified: 2026-07-31T20:30:35Z_
_Verifier: Claude (gsd-verifier) — goal-backward, adversarial stance_


---

## Amendment — 2026-08-02: criterion 7 closed, and this verification's verdict changes

**Status: `gaps_found` → `passed`.** The one gap this document found is closed. Nothing else
in it is revised — the six criteria it scored MET were met then and are met now.

### What was open

Two of the three routes by which a node comes to hold raw sovereign bytes:

1. **At rest.** `EgressGuard`'s registration is job-scoped and given back in a `finally`, so
   a peer asking after the job still received the raw bytes.
2. **Bare `submitJob`.** It `put`s every shard input into the store it was handed, sovereign
   ones included, and registered nothing.

### Why the obvious fix was the wrong one

Not releasing the hold would have closed both — and reintroduced a defect this repository had
deliberately removed. `egress.ts`'s `guard()` forbids eviction and says why: *the set is
bounded by giving holds back, which is a bound somebody can be shown.* A hold never given
back removes the only bound it has, and `submit-with-egress.ts` names that as *exactly the
unbounded scan cost Plan 13-07 was written to remove* — every later frame scanned against
every row the node ever submitted.

### What shipped instead

`EgressGuard` is keyed on **payload bytes** and answers by scanning. A `block` request names
a **CID**, so the at-rest question never needed the scan. A second mechanism — `SovereignCids`,
a durable per-node set of CIDs — answers it by lookup, and its growth costs memory rather
than per-frame work. **The two coexist**; neither replaces the other, and each answers the
question it is cheap at. `EgressGuard` stays job-scoped, which is why
`sovereign-block-refusal.node.test.ts`'s readings that `registrations` is `[]` after a job
remain true and remain valuable.

Registration happens at `submit.ts`'s blockstore-put — the boundary `submit-with-egress.ts`
itself named as the fix, in those words — which is what covers bare `submitJob` rather than
only the callers who remembered.

Both tiers: `FsSovereignCids` (dot-prefixed file beside the blocks) and `IdbSovereignCids`
(a **separate** IndexedDB database, because eviction is per-record and a set inside the
blockstore could be evicted while the blocks it guards survive — failing open).

### Evidence

`packages/node/src/sovereign-at-rest.node.test.ts`, 3/3, over two real `FabricNode`s:

| Mutation | Result |
|---|---|
| `block` branch stops consulting the durable set | ❌ both refusal cases; **control passes** |
| `submitJob` stops registering at the put | ❌ both refusal cases; **control passes** |
| the set never reaches disk | ❌ **only the restart case**, `ENOENT` |

The third is the load-bearing one: an in-memory set passes everything except the restart,
which is what proves "survives the job" and "survives the process" are separate readings.
The control — a public block from the same store, still served — is what stops "refuses
everything" scoring as a pass.

### Still open, and unchanged by this amendment

- **The browser tier's submitter path** — WIRE-03, Phase 19. `IdbSovereignCids` exists and is
  wired, but nothing measures it there. Deferred, as before, not counted as a gap.
- **IndexedDB eviction.** A tab that loses the database comes back not knowing a row was
  sovereign. Stated in `idb-sovereign-cids.ts`; unmeasured.
