---
phase: 37-the-six-stage-funnel-and-a-frozen-telemetry-schema
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/net/src/funnel-schema.ts
  - packages/net/src/funnel-schema.test.ts
  - packages/net/src/index.ts
  - packages/cloudflare/src/funnel-journal.ts
  - packages/cloudflare/src/funnel-journal.test.ts
  - packages/cloudflare/src/funnel-collector.e2e.test.ts
  - packages/cloudflare/src/worker.ts
  - packages/cloudflare/src/index.ts
  - packages/cloudflare/package.json
  - packages/browser/src/funnel-reporter.ts
  - packages/browser/src/funnel-reporter.test.ts
  - packages/browser/src/funnel-reporter.node.test.ts
  - packages/browser/src/ice-observer.ts
  - packages/browser/src/ice-observer.test.ts
  - packages/browser/src/index.ts
  - packages/browser/src/disclosure.ts
  - packages/browser/demo/main.ts
  - packages/browser/demo/policy.html
  - packages/node/src/funnel-live.e2e.test.ts
  - packages/node/src/funnel-attribution.e2e.test.ts
  - packages/node/src/proxy-figures.node.test.ts
  - packages/node/src/disclosure-four-elements.node.test.ts
  - vitest.config.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/phase-37-the-six-stage-funnel-and-a-frozen-telemetry-schema/37-RUNBOOK.md
  - .planning/phases/phase-37-the-six-stage-funnel-and-a-frozen-telemetry-schema/37-01-SUMMARY.md
autonomous: false
requirements: [RUN-04, RUN-05]

must_haves:
  truths:
    - "Six named stages are counted — page load, consent, WSS to bootstrap, ICE gathering, connection classified, first task executed — and every count is readable from a running fabric through one HTTP read, with a fresh store reading six honest zeros rather than an estimate"
    - "The counts survive the object being evicted and rebuilt, because they are banked to Durable Object storage by a writer that refuses a total lower than the stored one — the `relay-service-journal.ts` shape, and for the same reason"
    - "Six synthetic arms, one per stage: a client made to fail at exactly stage k moves `stalledAt[k]` by exactly one and every other `stalledAt` by zero. Two drop counters moving on one failure is the defect criterion 2 exists to catch"
    - "The schema answers exactly three questions — the scaling curve, the WebRTC failure rate by country and network class, diurnal churn — and every field names which one it answers. A field answering none fails a guard, not a review"
    - "The freeze is a mechanism: a hand-written digest over the field set is compared against the field set on every run, so ADDING a field reddens the suite. It is not a promise in a document"
    - "A dump of everything the store holds — `queryKeys({})` over the whole object, every value decoded — contains no IP address and no cross-session identifier, with a positive control proving an IP was in fact present in the request that produced the record"
    - "The proxy bounds are labelled as proxies wherever they are quoted, enforced by a guard that reddens when a label is removed, and no counter in the funnel is ever initialised to anything but zero"
    - "The telemetry legal basis is NOT chosen by this plan. The reporter is armed at CONSENT while the ruling is pending — the intersection of both readings — and the population the counts are over is stored beside them and echoed in every report"
    - "No deploy, no remote resource, no Cloudflare account touched, the three `ocr-checks-worker*` scripts untouched, and no remote origin literal anywhere on the browser side"
  artifacts:
    - path: "packages/net/src/funnel-schema.ts"
      provides: "RUN-05 — the six stages, the three questions, every field's question, and the frozen digest"
      contains: "FUNNEL_STAGES"
    - path: "packages/net/src/funnel-schema.test.ts"
      provides: "criterion 3 — the freeze as a guard, with a plant for a seventh field and a plant for a field answering no question"
    - path: "packages/cloudflare/src/funnel-journal.ts"
      provides: "criterion 1 — counters banked where eviction cannot reach them, refusing a rollback"
      contains: "FUNNEL_JOURNAL_KEY"
    - path: "packages/cloudflare/src/funnel-collector.e2e.test.ts"
      provides: "criterion 4 — the store dumped on a local workerd, with the IP proved present at the request and absent from the store"
      contains: "queryKeys"
    - path: "packages/browser/src/funnel-reporter.ts"
      provides: "RUN-04 — the six stages instrumented on the visitor's side, over a port so it runs in the node lane"
    - path: "packages/node/src/funnel-attribution.e2e.test.ts"
      provides: "criterion 2 — six arms, one failure per stage, each moving exactly one drop counter"
    - path: "packages/node/src/proxy-figures.node.test.ts"
      provides: "criterion 5 — a guard that reddens when a proxy figure loses its label"
  key_links:
    - from: "packages/cloudflare/src/worker.ts"
      to: "packages/cloudflare/src/funnel-journal.ts"
      via: "POST /funnel banks; GET /funnel reports the banked totals"
      pattern: "FunnelJournal|funnel"
    - from: "packages/browser/src/funnel-reporter.ts"
      to: "packages/net/src/funnel-schema.ts"
      via: "the reporter names stages from the frozen list, so a stage the schema does not have cannot be sent"
      pattern: "FUNNEL_STAGES"
    - from: "packages/browser/demo/main.ts"
      to: "packages/browser/src/funnel-reporter.ts"
      via: "the arming point — consent while the ruling is pending, page load only under reading B"
      pattern: "FunnelArming|armFunnel"
    - from: "packages/browser/src/ice-observer.ts"
      to: "packages/browser/demo/main.ts"
      via: "installed before libp2p constructs any RTCPeerConnection, or it observes nothing"
      pattern: "RTCPeerConnection"
---

# Phase 37, Plan 1 — The Six-Stage Funnel and a Frozen Telemetry Schema

**Goal, verbatim from `.planning/ROADMAP.md`:** *As the project owner, I want every visitor's
progress from page load to first task executed counted at six named stages and reporting live
before anyone is recruited, on a schema designed backward from exactly three questions, frozen
before recruitment, aggregate-only, and discarding raw IP at collection, so that I can see where
the funnel leaks without holding data about who leaked out of it.*

You are executing this with none of the planner's context. Everything you need to start is below
or is named by path and line. Read `<read_first>` on each task before touching anything.

---

## The five success criteria, VERBATIM from `.planning/ROADMAP.md`

Each clause is load-bearing. Plan and verify against these words, never against a paraphrase —
including the paraphrases in this plan's own task titles.

  1. Six stages are instrumented and reporting live — page load → consent → WSS to bootstrap → ICE gathering → connection classified → first task executed — with each stage's count readable while the fabric is running, **before recruitment begins in Phase 39**. Standing the funnel up after the first invite means the milestone's headline number was never taken
  2. Each stage's drop-off is attributable: a synthetic client made to fail at exactly one stage moves exactly that stage's count and no other. A funnel where one failure moves two counters cannot tell anyone where a cohort was lost
  3. The schema answers exactly three questions and is frozen before the first invite — the scaling curve, the WebRTC failure rate by country and network class, and diurnal churn. A field that answers none of the three is not collected, and adding a field after recruitment begins breaks the freeze rather than improving the dataset
  4. Telemetry is aggregate-only and raw IP is discarded at collection: a dump of everything stored contains no IP address and no cross-session identifier, checked against the store rather than against the collector's intent
  5. **No published figure derived from this instrumentation is merged with or backfilled from a proxy.** The bounds available today are proxies and are labelled as proxies wherever quoted — 10–20% relay-required industry guidance, and 70%±7.1% DCUtR hole-punch success with ~30% relay fallback and 11% symmetric NAT from a *different* protocol (arXiv:2510.27500). No published figure exists for what fraction of a general audience cannot participate; this milestone is how the real number gets taken

**Requirements, verbatim from `.planning/REQUIREMENTS.md`:**

`RUN-04` — *The six-stage connectivity funnel — page load → consent → WSS to bootstrap → ICE
gathering → connection classified → first task executed — is instrumented and reporting live
**before** recruitment begins. **No published figure exists for what fraction of a general
audience cannot participate.** The bounds available are proxies and are labelled as proxies
wherever quoted: 10–20% relay-required industry guidance, and 70%±7.1% DCUtR hole-punch success
with ~30% relay fallback and 11% symmetric NAT from a *different* protocol (arXiv:2510.27500).
This milestone is how the real number gets taken*

`RUN-05` — *The telemetry schema is designed backward from exactly three questions — the scaling
curve, the WebRTC failure rate by country and network class, and diurnal churn — is **frozen
before recruitment**, is aggregate-only, and discards raw IP at collection. **Open question 3
changes this row**: whether the legal basis is consent or legitimate interest is contested across
sources rather than merely unresolved. The engineering recommendation — carry telemetry consent
on the same gate as `BROW-06` rather than showing a second banner — is a **design recommendation,
not a compliance ruling**, and is flagged for legal review before shipping*

---

## THE OWNER DECISION THIS PHASE CARRIES AND DOES NOT CLOSE — read before Task 4

**Open question 3 is settled by legal review, not by engineering judgement**, and it arrives
here already half-drafted. Phase 35 wrote both readings verbatim and stopped; `.planning/phases/
phase-35-conditions-of-entry-in-the-browser/35-01-SUMMARY.md` § *Task 7* holds them, and
`packages/node/src/disclosure-four-elements.node.test.ts:212-231` holds an **absence guard**
asserting that `legitimate interest`, `legal basis` and `GDPR` appear nowhere in the disclosure.
That guard exists so an agent cannot record a compliance ruling as a code change. **It must be
inverted in the same commit as whichever sentence lands** — Task 7 — and never left half-turned.

**The engineering recommendation on record — carry telemetry consent on the same gate as
`BROW-06` rather than showing a second banner — is a design recommendation, not a compliance
ruling.** Do not present it, in code comments or in the SUMMARY, as settling anything.

### The consequence is real and asymmetric, and criterion 1 depends on it

| | **Reading A — consent** | **Reading B — legitimate interest** |
|---|---|---|
| Who may be counted | Only visitors who turned the report on | Every visitor |
| What the funnel then is | A measurement over a **self-selected opted-in subset** | A measurement over the whole population |
| What must be published beside it | That self-selection, as a property of the numbers | The minimal record's contents, stated **exactly** |
| What is owed before recruitment | Nothing further | A **documented balancing test** |
| Stage 1's count | Equals stage 2's by construction; the first drop-off is invisible | Independent of stage 2; the first drop-off is measurable |

### How this plan is sequenced so that NOTHING waits on the ruling

The reporter has one arming point and it is a single named value.

**While the ruling is pending, the reporter is armed at CONSENT.** That is not a choice between
the two readings — it is their **intersection**. Reading A permits only consent-armed collection;
reading B permits consent-armed collection *and* page-load-armed collection. So the pending
default is lawful under either ruling, and the ruling can only ever **widen** it. Collecting under
the wrong basis is the irreversible error; not collecting yet is the reversible one.

Tasks 1 to 6 each carry a `<ruling>` block stating exactly what changes if the ruling goes the
other way. **Three of them change nothing at all** (T1 the schema, T2 the collector, T3 the
privacy properties — both readings demand those). **T4 changes one value**, the arming point, and
adds what is owed beside the numbers. **T5 gains an arm** under reading B and loses none. **T6
flips one assertion**, the `page-load` arm's, and that is the only assertion in this plan the
ruling moves. T7 is the ruling itself; T8 records it.

**What this plan therefore cannot close.** Criterion 1's *"each stage's count readable while the
fabric is running"* closes now, over the population the arming point admits. The *whole-population*
reading of that criterion needs reading B, and the SUMMARY must say so rather than implying the
funnel already measures everybody. **Descoped is not satisfied; unmeasured is not met.**

---

## What already exists, and what it does NOT establish

**BROW-01 (consent before compute), BROW-02 (start-outcome gossip) and NET-14 (the traffic split)
are CLOSED and stay closed.** Nothing here re-plans them, and nothing here may be reported as
satisfied by whatever already satisfies them.

| Already in the tree | What it does | Why it is NOT `RUN-04` |
|---|---|---|
| `packages/core/src/start-outcome.ts` + `packages/net/src/start-report.ts` — BROW-02 | Every node holds a ledger of coarse start outcomes (`browser` family + `StartResult`) and gossips it peer to peer over `RpcEndpoint`. `START_FAILURES` is a closed list; `isStartBrowserLabel` refuses a full UA string | It travels **over the fabric**, so it can only be sent by a node that already started — i.e. by a visitor who already passed stages 3, 4 and 5. Its own header says so: *"A node blocked from starting usually cannot reach a peer, and a node that cannot reach a peer cannot say so."* A funnel whose whole subject is where visitors fall out cannot ride the channel that falls out with them. **The funnel must therefore go over plain HTTP**, and that difference is the reason this phase exists at all |
| Same | The ledger is per-node, in memory, and merged from gossip; two nodes legitimately disagree | Criterion 1 wants **one** count readable while the fabric runs, and criterion 4 wants a **store** that can be dumped. Gossip is neither |
| `packages/libp2p/src/traffic-split.ts` — `classifyConnection`, `TrafficSplitCounter` | Classifies a connection `direct` or `relayed`, WebRTC first, because a direct WebRTC address still names the relay that signalled it | This is stage **five's** classifier and this plan uses it directly. It counts *seconds and bytes*, not *visitors*, so it is not a funnel stage on its own |
| Phase 34's `Libp2pTransport.pathTo()` / `controlOnlyPeers` (`packages/libp2p/src/libp2p-transport.ts:512,532`) | Distinguishes a relayed pair that only carries control frames from one that carries work | Stage five must classify **honestly**: a control-only relayed pair is not a peer this visitor can compute with, and reporting it as `relayed` would inflate stage five against stage six |
| `packages/cloudflare/src/relay-service-journal.ts` | Lifetime totals in Durable Object storage, with a writer that **refuses a total lower than the stored one** and refuses to unset or move a marker | This is the **pattern to copy**, not a thing that already counts visitors. Its header states the failure it prevents: an alarm runs on a **fresh instance**, so read-then-write erases the history silently the first time |
| `packages/cloudflare/src/worker.ts` `#traffic` (`:228`) | Counters held by the **object**, not the fabric, so `GET /self` can report them before any connection exists | Same reasoning applies to the funnel, for the same reason: `GET /self` deliberately builds no libp2p node. But `#traffic` is **per-instance and holds no history** — the journal is what survives eviction |
| `packages/cloudflare/src/websocket-connection.ts:86` `CLIENT_ADDRESS_HEADER = 'CF-Connecting-IP'` | The inbound listener **derives every connection's `remoteAddr` from it** and libp2p rate-limits on it | This tier already handles a raw IP on the socket path. Criterion 4 is about the **funnel's** store, and the arm must prove the IP was present in the request and absent from the store — not that this tier never sees one |
| `packages/cloudflare/src/do-datastore.ts` | `queryKeys({})` with no prefix lists **everything** the object stores; `put` refuses `/dht/` and `/o2/` | `queryKeys({})` is criterion 4's instrument. `/journal/` is outside both refusals — `relay-service-journal.ts` picked it for exactly this reason |
| `packages/browser/src/start-probe.ts:38-49` | Reads `globalThis.RTCPeerConnection` to answer `webrtc: boolean` before start | It answers *does this browser have WebRTC*. It observes **no ICE gathering at all**. Nothing in this repository does; stage four has no existing observable and Task 4 builds one |

---

## THE TRAPS — read all four before writing any code

**1. `worker.ts` says the route list is closed, and this phase is the one allowed to open it.**
Its header (`packages/cloudflare/src/worker.ts:17-20`) reads: *"`GET /self` and nothing else.
Every route is a surface, and this tier's surfaces are not this phase's subject — Phase 30 owns
the inbound listener, Phase 31 the record store, Phase 32 the relay. A route added here 'while we
are in the file' would be a capability shipped without the phase that measures it."* Phase 37 owns
the funnel, so the route is this phase's subject and the sentence must be **amended and dated in
the same commit**, in the register this repository uses — see the `SUPERSEDED 2026-08-26 BY PHASE
30` block twenty lines below it. Do not silently add a route under a docblock that forbids it.

**2. A remote origin literal on the browser side is a scope-fence breach waiting to happen.**
If the demo page ever *defaults* its funnel endpoint to a deployed Worker, then every e2e run in
this repository posts to the owner's live Durable Object — money spent and a store polluted, by a
test suite. **The endpoint is configuration with no default.** Absent configuration, the reporter
is inert. Task 4 adds a guard asserting no `workers.dev` string and no `https://` origin literal
appears in `packages/browser/src/funnel-reporter.ts` or in `packages/browser/demo/main.ts`'s
funnel wiring. `wrangler.jsonc` already carries `o2-bootstrap.af-4a0.workers.dev` and that is fine
— it is the deploy configuration and it is not on a visitor's path.

**3. The demo catalogue does not move, and it must not.** Every visible figure on the demo page is
declared in `packages/browser/src/demo-regions.ts` and counted by `UI_SPEC_TALLY` (`:169`, total
**93** today). The funnel's readout is `GET /funnel` on the hosted tier — an operator's reading,
not a visitor's figure — so no `data-region` is owed. **If you find yourself putting a funnel
count on the demo page, stop.** You then own five edits in one commit: `REGIONS`,
`UI_SPEC_TALLY.total`, `UI_SPEC_TALLY.bySurface[…]`, `UI_SPEC_TALLY.byKind[…]`, and
`.planning/phases/phase-27-demo-ui-driven-by-real-fabric/UI-SPEC.md` §4.x plus §12 — and
`packages/browser/demo/render.ts:68/84/87` throws in **both** directions at paint time, before any
test runs. Criterion 1 does not ask for a visitor-visible count and you should not add one.

**4. Five words are refused repository-wide, and one of them is a word you will reach for.**
`packages/node/src/vocabulary.node.test.ts` runs as a **pre-commit** guard over every tracked file,
`.planning/` included, and refuses five cryptojacking-adjacent words in every inflection — because
each one reads as cryptocurrency to a reviewer who greps and does not stop to check the sense. **Go
and read that file's case titles before you write anything**; they name all five and say why each
was chosen. This plan itself tripped it five times while being drafted, in prose describing the
per-visit identifier criterion 4 forbids, and the sentences were rewritten rather than exempted —
which is the same repair `CLAUDE.md` records `hosted-tier-deploy.node.test.ts` needing twice in one
session. **The word you want for a per-visit value is `identifier`, `nonce` or `handle`.** Finding
this out at commit time costs a refused commit; finding it out here costs nothing.

**5. Prose that quotes a literal can trip the guard over that literal.** `CLAUDE.md` records this
happening twice in one session on `hosted-tier-deploy.node.test.ts`. Task 6 writes a guard that
scans tracked files for proxy figures; **its own registry file and this plan's own quotations of
those figures are inside the scan's reach.** Exclude the registry by path, exclude `.planning/`
deliberately or include it deliberately, and say which you chose and why. Use `grep -v '^#'`
before any `grep -c` count so a comment cannot satisfy a gate.

---

## Conventions — every one of these was paid for

- **`EXIT=$?` on the line immediately after the command.** No pipe, no trailing `tail`, no
  trailing `echo`. A trailing `tail` has made a failing vitest run report success here more
  than once.
- **Run vitest by project:** `npx vitest run --project node`, `--project e2e`,
  `--project browser`. A bare path fans out across all five. **`aot` is a LANE, not a subset** —
  it is not touched by this plan and is not part of its gate.
- **`@playwright/test` is not installed.** E2E specs drive bare `playwright@1.62.0`; browser mode
  runs through `@vitest/browser-playwright@4.1.10`. Import `{ chromium, firefox, webkit }` from
  `'playwright'` and launch via `launchFixtureBrowser` (`packages/node/src/e2e-browser-launch.ts`);
  the three-engine table to copy is `gated-seed.e2e.test.ts:127-131`.
- **Commit with explicit paths** — `git commit -m "..." -- <path> <path>`, never bare. Concurrent
  agents share one working tree and one index. `git add` immediately after each edit group, but
  only **between** test runs, never during one: `discover-arm.node.test.ts` and
  `bench-attestation.node.test.ts` snapshot `git status --porcelain` around themselves.
- **Plants run SERIALLY and never in parallel with another agent.** Snapshot the file immediately
  before planting, restore by the **surgical inverse of your own edit**, verify with `cmp` against
  that snapshot. Never `cp` the whole file, never `git stash`, never `git checkout --` a file you
  did not write. The hunk count is a one-way alarm: more hunks than your plant proves another
  writer; **equal hunks proves nothing**.
- **A proof that cannot fail is not a proof.** Every plant is watched red and the **observed
  failure text is recorded verbatim** in the SUMMARY. **Phase 35 hit a plant that stayed GREEN** —
  removing `worker.terminate()` was invisible to its instrument — and reported it rather than
  deleting the case. Several instruments in this plan can fail to see their own plant; each such
  task says so in advance and pre-authorises the widening.
- **An absence needs a positive control.** Criterion 4 is an absence claim about a store. Prove the
  thing that should be absent was **present at the input**, or the green means nothing.
- **An assertion must not reuse the value it tests.** Expected counts are written as literals.
- **Comparative readings inside one run**, not absolute thresholds. Measure the process with
  `/usr/bin/time -p`, not the machine, and read the host-conditions banner before quoting a
  duration.
- **Descoped is not satisfied; unmeasured is not met.** Never close a gap by widening what counts
  as passing.
- **Branch: `feature/v2-remaining-phases`.** Do not create another and do not switch branches —
  three agents share this working tree.

## SCOPE FENCE — non-negotiable

**No money, no public disclosure, no recruitment.** Do not deploy to the owner's Cloudflare
account, do not create any remote resource, and do not touch the three `ocr-checks-worker*`
scripts. **Local `workerd` only**, launched as
`npx wrangler dev --port <port> --local-protocol http --persist-to <a fresh mkdtemp dir>` with
`CLOUDFLARE_API_TOKEN: ''` and `WRANGLER_SEND_METRICS: 'false'` in the child environment. The
precedent is Phase 35 Task 4, whose SUMMARY records the address form a browser tab dials a local
workerd with: `/ip4/127.0.0.1/tcp/<port>/ws/p2p/<peerId>`, from an `http://localhost` Vite origin.

**Where a design needs a remote resource, it is an OWNER ACT with a runbook** (Task 8), never an
agent task. That includes the one thing this phase genuinely cannot do for itself: pointing a
deployed page at a deployed collector before Phase 39's first invite.

---

## Source coverage audit

| Source | Item | Covered by |
|---|---|---|
| GOAL | *counted at six named stages* | T1 (the names), T4 (the instrument) |
| GOAL | *reporting live before anyone is recruited* | T2 (the read), T5 (the live reading), T8 (the owner runbook) |
| GOAL | *a schema designed backward from exactly three questions* | T1 |
| GOAL | *frozen before recruitment* | T1 (the freeze as a digest guard) |
| GOAL | *aggregate-only, and discarding raw IP at collection* | T3 |
| GOAL | *without holding data about who leaked out of it* | T3 (no cross-session identifier), T4 (in-memory-only visit state) |
| REQ | RUN-04 | T2, T4, T5, T6 |
| REQ | RUN-05 | T1, T3, T7 |
| ROADMAP | Criterion 1 (six stages, live) | T2, T4, T5 |
| ROADMAP | Criterion 2 (one failure, one counter) | T6 |
| ROADMAP | Criterion 3 (exactly three questions, frozen) | T1 |
| ROADMAP | Criterion 4 (aggregate-only, raw IP discarded, checked against the store) | T3 |
| ROADMAP | Criterion 5 (no proxy presented as a measurement) | T6 |
| ROADMAP | Open question 3 reaches this phase | T7 — surfaced as the owner's, never taken; T4's arming point is the intersection so nothing waits |
| CONTEXT | *No third-party analytics SaaS* | Whole plan: the collector is this repository's own Worker. T3 asserts it |
| CONTEXT | *No deploy, no remote resource, no recruitment* | Scope fence; T4's no-remote-origin guard; T8's runbook names the owner acts |

No item is unplanned. Nothing is deferred.

---

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/phase-35-conditions-of-entry-in-the-browser/35-01-SUMMARY.md
</context>

<interfaces>
Contracts the executor needs. Extracted from the tree on 2026-09-02. Use these directly; no
exploration is required to start.

From `packages/cloudflare/src/relay-service-journal.ts` — **the pattern Task 2 copies**:
  `export const RELAY_SERVICE_JOURNAL_KEY: Key = new Key('/journal/relay-service')`
  `export class MalformedRelayJournalError extends Error`
  `export class RelayJournalRollbackError extends Error`
  `export async function readRelayServiceJournal(store: Datastore): Promise<RelayServiceTotals>`
  `export async function writeRelayServiceJournal(store: Datastore, totals: RelayServiceTotals):
  Promise<RelayServiceTotals>` — refuses any field lower than the stored one.
  `has` before `get`, never `get`-and-catch: *"`Datastore.get` signals a miss by throwing, so a
  `catch` would swallow a genuine storage fault as 'no history yet'."*

From `packages/cloudflare/src/do-datastore.ts`:
  `export const REFUSED_NAMESPACE = { dhtDatastore: '/dht/', fabricKeyspace: '/o2/' }`
  `export class DoDatastore extends BaseDatastore` — `queryKeys({})` with **no prefix lists
  everything the object stores**. That is criterion 4's instrument. Measured storage wall:
  *"smallest refused 4_194_304 B → 'string or blob too big: SQLITE_TOOBIG'"* (`:231`).

From `packages/cloudflare/src/worker.ts`:
  `export class BootstrapObject` — `#traffic = new TrafficSplitCounter()`, `#relayLog = new
  RelayServiceLog()`, `#relayRestored: Promise<RelayServiceLog> | undefined`, `#node = new
  HostedNode(state.storage)`; `#node.store` is the `DoDatastore`.
  `async fetch(request: Request)` — upgrade first, then `pathname !== '/self'` → 404, then
  `Response.json({ peerId, nodeKey, instance, version, traffic, relayService })`.
  `export default { async fetch(request, env) { return stubFor(env.BOOTSTRAP, SERVED_BY).fetch(request) } }`
  `const SERVED_BY: HostedObjectName = 'bootstrap-us'` — **a constant, never derived from the
  request**, because a visitor must not be able to cause an object to be created.

From `packages/cloudflare/src/websocket-connection.ts`:
  `export const CLIENT_ADDRESS_HEADER = 'CF-Connecting-IP'`

From `packages/libp2p/src/traffic-split.ts`:
  `export type ConnectionClass = 'direct' | 'relayed'`
  `export function classifyConnection(address: Multiaddr): ConnectionClass` — **WebRTC first**,
  because a direct WebRTC address still names the relay that signalled it.
  `export interface TrafficLeg { readonly connectionSeconds: number; readonly bytes: number }`

From `packages/libp2p/src/libp2p-transport.ts`:
  `export type PeerPath = …` (`:67`), `pathTo(to: string): PeerPath` (`:512`),
  `get controlOnlyPeers(): readonly string[]` (`:532`).

From `packages/core/src/start-outcome.ts` — **the precedent for coarse, closed-list labels**:
  `export const START_FAILURES = ['wasm-unavailable', 'webrtc-unavailable', 'storage-denied',
  'crypto-unavailable', 'no-relay-reachable', 'other'] as const`
  `export const BROWSER_FAMILIES = ['chromium', 'edge', 'firefox', 'safari', 'other'] as const`
  `export function isStartBrowserLabel(value: unknown): value is string` — *"`{@link
  StartOutcome.browser}` has said 'never a full UA string' since it was written, and nothing
  enforced it… The disclosure promise rests on the coarseness."*

From `packages/browser/src/consent.ts`:
  `export class GrantedConsent { readonly disclosureVersion: string; readonly grantedAt: number;
  readonly reportingAllowed: boolean; readonly anchoredTo: string }` — constructor demands a
  module-private symbol, so it cannot be forged.
  `export function readConsent(store, anchoredTo): { ok: true; consent } | { ok: false; gap }`
  `export function grantConsent(store, options): GrantedConsent`
  `export const CONSENT_KEY = 'o2:consent'`

From `packages/browser/src/disclosure.ts`:
  `export const DISCLOSURE_VERSION = '4'` — bumped twice in Phase 35 (`'2'→'3'` for the four
  elements, `'3'→'4'` for the data cost).
  `export const CONSENT_VERSION_NOTE: string`
  `export interface DisclosureLine { readonly question: string; readonly answer: string }`
  `export const DISCLOSURE: Disclosure` — **eight** lines today (counted 2026-09-02; Phase 35
  added two, the telemetry line and the data-cost line); `reporting` is a separate unticked
  extra. Three surfaces read this module (`disclosure.ts`, `demo/index.html`'s inline module
  script, `demo/policy.html`) *"so three things cannot drift apart"*.

From `packages/browser/src/start-probe.ts`:
  `readonly RTCPeerConnection?: unknown` (`:38`); `webrtc: typeof globals.RTCPeerConnection ===
  'function'` (`:49`). **Nothing observes ICE gathering. Stage four has no existing observable.**

From `packages/browser/src/tab-api.ts`:
  `TabActivity { running, tasksExecuted, dutyCycle, hidden, peers, servedFor, fetched, rejected }`
  `TabApi.activity(): TabActivity | null`, `TabApi.stop(): Promise<void>`,
  `TabApi.startReport(): Promise<TabStartReport>`

Demo page DOM ids (`packages/browser/demo/index.html`): `#gate`, `#gate-headline`, `#gate-terms`
(a `<dl>`), `#reporting`, `#allow`, `#decline`, `#gate-version`, `#main`, `#bar`.
The demo accepts `?relay=<multiaddr>` on the URL; `built-bundle.e2e.test.ts` and Phase 35's
Task 4 both use that route.
</interfaces>

---

<tasks>

<task type="auto">
  <name>Task 1: RUN-05 — exactly three questions, six named stages, and a freeze that is a guard rather than a promise</name>
  <files>packages/net/src/funnel-schema.ts, packages/net/src/funnel-schema.test.ts, packages/net/src/index.ts</files>
  <read_first>
    `packages/core/src/start-outcome.ts:41-105` — `START_FAILURES`, `BROWSER_FAMILIES`,
    `MAJOR_DIGITS` and `isStartBrowserLabel`. Read the sentence at `:66-73`: *"a width written
    twice is a width that drifts, and the copy that drifts is the one nobody tests."* Every
    derived value in your schema is derived from one list for that reason.

    `packages/net/src/index.ts:1-8` — this package's charter: *"Portable by the same rule as
    `@o2/core`: no platform imports, no libp2p, no `node:*`. It only knows the ports."*

    `packages/net/src/protocol.ts` and `packages/net/src/start-report.ts` — the two wire
    contracts already living here. That is the precedent for putting a **cross-tier wire
    contract** in this package rather than in `@o2/core`.
  </read_first>
  <behavior>
    - `FUNNEL_STAGES` is a six-element `as const` tuple, in the roadmap's own order:
      `page-load`, `consent`, `wss-bootstrap`, `ice-gathering`, `connection-classified`,
      `first-task`. Any other spelling of a stage does not typecheck.
    - **The stage names are the identifiers; the ordinals are not.** Phase 37's `**Depends on**`
      line in `.planning/ROADMAP.md` says *"Phase 32 (a relay, which is where **stage four's**
      classification comes from)"*, while the six-name success-criterion order makes classification
      the **fifth**. Both sentences are in the same block and they disagree by one. This plan calls
      it stage five throughout because that is the order the success criterion states; keying every
      counter by NAME rather than by index makes the discrepancy harmless, which is why it is done
      that way. Do not reconcile it by renumbering anything.
    - `FUNNEL_QUESTIONS` is a **three**-element `as const` tuple: `scaling-curve`,
      `webrtc-failure-by-country-and-class`, `diurnal-churn`. A guard asserts its length is
      exactly 3, written as the literal `3`.
    - `FUNNEL_FIELDS` names every field the schema collects and, for each, **which question it
      answers**. A field whose `answers` is empty fails a guard.
    - `FUNNEL_SCHEMA_DIGEST` is a hand-written hex literal. A guard recomputes a digest over the
      canonical rendering of `FUNNEL_FIELDS` + `FUNNEL_STAGES` + `FUNNEL_QUESTIONS` and asserts
      equality. **Adding, removing or renaming a field reddens the suite.**
    - `FUNNEL_SCHEMA_FROZEN_AT` is a date literal, and a comment beside it says frozen against
      what.
    - `isFunnelCountry` accepts exactly two uppercase ASCII letters and nothing else; `ZZ` is the
      value for *unknown*. `FUNNEL_NETWORK_CLASSES` is a closed list ending in `unknown`.
    - `FUNNEL_POPULATION` is a two-element list: `opted-in-only`, `all-visitors`. Every report
      carries one. **A count with no population is not readable.**
  </behavior>
  <action>
    Create `packages/net/src/funnel-schema.ts`. It is a pure constants-and-predicates module —
    no I/O, no platform globals, no `Date.now()` — so it runs in the node lane and the browser
    lane from one file, and both tiers import it. **It goes in `@o2/net` and not `@o2/core`
    because `@o2/cloudflare` declares `@o2/net` as a dependency and does not declare `@o2/core`**
    (`packages/cloudflare/package.json`); it is a wire contract between two tiers, which is what
    `protocol.ts` and `start-report.ts` already are. State that reasoning in the header.

    **Design the fields backward from the three questions and write the derivation down.** For
    each field, the header must say which question would go unanswered without it. The
    working set, which you may narrow but must not widen without deleting a question:

    | field | answers | why it is coarse enough |
    |---|---|---|
    | `stage` | all three | one of the six names; nothing else |
    | `entered` / `stalledAt` counts | scaling curve, diurnal churn | integers, per stage |
    | `country` | webrtc-failure-by-country-and-class | ISO-3166 alpha-2 from the edge, `ZZ` when absent. Two letters is a **country**, not a person |
    | `networkClass` | webrtc-failure-by-country-and-class | a closed list, `unknown` when the platform says nothing |
    | `hourBucket` | diurnal churn | integer 0–23, UTC. An hour is not a timestamp |

    **Bound the cardinality by construction and say what the bound is.** The stored record is not
    a cross-product of everything: keep (a) `byStage[6]`, (b) `byStageHour[6 × 24]`, and (c)
    `byCountryClass` **only at the two WebRTC stages** — that is question 2's whole scope, and
    the other four stages do not need a country. Compute the worst-case cell count as a derived
    constant `FUNNEL_MAX_CELLS` and state it in the header beside the measured storage wall
    (`do-datastore.ts:231`: *smallest refused 4 194 304 B*). Task 2 asserts the encoded record
    stays under a stated ceiling; this is where the ceiling's arithmetic lives.

    Write `packages/net/src/funnel-schema.test.ts` — plain `.test.ts`, so it runs in **both** the
    node and browser lanes, which is what a portable module deserves. Cases:
    (1) `FUNNEL_QUESTIONS.length` is `3` — the literal, never `FUNNEL_QUESTIONS.length`;
    (2) every field in `FUNNEL_FIELDS` answers **at least one** question drawn from
    `FUNNEL_QUESTIONS`, and answers **no** question outside it;
    (3) `FUNNEL_STAGES` is the six names in that order, asserted against six string literals;
    (4) **the freeze**: the recomputed digest equals `FUNNEL_SCHEMA_DIGEST`, with a failure
    message that says, in one sentence, that the schema moved and that after recruitment begins
    the correct response is to revert the field rather than to update the digest;
    (5) the floor: `FUNNEL_FIELDS` is non-empty and every stage is reachable from the field set —
    without it, cases 2 and 4 pass over an empty list.

    **The digest must not be derived from the thing it tests, at runtime or in source.** Write it
    by hand from a first run; never `const DIGEST = compute(FIELDS)`. This repository has twice
    had a plant stay green because both sides of an assertion moved together.

    Export from `packages/net/src/index.ts`. Check `packages/node/src/reachability.node.test.ts`
    and `reachability-dispositions.ts` before adding barrel entries — an exported-but-unreached
    symbol is exactly what that guard exists to catch, and Phase 35 left `computing-indicator.ts`
    out of the browser barrel for that reason while `data-cost.ts` went in because it had a
    consumer that was not a page.
  </action>
  <ruling>
    **Nothing here changes under either reading.** The three questions, the six stage names and
    the field set are the same whoever is counted. What changes is only *which visitors* produce
    a row, which is Task 4's arming point. `FUNNEL_POPULATION` exists so the ruling is a value in
    the data rather than a rewrite of the schema.
  </ruling>
  <verify>
    <automated>npx vitest run --project node packages/net/src/funnel-schema.test.ts</automated>
    `EXIT=$?` on the line immediately after. Then
    `npx vitest run --project browser packages/net/src/funnel-schema.test.ts`, `EXIT=$?` next
    line. Then `npx tsc --noEmit`, `EXIT=$?` next line.

    **Three plants, one at a time, each snapshotted with `cp` immediately before the edit.**
    (1) **Add a seventh field** to `FUNNEL_FIELDS`, mapped to a real question. The freeze case
    must go RED. This is criterion 3's own sentence — *"adding a field after recruitment begins
    breaks the freeze rather than improving the dataset"* — made mechanical, and a plant that
    leaves it green means the digest is being recomputed from the field set somewhere.
    (2) **Add a field whose `answers` is empty.** Case 2 must go RED: *"A field that answers none
    of the three is not collected."*
    (3) **Add a fourth question** to `FUNNEL_QUESTIONS`. Case 1 must go RED at the literal `3`.
    Record each observed failure text verbatim. Restore by the surgical inverse and `cmp` against
    the snapshot; `cmp` silence is the check and the hunk count is not.
  </verify>
  <done>
    Five cases green in both lanes, three plants watched red with their texts recorded, the digest
    hand-written and provably not derived from the field set, `FUNNEL_MAX_CELLS` computed and
    stated beside the measured storage wall, and `tsc --noEmit` exit 0. Committed with explicit
    paths.
  </done>
</task>

<task type="auto">
  <name>Task 2: criterion 1's read — six counters banked where eviction cannot reach them, and one HTTP read of them</name>
  <files>packages/cloudflare/src/funnel-journal.ts, packages/cloudflare/src/funnel-journal.test.ts, packages/cloudflare/src/worker.ts, packages/cloudflare/src/index.ts, packages/cloudflare/package.json</files>
  <read_first>
    `packages/cloudflare/src/relay-service-journal.ts` **in full**. Every design decision in this
    task is already argued there: why the write refuses to go backwards (*"`alarm()` runs on a
    fresh instance… the obvious implementation erases the node's whole history the first time an
    alarm fires, and does it silently"*), why the value is JSON **bytes** (*"`has` says true, `get`
    throws `StoredValueNotBytesError`, `queryKeys` answers `[]` — three different answers about
    one key"*), and why the key is under `/journal/` (*"`DoDatastore.put` refuses `/dht/` and
    `/o2/`… it is one key holding six numbers, overwritten in place"*).

    `packages/cloudflare/src/worker.ts:17-45` — the route-list sentence and the
    `SUPERSEDED 2026-08-26 BY PHASE 30` block twenty lines below it. **That block is the format
    for amending the sentence above it. Read both before you touch either.**

    `packages/cloudflare/src/worker.ts:228-260` — `#traffic` and `#relayLog`, and why they are
    held by the **object** rather than by the fabric: *"`GET /self` deliberately does not build
    one — so a counter reachable only through the fabric would make reporting the split either
    impossible before the first connection or expensive on every read."*

    `packages/cloudflare/src/worker.ts:290-330` — `#relayLogOnce()`, held as the **promise** so
    two concurrent requests cannot each restore and double every stored total. Your journal
    restore takes the same shape and for the same reason.

    `packages/cloudflare/src/hosted-record-store.e2e.test.ts:150-190` — the local-workerd
    harness: `waitForReady` polling `GET /self`, and the `spawn('npx', ['wrangler','dev','--port',
    …,'--local-protocol','http'], { cwd: PACKAGE_DIR, env: { …process.env, CLOUDFLARE_API_TOKEN:
    '', WRANGLER_SEND_METRICS: 'false' }, stdio: 'ignore' })`. **It does NOT pass `--persist-to`
    and yours must** — Phase 35's SUMMARY records that omission as a standing finding.
  </read_first>
  <behavior>
    - `POST /funnel` accepts one report and banks it. `GET /funnel` answers the whole banked
      record as JSON, including `population` and the schema digest the record was written under.
    - A **fresh** object answers `GET /funnel` with six honest zeros and the current population —
      never a missing field, never an estimate. `#traffic`'s own comment is the precedent: *"Two
      zeroed columns is a reading; a missing field is not."*
    - Totals **do not go backwards**. A write offering a lower total for any counter is refused
      with a named error, exactly as `RelayJournalRollbackError` is.
    - The record survives the object being destroyed and rebuilt: written to `state.storage` under
      `/journal/funnel`, restored before it is reported.
    - A malformed stored value is a **refusal**, not a silent reset to zero.
    - The encoded record is asserted to stay below a stated byte ceiling derived from
      `FUNNEL_MAX_CELLS`, well under the measured `4_194_304 B` wall.
    - `GET /self` still answers exactly what it answered before. Its response shape does not move.
  </behavior>
  <action>
    Create `packages/cloudflare/src/funnel-journal.ts` modelled line-for-line on
    `relay-service-journal.ts`: `FUNNEL_JOURNAL_KEY = new Key('/journal/funnel')`,
    `MalformedFunnelJournalError`, `FunnelJournalRollbackError`, `readFunnelJournal(store)`,
    `writeFunnelJournal(store, totals)`. `has` before `get`, never `get`-and-catch. JSON bytes,
    never a raw object. **State in the header why this one is not "one key holding six numbers"**
    like its sibling — it holds `FUNNEL_MAX_CELLS` numbers — and why that is still bounded by
    construction rather than by a sweep: the country list is a two-letter pattern, the class list
    is closed, the hour bucket is 0–23, and the stage list is frozen. An unbounded key under
    `/journal/` would be the accumulation window `DoDatastore`'s refusals exist to keep shut.

    Add `"@o2/net": "*"` to `packages/cloudflare/package.json` if it is not already there — it is
    (checked 2026-09-02), so this should be a no-op; verify rather than assume.

    Wire the two routes into `BootstrapObject.fetch`. Hold the journal as a **promise**
    (`#funnelRestored`) exactly as `#relayLogOnce()` does. Keep the counters on the **object**,
    not the fabric, for the reason `#traffic` is there: `GET /funnel` must answer before any
    libp2p node exists.

    **Amend the header's route sentence in the same commit.** Add a dated block in the register
    the file already uses: the sentence *"`GET /self` and nothing else"* was correct when written
    and Phase 37 is the phase that owns this surface, so `/funnel` is a capability shipped **with**
    the phase that measures it rather than without one. Do not delete the old sentence; this
    repository retires a premise by dating it.

    **CORS is required and is not decoration.** The demo page is served from a different origin
    from the Worker in every arrangement that will ever exist — a Vite dev server locally,
    GitHub Pages in production. `POST /funnel` must answer an `OPTIONS` preflight, or accept a
    `text/plain` body so that `navigator.sendBeacon` needs no preflight at all. **Prefer the
    second**: `sendBeacon` is what Task 4 uses for the terminal report, it is the only send that
    survives a page unloading, and `text/plain` is a CORS-safelisted content type. Decide, do it,
    and write down which you chose and why. Keep `Access-Control-Allow-Origin` as narrow as the
    arrangement allows and state what it is set to.

    Write `packages/cloudflare/src/funnel-journal.test.ts` — a pure node-lane spec over the
    existing `do-storage.fixture.ts`, no workerd. Cases: an empty store reads six zeros; a write
    then a read round-trips; a **lower** total is refused by name; a malformed stored value throws
    rather than reading as empty; the encoded record for a saturated cell set stays under the
    ceiling.

    **Do not add a route that does anything else.** `SERVED_BY` stays a constant; nothing in this
    task may derive an object name from a request. That is criterion 6 of Phase 29 and it is not
    reopened here.
  </action>
  <ruling>
    **Nothing here changes under either reading.** The collector counts what it is sent. Under
    reading A it is sent fewer rows; under reading B it is sent more. `population` is a field it
    stores and echoes, and it is written by the reporter, not chosen here.
  </ruling>
  <verify>
    <automated>npx vitest run --project node packages/cloudflare/src/funnel-journal.test.ts</automated>
    `EXIT=$?` on the line immediately after. Then `npx tsc --noEmit`, `EXIT=$?` next line. Then
    the existing hosted-tier guards, which this task's `worker.ts` edit is most likely to break:
    `npx vitest run --project node packages/cloudflare/src/worker.test.ts packages/node/src/hosted-tier-deploy.node.test.ts`,
    `EXIT=$?` next line.

    **Two plants.** Snapshot `funnel-journal.ts` before each.
    (1) **Remove the rollback refusal** from `writeFunnelJournal` — leave the read, delete the
    comparison. The lower-total case must go RED. Record the text.
    (2) **Make `readFunnelJournal` return zeros on a malformed value** instead of throwing. The
    malformed case must go RED. This is `relay-service-journal.ts`'s own stated failure — *"a
    silent reset would make the node's history vanish while every reading still looked
    plausible"* — and watching it red is what says the refusal is doing work.
    Restore each by the surgical inverse and `cmp` before starting the next.
  </verify>
  <done>
    Five pure cases green, both plants watched red with their texts recorded, `GET /self`'s
    response shape unchanged and its guards still green, the route-list docblock amended and dated
    in the same commit, the CORS decision written down with its reason, and no remote resource
    touched. Committed with explicit paths.
  </done>
</task>

<task type="auto">
  <name>Task 3: criterion 4 — the raw IP is discarded at collection, proved by dumping the store with the IP proved present at the door</name>
  <files>packages/cloudflare/src/worker.ts, packages/cloudflare/src/funnel-collector.e2e.test.ts, vitest.config.ts</files>
  <read_first>
    `packages/cloudflare/src/inbound-listener.e2e.test.ts:275-340` — **the finding that decides
    how this task's arm is built.** Its own words: the case was *"written first as 'send no
    `CF-Connecting-IP`, expect a refusal', which failed with `{"cf-connecting-ip": "127.0.0.1", …}`
    — sending one explicitly passes it through"*, and `:331` records `127.0.0.1` as *"what workerd
    stamps"*. **So a local workerd supplies a real client IP header without being asked.** That is
    your positive control and you do not have to synthesise it.

    `packages/cloudflare/src/websocket-connection.ts:32,86` — `CLIENT_ADDRESS_HEADER =
    'CF-Connecting-IP'` and consult §19's sentence about why `remoteAddr` must come from it. This
    tier **legitimately handles a raw IP** on the socket path; criterion 4 is about what the
    funnel's store holds afterwards.

    `packages/cloudflare/src/do-datastore.ts:169-230` — `has(k)` is `true` ⟺ `get(k)` resolves ⟺
    `k` appears in `queryKeys({})`. **`queryKeys({})` with no prefix lists everything.** That is
    the instrument criterion 4 names: *"checked against the store rather than against the
    collector's intent."*

    `packages/core/src/start-outcome.ts:95-105` — `isStartBrowserLabel` and its sentence *"The
    disclosure promise rests on the coarseness, so the range is checked where the range is
    declared rather than at each place a label lands."* Your country and class validators go in
    the same place and for the same reason.

    `MEMORY: an absence needs a positive control.` A phase criterion nearly closed here once on an
    empty read; the control showed a record written seconds earlier read empty too.
  </read_first>
  <behavior>
    - The collector reads `CF-Connecting-IP` **never**, or reads it and writes nothing derived from
      it that could name a device. Whichever you implement, the arm proves it against the store.
    - `country` is taken from the edge's own country header, validated to two uppercase letters,
      and `ZZ` when absent or malformed. It is never derived from the IP by this code.
    - `networkClass` is taken from a named platform field, validated against the closed list, and
      `unknown` when the platform says nothing. **What local workerd actually supplies is measured
      and recorded, not assumed.**
    - A dump of every key the object holds, with every value decoded, contains **no** IP address
      and **no** cross-session identifier: no field of ≥16 hex or base64url characters, nothing
      matching an IPv4 dotted quad or an IPv6 form, and no `CONSENT_KEY`-shaped visitor identifier.
    - **The positive control**: the same run asserts the request that produced the record **did**
      carry a client IP, read back out of the arrangement. An absence proved against an input that
      was never present is not a measurement.
    - No third-party endpoint is contacted. The collector is this repository's own Worker.
  </behavior>
  <action>
    Implement the collection-side derivation in `worker.ts` (or a small pure module beside it, if
    that is cleaner — a pure `funnelDimensionsFrom(headers)` is testable in the node lane and a
    method on the object is not). **The rule is one sentence: the IP may be read, and nothing
    derived from it that survives the request may be stored.** Country is a two-letter code and
    that is a country, not a person. If the platform supplies no country locally, `ZZ` is the
    honest answer and the SUMMARY says so.

    **Measure what local workerd supplies before designing around it.** As step one, run a
    throwaway probe: `wrangler dev` on its own port with `--persist-to` a fresh `mkdtemp`, `curl`
    a `POST /funnel`, and have the handler echo back every request header it received plus the
    keys of `request.cf` if that object exists at all. **Record the result verbatim in the
    SUMMARY.** If `CF-IPCountry` and `request.cf` are absent under local workerd — which is
    likely — then the arm supplies the country header explicitly, exactly as
    `inbound-listener.e2e.test.ts` had to for `CF-Connecting-IP`, and the SUMMARY records that
    the country path is **exercised locally with a supplied header and unmeasured on the edge**.
    Do not report an edge behaviour you did not see.

    Write `packages/cloudflare/src/funnel-collector.e2e.test.ts` — the `e2e` lane, since it spawns
    `wrangler dev` and binds a port. Launch on its own port with `--persist-to` a fresh directory,
    `CLOUDFLARE_API_TOKEN: ''`, `WRANGLER_SEND_METRICS: 'false'`. Then:

    (1) **The floor.** `GET /funnel` on a fresh object answers six zeros. Assert the zeros as
    literals. Without this, every absence below is an absence in an empty store.
    (2) Send a handful of `POST /funnel` reports across different stages, with a client IP present
    (workerd stamps one; assert the handler saw it by echoing a boolean, never the value) and a
    supplied country header.
    (3) `GET /funnel` shows the counts moved — the second floor, and the one that makes (4) mean
    something. A store that never accepted a write cannot be shown to hold no IP.
    (4) **The dump.** Add a test-reachable read of `queryKeys({})` over the whole object — either
    a `GET /funnel?dump=1` guarded to the local arrangement, or a direct read through the
    `DoDatastore` in a node-lane companion case; **prefer whichever does not add a production
    surface**, and if you add one, say in its docblock why it is not a capability shipped without
    a phase. Decode every value and assert: no IPv4 dotted quad, no IPv6, no run of ≥16 hex or
    base64url characters, and no `o2:consent`-shaped identifier. Print the full key list in the failure
    message so a red is diagnosable in one read.
    (5) **The cross-session identifier case, separately.** Send two reports that a naive
    implementation would correlate — same country, same class, same hour, different stages — and
    assert the store gained **no new key** between them. A per-visit key appearing is precisely
    the cross-session identifier criterion 4 forbids, and counting cells is how you see it.

    Register the spec in `vitest.config.ts`'s duration table if `slow-specs.node.test.ts` demands
    it, and move `files`/`unitFiles` by the identity that guard asserts —
    `unitFiles === files - excludedInNode` — which Phase 35's SUMMARY records taking a red test to
    establish.

    **Do not weaken the assertion to make it pass.** If the store does hold something that looks
    like an identifier, that is the finding, and criterion 4 is Partial until the field is removed.
  </action>
  <ruling>
    **Nothing here changes under either reading.** Aggregate-only and IP-discarded are properties
    of the *store*, and both readings of open question 3 demand them. Reading B additionally
    requires the minimal record's contents to be **stated exactly** in the disclosure — that is
    Task 4's disclosure line, and it is written to be true under either ruling.
  </ruling>
  <verify>
    <automated>npx vitest run --project e2e packages/cloudflare/src/funnel-collector.e2e.test.ts</automated>
    `EXIT=$?` on the line immediately after.

    **The plant, and it is the whole task.** Snapshot the collector file. Make the handler store
    the client IP — one field, under the same journal key. Case (4) must go RED **naming the key
    and the value it found**. Record the observed text verbatim. Restore by the surgical inverse
    and `cmp`.

    **The plant may stay green, and you are pre-authorised to widen it.** If the dump instrument
    reads only the journal key rather than the whole object, a plant that writes the IP under a
    *different* key is invisible to it — a green that says nothing about what the object holds.
    If that happens, do not accept it and do not narrow the case: widen the dump to `queryKeys({})`
    with **no prefix**, which is what criterion 4's words require, and record which instrument
    carried the claim.

    **Second plant — the cross-session identifier, which is criterion 4's OTHER absence and needs
    its own watched red.** The criterion names two things that must not be in the store, and the
    plant above only covers the IP. Snapshot the collector, make it write a per-visit nonce of
    ≥ 16 characters into the stored record, and watch **case 4's identifier scan** go red naming the
    field and its value. Record the text.

    **It must be case 4 and not case 5, and the reason is a structural limit of your own design.**
    Case 5 counts *keys*. Under a one-key journal an identifier written **inside** the value adds no
    key, so case 5 cannot see it and would stay green — which is precisely the shape of a proof that
    cannot fail. Case 5 catches a *per-visit key*; case 4's identifier scan catches a *per-visit field*.
    Both failures are real and they are caught by different cases. Say so in the file's docblock so
    a later reader does not delete one as redundant.

    **Third plant, for the positive control itself.** Make the arrangement send **no** client IP
    and confirm the control case goes red — i.e. that "the request carried an IP" is a claim this
    run actually checks and not a sentence in a docblock.
  </verify>
  <done>
    The floor, the movement, the dump and the no-new-key case all green; the IP-storing plant
    watched red with the key it found named in the text; the **nonce plant watched red on case 4**
    with the field it found named; the positive control watched red when the IP is withheld; what local workerd supplies for country and network class recorded verbatim
    whichever way it went; and the spec registered in the duration table with both counts moved by
    the identity. Committed with explicit paths.
  </done>
</task>

<task type="auto">
  <name>Task 4: RUN-04 — six stages instrumented on the visitor's side, with the arming point as the one switch the ruling moves</name>
  <files>packages/browser/src/funnel-reporter.ts, packages/browser/src/funnel-reporter.test.ts, packages/browser/src/funnel-reporter.node.test.ts, packages/browser/src/ice-observer.ts, packages/browser/src/ice-observer.test.ts, packages/browser/src/index.ts, packages/browser/src/disclosure.ts, packages/browser/demo/main.ts, packages/browser/demo/policy.html</files>
  <read_first>
    `packages/browser/src/consent.ts`'s closing paragraphs on lazy environment detection —
    *"Nothing touches `localStorage` at import time — a module that reads browser globals when it
    is loaded cannot be imported by a Node test at all."* Both new modules follow that rule.

    `packages/browser/src/computing-indicator.ts` — Phase 35's model for a **pure module over an
    injected port** that runs in the node lane with no DOM. Your reporter takes a send port
    (`{ send(body: string): boolean }`) for the same reason.

    `packages/browser/demo/main.ts:1150-1290` — `start()`: `requireConsent()` is the **first
    statement**, then `probeEnvironment()`, then `enrolledIssuer`, then `BrowserNode.start`. This
    is where stages two and three are observable and where the arming point goes.

    `packages/browser/src/start-probe.ts:38-49` — `RTCPeerConnection` is read as a *capability*
    and nothing observes ICE. **Stage four has no existing observable in this repository.**

    `packages/libp2p/src/traffic-split.ts:14-45` — why the classifier asks WebRTC **first**: a
    direct browser-to-browser WebRTC address still names the relay that signalled it, so a
    circuit-first classifier *"reports every direct browser-to-browser connection as relayed"*.

    `packages/libp2p/src/libp2p-transport.ts:512,532` — `pathTo()` and `controlOnlyPeers`.

    **Phase 35's Task 3 finding, carried forward because it decides stage six.**
    `35-01-SUMMARY.md` records that `activity().tasksExecuted` is `GovernedExecutor`'s `#executed`
    and that **at a duty cycle of 1 it increments BEFORE `inner.execute(task)`** — so it counts
    tasks *admitted*, not tasks *finished*, and went `0 → 128` inside 800 ms. **Stage six is
    "first task EXECUTED".** Choose the observable by measurement and record which one you chose
    and why, exactly as that task had to.

    `packages/browser/src/disclosure.ts` — `DISCLOSURE_VERSION` is `'4'`. The header's *"Version 2,
    2026-08-14 — and the reason is that version 1 had become FALSE"* section is the model for what
    you write when the terms move: *"The bump is therefore the repair, not the paperwork."*
  </read_first>
  <behavior>
    - `FunnelReporter` is a pure class over a send port and a clock port. Constructed with an
      endpoint or with **nothing**; with nothing it is **inert** and every method is a no-op that
      returns `false`. That is the production default.
    - `enter(stage)` sends one report for that stage, at most once per visit. Re-entering a stage
      sends nothing. The "already sent" set lives in **memory only** — it dies with the tab, which
      is why it is not a cross-session identifier.
    - A visit's furthest stage is held in memory. On `pagehide` the reporter sends one terminal
      `stalledAt` report naming that stage, unless the visit reached `first-task`. It sends the
      terminal report **at most once**.
    - The reporter carries `population` on every send: `opted-in-only` while armed at consent.
    - **The arming point is one value.** `FUNNEL_ARMING` is `'at-consent'` or `'at-page-load'`.
      While open question 3 is pending it is `'at-consent'`, and a comment says that is the
      intersection of both readings and not a choice between them.
    - The ICE observer wraps `RTCPeerConnection` once, before libp2p constructs any, and reports
      `ice-gathering` on the first transition to `gathering` and nothing on later ones. It never
      changes the wrapped object's behaviour: the wrapper delegates and returns.
    - Stage five reports the class as `direct`, `relayed` or `control-only` — `classifyConnection`
      for the first two and `pathTo()` for the third. **A control-only relayed pair is not a peer
      this visitor can compute with**, so folding it into `relayed` would inflate stage five
      against stage six and make the funnel's biggest leak invisible.
  </behavior>
  <action>
    **Why HTTP and not the fabric, stated in the header because it is the whole design.** BROW-02
    already gossips a start outcome over `RpcEndpoint`, and `packages/net/src/start-report.ts`'s
    own header says why that channel cannot carry this: *"A node blocked from starting usually
    cannot reach a peer, and a node that cannot reach a peer cannot say so."* A funnel whose
    subject is where visitors fall out must not ride the channel that falls out with them. So the
    reporter posts over plain HTTP with `navigator.sendBeacon` — which is also the only send that
    survives a page unloading, which is what the terminal report needs.

    **THE TRAP, restated because it is the one that costs money.** The endpoint has **no default
    and no literal**. It arrives from `?funnel=<origin>` on the URL or from a build-time env var,
    and absent both the reporter is inert. Add a case asserting the module source contains no
    `workers.dev` and no `https://` origin literal, reading the file with `readFileSync`.

    **That case must go in `packages/browser/src/funnel-reporter.node.test.ts`, NOT in the plain
    `.test.ts`, and the reason is measured.** `vitest.config.ts:1224` gives the `node` project
    `include: ['packages/*/src/**/*.test.ts']` and the `browser` project the same glob — so a
    plain `.test.ts` runs in **both** lanes, and `readFileSync` does not exist in the browser one.
    `packages/browser/src/gateway-module.node.test.ts` is the precedent for a Node-only spec living
    beside a browser module. Keep the behavioural cases in `funnel-reporter.test.ts` so they run in
    both lanes, and put only the source scan in the `.node.test.ts`.

    Strip comment lines before counting so a sentence explaining the rule cannot fail it — this
    repository has reddened a literal-count guard with the comment explaining that guard, twice.

    Create `packages/browser/src/ice-observer.ts`. It installs a wrapper over
    `globalThis.RTCPeerConnection` that delegates everything and adds one
    `icegatheringstatechange` listener per instance. **Install order is load-bearing**: it must
    run before `BrowserNode.start`, because libp2p constructs its peer connections inside. It must
    be idempotent — installing twice must not double-report — and it must be removable, or the
    node-lane spec cannot run two cases in one file. Give it a unit spec over a fake
    `RTCPeerConnection` covering: first transition reports once; later transitions report nothing;
    two connections report once between them; installing twice does not double-report; the wrapper
    returns what the wrapped constructor returned.

    Wire six call sites in `demo/main.ts`:
    1. **page-load** — module init, held but not sent while armed at consent, then flushed at
       consent so the stage-one count is truthful about when it happened.
    2. **consent** — immediately after `grantConsent` returns, and this is the arming point.
    3. **wss-bootstrap** — when the tab's websocket dial to the bootstrap peer completes. Candidate
       observables: libp2p's `peer:connect` / `peer:identify` (`browser-node.ts:2331-2345` uses
       `peer:identify` for kad-dht for a stated reason), the relay reservation appearing, or
       `Libp2pTransport.peers` gaining the bootstrap peer id. **Choose by measurement, not by
       plausibility**, and record which fired first and which fired at all.
    4. **ice-gathering** — from the observer.
    5. **connection-classified** — on the first connection classified `direct`, `relayed` or
       `control-only`, carrying the class.
    6. **first-task** — the first task this tab actually **executed**. Not `tasksExecuted` at duty
       cycle 1; see `<read_first>`.

    **The disclosure moves and that is an engineering fact, not a legal one.** Add one line stating
    exactly what the funnel sends: six stage names, a two-letter country, a coarse network class,
    an hour of the day, and **no identifier of any kind** — written so it is true under either
    ruling, and written from the code rather than composed. That is what reading B would require to
    be *"stated exactly"*, and reading A does not forbid it. **Do not write a legal basis
    sentence**; `disclosure-four-elements.node.test.ts:212-231` will refuse it and is right to.
    Bump `DISCLOSURE_VERSION` `'4'` → `'5'` with a `CONSENT_VERSION_NOTE` saying what changed, and
    mirror it into `demo/policy.html` **in the same commit** so the three readers cannot drift.
    Then re-run the suites the bump touches — see `<verify>`.

    Check `packages/node/src/reachability.node.test.ts` before adding barrel exports. Phase 35's
    precedent: `computing-indicator.ts` stayed out because the demo's `window.o2` hop is not
    traced; `data-cost.ts` went in because a guard in `packages/node` needed the literal.
  </action>
  <ruling>
    **This is the one task the ruling moves, and it moves one value.**
    - **Under reading A (consent):** `FUNNEL_ARMING` stays `'at-consent'` and `population` stays
      `'opted-in-only'`. **Nothing changes** — the pending default already is reading A. What is
      owed is a sentence in the SUMMARY and beside every published figure saying the funnel
      measures a **self-selected** subset, so stage one equals stage two by construction and the
      first drop-off is not measurable.
    - **Under reading B (legitimate interest):** `FUNNEL_ARMING` becomes `'at-page-load'` and
      `population` becomes `'all-visitors'`. Stage one is then sent before consent, which makes the
      consent drop-off measurable for the first time. Two further things become owed and neither is
      code: the disclosure's minimal-record sentence must state the record's contents **exactly**
      (the line written above already does — check it against the ruling rather than assuming), and
      a **documented balancing test** is owed before recruitment. Task 8's runbook carries it as an
      owner act.
    - **Either way, `population` is stored beside the counts and echoed in every report.** A count
      whose population is not on the same page as the count is a count that will be quoted wrong.
  </ruling>
  <verify>
    <automated>npx vitest run --project node packages/browser/src/funnel-reporter.test.ts packages/browser/src/funnel-reporter.node.test.ts packages/browser/src/ice-observer.test.ts</automated>
    `EXIT=$?` on the line immediately after. Then the two **plain** `.test.ts` files with
    `--project browser` — not the `.node.test.ts`, which that project excludes by suffix
    (`vitest.config.ts`, the browser project's `exclude`) — `EXIT=$?` next line. Then `npx tsc --noEmit`, `EXIT=$?` next line.

    **The version bump breaks things downstream and those are gates, not collateral.** Run
    `npx vitest run --project node packages/browser/src/consent.test.ts packages/node/src/disclosure-four-elements.node.test.ts`,
    `EXIT=$?`; and
    `npx vitest run --project e2e packages/node/src/built-bundle.e2e.test.ts packages/node/src/disclosure-before-optin.e2e.test.ts`,
    `EXIT=$?`. A green here is what proves the bump did not break the gate.

    **Three plants, one at a time.**
    (1) **Make the reporter default to a remote origin** — add an `https://…workers.dev` fallback.
    The no-literal case must go RED. This is the scope fence made mechanical.
    (2) **Make `enter(stage)` send on every call** instead of once per visit. The idempotence case
    must go RED. A stage that double-counts makes every ratio in the funnel wrong in a way no
    reader can see.
    (3) **Remove the `icegatheringstatechange` listener** from the observer while leaving the
    wrapper. The ICE spec must go RED. Record each observed text and restore by the surgical
    inverse with `cmp`.

    **A plant here may stay green and the reason is worth stating in advance.** If the wrapper is
    installed *after* libp2p has already constructed a peer connection, the observer sees nothing
    in **either** arm and plant (3) is invisible. If that happens, do not accept it: move the
    install earlier, prove the ordering with a case that asserts the observer saw at least one
    construction, and record what had to move.
  </verify>
  <done>
    Both unit specs green in both lanes, six call sites wired with the stage-three and stage-six
    observables chosen by measurement and recorded, the disclosure line landed with
    `DISCLOSURE_VERSION` at `'5'` and `policy.html` moved in the same commit, the consent and
    built-bundle suites still green, three plants watched red with their texts recorded, and the
    no-remote-origin guard in place. **No legal basis sentence anywhere.** Committed with explicit
    paths.
  </done>
</task>

<task type="auto">
  <name>Task 5: criterion 1's live half — six stages moving on a real tab against a local workerd, read while the fabric is running</name>
  <files>packages/node/src/funnel-live.e2e.test.ts, vitest.config.ts</files>
  <read_first>
    `.planning/phases/phase-35-conditions-of-entry-in-the-browser/35-01-SUMMARY.md` § *Task 4* —
    **a browser tab dialling a LOCAL workerd, proved for the first time in this repository.** The
    address form that worked is recorded there verbatim:
    `/ip4/127.0.0.1/tcp/<port>/ws/p2p/<peerId>`, handed to the demo through `?relay=`, from an
    `http://localhost` Vite origin. **You do not have to rediscover this.** The same section
    records `SETTLE_MS` being raised 2 s → 20 s because the object learns of a close ~5.2 s after
    the tab initiates it, and that the delay grows with host load.

    `packages/cloudflare/src/stop-closes-the-billed-socket.e2e.test.ts` — the whole arrangement:
    wrangler dev on its own port with `--persist-to` a fresh `mkdtemp`, `waitForReady` on `/self`,
    a Vite dev server for the demo, one page opened with `?relay=` naming **only** the local
    workerd, consent, start.

    `packages/node/src/background-tab.e2e.test.ts` — the Vite dev server pattern for
    `packages/browser/demo/index.html`.

    `packages/node/src/e2e-browser-launch.ts` — `launchFixtureBrowser`, and its recorded note about
    a bare `RTCPeerConnection` probe between two Chromiums.
  </read_first>
  <behavior>
    - One real tab, one local workerd, one Vite-served demo page. The tab is handed the workerd's
      address through `?relay=` and the funnel endpoint through `?funnel=`.
    - `GET /funnel` read **before** the tab opens shows six zeros. That is the floor and it is
      asserted as literals.
    - As the tab progresses, `GET /funnel` read **while the tab is still open and the fabric is
      still running** shows the stage counts moving. Criterion 1's word is *live*; a reading taken
      after the page closed is not it.
    - At minimum stages one through three are observed moving in this arrangement, and every stage
      that does **not** move is named in the SUMMARY with what was observed instead.
    - The reading is a comparison of two `GET /funnel` reads inside **one run**, with both raw
      vectors recorded. No absolute threshold anywhere.
  </behavior>
  <action>
    **Prove the arrangement before building on it**, exactly as Phase 35's Task 4 did. Step one is
    a throwaway probe: workerd up, demo up, one page, `?relay=` and `?funnel=` both pointed at the
    local workerd's origin, consent, start, and one `GET /funnel`. Record what moved. If the tab
    cannot reach the workerd over HTTP from the Vite origin — a CORS refusal is the likely shape —
    that is the finding and it belongs in the SUMMARY with the observed console error, not a
    silent workaround.

    Then write `packages/node/src/funnel-live.e2e.test.ts`, chromium only. Criterion 1 names no
    engine requirement; only Phase 35's criterion 2 did.

    Sequence: workerd up → `GET /funnel` (the **zero floor**, asserted as six literal zeros) →
    Vite up → page open with both parameters → consent → start → poll `GET /funnel` until stages
    one, two and three have moved or a bounded deadline expires → **read once more while the page
    is still open** and record the whole vector → dispatch a task and poll for stages four, five
    and six → record the final vector. Close the page last.

    **Site every deadline against something you measured in this run, not against a number you
    liked.** Phase 35's SUMMARY records a settling beat that was wrong at 2 s on a loaded host and
    right at 20 s, *"written down rather than tuned away"*. Read the host-conditions banner the
    suite prints before quoting any duration.

    **Say what this proves and what it does not, in the file's docblock.** It proves the six
    counters move on a real tab against a real workerd and are readable while that tab is running.
    It does **not** prove anything about a deployed object; nothing local can. And it measures one
    tab, so it is a proof of instrumentation, not a population reading.

    **The stages that do not move are the interesting result and must not be smoothed over.** If
    stage four never fires because a single-tab arrangement negotiates no WebRTC at all, say so:
    ICE gathering happens on a browser-to-browser dial, and one tab against a WSS relay may never
    reach it. If that is what you observe, add a **second page** in the same context so a
    browser-to-browser dial is attempted, and record whether that is what it took. Do not report
    stage four as instrumented on the strength of a unit test alone — `CLAUDE.md`'s *wired is not
    used* was caught three times on the DHT and this is the same shape.

    Register the spec in `vitest.config.ts`'s duration table, moving `files` and `unitFiles`
    together by the identity `slow-specs.node.test.ts` asserts.
  </action>
  <ruling>
    **Under reading A this test is unchanged** — it consents before it starts, so every stage is
    reported either way. **Under reading B**, stage one would additionally be observable in an arm
    that never consents; add that arm then, and until then record in the docblock that the
    unconsented arm is **not measured** rather than that it produces nothing.
  </ruling>
  <verify>
    <automated>npx vitest run --project e2e packages/node/src/funnel-live.e2e.test.ts</automated>
    `EXIT=$?` on the line immediately after.

    **The plant.** Snapshot `demo/main.ts`. Disarm the reporter at the arming point — one line, so
    the reporter is constructed inert regardless of the `?funnel=` parameter. The case must go RED
    with the counts still at the floor, and the failure message must print both vectors. Record the
    observed text verbatim. Restore by the surgical inverse and `cmp`.

    **`demo/main.ts` is a 2 692-line file other agents work in.** Do not plant it in parallel with
    any other agent, give it a clean `git status --porcelain` before starting, and if `cmp` reports
    a difference you did not make, **stop and report** rather than reconciling.
  </verify>
  <done>
    The zero floor asserted as literals, at least stages one through three observed moving while
    the tab is still open, both raw vectors recorded in the SUMMARY, every stage that did not move
    named with what was observed instead, the disarm plant watched red with its text, the deadlines
    sited against measurements taken in the same run, and no remote resource touched. Committed
    with explicit paths.
  </done>
</task>

<task type="auto">
  <name>Task 6: criterion 2 and criterion 5 — six failure arms that move one counter each, and a guard that catches a proxy dressed as a measurement</name>
  <files>packages/node/src/funnel-attribution.e2e.test.ts, packages/node/src/proxy-figures.node.test.ts, vitest.config.ts</files>
  <read_first>
    Criterion 2, verbatim, because its second sentence is the design: *"a synthetic client made to
    fail at exactly one stage moves exactly that stage's count and no other. A funnel where one
    failure moves two counters cannot tell anyone where a cohort was lost."*

    `packages/browser/src/start-probe.ts` and `packages/core/src/start-outcome.ts:41-48`
    (`START_FAILURES`) — the existing vocabulary for *this browser cannot do X*. Two of your six
    arms can be built by making a probe answer false rather than by breaking the network.

    `packages/node/src/vocabulary.node.test.ts` — this repository's model for a **guard that scans
    tracked files for a pattern**. Read how it selects its file set and how it reports a hit;
    Task 6's proxy guard is the same shape aimed at a different pattern.

    `CLAUDE.md` § Conventions and § Proofs, and the recorded incident in
    `hosted-tier-deploy.node.test.ts`: **prose explaining a literal has tripped the guard over that
    literal, twice in one session.** Your registry file quotes all five proxy figures and must
    exclude itself.

    `.planning/REQUIREMENTS.md` `RUN-04`'s own sentence, which is the text the guard enforces:
    *"The bounds available are proxies and are labelled as proxies wherever quoted."*
  </read_first>
  <behavior>
    - Six arms. Each drives a real tab to fail at exactly one of the six stages.
    - For each arm: read the whole counter vector before and after. `stalledAt[k]` moves by
      **exactly one**; every other `stalledAt[j]` moves by **exactly zero**. Both assertions are
      written against integer literals, never against a value read from the same vector.
    - `entered[1..k]` each move by one — the visit truthfully passed through those stages — and
      `entered[k+1..6]` move by zero. **This is not a violation of criterion 2**: the criterion's
      own sentence is about *drop* counters, and a truthful prefix is not a second attribution.
      State that reading in the file's docblock so a later reader does not have to reconstruct it.
    - The proxy guard scans a stated set of tracked files for each of the five proxy quotations and
      requires a proxy label within a stated distance of each occurrence. Removing a label reddens.
    - A second case asserts **no funnel counter is ever initialised to a non-zero value**: a fresh
      store reads six zeros and the code contains no seeded default. A backfill has to start
      somewhere, and this is where.
  </behavior>
  <action>
    **Build the six arms, and say for each one how the failure is induced.** Induce at the visitor,
    never at the collector — a collector-side fault would move no counter at all and would prove
    nothing about attribution.

    | stage | how the arm fails there |
    |---|---|
    | `page-load` | the page loads and the visit ends before consent — `page.goto('about:blank')` immediately after the gate renders |
    | `consent` | click `#decline`, or grant and then navigate away before `start()` |
    | `wss-bootstrap` | `?relay=` naming a port nothing listens on, so the dial cannot complete |
    | `ice-gathering` | make the environment probe answer no WebRTC, or block the ICE path so gathering never transitions. `START_FAILURES` already has `webrtc-unavailable` as a vocabulary |
    | `connection-classified` | reach a peer over the relay and stop before any connection is classified — a control-only pair is the honest shape here, and `pathTo()` is what names it |
    | `first-task` | connect and classify, then never dispatch |

    **The terminal report is the mechanism these arms rest on and it may not survive the harness.**
    The reporter sends its `stalledAt` on `pagehide` via `sendBeacon`. In Playwright, navigating to
    `about:blank` fires `pagehide` reliably; closing a context may not, and a beacon in flight when
    the browser dies is a beacon that never arrives. **Drive each arm's ending by navigation, not
    by close**, and if the beacon still proves unreliable, say so, name what carries the claim
    instead, and do not quietly replace the real path with a `page.evaluate` that calls the sender
    directly — that would be an instrument measuring itself.

    **Isolation is by construction and must be stated as such.** `GET /funnel` offers no per-visit
    breakdown, so you cannot assert "these counts came from my tab". Give each arm a **fresh
    workerd on its own port with its own `--persist-to` directory**, and assert the vector is six
    zeros before the arm's page opens. A non-zero floor fails the arm **naming the arrangement**
    — do not subtract a contaminated reading, report it. This is exactly the discipline Phase 35's
    Task 4 recorded for `/self`.

    Then write `packages/node/src/proxy-figures.node.test.ts`. Hold the five quotations in one
    exported registry with the source beside each:

    - `10–20%` relay-required industry guidance
    - `70%` ± `7.1%` DCUtR hole-punch success
    - `~30%` relay fallback
    - `11%` symmetric NAT
    - `arXiv:2510.27500` — and the fact that all of the above come from a **different protocol**

    The guard: for every occurrence of a quotation in the scanned file set, a proxy label
    (`proxy`, `proxies`, or a stated marker) must appear within a stated character distance.
    Choose the file set deliberately — tracked `.md` under `.planning/` plus any published-results
    file — **exclude this registry file by path**, and write down which you chose and why. Strip
    comment lines with `grep -v '^#'` before any counting so a comment cannot satisfy a gate.

    Add the zero-initialisation case beside it: a fresh journal reads six zeros, and the source of
    `funnel-journal.ts` contains no non-zero seed. Criterion 5's sentence is about *merging and
    backfilling*, and a counter that starts at an estimate is a backfill that happened before
    anybody published anything.

    Register both specs in `vitest.config.ts`'s duration table as their lanes require.
  </action>
  <ruling>
    **Under reading A the `page-load` arm is degenerate and that is a finding, not a gap.** With the
    reporter armed at consent, a visit that ends before consenting reports **nothing** — so
    `stalledAt['page-load']` cannot move, and the arm must assert *the whole vector is unchanged*
    and record that the first stage's drop-off is unmeasurable under this ruling. Write the arm
    that way now, with the assertion parameterised on `FUNNEL_ARMING`, so reading B flips it to the
    positive form with one value and no rewrite. **Under reading B** the same arm asserts
    `stalledAt['page-load']` moved by exactly one. **This is the single place in the plan where the
    ruling changes an assertion, and it is one branch.**
  </ruling>
  <verify>
    <automated>npx vitest run --project e2e packages/node/src/funnel-attribution.e2e.test.ts</automated>
    `EXIT=$?` on the line immediately after. Then
    `npx vitest run --project node packages/node/src/proxy-figures.node.test.ts`, `EXIT=$?` next
    line.

    **The attribution plant — this is criterion 2's own sentence made testable.** Snapshot the
    reporter. Make the terminal report increment **two** adjacent drop counters instead of one
    (the shape a real implementation gets wrong: attributing a stall to both the stage entered and
    the stage not reached). Every arm must go RED, and the failure text must name both counters
    that moved. Record it verbatim. Restore by the surgical inverse and `cmp`.

    **The proxy plant.** Snapshot one scanned file. Remove the word `proxy` from beside one
    quotation. The guard must go RED naming the file, the line and the quotation. Record the text.
    Restore and `cmp`.

    **The zero plant.** Seed one counter in `funnel-journal.ts` with a non-zero default. The
    zero-initialisation case must go RED. Record the text.

    **One arm may fail to redden and you are pre-authorised to widen.** If an arm's failure is
    induced so early that the page never sends anything at all, the two-counter plant is invisible
    to it — a green that says nothing. Record which arms carried the claim and which could not, and
    do not report criterion 2 as closed on fewer than the arms that actually reddened.
  </verify>
  <done>
    Six arms green with each one's before/after vector recorded in the SUMMARY; the two-counter
    plant watched red across the arms that can see it, with the arms that cannot named; the proxy
    guard green with its label-removal plant watched red; the zero-initialisation case green with
    its seed plant watched red; the registry file excluded from its own scan and the file-set
    choice written down. Committed with explicit paths.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 7: open question 3 — the telemetry legal basis, and it is the owner's, not an engineering judgement</name>
  <files>packages/browser/src/disclosure.ts, packages/browser/demo/policy.html, packages/browser/src/funnel-reporter.ts, packages/node/src/disclosure-four-elements.node.test.ts, .planning/REQUIREMENTS.md</files>
  <decision>
    Which legal basis the telemetry disclosure states: **consent**, or **legitimate interest** —
    and consequently whether Phase 37's funnel measures an opted-in subset or the whole population.
  </decision>
  <context>
    `.planning/REQUIREMENTS.md` § Open questions, item 3, verbatim: *"**Telemetry legal basis:
    consent versus legitimate interest under GDPR.** **Contested across sources, not merely
    unresolved** — the sources consulted disagreed with each other on whether IP-based analytics
    needs consent or can rest on legitimate interest. **Settled by: legal review, not engineering
    judgement.** The engineering recommendation (carry telemetry consent on the same gate as the
    CPU-use consent rather than a second banner) is explicitly a **design recommendation and not a
    compliance ruling**, and is flagged for legal review before shipping. **Changes:** `RUN-05`,
    `BROW-09`."*

    **Phase 35 drafted both readings and stopped.** They are reproduced there verbatim; this task
    does not redraft them. What Phase 35 could not say, and this phase can, is what each one costs
    the funnel:

    **Reading A — consent.** No telemetry at all may be collected from a visitor who left the
    toggle off, **including aggregate counts**. The funnel then measures a **self-selected
    opted-in subset**, and that self-selection is a property of the numbers that must be published
    beside them. Stage one equals stage two by construction, so the funnel's first drop-off —
    *how many visitors arrive and never consent* — is not measurable at all. **This is what the
    plan already implements**, because it is the intersection of both readings.

    **Reading B — legitimate interest.** The funnel measures the whole population and the consent
    drop-off becomes measurable for the first time. Two things are then owed: the disclosure must
    state the minimal record's contents **exactly** — Task 4's line already enumerates them, and it
    must be checked against the ruling rather than assumed to satisfy it — and a **documented
    balancing test** is owed before recruitment.

    **What is NOT contested and has already landed** (Task 4): the factual statement of what the
    funnel sends. This task adds only the basis sentence and the arming value.

    **The engineering recommendation is not a ruling.** Carrying telemetry consent on the same gate
    as `BROW-06` is a design recommendation on record and it settles nothing here. Do not present
    it as though it does.
  </context>
  <action>
    Present both readings to the owner with the funnel consequences above, and **STOP**. Do not
    choose. Do not merge them into a sentence that covers both — a disclosure that hedges its own
    legal basis states neither.

    After the ruling, and only then, in **one commit**:
    1. Add the chosen sentence to `DISCLOSURE`, bump `DISCLOSURE_VERSION` again with a
       `CONSENT_VERSION_NOTE` saying what changed, and mirror it into `demo/policy.html`.
    2. **Invert the absence guard** at `packages/node/src/disclosure-four-elements.node.test.ts:212-231`
       — today it asserts `legitimate interest`, `legal basis` and `GDPR` appear **nowhere**. It
       becomes a case asserting the chosen basis sentence **is present**. Phase 35's SUMMARY names
       this exact requirement: *"has that guard inverted in the same commit, so it cannot be left
       half-turned."* Plant its removal and watch it red.
    3. Set `FUNNEL_ARMING` and `population` to match the ruling. Under reading A both are already
       correct and nothing moves. Under reading B, flip them and flip the `page-load` arm's
       assertion in `funnel-attribution.e2e.test.ts` from *the vector is unchanged* to
       *`stalledAt['page-load']` moved by exactly one*.
    4. Record the ruling with its date in `.planning/REQUIREMENTS.md` § Open questions item 3, the
       way item 1's settlement is recorded, and update `BROW-09`'s ledger row — which
       `REQUIREMENTS.md:1864` currently records as **Partial** pending exactly this sentence.
    5. Under reading B only: add the balancing test to Task 8's runbook as an **owner act** with a
       named deliverable. It is a document, not code, and no agent writes it.
  </action>
  <verify>
    Before the ruling: the SUMMARY records `RUN-05` as **Partial** with the pending sentence named,
    and says explicitly which criteria do and do not depend on it — criteria 2, 3, 4 and 5 do not;
    criterion 1 closes over the opted-in population only.

    After the ruling:
    `npx vitest run --project node packages/node/src/disclosure-four-elements.node.test.ts packages/browser/src/consent.test.ts`
    with `EXIT=$?` on the next line, the inverted guard's removal plant watched red, and
    `npx vitest run --project e2e packages/node/src/funnel-attribution.e2e.test.ts`, `EXIT=$?`.
  </verify>
  <resume-signal>Reply with `reading-a`, `reading-b`, or a different sentence to use verbatim.</resume-signal>
  <done>
    Either the owner has ruled and the sentence is in `DISCLOSURE` with the version bumped, the
    absence guard inverted in the same commit, the arming value set and the `page-load` arm's
    assertion matched to it — or the phase closes with `RUN-05` recorded as **Partial**, the
    pending sentence named, and the funnel's population recorded as `opted-in-only`. **Never with
    a basis an agent chose.**
  </done>
</task>

<task type="auto">
  <name>Task 8: the runbook, the ledger and the SUMMARY — what closed, what did not, and what only the owner can do</name>
  <files>.planning/phases/phase-37-the-six-stage-funnel-and-a-frozen-telemetry-schema/37-RUNBOOK.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/phase-37-the-six-stage-funnel-and-a-frozen-telemetry-schema/37-01-SUMMARY.md</files>
  <read_first>
    `.planning/phases/phase-35-conditions-of-entry-in-the-browser/35-01-SUMMARY.md` — the voice,
    the frontmatter shape, and in particular its opening section *"The plant that stayed GREEN, and
    the widened plant that carried the claim"*. A plant that did not redden is a finding about the
    criterion and is the most valuable thing a summary can carry, so it goes **first**.

    `packages/node/src/requirements-ledger.node.test.ts` — a requirement is recorded in **two**
    places in `REQUIREMENTS.md` (the checklist row and the ledger row) and a guard reads both.
    Flip both or flip neither. Its `REREAD_REGISTER` is where a `Partial` row whose claim cannot be
    machine-checked goes; Phase 35 put `BROW-09` there and raised `REREAD_REGISTER_CEILING` 3 → 4
    with the reason written beside it in the same commit. **`RUN-05` may need the same treatment**
    if the ruling has not landed — read that entry before writing yours.

    `packages/node/src/acceptance-traceability.node.test.ts` — `RECOGNISED_STATUSES`. Phase 35
    learned that `Done, both halves` is not a status word: *"The status word is a join key, not a
    headline."* Write `**Done** — …` or `**Partial** — …` and put the qualification after the dash.

    The Phase 41 block in `.planning/ROADMAP.md` — the model for a status paragraph that records an
    **owner act** honestly: *"The dispatch is an owner act, because it is a push to a public
    repository and this project treats publication as a separately-triggered gate."*
  </read_first>
  <action>
    **Write `37-RUNBOOK.md` first, because it is the artifact the scope fence forces into
    existence.** Criterion 1 requires the funnel *reporting live before recruitment begins in
    Phase 39*, and the last mile of that is a deploy — which is an owner act by the 2026-08-25
    ruling and is money. The runbook names, in order, exactly what the owner does and what evidence
    each step produces:

    1. Deploy the Worker carrying `/funnel` (the one command, from `scripts/deploy-hosted.sh`, with
       the version injection already in place). Evidence: `GET /self` answers the expected
       `version`.
    2. Read `GET /funnel` on the deployed object. Evidence: six zeros and the schema digest.
       **This is the timestamped reading Phase 39 criterion 2 requires to precede the first
       invite** — say so, and name where it gets recorded.
    3. Point the published page at the collector's origin — the build-time value, not a code
       change. Evidence: one real visit moves stage one.
    4. **Under reading B only:** the documented balancing test, written before recruitment.
    5. Freeze confirmation: `FUNNEL_SCHEMA_DIGEST` unchanged between the pre-invite reading and the
       first invite. Evidence: the digest in both `GET /funnel` responses.

    **Nothing in the runbook is an agent task and the runbook says so at the top.**

    Then write `37-01-SUMMARY.md`. It must carry, at minimum:

    - **Every plant and its observed failure text verbatim**, grouped by task. Any plant that
      stayed green gets its own section, first, naming which case actually carries the claim.
      This plan pre-authorised four widenings (T3's dump prefix, T4's observer install order, T5's
      arrangement, T6's degenerate arms) — record which were needed.
    - **Every raw number**: T5's two `GET /funnel` vectors; T6's six before/after vectors; the
      encoded record's measured byte size against the stated ceiling; the schema digest.
    - **What local workerd actually supplied** for `CF-Connecting-IP`, the country header and
      `request.cf` — measured in T3's probe, recorded whichever way it went. The next phase that
      needs it should not have to rediscover it.
    - **Which observable was chosen for stage three and stage six, and why** — both were chosen by
      measurement, and Phase 35's `tasksExecuted` finding is the reason stage six needed care.
    - **Which stages did not move in T5's live arrangement**, with what was observed instead.
    - **Open question 3 recorded as the owner's**, with the funnel's population named
      (`opted-in-only` unless the ruling landed) and the self-selection stated as a property of the
      numbers.
    - **What closed and what did not**, plainly:
      criterion 1 → `RUN-04`, closed over the population the arming point admits;
      criterion 2 → the six arms; criterion 3 → the freeze digest; criterion 4 → the store dump;
      criterion 5 → the proxy guard; `RUN-05` → **Partial** unless Task 7's ruling landed.
    - **What was NOT proved.** At minimum: no reading was taken on a deployed object; the funnel
      has never seen more than a handful of synthetic visits, so nothing here is a population
      measurement; the country and network-class path is exercised with supplied headers and is
      **unmeasured on the edge**; and the terminal `stalledAt` report depends on `pagehide` firing,
      which was measured in a harness and not on a real visitor's device.

    Flip requirement rows in `REQUIREMENTS.md` — **both** the checklist row and the ledger row for
    each — only for rows whose criteria actually closed. `RUN-04` and `RUN-05` are both currently
    *"Not started — v2.0, phase not yet numbered"* (`:1889-1890`); at minimum they gain a phase
    name and an honest status. Update the Phase 37 block in `ROADMAP.md`: replace `**Plans**: TBD`
    with the SUMMARY path, and add a dated status paragraph in the register the Phase 41 block
    uses.

    **Do not tick a row on partial evidence.** *Descoped is not satisfied; unmeasured is not met.*
  </action>
  <verify>
    <automated>npx vitest run --project node packages/node/src/requirements-ledger.node.test.ts packages/node/src/acceptance-traceability.node.test.ts</automated>
    `EXIT=$?` on the line immediately after.

    Then the full gates, each read directly, each `EXIT=$?` on the very next line:
    `npx vitest run --project node`;
    `npx vitest run --project e2e`;
    `npx vitest run --project browser`;
    `npx tsc --noEmit`.
    Record the file and test counts in the SUMMARY the way 35-01 does. **Never read an exit code
    after a pipe.**

    **A red taken on an oversubscribed host is re-run before it is attributed.** Phase 35's node
    lane took three runs and recorded both reds rather than smoothing them over; one was its own
    (`trust-anchors.node.test.ts`, fixed in its own files and never by raising the ceiling) and one
    was a load-sensitive spec that passed in isolation. Read the host-conditions banner. If a guard
    of yours reddens because this phase's new files moved a population count, **fix it in your own
    files, never by raising the ceiling.**
  </verify>
  <done>
    The runbook exists and names only owner acts; the SUMMARY exists and carries every plant text,
    every raw number, the measured workerd header findings and the two observable choices;
    requirement rows are flipped in both places or not at all; `ROADMAP.md`'s Phase 37 block names
    the SUMMARY; all four gates read green from a directly-read exit code. Committed with explicit
    paths.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a visitor's browser → the collector | The whole phase. Everything crossing here is a claim about a visit, sent by a party the collector cannot authenticate and must not try to |
| the edge → the Durable Object | `CF-Connecting-IP` and the country header arrive here. The IP is a raw identifier crossing into a process that writes to durable storage |
| the object's memory → its durable storage | What survives the request is what a subpoena, an export or a leak would reach. Criterion 4 is a claim about this boundary and about no other |
| the funnel's numbers → a published figure | A proxy quoted without its label, or a measured number merged with one, is a false claim about the world made by this project |
| a guard → the tree it reads | An absence assertion with no positive control, and a plant that cannot redden, both report green for a reason unrelated to the property |
| an agent → a contested legal question | An agent choosing a GDPR basis is an agent recording a compliance ruling as a code change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Information disclosure | The funnel's durable store | mitigate | Every key the object holds is dumped with `queryKeys({})` and every value decoded; no IPv4/IPv6 form and no run of ≥16 identifier-shaped characters may appear. Proved with a positive control that the request **did** carry an IP, and with a plant that stores one watched red (T3) |
| T-37-02 | Information disclosure | A cross-session identifier arriving by accident | mitigate | The visit's "already sent" set is in memory only and dies with the tab; the arm asserts two correlatable reports add **no new key** to the store (T3, T4) |
| T-37-03 | Information disclosure | Country and network class becoming a fingerprint | mitigate | Two uppercase letters and a closed-list class, validated where the range is declared — `isStartBrowserLabel`'s stated discipline, applied here (T1, T3) |
| T-37-04 | Tampering | An unauthenticated visitor inflating a counter | accept | Every count is anonymous and unauthenticated by construction, and authenticating a visitor is exactly what criterion 4 forbids. `start-report.ts` already states the same acceptance: *"Counts are unauthenticated: a peer can inflate its own."* Recorded in the SUMMARY beside the figures, not mitigated |
| T-37-05 | Repudiation | The schema drifting after recruitment | mitigate | `FUNNEL_SCHEMA_DIGEST` is a hand-written literal compared against the field set on every run; adding a field reddens, and the failure text says the correct response is to revert rather than to update the digest (T1) |
| T-37-06 | Repudiation | A proxy figure read as a measurement | mitigate | A guard scans the tracked file set for all five proxy quotations and requires a proxy label within a stated distance; removing a label is watched red (T6) |
| T-37-07 | Repudiation | A counter backfilled from an estimate | mitigate | No counter is initialised to a non-zero value; a fresh store reads six literal zeros and a seed plant is watched red (T2, T6) |
| T-37-08 | Repudiation | A drop-off attributed to two stages | mitigate | Six arms, each asserting one `stalledAt` moved by exactly one and every other by exactly zero, with a two-counter plant watched red across the arms that can see it (T6) |
| T-37-09 | Denial of service | Unbounded growth in Durable Object storage | mitigate | One key under `/journal/`, cardinality bounded by construction — six frozen stages, 24 hour buckets, a two-letter country pattern and a closed class list — with `FUNNEL_MAX_CELLS` derived and the encoded size asserted below a ceiling sited against the measured `4 194 304 B` wall (T1, T2) |
| T-37-10 | Tampering | A counter going backwards on an evicted instance | mitigate | `writeFunnelJournal` refuses any total lower than the stored one, and the refusal is watched red — the `relay-service-journal.ts` shape, adopted because that file records the silent-erasure failure it prevents (T2) |
| T-37-11 | Elevation of cost | An e2e run posting to the owner's deployed object | mitigate | The reporter has **no default endpoint and no origin literal**; a guard asserts the source contains none, and a plant adding one is watched red. Every reading in this phase is local `workerd` with `--persist-to` a temporary directory (T4, scope fence) |
| T-37-12 | Elevation of cost | An accidental deploy or remote resource | mitigate | No deploy step anywhere in this plan; `CLOUDFLARE_API_TOKEN: ''`; the three `ocr-checks-worker*` scripts untouched; the deploy is a runbook step for the owner and is named as one (T8) |
| T-37-13 | Elevation of privilege | A new route becoming a capability shipped without its phase | mitigate | `/funnel` is added by the phase that measures it, and `worker.ts`'s route-list sentence is amended and dated in the same commit rather than quietly contradicted (T2) |
| T-37-14 | Repudiation | An agent choosing a contested legal basis | transfer | The choice is the owner's. The reporter is armed at the **intersection** of both readings while the ruling is pending, so nothing waits on it, and Phase 35's absence guard stays armed until the ruling lands (T4, T7) |
</threat_model>

<verification>
Every command's exit code is read with `EXIT=$?` on the line immediately after it. No pipe, no
trailing `tail`, no trailing `echo`.

```
npx vitest run --project node       exit 0
npx vitest run --project e2e        exit 0
npx vitest run --project browser    exit 0   (chromium, firefox, webkit)
npx tsc --noEmit                    exit 0
```

`aot` is a lane, not a subset. It is untouched by this plan and is not part of this phase's gate.

Per-task verification, plants included, is in each task's `<verify>` block. **A phase report that
does not carry every plant's observed failure text has not verified this phase**, and one that
does not name the plants that stayed green has verified it less than it claims.
</verification>

<success_criteria>
- **Criterion 1 closes over the population the arming point admits, on a LOCAL workerd, and owes
  two caveats rather than one.** Six stages instrumented, six counters banked where eviction cannot
  reach them, and the whole vector readable through one HTTP read **while a real tab and a real
  workerd are still running**. (a) The **whole-population** reading depends on open question 3.
  (b) The criterion's own words are *reporting live before recruitment begins in Phase 39*, and the
  last mile of that is a reading taken on the **deployed** object — which is money, is an owner act
  by the 2026-08-25 ruling, and is Phase 39 criterion 2's gate. It is `37-RUNBOOK.md` step 2, not an
  agent task. The SUMMARY states both caveats rather than implying the criterion closed further than
  it did.
- **Criterion 2 closes.** Six arms, one induced failure each, each moving exactly one drop counter
  and no other, with the two-counter plant watched red.
- **Criterion 3 closes.** Exactly three questions, every field naming which one it answers, and a
  hand-written digest that reddens when a field is added — the freeze as a mechanism, not a
  promise.
- **Criterion 4 closes.** A dump of everything the object stores holds no IP and no cross-session
  identifier, with a positive control proving the IP was present at the door and a plant that
  stores one watched red.
- **Criterion 5 closes.** Every proxy quotation carries its label under a guard that reddens when
  one is removed, and no counter is ever initialised to anything but zero.
- **`RUN-05` lands as Partial** until the owner rules on open question 3 — that is this phase's
  honest end state, and the reason is recorded in Task 7 rather than resolved by an agent.
- **`BROW-01`, `BROW-02` and `NET-14` are untouched and are counted toward none of the above.**
- Nothing was deployed, no remote resource was created, no third-party analytics service was
  contacted, and the three `ocr-checks-worker*` scripts are unchanged.
</success_criteria>

<output>
After completion, two files exist in
`.planning/phases/phase-37-the-six-stage-funnel-and-a-frozen-telemetry-schema/`:

- `37-RUNBOOK.md` — the owner acts this phase cannot perform, in order, each with the evidence it
  produces. Nothing in it is an agent task.
- `37-01-SUMMARY.md` — **the only artifact of this phase that survives it.** Write it for a reader
  who has none of this plan's context: what closed, what did not, every plant's observed text,
  every raw number, which plants stayed green and what carried the claim instead, what local
  workerd actually supplied, which observables were chosen by measurement and why, and exactly what
  changes under each reading of open question 3.
</output>
