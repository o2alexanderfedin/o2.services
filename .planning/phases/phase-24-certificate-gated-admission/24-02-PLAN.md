---
phase: phase-24-certificate-gated-admission
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - packages/node/src/bin/bench.ts
  - packages/bench/src/perf-workload.ts
  - packages/browser/demo/main.ts
  - packages/browser/src/tab-api.ts
  - packages/node/src/relay-admission.node.test.ts
  - packages/node/src/bench-admission.node.test.ts
autonomous: true
requirements: [AUTH-02, BENCH-03]

# SEAM LETTER C of 24-CONTEXT.md's four-plan split — "the bench and demo posture".
#
# WAVE NOTE — THIS PLAN RUNS BEFORE 24-03 AND THE ORDER IS THE POINT. Both bench rigs
# deliberately hold no certificate and one PUBLISHES A CURVE. If the gate arms first, the
# curve moves and nobody can say whether admission or the rig's posture caused it. This
# dependency runs through NODE BEHAVIOUR, not through a file: 24-03 touches none of these
# files and `depends_on` cannot express it. The baseline captured here is what 24-04 compares
# against.

must_haves:
  truths:
    - "Every rig and every demo surface states whether it holds a certificate, at its own call site, with a reason"
    - "The published benchmark curve has a recorded baseline taken before any gate exists"
    - "A tab that carries no certificate says so to the person looking at it, as it already does"
    - "A guard pins which sites hold the door open, so a later silent change in either direction reddens"
  artifacts:
    - path: "packages/node/src/bin/bench.ts"
      provides: "a stated admission posture with the reason at the call site"
      contains: "admits-any-peer"
    - path: "packages/node/src/bench-admission.node.test.ts"
      provides: "the recorded pre-gate baseline and the guard over it"
  key_links:
    - from: "packages/browser/demo/main.ts"
      to: "packages/browser/src/browser-node.ts"
      via: "the demo states its posture where it already states 'carries-no-certificate'"
      pattern: "carries-no-certificate"
---

# Phase 24 · Plan 02 — The rigs and the demo say what they are, before anything is armed

**Closes (mechanism, second third):** the precondition that stops criterion 1 from silently
reshaping a published measurement. Nothing here refuses anybody either.

<objective>
`bin/bench.ts` stands up *"every node this driver has ever built"* with no certificate, and
`perf-workload.ts` does the same. One of them publishes a scaling curve. `demo/main.ts` leaves tabs
unenrolled unless a user key is supplied — and **already renders `'carries-no-certificate'` as a
first-class state**, which is an asset this plan builds on rather than a gap it closes.

If 24-03 arms the gate first, every one of those numbers moves and **nobody can attribute the
movement**. Was it admission? Was it the extra handshake? Was it the rig quietly falling back? A
published measurement whose conditions changed underneath it is worse than no measurement, and this
repository has the standing rule that says so.

So this plan does two things and neither of them is a refusal: it makes every rig and demo surface
**state** its posture at its own call site, and it **records the pre-gate baseline** that 24-04 will
compare against.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Branch: a Phase 24 branch cut at execution time. **Shared working tree.** Restore any planted
mutation by `cp` + `cmp`. Never `git add -A`. Never `git stash` a path you do not own. Never switch
branches.

Run vitest by project. Read `EXIT=$?` immediately after the command, no pipes.

`packages/node/src/bench-attestation.node.test.ts` snapshots `git status --porcelain` around
itself. **`git add` only between runs, never during one.**

`packages/node/src/relay-admission.node.test.ts` was created by 24-01 in the previous wave. Extend
it; do not rewrite it.
</execution_context>

<context>
@.planning/phases/phase-24-certificate-gated-admission/24-CONTEXT.md
@.planning/phases/phase-24-certificate-gated-admission/24-01-SUMMARY.md
@packages/node/src/bin/bench.ts
@packages/bench/src/perf-workload.ts
</context>

<interfaces>
<!-- Read off the tree 2026-08-04. VERIFY BEFORE BUILDING ON IT. Cited by symbol. -->

24-01 added `RelayAdmission = ReadonlySet<PublicKeyHex> | 'admits-any-peer'` as a **required** field
and wrote `'admits-any-peer'` at every construction site, including all four rig sites. **So this
plan inherits a tree where every rig already compiles and already states today's posture.** What is
missing is the *reason* beside each statement, and the baseline.

`bin/bench.ts` and `perf-workload.ts` each hold **two** `serveAgent(` sites — the count pinned by
`serve-agent-hooks.node.test.ts`, which also asserts the two node factories hand their authorizers
**identical argument lists**, so a change made to one tier and not the other is caught there.

`Observation.speculationMultiplier` and `Observation.redispatches` are literals in both rigs, each
with a comment saying why; `harness.ts` averages them into `speculationTax` / `churnTax` and
`report.ts` prints both columns. **Do not touch them** — Phase 20's plan 20-09 owns that surface and
may already have changed it by the time this runs. Read `20-09-SUMMARY.md` if it exists.

`demo/main.ts` already renders `'carries-no-certificate'`. `tab-api.ts` takes `relayAddrs`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: each of the four rig sites states a posture and the reason it holds</name>
  <files>packages/node/src/bin/bench.ts, packages/bench/src/perf-workload.ts, packages/node/src/relay-admission.node.test.ts</files>
  <read_first>
    packages/node/src/bin/bench.ts (every node-construction site and what each is standing up; the header's account of what the driver measures)
    packages/bench/src/perf-workload.ts (the same, and its header's account of what the workload measures)
    packages/node/src/serve-agent-hooks.node.test.ts (the count-pin idiom, the identical-arguments assertion, and the prefix-match warning in its own comment)
    packages/node/src/relay-admission.node.test.ts (what 24-01 pinned and how it phrases each needle)
  </read_first>
  <behavior>
    - Each rig site either pins issuers or keeps `'admits-any-peer'` **with one sentence saying why,
      at the call site**.
    - The deciding rule, applied per site and recorded: **a site standing up a fabric a reader will
      draw conclusions from states the posture those conclusions assume; a site standing up a
      measurement fixture may hold the door open and must say so.** Do not decide from the file
      name — read what each site constructs.
    - Where a rig keeps the door open, the sentence says **what its published numbers therefore do
      and do not claim about admission**. A curve measured on an open fabric says nothing about a
      gated one, and a reader is entitled to learn that from the code rather than from a summary.
    - `relay-admission.node.test.ts` pins whatever the answer is, per file, with a docblock saying
      what the counts mean.
  </behavior>
  <action>
    Read each site, decide, write the sentence, update the pin.

    **Touch nothing in these files but the admission argument and the sentence beside it.** Phase
    20's 20-09 edits `bin/bench.ts`'s `Observation`, and the two phases share a working tree. A
    wider edit here collides with a plan that may not have run yet.

    If any rig turns out to construct nodes through a helper rather than directly, pin the helper
    and say so — a posture stated in one place and consumed in four is better than four copies, and
    the guard should follow the real shape.
  </action>
  <proof>
    - **the pin can fail** — change one count by one and confirm `relay-admission.node.test.ts`
      reddens naming the file. Restore by `cp` + `cmp`.
    - **the pin is not a prefix match** — confirm the `'admits-any-peer'` needle cannot match a line
      that pins a set instead. **Measure it**; the recorded precedent for reasoning about this
      wrongly is `serve-agent-hooks.node.test.ts`'s own `index: records` warning.
    - **the rig still runs** — a short benchmark rung completes with the posture stated. If a rig
      only compiles and is never run in this plan, say so; a green `tsc` is not a green rig.
    - **WHAT THIS TASK CANNOT REDDEN ON:** it cannot show any node being refused, because nothing
      refuses yet. A green result here is a statement of intent, not evidence of admission.
  </proof>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/o2.services && npx tsc --noEmit; EXIT=$?; echo "tsc exit $EXIT"; npx vitest run --project node packages/node/src/relay-admission.node.test.ts packages/node/src/serve-agent-hooks.node.test.ts</automated>
  </verify>
  <done>
    All four rig sites state a posture with its reason at the call site, the guard pins the new
    counts with a docblock explaining them, and any site holding the door open says what its
    published numbers do not claim.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the pre-gate baseline, recorded as a comparison and not as a number</name>
  <files>packages/node/src/bench-admission.node.test.ts</files>
  <read_first>
    packages/node/src/bin/bench.ts (how a rung is invoked, what it emits, and how long the smallest one takes)
    packages/node/src/bench-attestation.node.test.ts (the existing pattern for guarding a benchmark's own claims, and its `git status --porcelain` snapshot)
    packages/node/src/slow-specs.node.test.ts (it parses `vitest.config.ts`'s source, so `SLOW_NODE_SPECS` / `MEASURED_NODE_SPANS` edits are guarded — NEVER write a span you did not measure)
    .planning/phases/phase-24-certificate-gated-admission/24-CONTEXT.md (§ Comparative readings)
  </read_first>
  <behavior>
    - A reproducible reading of the smallest useful rung, on an **open** fabric, recorded with the
      conditions it was taken under: node count, transport, host, and the posture every node held.
    - **The reading is a ratio or a paired arm, not a millisecond.** An absolute threshold encodes
      the host and this repository's standing rule forbids it. The honest shape is a comparison
      available inside one run — for example the same rung with N and 2N nodes, or the cost of a
      reservation against the cost of the connection that carried it. Where an absolute is
      unavoidable, **say what it was sited against**.
    - What is recorded is the **process**, not the machine: how many reservations were granted, how
      many peers appeared in the advertisement, how many were dialled. Those counts are what 24-04
      compares, and unlike a duration they are host-independent.
    - The guard fails if a later change moves a **count**, and states plainly that it does **not**
      guard the timings.
  </behavior>
  <action>
    Build the smallest rig that produces the counts and nothing more. Do not stand up a full
    benchmark run inside a test if a two- or three-node fabric produces the same counts — the
    question is *how many peers got in*, and that answer does not need a curve.

    Write the measured numbers and the conditions into the file's own docblock, dated. If any span
    is added to `SLOW_NODE_SPECS` or `MEASURED_NODE_SPANS`, **measure it** — `slow-specs.node.test.ts`
    parses the config's source and an unmeasured span is a guard telling a lie.

    State explicitly, in the docblock, that this baseline was taken **before any admission gate
    exists**, and that 24-04 is the plan entitled to compare against it.
  </action>
  <proof>
    - **the baseline can fail** — plant: make one node fail to obtain a reservation (drop its relay
      address). The count assertion must redden, naming which count moved. Restore by `cp` + `cmp`.
      If the count does not move, the baseline is measuring something that does not depend on
      admission and is worthless for 24-04 — **report that and re-choose the counts** rather than
      keeping a reading that cannot move.
    - **the baseline is not a timing** — confirm the file asserts no absolute duration. If one is
      unavoidable, the docblock names what it was sited against.
    - **it survives a re-run** — run it at least twice and record whether the counts are identical.
      A count that varies run to run is not a baseline; say so and bound it rather than averaging
      it away.
    - **WHAT THIS TASK CANNOT REDDEN ON:** it cannot detect a gate that does not exist, and it
      cannot show that the counts *should* be what they are. It records what is, so that a later
      change has something to move.
  </proof>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/o2.services && npx vitest run --project node packages/node/src/bench-admission.node.test.ts packages/node/src/slow-specs.node.test.ts</automated>
  </verify>
  <done>
    A pre-gate baseline exists as host-independent counts with its conditions recorded and dated,
    demonstrated able to move by a plant, re-run at least twice, and marked as the comparison 24-04
    is entitled to make.
  </done>
</task>

<task type="auto">
  <name>Task 3: the demo and the tab API say what they carry, to the person looking</name>
  <files>packages/browser/demo/main.ts, packages/browser/src/tab-api.ts, packages/node/src/relay-admission.node.test.ts</files>
  <read_first>
    packages/browser/demo/main.ts (the `'carries-no-certificate'` render and the enrolment branch around it — what a user actually sees today)
    packages/browser/src/tab-api.ts (its `relayAddrs` surface and what a host page can and cannot pass)
    packages/node/src/browser-enrollment.e2e.test.ts (its own note that the provider and the relay are ONE node — "The provider, not the gate… `relayAddrs: [provider]`")
  </read_first>
  <behavior>
    - The demo's existing `'carries-no-certificate'` state gains one sentence, in the UI or beside
      it, saying what that will mean once admission is gated: this tab can enrol, and until it does
      it will not be admitted. **Written as a fact about this tab, never as a kind of node.**
    - `tab-api.ts` states, in its own docblock, that a host page supplying `relayAddrs` is supplying
      a front door and that the front door is where admission will be decided.
    - **No behaviour change in either file.** A tab that joins today still joins.
    - The wording of the demo's sentence is checked by whatever guard already reads that string, so
      it cannot be silently dropped.
  </behavior>
  <action>
    Write the sentences. Do not add a certificate flow, do not change the enrolment branch, do not
    add a second UI state.

    **Do not introduce a "gated" or "open" node label anywhere in the demo.** Certificate-holding is
    a per-node fact and the demo already renders it as one; a second vocabulary for the same fact is
    how a fact becomes a kind.

    If the demo's existing string is pinned by a guard, extend the pin rather than replacing it, and
    update that guard's docblock to say what the string now promises.
  </action>
  <proof>
    - **the string is still pinned** — confirm the guard that reads `'carries-no-certificate'`
      still reddens when the string is removed. Plant it, observe, restore by `cp` + `cmp`. If no
      guard reads it, that is the finding: add one, because a first-class UI state nothing pins is
      one edit from disappearing.
    - **nothing moved** — the browser project suite passes unchanged. A demo edit that reddens an
      e2e spec is not a wording change and should be reported as a behaviour change.
    - **WHAT THIS TASK CANNOT REDDEN ON:** it cannot show that an unenrolled tab is refused, and
      the sentence it writes is a promise about 24-03, not a description of today. Phrase it so a
      reader cannot mistake one for the other.
  </proof>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/o2.services && npx tsc --noEmit; EXIT=$?; echo "tsc exit $EXIT"; npx vitest run --project browser; npx vitest run --project node packages/node/src/relay-admission.node.test.ts</automated>
  </verify>
  <done>
    The demo and the tab API each state, where a reader will meet them, what carrying no certificate
    will mean; the existing first-class state is still pinned; and no behaviour changed.
  </done>
</task>

</tasks>

## Files touched

- `packages/node/src/bin/bench.ts`, `packages/bench/src/perf-workload.ts` — posture and reason at
  each site (admission argument only)
- `packages/browser/demo/main.ts`, `packages/browser/src/tab-api.ts` — the sentences a reader meets
- `packages/node/src/relay-admission.node.test.ts` — pins extended
- `packages/node/src/bench-admission.node.test.ts` — the pre-gate baseline

## Recorded decisions

- **The rigs state their posture before the gate exists**, so a curve that moves later can be
  attributed.
- **The baseline is counts, not timings** — host-independent, and able to move under a plant.
- **The demo keeps one vocabulary for one fact.** No node kinds.
- **`Observation`'s literals are untouched** — Phase 20's 20-09 owns them.

## Out of scope

- Any refusal, any gater, any `circuitRelayServer` argument — 24-03.
- Reading the criterion — 24-04.
- Changing what any benchmark publishes.

<verification>
`npx tsc --noEmit` exits 0. `npx vitest run --project node` and `--project browser` pass. The
load-bearing artefact is `bench-admission.node.test.ts`'s baseline **together with the plant showing
its counts can move** — a baseline that cannot move is a decoration, and 24-04's comparison would
inherit the defect.
</verification>

<success_criteria>
Every rig and demo surface states its admission posture with a reason at its own call site, pinned
by a guard; a pre-gate baseline of host-independent counts exists, dated, with its conditions and a
demonstrated ability to move; and nothing in the tree behaves differently than before.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-24-certificate-gated-admission/24-02-SUMMARY.md`
</output>
