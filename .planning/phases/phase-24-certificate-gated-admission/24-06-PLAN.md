---
phase: phase-24-certificate-gated-admission
plan: 06
type: execute
wave: 2
depends_on: [05]
gap_closure: true
autonomous: true
requirements: [AUTH-02, AUTH-04]
files_modified:
  - packages/node/src/seed-server.ts
  - packages/node/src/bin/seed.ts
  - packages/libp2p/src/relay-admission.ts
  - packages/node/src/relay-admission.node.test.ts
  - packages/node/src/start-unwind.node.test.ts
  - packages/node/src/seed-discovery.e2e.test.ts
  - packages/node/src/reservation-exhaustion.node.test.ts

must_haves:
  truths:
    - "An operator can tell a seed which issuers it admits, from the command line, without editing source"
    - "A seed told nothing behaves byte-identically to the seed before this plan existed"
    - "A seed told a garbage issuer refuses to start, naming the flag and the value"
    - "The banner an operator reads states this seed's own posture in both arms, and a fixture can read which arm it is in"
    - "An uncertificated peer obtains no circuit through a spawned bin/seed.ts that was told to close"
  artifacts:
    - path: "packages/node/src/seed-server.ts"
      provides: "SeedServerOptions.relayAdmission, required, forwarded to FabricNode.start"
      contains: "relayAdmission: options.relayAdmission"
    - path: "packages/node/src/bin/seed.ts"
      provides: "--admit-issuer, repeatable, hex-validated, two-armed banner"
      contains: "admit-issuer"
    - path: "packages/node/src/relay-admission.node.test.ts"
      provides: "the census repaired at the seed's row, and the declaration guard split rather than widened"
      contains: "PRODUCTION_SITES"
  key_links:
    - from: "packages/node/src/bin/seed.ts"
      to: "packages/node/src/seed-server.ts"
      via: "relayAdmission through a ternary whose absent arm is the open literal"
      pattern: "values\\['admit-issuer'\\]"
    - from: "packages/node/src/seed-server.ts"
      to: "packages/node/src/fabric-node.ts"
      via: "options.relayAdmission passed to FabricNode.start"
      pattern: "relayAdmission: options\\.relayAdmission"
---

<objective>
Give the seed a door, on the owner's ruling of 2026-08-06.

`24-VERIFICATION.md` scores criterion 8 PARTIAL on one structural fact: `seed-server.ts`
hardcodes `relayAdmission: 'admits-any-peer'` at its `FabricNode.start` call, `SeedServerOptions`
carries no field that could change it, and `bin/seed.ts` has no `--admit-issuer`. The seed is
therefore *"not merely left open, it cannot be told to close"* — and the seed is the relay every
browser tab in this fabric reserves on and the source of `BootstrapInfo.peerAddrs`.

The verifier's own words on what would change the disposition:

> *"'Pass-with-a-stated-bound' would be the right disposition if the bound were a deployment
> posture an operator can remove. It is not: for the seed there is no knob."*

**This plan builds the knob.** It changes no default: a seed told nothing is byte-identical to
the seed before it. What it changes is that the open posture becomes a **deployment choice an
operator can reverse**, which is the precondition for reading criterion 8 over a fabric.

Purpose: 24-07 and 24-08 both need a seed that can be told to close. Neither can be written
until this lands.

Output: `SeedServerOptions.relayAdmission` (required, the existing `RelayAdmission` union),
`bin/seed.ts --admit-issuer`, a two-armed banner, and the guards that pinned the seed's posture
open repaired **without widening any of them**.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/phase-24-certificate-gated-admission/24-VERIFICATION.md
@.planning/phases/phase-24-certificate-gated-admission/24-05-SUMMARY.md

@packages/node/src/seed-server.ts
@packages/node/src/bin/seed.ts
@packages/node/src/bin/agent.ts
@packages/libp2p/src/relay-admission.ts

<interfaces>
<!-- Extracted from the tree. Use these directly. -->

The union already exists and must not be re-declared:

  `packages/libp2p/src/relay-admission.ts`
    export type RelayAdmission = ReadonlySet<PublicKeyHex> | 'admits-any-peer'
    export const ADMITS_ANY_PEER = 'admits-any-peer'
    export function admitsAnyPeer(admission: RelayAdmission): boolean

`PublicKeyHex` is `export type PublicKeyHex = string` (`packages/core/src/capability.ts:46`),
so `new Set(values['admit-issuer'])` types cleanly — which is exactly what `bin/agent.ts`
already does.

`SeedServerOptions` today (`seed-server.ts:90`) carries `httpPort?`, `wsPort?`,
`blockstoreDir`, `trustAnchors` (**required**, typed `FabricNodeOptions['trustAnchors']`),
`trustedIssuers?` (typed `FabricNodeOptions['trustedIssuers']`), `maxReservations?`,
`allowedHosts?`. `trustAnchors` is the precedent for a required, forwarded, indexed-access
field.

`bin/agent.ts`'s three-flag shape, to be mirrored exactly rather than paraphrased:

  - `'admit-issuer': { type: 'string', multiple: true }` in `parseArgs`.
  - A **separate** `parseKeyHex` / `refuse` / exit-2 loop from `--trusted-issuer`'s, because
    the message has to name which flag was rejected and an operator who passed both must not be
    sent to the wrong one (`bin/agent.ts` lines 612–634 state this at the line).
  - `relayAdmission: values['admit-issuer'] === undefined ? 'admits-any-peer' : new
    Set(values['admit-issuer'])` — a **ternary**, not the conditional spread its two sibling
    flags use, because `relayAdmission` is required so there is no key to omit
    (`bin/agent.ts` lines 878–901).

`bin/seed.ts` already imports `parseKeyHex` from `@o2/libp2p` and already has a `refuse` that
writes `seed.ts: <reason>` and exits 2. There are **six** `SeedServer.start` construction sites
in the tree: `bin/seed.ts:125`, `seed-discovery.e2e.test.ts:43`, `start-unwind.node.test.ts:244`
and `:258`, `relay-admission.node.test.ts:638` and `:648`. Re-derive that list with
`grep -rn "SeedServer.start" packages/` before editing; this phase's inventory has been wrong
about site counts three times.

The guards that will go red, and where:

  - `relay-admission.node.test.ts` `PRODUCTION_SITES` pins
    `packages/node/src/seed-server.ts` at `open: 1, total: 1`. Removing the literal makes it
    `open: 0, total: 1`.
  - `relay-admission.node.test.ts` `declares the field in exactly one type, so two factories
    cannot drift` asserts `REPO.declarations === 1` against
    `DECLARATION = \`readonly ${FIELD}:\``. A forwarded field on `SeedServerOptions` makes it 2.
  - `relay-admission.node.test.ts:1317` asserts `bin/seed.ts` contains
    `'admits     every peer that completes a handshake'`. A two-armed banner keeps that string
    in one arm only.
  - `reservation-exhaustion.node.test.ts`'s header states the binary *"deliberately has no
    admission flag"*. That sentence becomes false.
</interfaces>
</context>

<constraints>
- **Do not `git add`, `git commit` or `git stash`.** Restore every plant with `cp` + `cmp`.
  Another agent is running vitest in this tree and two specs snapshot `git status --porcelain`
  around themselves.
- **Do not kill or signal any process you did not start.**
- **Do not edit `.planning/STATE.md`, `.planning/ROADMAP.md` or `.planning/REQUIREMENTS.md`.**
- **Criterion 8's wording may not be edited.**
- **Never close a gap by widening what counts as passing.** Two guards in this plan must be
  repaired rather than relaxed, and the repairs must be strictly stronger than what they
  replace. If you cannot make a repair strictly stronger, stop and report it.
- Run vitest **by project**. `EXIT=$?` on the line immediately after the command.
- **ALL NODES HAVE EQUAL FUNCTIONALITY. Only discovery differs.** `relayAdmission` on a seed
  is a per-node value, exactly as it is on an agent. Nothing may key a decision on whether a
  node was built by `SeedServer` or by `bin/agent.ts` — that shape was retracted once already
  (commit 0314208).
- **The default stays open.** Nineteen `bin/agent.ts` and three `bin/seed.ts` argv sites depend
  on it, and whether the binaries should refuse to start absent an explicit posture is
  `24-CONTEXT.md` open ruling 1 — **still undecided, and not this plan's to decide.**
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SeedServerOptions.relayAdmission and bin/seed.ts --admit-issuer</name>
  <files>packages/node/src/seed-server.ts, packages/node/src/bin/seed.ts, packages/libp2p/src/relay-admission.ts, packages/node/src/start-unwind.node.test.ts, packages/node/src/seed-discovery.e2e.test.ts, packages/node/src/relay-admission.node.test.ts</files>
  <behavior>
    - `npx tsc --noEmit` exits 1 naming every `SeedServer.start` site that omits the new field,
      and exits 0 once each states a posture. The required-field property is measured, not
      argued: plant the field as optional at the declaration and watch the same omission
      compile clean.
    - A spawned `bin/seed.ts` with no `--admit-issuer` prints the open arm of the `admits` line
      and grants a reservation to an uncertificated peer, exactly as before this plan.
    - A spawned `bin/seed.ts --admit-issuer <64 hex>` prints the closed arm naming the pinned
      key, and grants that peer nothing.
    - A spawned `bin/seed.ts --admit-issuer nothex` exits 2 and its refusal line names
      `--admit-issuer` and `nothex`, and does **not** name `--trusted-issuer`.
  </behavior>
  <action>
Add to `SeedServerOptions`, **required, with no `?`**:

`readonly relayAdmission: FabricNodeOptions['relayAdmission']`

Indexed access, not a re-written union — `trustAnchors` on the same interface is the precedent
and the reason is stronger here: a hand-written second union would be a second answer to *"who
does this node admit"*, and that is exactly what `relay-admission.node.test.ts`'s declaration
guard exists to prevent. Task 2 turns that reasoning into an assertion.

Its docblock must state, in this order: the subject (who gets a circuit reservation on this
seed); that it is **admission** and `trustedIssuers` immediately above is **selection**, with
the one-sentence disambiguation `bin/agent.ts` already carries for its three flags; why it is
required rather than optional (silence and consent must not be indistinguishable at the one
boundary where the difference is the whole security claim — the argument is written out at
`RelayAdmission` and at `FabricNodeOptions.relayAdmission`, cite them rather than restating);
and the deployment requirement a seed that pins issuers takes on — *"a relay that pins issuers
must either serve enrolment itself, or name a provider a joining peer can reach without a
reservation"* — with the measurement 24-05 took beside it.

In `SeedServer.#compose`, replace `relayAdmission: 'admits-any-peer',` with
`relayAdmission: options.relayAdmission,`. **Rewrite the comment block above it.** It currently
argues at length that pinning is *"a later decision and is deliberately not taken here"* and
that the deferral *"now has a measured price"*. Both are now false. The replacement states: the
posture comes from the operator; the seed still does not derive it from `trustedIssuers`,
because collapsing them is the conflation that field exists to prevent; and it names the owner
ruling of 2026-08-06 that authorised the field.

State a posture at every other `SeedServer.start` site. Find them with
`grep -rn "SeedServer.start" packages/` — do not trust the list in `<interfaces>`, verify it.
Each site writes one added line, and each writes the value that is truthful for what that
fixture does. `start-unwind.node.test.ts`'s failure-path site and `seed-discovery.e2e.test.ts`
are open; `relay-admission.node.test.ts`'s two seed arms are open today and stay open unless
task 3 changes one of them deliberately.

In `bin/seed.ts`:

  - Add `'admit-issuer': { type: 'string', multiple: true }` to `parseArgs`, with a docblock
    that disambiguates the three flags in the same three-subject form `bin/agent.ts` uses:
    `--trust-anchor` pins a **module**'s build authority, `--trusted-issuer` pins whose word
    this seed takes about **a peer it is already talking to**, `--admit-issuer` pins whose
    certificate gets **a peer asking to come in** a circuit reservation here.
  - Add a **separate** hex-validation loop, not merged with `--trusted-issuer`'s, so the
    refusal names the right flag. State at the line that the consequence of skipping it is
    worse here than for `--trusted-issuer`: a zero-filled admission key does not merely stop
    this seed trusting anybody, it stops **every** peer obtaining a reservation, and the peers
    see only `PERMISSION_DENIED`.
  - Thread it at the `SeedServer.start` call with the ternary shape, byte-identical in form to
    `bin/agent.ts`'s: `relayAdmission: values['admit-issuer'] === undefined ? 'admits-any-peer'
    : new Set(values['admit-issuer'])`. Not a conditional spread — the field is required, so
    there is no key to omit, and a spread that compiled would mean the field had stopped being
    required.
  - Make the `admits` banner line **two-armed**. The open arm keeps its exact current text so a
    guard reading it still finds it. The closed arm names the count **and** the sorted pinned
    hex values, so a fixture spawning a closed seed can assert the arm pinned *what it was
    told* rather than merely that it pinned something. Public by construction, on the same
    ground `bin/agent.ts` publishes its posture on the handshake line: these are pinned issuer
    **public** keys.
  - Keep the label `admits`. `trust-anchors.node.test.ts` finds the anchor line by searching for
    the word `trusts`, and the `issuers` label is already taken by `--trusted-issuer`; a second
    line containing either would be found first by half the runs.

In `packages/libp2p/src/relay-admission.ts`, repair the one clause that this plan falsifies:
the module docblock's *"and `bin/seed.ts` cannot be told to close at all"*. Record the
correction rather than swapping it in silently — that is this file's own established practice
for its two prior corrections, and the reason it gives is the reason: *a comment asserting a
mechanism is inert is the exact shape this repository has been bitten by; a reader who believes
it stops looking.*

**Measure the required-field property rather than asserting it.** 24-01 did this and it is the
only proof that "required" is doing work: plant the field as `readonly relayAdmission?:` at the
declaration, omit it at one `SeedServer.start` site, run `npx tsc --noEmit`, and record that it
exits **0** with zero output; then restore the required form and record that the same omission
exits **1** naming the site and TS2741. Restore with `cp` + `cmp`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>`tsc --noEmit` exits 0 with zero output; every `SeedServer.start` site states a posture; the required-field plant recorded in both directions.</done>
</task>

<task type="auto">
  <name>Task 2: Repair the guards that pinned the seed's posture open — strictly stronger, never wider</name>
  <files>packages/node/src/relay-admission.node.test.ts, packages/node/src/reservation-exhaustion.node.test.ts</files>
  <action>
Four guards go red on task 1's edit. Each repair must be **strictly stronger** than what it
replaces. If any cannot be, stop and report rather than relaxing it.

**(a) `PRODUCTION_SITES`, the `seed-server.ts` row.** `open: 1 → 0`, `total: 1` unchanged, and
rewrite the `reason` to say what the file now does: the seed takes its posture from its
operator, so the front door of the LAN demo is a deployment choice rather than a literal.

**(b) `PRODUCTION_SITES` gains a `bin/seed.ts` row.** After task 1 that file contains one
`ANY_POSTURE` occurrence and zero `OPEN_POSTURE` occurrences — count it, do not predict it —
and it is a production entry point, so leaving it out would create the first unguarded
production posture site in the tree. Its `reason` states that the ternary's absent arm is the
open literal, so nineteen agent and three seed argv sites keep working unchanged.

**(c) The declaration guard.** It reads `expect(REPO.declarations).toBe(1)` and its stated
purpose is that *"two factories cannot drift"*. A forwarded `FabricNodeOptions['relayAdmission']`
on `SeedServerOptions` is not a second factory — it is definitionally the same type — but the
census reads text and cannot tell them apart, and **the repair goes on this side, not in the
guard's bar.**

Split the count instead of raising it. Assemble both needles from `FIELD` (this file is inside
its own jurisdiction; a needle written whole makes the census report itself, which is how this
block first went red):

  - a **defining** declaration is the field followed by the union type name;
  - a **forwarding** declaration is the field followed by an indexed access into
    `FabricNodeOptions`.

Then assert: exactly **one** defining declaration, and it is in `fabric-node.ts`; every other
declaration is a forwarding one; and defining + forwarding equals `REPO.declarations` exactly,
so a **third** shape — a hand-written union anywhere in the repository — fails here by name.

That is strictly stronger than the row it replaces: today a `readonly relayAdmission: 'yes' |
'no'` inside `fabric-node.ts` would satisfy `toBe(1)`. Under the repair it does not. Say so in
the docblock, because a reader who meets a `1` becoming a `2` will otherwise read it as a bar
being lowered.

Leave `census()`'s subtraction alone. `DECLARATION` is a prefix of both shapes, so
`occurrences(code, DECLARATION)` still counts both and the posture arithmetic stays correct.

**(d) The banner guard at `has no banner in either binary saying certificate-gated admission is
not armed`.** Keep the `not.toContain('is not armed')` negative on both binaries. Replace the
single positive with a two-armed one: `bin/seed.ts` must contain **both** the open-arm text and
the closed-arm text. A banner with only one arm satisfies the old assertion and lies to half
the operators; a banner with neither satisfies the negative perfectly, which is why the
positive exists at all.

**(e) A new case mirroring `wires the new flag to admission alone`.** Read `bin/seed.ts` as
stripped source and assert: `--admit-issuer` reaches `relayAdmission` through the ternary;
`--trusted-issuer` still reaches `trustedIssuers`; and neither crosses into the other, nor into
`trustAnchors`. `trust-anchors.node.test.ts` already reddened when 24-01 planted a cross-wiring
between the two older flags; this extends that reading to the third on the second binary.

**(f) `reservation-exhaustion.node.test.ts`'s header.** Two sentences become false: *"that
binary deliberately has no admission flag"* and *"24-01 recorded pinning the seed's posture as
a later decision"*. Repair them, and record what that file's arm A now **is**: it passes no
`--admit-issuer`, so its assertion that joiner A was granted — with
`expect(a.stderr()).not.toContain('PERMISSION_DENIED')` beside it — is now a **live guard that
the default stays open**. That is a strengthening of that file rather than a cost, and it
should be said at the line.

**Plants. Counted before applied, watched red, restored with `cp` + `cmp`, observed text
recorded.**

  - **P1** — restore `relayAdmission: 'admits-any-peer',` at `seed-server.ts`'s
    `FabricNode.start` call. Row (a) must redden with the observed count. This is the plant
    that proves the census reads the production file and not a comment.
  - **P2** — replace `SeedServerOptions.relayAdmission`'s indexed access with a hand-written
    `ReadonlySet<PublicKeyHex> | 'admits-any-peer'`. Row (c) must redden. **This is the
    load-bearing plant of this task**: it is the one that proves the split is a repair and not
    a widening, because the state the old row forbade is still forbidden.
  - **P3** — cross-wire `bin/seed.ts` so `--trusted-issuer` reaches `relayAdmission`. Row (e)
    must redden.
  - **P4** — delete the closed arm of the banner line, leaving only the open text. Row (d)
    must redden. A two-armed guard that cannot detect a missing arm is not two-armed.

Record for each plant which rows stayed **green**; the separation is the evidence.
  </action>
  <verify>
    <automated>npx vitest run --project node relay-admission.node.test.ts reservation-exhaustion.node.test.ts</automated>
  </verify>
  <done>All rows green on the repaired tree; four plants watched red on the row each names and green elsewhere; no tolerance or bar moved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The flag behaves, across a real bin/seed.ts process</name>
  <files>packages/node/src/relay-admission.node.test.ts</files>
  <behavior>
    - A spawned `bin/seed.ts --dir <tmp> --port 0 --ws-port 0 --admit-issuer <64 hex nobody
      holds>` prints the closed arm of the `admits` line naming that key, and an uncertificated
      in-process `FabricNode` given its loopback relay address connects — the plain connection
      stands — and never obtains a circuit, held over a window rather than sampled once.
    - The byte-identical fixture with **no** `--admit-issuer` prints the open arm and the same
      uncertificated node obtains a circuit. Established **first**, so the absence above is
      read against a seed this run has already watched grant one.
    - `bin/seed.ts --admit-issuer nothex` exits 2; the refusal line names `--admit-issuer` and
      `nothex` and does not name `--trusted-issuer`.
  </behavior>
  <action>
Add a `describe` to `relay-admission.node.test.ts`, beside the existing
`a spawned agent is a fixture rather than a constant` block and reusing its shape.

Spawn `bin/seed.ts` with `stdio: ['pipe','pipe','pipe']` — fd 0 must be a pipe or the seed's
orphan leash opts out and `orphan-leash.node.test.ts` fails the spawn site. Wait for the
sentinel `Ctrl-C to stop.` and read the banner, exactly as `reservation-exhaustion.node.test.ts`
does. Take the **loopback** `relay ` line — the seed binds `0.0.0.0` and libp2p expands that per
interface, so the listing also carries LAN addresses a CI host may not be able to dial back to
itself.

**Read the posture off the seed's own banner, never off the argv this file passed.** A typo in
a spawn argument otherwise produces an open seed and the reading passes for the wrong reason —
the single most likely way a cross-process admission proof lies, and this repository has
already carried two criteria at PARTIAL for exactly that shape.

Order the arms open-first. The open arm establishes that this fixture's uncertificated node can
obtain a circuit from a `bin/seed.ts` on this host at all; only then is the closed arm's absence
attributable to the flag rather than to a fixture that never got there.

Hold the closed arm's absence over a window using the file's existing polling idiom, not a
single sample: an absence read once is also what a fabric that has not got there yet looks like.

Measure the file's solo `real` **before and after** this task with
`/usr/bin/time -p npx vitest run --project node relay-admission.node.test.ts`, `EXIT=$?` on the
line immediately after, and report both readings with the host's 1-minute load beside each.
A seed spawn boots a Vite dev server; `trust-anchors.node.test.ts` measured the full banner at
**590 ms** after spawn, so this is a known and small cost — but it is a measured one, not an
assumed one, and if a `slow-specs.node.test.ts` row goes red the repair is to re-site the
recorded span in `vitest.config.ts`, **never** to widen a tolerance.
  </action>
  <verify>
    <automated>npx vitest run --project node relay-admission.node.test.ts</automated>
  </verify>
  <done>Both arms green in one run against real `bin/seed.ts` processes; the closed arm's posture read off the seed's own banner; the garbage-hex arm exits 2 naming the right flag.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator argv → `bin/seed.ts` | untrusted text becomes a pinned issuer set that decides who joins |
| joining peer → seed's `denyInboundRelayReservation` | an unauthenticated peer id crosses here |
| joining browser tab → `/bootstrap.json` | the seed publishes who else is reachable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-06-01 | Spoofing | a mistyped `--admit-issuer` value | mitigate | `parseKeyHex` in its own loop; `fromHex` zero-fills rather than throwing, so without it a typo produces a seed refusing **every** peer with nothing reporting the input was never hex. Exit 2, naming the flag and the value |
| T-24-06-02 | Elevation of privilege | `--admit-issuer` folded into `trustedIssuers` or `trustAnchors` | mitigate | three separate flags, three separate loops, and task 2's case (e) reads the wiring as source text and reddens on a cross-wire |
| T-24-06-03 | Tampering | the open default silently becoming closed, stranding every existing spawn fixture | mitigate | the ternary's absent arm is the open literal; `reservation-exhaustion.node.test.ts`'s arm A is a live behavioural guard on it; task 3's open arm reads it across a real process |
| T-24-06-04 | Repudiation | an operator unable to tell which posture their seed is running | mitigate | the banner is two-armed and the closed arm names the pinned keys; task 2's case (d) reddens if either arm goes missing |
| T-24-06-05 | Denial of service | an operator closing a seed with no reachable provider, stranding every new tab | accept, and **document** | the deployment requirement is written at `RelayAdmission`, at `SeedServerOptions.relayAdmission`, and printed in the banner. 24-05 measured that enrolment survives a closed provider; a seed with no provider at all is an operator error the mechanism cannot detect |
| T-24-06-06 | Information disclosure | the banner printing pinned issuer hex | accept | pinned issuer **public** keys; `bin/agent.ts` already publishes the identical value on its handshake line, and a fixture must be able to assert which arm a spawned seed is in |
</threat_model>

<verification>
- `npx tsc --noEmit` — exit 0, zero output.
- `npx vitest run --project node relay-admission.node.test.ts` — exit 0.
- `npx vitest run --project node reservation-exhaustion.node.test.ts` — exit 0, and arm A still
  granted, which is the live proof the default stayed open.
- `npx vitest run --project node trust-anchors.node.test.ts orphan-leash.node.test.ts` — exit 0.
  Both spawn `bin/seed.ts` and both are sensitive to its banner and its leash.
- `npx vitest run --project node start-unwind.node.test.ts` — exit 0.
- `npx vitest run --project e2e seed-discovery.e2e.test.ts` — exit 0.
- `npx vitest run --project node` — exit 0. Record `real`/`user`/`sys`, the derived ratio, and
  the 1-minute load at both ends, so the reading is comparable to 24-04's 1.79 and the
  verifier's 1.85 rather than merely green.
- `npx vitest run --project browser` — exit 0. Nothing here should touch it; a move is a finding.
- `git status --porcelain` free of plant residue **for this plan's files only**. Do not clean
  paths you did not write.
</verification>

<success_criteria>
- `SeedServerOptions.relayAdmission` exists, is required, is the existing union by indexed
  access, and is forwarded to `FabricNode.start`.
- `bin/seed.ts --admit-issuer` exists, is repeatable, is hex-validated in its own loop, threads
  through the ternary, and is reported in a two-armed banner an operator and a fixture can both
  read.
- A seed told nothing behaves byte-identically to before — measured, across a real process, not
  argued.
- Four guards repaired, each strictly stronger than what it replaced, each watched reddening
  under a plant that restores the state it forbade.
- Every claim in `seed-server.ts`, `bin/seed.ts` and `relay-admission.ts` that says the seed
  cannot be told to close is repaired, and the correction is recorded rather than swapped in
  silently.
</success_criteria>

<ledger_edits>
Recommend only; this plan applies none.

- `.planning/REQUIREMENTS.md` AUTH-02 traceability row — the text 24-04 drafted and landed
  contains *"and every `bin/seed.ts` is still open — 24-01 hardcoded that posture
  deliberately"*, and `24-VERIFICATION.md` L1 added the stronger *"cannot be told to close"*.
  **Both become false when this plan lands.** The row needs an appended sentence recording that
  a seed can now be told to close and naming `SeedServerOptions.relayAdmission` /
  `bin/seed.ts --admit-issuer`. **The checkbox does not move** and the verdict cell stays
  `Partial`: the mechanism existing is not the criterion being read over a fabric, and that
  reading is 24-07's.
- `.planning/ROADMAP.md` Phase 24 block — the comment stating the seed's posture is a literal
  needs the ruling of 2026-08-06 recorded beside it. Not this plan's edit.
- `.planning/STATE.md` — untouched.
</ledger_edits>

<output>
After completion, create
`.planning/phases/phase-24-certificate-gated-admission/24-06-SUMMARY.md`.

It must carry: the verified list of `SeedServer.start` sites (re-derived, not inherited); the
plant table with observed failure text and which rows stayed green under each; the
required-field `tsc` reading in both directions; the before/after solo span for
`relay-admission.node.test.ts`; and an explicit statement that the default posture did not move,
with the measurement that establishes it.
</output>
