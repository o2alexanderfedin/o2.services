---
phase: 01-determinism-gate-trust-model-verdict
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-determinism-gate-trust-model-verdict/01-BENCHMARK-METHODOLOGY.md
  - tools/docs/verify-benchmark-methodology.mjs
autonomous: true
requirements: [BENCH-02]
must_haves:
  truths:
    - "The benchmark methodology is written and committed before any benchmark number exists anywhere in the repository"
  artifacts:
    - path: ".planning/phases/01-determinism-gate-trust-model-verdict/01-BENCHMARK-METHODOLOGY.md"
      provides: "BENCH-02 pre-registration: metrics, machine inventory, run counts, cold/warm code-cache policy, redundancy factor, skew profile, single-threaded baseline definition"
      contains: "## Metrics"
    - path: "tools/docs/verify-benchmark-methodology.mjs"
      provides: "automated completeness check for the methodology document"
  key_links:
    - from: "tools/docs/verify-benchmark-methodology.mjs"
      to: "01-BENCHMARK-METHODOLOGY.md"
      via: "reads the file and asserts required headings are present"
      pattern: "readFileSync.*01-BENCHMARK-METHODOLOGY"
---

<objective>
Write and commit the benchmark methodology pre-registration (BENCH-02) before any
benchmark number exists anywhere in this repository — including before this phase's
own divergence-harness or verdict work produces any measurement. This plan has no
dependency on the skeleton (Plan 01-01) or any other plan; it is a self-contained
document plus a lightweight completeness check.

Purpose: Per CONTEXT.md's locked decision, post-hoc methodology is how benchmarks
inflate — registering the methodology in full, in writing, before any number exists
is the only credible order of operations.
Output: `01-BENCHMARK-METHODOLOGY.md`, `tools/docs/verify-benchmark-methodology.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-determinism-gate-trust-model-verdict/01-CONTEXT.md
@.planning/phases/01-determinism-gate-trust-model-verdict/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft 01-BENCHMARK-METHODOLOGY.md</name>
  <files>.planning/phases/01-determinism-gate-trust-model-verdict/01-BENCHMARK-METHODOLOGY.md</files>
  <read_first>
    .planning/phases/01-determinism-gate-trust-model-verdict/01-RESEARCH.md (§State of the Art — "Benchmark methodology template (BENCH-02)" block, COST paper structure)
    .planning/phases/01-determinism-gate-trust-model-verdict/01-CONTEXT.md ("Verdict & Benchmark Pre-registration" decisions)
    .planning/REQUIREMENTS.md (BENCH-01 through BENCH-06 — for the shape of what Phase 9 will eventually measure, so this pre-registration anticipates it correctly)
  </read_first>
  <action>
    Write `01-BENCHMARK-METHODOLOGY.md` in the phase directory with exactly these
    `##`-level headings, in this order, each with substantive content (not a
    placeholder sentence): "## Metrics" (commit to p50/p95/p99 makespan per BENCH-03
    — never a bare mean — and to reporting gross vs. useful node-seconds with the
    verification tax as an explicit line item per BENCH-04); "## Machine Inventory"
    (commit to disclosing exact hardware/runner specs for every machine in a
    published run, including relay and aggregator hardware, per BENCH-06, and to
    labeling any same-machine multi-tab measurement as such rather than counting it
    as N independent nodes); "## Run Counts & Statistical Treatment" (how many runs
    per configuration, outlier and warmup handling policy); "## Cold/Warm Code-Cache
    Policy" (both a cold-cache and a warm-cache condition must be measured and
    labeled separately, since `WebAssembly.compileStreaming`'s V8 code cache
    materially affects wall-clock numbers); "## Redundancy Factor" (the redundancy
    dial's tested value(s) recorded per run, never left implicit); "## Skew Profile"
    (a skewed-data configuration is part of the published run set, not only a
    uniform one); "## Single-Threaded Baseline Definition" (name and justify the
    baseline as competent, on the same hardware, per the COST paper's discipline,
    feeding the BENCH-05 crossover number). Open the document with a one-line
    statement: this methodology is pre-registered before any benchmark number exists
    in this repository, per BENCH-02.
  </action>
  <acceptance_criteria>
    File exists at the specified path and contains, verbatim, all seven `##`
    headings listed above, each followed by non-empty prose (not "TBD").
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^## " ".planning/phases/01-determinism-gate-trust-model-verdict/01-BENCHMARK-METHODOLOGY.md"</automated>
  </verify>
  <done>01-BENCHMARK-METHODOLOGY.md committed with all seven required sections, written before any benchmark number exists anywhere in the repository.</done>
</task>

<task type="auto">
  <name>Task 2: Automated section-presence check</name>
  <files>tools/docs/verify-benchmark-methodology.mjs</files>
  <read_first>
    .planning/phases/01-determinism-gate-trust-model-verdict/01-BENCHMARK-METHODOLOGY.md (the file just written in Task 1)
  </read_first>
  <action>
    Write a plain Node script (no framework dependency — runnable with a bare `node`
    invocation) at `tools/docs/verify-benchmark-methodology.mjs` that reads
    `01-BENCHMARK-METHODOLOGY.md` from the phase directory, checks for the presence
    of each of the seven required headings from Task 1 by exact string match, prints
    a `PASS`/`FAIL` line per heading to stdout, and sets `process.exitCode = 1` if
    any heading is missing or if the file does not exist.
  </action>
  <acceptance_criteria>
    Running the script against the Task 1 output prints `PASS` for all seven
    headings and exits 0. Running it against a copy of the file with one heading
    deleted (a manual dry-run, not a committed artifact) prints `FAIL` for that
    heading and exits 1.
  </acceptance_criteria>
  <verify>
    <automated>node tools/docs/verify-benchmark-methodology.mjs</automated>
  </verify>
  <done>An automated, framework-free lint confirms the methodology document's completeness; it can be re-run in CI or by any future plan that adds a benchmark number.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted WASM binary -> admission-gate scanner | Not applicable to this plan's scope (document-only); addressed in Plan 01-03 |
| CI measurement artifacts -> written verdict | Not applicable to this plan's scope; this plan's document is read by no automated system except its own completeness lint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-1-01 | Denial of Service | n/a — no binary parsing in this plan | accept | Out of scope; this plan produces prose and a doc-lint script only |
| T-1-02 | Tampering / Elevation of Privilege | n/a — no scanner in this plan | accept | Out of scope; implemented in Plan 01-05 |
| T-1-03 | Tampering | n/a — no LEB128 decoding in this plan | accept | Out of scope; implemented in Plan 01-03 |
</threat_model>

<verification>
- `node tools/docs/verify-benchmark-methodology.mjs` exits 0
- `git log` shows `01-BENCHMARK-METHODOLOGY.md` committed in an early wave, before any plan in this phase produces a numeric benchmark result
</verification>

<success_criteria>
`01-BENCHMARK-METHODOLOGY.md` is committed, complete against its seven required
sections, and its completeness is independently checkable by a script — all before
any benchmark number exists anywhere in this repository.
</success_criteria>

<output>
After completion, create `.planning/phases/01-determinism-gate-trust-model-verdict/01-02-SUMMARY.md`
</output>
