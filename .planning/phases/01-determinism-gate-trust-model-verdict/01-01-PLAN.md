---
phase: 01-determinism-gate-trust-model-verdict
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - .gitignore
  - packages/core/package.json
  - packages/core/tsconfig.json
  - packages/core/src/index.ts
  - vitest.config.ts
  - package-lock.json
autonomous: true
requirements: [DET-01, DET-04, DET-05, VER-07, BENCH-02]
must_haves:
  truths:
    - "A private, ESM-only TypeScript monorepo skeleton exists with @o2/core as its first package, and no code beyond this skeleton exists in the repository yet"
    - "vitest can run @o2/core's future test files under Node and under Chromium/Firefox/WebKit from one config, using the corrected vitest 4.x playwright() provider function"
  artifacts:
    - path: "package.json"
      provides: "root workspace manifest, private:true, exact-pinned dev toolchain"
      contains: "\"private\": true"
    - path: "packages/core/package.json"
      provides: "@o2/core package manifest, private:true, ESM"
      contains: "\"name\": \"@o2/core\""
    - path: "vitest.config.ts"
      provides: "multi-project test config (node + chromium + firefox + webkit)"
      contains: "playwright("
    - path: "tsconfig.json"
      provides: "base compiler config: isolatedDeclarations, moduleResolution bundler"
      contains: "isolatedDeclarations"
  key_links:
    - from: "vitest.config.ts"
      to: "@vitest/browser-playwright"
      via: "import { playwright } from '@vitest/browser-playwright'; browser: { provider: playwright() }"
      pattern: "playwright\\(\\)"
---

<objective>
Create the repository's first code: a private, ESM-only TypeScript monorepo skeleton
holding `@o2/core`, plus a vitest config that can run tests under Node and under
Chromium/Firefox/WebKit from one file. This is the enabling infrastructure every
other plan in this phase builds on — it delivers no requirement on its own but is
the precondition for all five.

Purpose: Establish the project skeleton per the phase's Walking-Skeleton override
(measurement infrastructure, not a product skeleton) so downstream plans can add
the admission gate, canonical hasher, and divergence harness without first solving
packaging.
Output: `package.json`, `tsconfig.json`, `packages/core/{package.json,tsconfig.json,src/index.ts}`,
`vitest.config.ts`, `package-lock.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/phases/01-determinism-gate-trust-model-verdict/01-RESEARCH.md
@.planning/phases/01-determinism-gate-trust-model-verdict/01-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root workspace scaffold — package.json, tsconfig.json, .gitignore</name>
  <files>package.json, tsconfig.json, .gitignore</files>
  <read_first>
    .planning/research/STACK.md (Core Technologies, Development Tools, Installation sections)
    .planning/phases/01-determinism-gate-trust-model-verdict/01-RESEARCH.md (Standard Stack table, "tsconfig.json (TS 7.0)" code block under Build/packaging)
    CLAUDE.md (Constraints section — Disclosure invariant, sole-authorship)
  </read_first>
  <action>
    Create root `package.json`: `"private": true`, `"type": "module"`, `"workspaces": ["packages/*"]`.
    Add scripts: `"test": "vitest run"`, `"test:quick": "vitest run --project node"`.
    Add exact-pinned (no `^`/`~`) devDependencies: `typescript@7.0.2`, `tsdown@0.22.14`,
    `vitest@4.1.10`, `@vitest/browser@4.1.10`, `@vitest/browser-playwright@4.1.10`,
    `playwright@1.62.0`, `@playwright/test@1.62.0`, `wabt@1.0.39`, `binaryen@131.0.0`,
    `assemblyscript@0.28.20`. Do not add any deploy-related package (no `gh-pages`,
    `vercel`, `netlify`, `wrangler`).
    Create root `tsconfig.json` per RESEARCH.md's tsconfig block: `target: "es2023"`,
    `module: "esnext"`, `moduleResolution: "bundler"`, `strict: true`,
    `isolatedDeclarations: true`, `verbatimModuleSyntax: true`,
    `noUncheckedIndexedAccess: true`, `exactOptionalPropertyTypes: true`, `types: []`,
    `lib: ["es2023", "dom", "webworker"]`.
    Update `.gitignore` (read existing file first) to ensure it ignores `node_modules/`,
    `dist/`, `*.tsbuildinfo`, `coverage/` — append only what is missing, do not remove
    existing entries.
  </action>
  <acceptance_criteria>
    `package.json` contains `"private": true` at the top level.
    No dependency or devDependency version string in `package.json` contains `^` or `~`.
    `tsconfig.json`'s `compilerOptions.moduleResolution` is exactly `"bundler"` and
    `compilerOptions.isolatedDeclarations` is exactly `true`.
    `.gitignore` contains `node_modules/`.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const p=require('./package.json'); const all={...(p.dependencies||{}),...(p.devDependencies||{})}; if(p.private!==true) throw new Error('not private'); if(Object.values(all).some(v=>/[\^~]/.test(v))) throw new Error('range specifier found'); console.log('OK')"</automated>
  </verify>
  <done>Root package.json (private:true, exact-pinned deps, no deploy tooling) and tsconfig.json (isolatedDeclarations, moduleResolution bundler) committed; .gitignore covers build artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: packages/core scaffold + vitest multi-project config</name>
  <files>packages/core/package.json, packages/core/tsconfig.json, packages/core/src/index.ts, vitest.config.ts</files>
  <read_first>
    .planning/phases/01-determinism-gate-trust-model-verdict/01-RESEARCH.md (Recommended Project Structure diagram; the `@vitest/browser-playwright` correction note under Standard Stack; Pattern 6 note on vitest projects)
    .planning/research/STACK.md (the `@vitest/browser-playwright` — CORRECTED 2026-07-24 row)
  </read_first>
  <action>
    Create `packages/core/package.json`: `"name": "@o2/core"`, `"private": true`,
    `"type": "module"`, `"version": "0.0.0"`, `"exports": { ".": { "types": "./src/index.ts", "default": "./src/index.ts" } }`.
    A built `dist/` via tsdown is not required for this phase's success criteria —
    source-direct exports are sufficient; downstream phases may add a build step.
    Create `packages/core/tsconfig.json` with `"extends": "../../tsconfig.json"` and
    `"include": ["src"]`.
    Create `packages/core/src/index.ts` as an empty barrel export (`export {}`) —
    Plans 01-03 and 01-04 add real exports later (interface-first: this task only
    establishes the file; it does not define the admission or canonical contracts).
    Create root `vitest.config.ts` with a `projects` array: one entry named `node`
    with `test.environment: 'node'` and `test.include: ['packages/core/src/**/*.test.ts']`;
    one entry named `browser` with the same `include`, plus `test.browser.enabled: true`,
    `test.browser.provider: playwright()` (imported as `import { playwright } from
    '@vitest/browser-playwright'` — a function call, NOT the string `'playwright'`),
    and `test.browser.instances: [{ browser: 'chromium' }, { browser: 'firefox' }, { browser: 'webkit' }]`.
    This is the vitest 4.x provider API correction STACK.md flags — using the string
    form will fail silently or error at config load.
  </action>
  <acceptance_criteria>
    `vitest.config.ts` contains the literal substring `playwright()` (the function-call
    form) and does NOT contain the literal substring `provider: 'playwright'`.
    `npx vitest list --project node` exits 0 (even with zero matched test files).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --project node</automated>
  </verify>
  <done>packages/core exists as a private ESM package with source-direct exports; vitest.config.ts defines node + 3-browser projects via the corrected playwright() provider function.</done>
</task>

<task type="auto">
  <name>Task 3: Toolchain version verification and install</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    .planning/phases/01-determinism-gate-trust-model-verdict/01-RESEARCH.md (Assumptions Log A1/A3; Open Questions 2 and 3; Installation section)
  </read_first>
  <action>
    Run `npm view playwright version`, `npm view @vitest/browser-playwright version`,
    `npm view @vitest/browser-playwright peerDependencies`, and `npm view playwright
    peerDependencies` to confirm mutual compatibility before trusting the versions
    pinned in Task 1 (RESEARCH.md's Open Question 3 — this was not cross-checked in
    research). If any queried version has moved past the pin recorded in Task 1,
    update `package.json` to the newly-confirmed exact version (still no `^`/`~`) and
    record the change in the task's own commit message. Run `npm install` to
    materialize `package-lock.json`.
  </action>
  <acceptance_criteria>
    `npm ls vitest @vitest/browser @vitest/browser-playwright playwright typescript
    tsdown wabt binaryen assemblyscript` exits 0 with no `UNMET PEER DEPENDENCY` or
    `invalid` entries.
    `package-lock.json` exists and is tracked.
  </acceptance_criteria>
  <verify>
    <automated>npm ls --all 2>&1 | grep -iE "unmet|invalid" | grep -v "^$" ; test $? -ne 0 && echo OK</automated>
  </verify>
  <done>package-lock.json committed; every pinned dependency installs with no peer conflicts; any version drift from research is reconciled and pinned exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted WASM binary -> admission-gate scanner | A WASM module's raw bytes cross from arbitrary/attacker-controllable input into the pure-TS parser (`packages/core/src/admission/*`, built in Plan 01-03); this plan only creates the empty package shell those files will live in |
| CI measurement artifacts -> written verdict | Raw canonical-hash comparison data crosses from an automated CI run into a human-authored `01-VERDICT.md` (Plan 01-08); this plan establishes the toolchain both depend on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-1-01 | Denial of Service | packages/core/src/admission/* (not yet created) | mitigate | Bounds-checking implemented in Plan 01-03; this plan only provides the package location and test runner |
| T-1-02 | Tampering / Elevation of Privilege | packages/core vs tools/admission-check (not yet created) | mitigate | Scanner-parity testing implemented in Plan 01-05; this plan provides the shared toolchain (wabt/binaryen pins) both scanners depend on |
| T-1-03 | Tampering | packages/core/src/admission/sections.ts (not yet created) | mitigate | Permissive LEB128 decoding implemented in Plan 01-03 |
</threat_model>

<verification>
- `npx vitest run --project node` exits 0
- `npm ls --all` shows no unmet peer dependencies
- `package.json` and `packages/core/package.json` both contain `"private": true`
- No deploy-related package name appears anywhere in `package.json`
</verification>

<success_criteria>
Repository contains a private, ESM-only monorepo skeleton with `@o2/core` as its
first package, and a working multi-project vitest config (node + chromium + firefox
+ webkit) using the vitest 4.x `playwright()` provider function. Nothing beyond
skeleton and config exists yet — no admission logic, no canonical hashing, no
divergence harness.
</success_criteria>

<output>
After completion, create `.planning/phases/01-determinism-gate-trust-model-verdict/01-01-SUMMARY.md`
</output>
