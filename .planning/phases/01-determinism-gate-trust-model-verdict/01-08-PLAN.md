---
phase: 01-determinism-gate-trust-model-verdict
plan: 08
type: execute
wave: 4
depends_on: ["01-07"]
files_modified:
  - .planning/phases/01-determinism-gate-trust-model-verdict/divergence-results/
  - .planning/phases/01-determinism-gate-trust-model-verdict/01-VERDICT.md
autonomous: true
requirements: [VER-07]
must_haves:
  truths:
    - "A committed written verdict names the v1 integrity mechanism: N-version comparison if the harness is byte-identical everywhere, backbone-anchored audit sampling if it is not"
    - "The verdict states which later phases and REQ-IDs change shape under the negative branch"
    - "The verdict's branch selection is a mechanical consequence of the CI harness's own diff-job output, with raw data attached rather than summarised"
    - "No deploy workflow exists anywhere in the repository, and every package.json carries private:true — verified as part of the phase close"
  artifacts:
    - path: ".planning/phases/01-determinism-gate-trust-model-verdict/01-VERDICT.md"
      provides: "VER-07's committed written verdict, with raw per-probe per-leg hash data attached"
      contains: "VERDICT:"
    - path: ".planning/phases/01-determinism-gate-trust-model-verdict/divergence-results/"
      provides: "the raw downloaded CI artifacts (per-OS JSON results + diff job report) the verdict is derived from"
  key_links:
    - from: "01-VERDICT.md"
      to: "divergence-results/*.json"
      via: "the verdict's raw-data table is drawn directly from these downloaded CI artifacts, not paraphrased"
      pattern: "divergence-results"
---

<objective>
Trigger the real cross-architecture CI run, download its raw results, and write the
phase's capstone deliverable: `01-VERDICT.md`, naming the v1 integrity mechanism as
a mechanical consequence of the harness's own output — never an asserted claim. If
the harness diverges anywhere, including on the NaN-bit-leak probe, the verdict
selects backbone-anchored audit sampling and names every affected downstream phase
and REQ-ID by name, per CONTEXT.md's locked branch criterion.

Purpose: This is the phase's actual goal — the project commits in writing to one
trust model before any kernel code (Phase 2 onward) is designed around either. The
verdict must be traceable to raw data, not read as an opinion.
Output: `01-VERDICT.md`, downloaded CI artifacts under `divergence-results/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-determinism-gate-trust-model-verdict/01-CONTEXT.md
@.planning/phases/01-determinism-gate-trust-model-verdict/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trigger CI, monitor, and download raw results</name>
  <files>.planning/phases/01-determinism-gate-trust-model-verdict/divergence-results/</files>
  <read_first>
    .github/workflows/divergence.yml (from Plan 01-07 — read the actual job/step names and artifact naming before scripting the download)
  </read_first>
  <action>
    Push the current branch (the workflow has no branch filter — any push to a
    branch this repository's Actions are enabled for triggers it) to fire
    `.github/workflows/divergence.yml`'s `push` event; if it does not fire
    automatically (e.g. Actions not enabled on this branch), trigger it explicitly
    via `gh workflow run divergence-harness` or the workflow's filename. Monitor via
    `gh run watch` or repeated `gh run list --workflow=divergence.yml --limit 1`
    until both the `measure` (both matrix legs) and `diff` jobs report a terminal
    status. Once complete, download every `measure` job's uploaded artifact (`gh run
    download RUN_ID`) into
    `.planning/phases/01-determinism-gate-trust-model-verdict/divergence-results/`,
    and capture the `diff` job's full log output (`gh run view RUN_ID --log
    --job=DIFF_JOB_ID`) into a sibling text file in the same directory,
    preserving the diff report and the job's exit code verbatim.
  </action>
  <acceptance_criteria>
    `divergence-results/` contains one JSON file per OS leg (2 files: one for
    ubuntu-latest, one for macos-14), each covering all four runtimes per Plan
    01-07's schema, plus a captured copy of the diff job's stdout report.
    `gh run list --workflow=divergence.yml --limit 1` shows the triggering run's
    status as completed (conclusion may be success or failure — a failure is
    itself informative data, not an error to work around).
  </acceptance_criteria>
  <verify>
    <automated>ls .planning/phases/01-determinism-gate-trust-model-verdict/divergence-results/ | wc -l | awk '{ if ($1 >= 3) print "OK"; else print "MISSING ARTIFACTS" }'</automated>
  </verify>
  <done>The real CI divergence matrix has run to completion on ubuntu-latest and macos-14; its raw per-OS JSON results and the diff job's report are downloaded and committed alongside the verdict.</done>
</task>

<task type="auto">
  <name>Task 2: Write 01-VERDICT.md</name>
  <files>.planning/phases/01-determinism-gate-trust-model-verdict/01-VERDICT.md</files>
  <read_first>
    .planning/phases/01-determinism-gate-trust-model-verdict/divergence-results/ (Task 1's downloaded raw data — the evidence this document must reflect, not paraphrase)
    .planning/phases/01-determinism-gate-trust-model-verdict/01-CONTEXT.md ("Branch criterion (strict)" and "The verdict must name affected phases and REQ-IDs explicitly" decisions)
    .planning/ROADMAP.md (Phase 2, 4, 5, 6, 7 goals and requirement lists — needed to name affected phases/REQ-IDs correctly if the negative branch is selected)
    .planning/REQUIREMENTS.md (VER-01 through VER-10 — the requirements whose design depends on which branch is selected)
  </read_first>
  <action>
    Apply the strict branch criterion mechanically against Task 1's downloaded data:
    read every probe's hash across every (os, runtime) leg. IF every probe —
    including nan_bit_leak — has an identical hash across all 8 legs, THEN the
    verdict names "N-version comparison" as the v1 integrity mechanism. ELSE the
    verdict names "backbone-anchored audit sampling" and explicitly lists every
    downstream requirement whose design changes shape under this branch — at
    minimum VER-01, VER-02, VER-05, VER-06 (Phase 2's N-version dispatch/commit-reveal
    design) and, if the divergence is architecture-general rather than scoped to a
    narrow opcode class, VER-03, VER-04, VER-08, VER-09, VER-10 (Phase 7's quorum
    design) — name the specific phases (by number and title from ROADMAP.md) each
    REQ-ID belongs to. Do not redesign the audit-sampling mechanism itself — CONTEXT.md
    explicitly defers that to downstream work; only name what it affects.
    Open the document with a single, unambiguous, machine-greppable line of the
    exact form "VERDICT: MECHANISM_NAME" (e.g. "VERDICT: N-version comparison"
    or "VERDICT: backbone-anchored audit sampling"), so downstream phases and
    tooling can parse the decision without prose interpretation. Attach the raw
    per-probe, per-leg hash-comparison table verbatim (copy from Task 1's downloaded
    JSON / diff report — do not hand-summarize or round-trip through prose). Add a
    distinct callout section for any same-architecture, cross-engine divergence
    observed (e.g. Firefox vs. Chrome on the same OS) even though it falls outside
    the strict cross-architecture branch criterion — per 01-RESEARCH.md's Open
    Question 1, this is free signal the harness produces and should not be discarded.
    Close with a disclosure-hygiene confirmation section: the output of `ls
    .github/workflows/` (expect exactly `divergence.yml`), and confirmation that
    every `package.json` in the repository (root and `packages/core`) contains
    `"private": true` (`grep -L '"private": true' $(find . -name package.json -not -path
    '*/node_modules/*')` should print nothing).
  </action>
  <acceptance_criteria>
    01-VERDICT.md exists in the phase directory and contains exactly one line
    matching `^VERDICT: `.
    The document contains a raw hash-comparison table (not prose-only summary) with
    a row per probe and columns per (os, runtime) leg.
    If "VERDICT: backbone-anchored audit sampling" was selected, the document names
    at least VER-01, VER-02, VER-05, VER-06 explicitly along with their owning
    phase(s) from ROADMAP.md.
    The disclosure-hygiene section confirms `ls .github/workflows/` shows only
    `divergence.yml` and that no `package.json` in the repository is missing
    `"private": true`.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^VERDICT: " .planning/phases/01-determinism-gate-trust-model-verdict/01-VERDICT.md</automated>
  </verify>
  <done>01-VERDICT.md is committed, names exactly one integrity mechanism as a mechanical function of the attached raw CI data, names affected phases/REQ-IDs under the negative branch if selected, and confirms repository disclosure hygiene (no deploy workflow, private:true everywhere).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted WASM binary -> admission-gate scanner | Not directly touched by this plan |
| CI measurement artifacts -> written verdict | The core boundary this plan crosses: raw CI-produced hash data becomes a human-authored claim in 01-VERDICT.md. The mitigation is procedural, not code — the verdict must quote the data verbatim (Task 1's downloaded artifacts) rather than re-deriving or paraphrasing it, so the claim remains auditable against its own evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-1-01 | Denial of Service | n/a — this plan reads already-produced JSON artifacts, no binary parsing | accept | Out of scope; mitigated in Plan 01-03 |
| T-1-02 | Tampering / Elevation of Privilege | n/a — no scanner comparison in this plan | accept | Out of scope; mitigated in Plan 01-05 |
| T-1-03 | Tampering | n/a — no LEB128 decoding in this plan | accept | Out of scope; mitigated in Plan 01-03 |
</threat_model>

<verification>
- `gh run list --workflow=divergence.yml --limit 1` shows the triggering run completed
- `divergence-results/` contains the raw per-OS JSON artifacts and the diff job's report
- `grep -c "^VERDICT: " 01-VERDICT.md` equals 1
- Disclosure-hygiene confirmation present and correct: single workflow file, private:true everywhere
</verification>

<success_criteria>
The real cross-architecture divergence matrix has run in CI; `01-VERDICT.md` is
committed, names the v1 integrity mechanism as a mechanical function of that run's
raw data (attached, not summarized), names affected phases/REQ-IDs if the negative
branch was selected, and confirms the repository's disclosure-hygiene invariant
holds. This closes Phase 1.
</success_criteria>

<output>
After completion, create `.planning/phases/01-determinism-gate-trust-model-verdict/01-08-SUMMARY.md`
</output>
