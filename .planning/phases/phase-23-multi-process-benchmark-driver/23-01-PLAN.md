---
phase: phase-23-multi-process-benchmark-driver
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/BENCHMARK-METHODOLOGY.md
  - packages/bench/src/harness.ts
  - packages/bench/src/report.ts
  - packages/bench/src/integrity.ts
  - packages/bench/src/index.ts
  - packages/bench/src/harness.test.ts
  - packages/bench/src/integrity.test.ts
autonomous: true
requirements: [BENCH-07]

must_haves:
  truths:
    - "The amendment declaring a second fixture, a third driver and a fixed-redundancy sweep is committed before any number produced under that plan exists — checkable in git log, which is the only property BENCH-02 has"
    - "A RunConfig cannot be constructed without saying which driver and which fixture produced it, so provenance reaches .planning/bench/raw.json without anyone remembering to write a heading"
    - "Every makespan row in the rendered report carries a driver cell and a fixture cell, and every makespan heading names its driver alongside the derived same-machine label"
    - "Two sweep results at the same node count can no longer collapse into one inside connectivityTax — the second is a loud error rather than a silent overwrite"
    - "A harness that spawned the wrong number of processes, reused a pid, kept the work in the driver's own CPU, or ran a fixture whose shards did not all spend their budget is detectable from plain data, without running a benchmark"
    - "An instrument that read nothing is a violation, not a clean rung: zero executors, zero agents, a zero driver-CPU delta and a zero shard-compute total each report, because an empty result is not a clean run — it can be an absent instrument"
    - "Which fixture a rung ran is derived from the CID of the bytes it dispatched and compared against what the call site declared, so `fixture: 'saturating'` cannot be true of a rung that dispatched something else"
    - "The gate is one exported function with one call site per rung, so a check whose result is discarded is a deletion a test can name rather than a shape a regex can be fooled by"
  artifacts:
    - path: "packages/bench/src/integrity.ts"
      provides: "HarnessIntegrityError, the assertIntegrity gate, and four pure violation functions over observed run data"
      min_lines: 140
    - path: "packages/bench/src/integrity.test.ts"
      provides: "Planted-input proof that each violation function reports, and reports only, what it claims, and that the gate throws on a non-empty list and returns on an empty one"
      min_lines: 140
    - path: ".planning/BENCHMARK-METHODOLOGY.md"
      provides: "Three dated amendment entries under the reserved Amendments heading"
      contains: "## Amendments"
  key_links:
    - from: "packages/bench/src/report.ts"
      to: "packages/bench/src/harness.ts"
      via: "sweepTable renders config.driver and config.fixture as columns on every row"
      pattern: "config\\.driver"
    - from: "packages/bench/src/index.ts"
      to: "packages/bench/src/integrity.ts"
      via: "barrel re-export, so bin/bench.ts can reach the checker in Plan 23-03"
      pattern: "from './integrity.ts'"
---

# Phase 23 · Plan 01 — Pre-registration amendment, declared provenance, and the integrity checker

**Closes:** BENCH-07 (criterion 4's structural half, and the mechanism criterion 1 is checked
by), under BENCH-06's reporting discipline. **Wave 1. Depends on nothing.**

<objective>
Three things have to exist before the multi-process driver can publish anything honest, and
none of them needs the driver.

1. **The amendment.** `.planning/BENCHMARK-METHODOLOGY.md` reserves an `## Amendments`
   section and says in its own preamble that changes to the pre-registered plan go there,
   dated, with the reason. Phase 23 changes the plan in three ways — a second fixture, a
   third driver, and a fixed-redundancy speedup sweep. Writing them into the plan body
   would defeat the ordering property BENCH-02 exists for; writing them *after* the run
   would defeat it more thoroughly. This plan is wave 1 and the run is wave 4, so the
   ordering is a fact about `git log` rather than an intention.
2. **The declared dimensions.** `RunConfig` already carries `transport` and `skew` — data
   that is rendered nowhere and travels everywhere, into `SweepResult` and thence into
   `.planning/bench/raw.json`. `driver` and `fixture` join them as **required** fields, for
   the same reason `Inventory.machines` is required: a figure that *can* be published
   without its provenance eventually is.
3. **The checker.** Criterion 1 requires that a run which silently falls back to in-process
   nodes fails the harness rather than reporting a curve. That check has to be a pure
   function over plain data, because `bin/bench.ts` runs `main()` on import and nothing in
   CI will ever execute it (see 23-CONTEXT.md Risk 3). A guard nobody has watched fail is
   worth nothing, so the checker ships with planted inputs proving each of its four
   readings can report — including the two cardinality readings that stop an *absent*
   instrument from being mistaken for a clean rung, and one gate function so there is
   exactly one place a rung's verdict is consumed.

Purpose: make provenance structural instead of editorial, and make "the harness is not
measuring what it says it is" an error class rather than a hope.
Output: three dated amendment entries; two new required `RunConfig` fields rendered as
columns and headings; a `multiProcess` curve the report knows how to render; a
`connectivityTax` that refuses to silently collapse two drivers into one row; and
`@o2/bench`'s `integrity.ts`, barrel-exported and wired by Plan 23-03.

**Scope note on the checker's reach, stated because it bounds what Plan 23-03 can claim.**
Every function here is pure and every one of them is proved by planting an input and
watching it report. What that does *not* prove is that the driver builds its observation
from the live fabric rather than from a constant — nothing executes `bin/bench.ts` in any
suite (`main()` runs on import), so the wiring is held by a source-shape guard and by the
call-site count in Plan 23-03 Task 3, not by an execution. That half is **unmeasured**;
what would measure it is an entry point that runs one rung under a stub fabric with a
planted defect and asserts the abort, which needs `main()` split from the module body and
is not this phase's work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-23-multi-process-benchmark-driver/23-CONTEXT.md
@.planning/BENCHMARK-METHODOLOGY.md
@packages/bench/src/harness.ts
@packages/bench/src/report.ts
@packages/bench/src/harness.test.ts
</context>

<interfaces>
<!-- What this plan builds against. Extracted so no exploration is needed. -->

`packages/bench/src/harness.ts` — the two declarations this plan changes:

```ts
export type TransportKind = 'memory' | 'real'
export type Skew = 'uniform' | 'skewed'

export interface RunConfig {
  readonly nodes: number
  readonly shards: number
  readonly redundancy: number
  readonly transport: TransportKind
  readonly skew: Skew
}

export function connectivityTax(
  memory: readonly SweepResult[],
  real: readonly SweepResult[],
): readonly ConnectivityTax[] {
  const byNodes = new Map(real.map((point) => [point.config.nodes, point]))
  // …two entries at the same node count collapse here, silently.
}
```

`packages/bench/src/report.ts` — the three sites this plan changes:

```ts
export interface Report {
  readonly title: string
  readonly at: string
  readonly inventory: Inventory
  readonly baseline: Summary
  readonly memoryTransport: readonly SweepResult[]
  readonly realTransport: readonly SweepResult[]
  readonly connectivity: readonly ConnectivityTax[]
  readonly crossover: Crossover
  readonly unmet: readonly string[]
  readonly excluded?: readonly { readonly config: string; readonly reason: string }[]
}

function sweepTable(results: readonly SweepResult[]): string   // 12 columns today
// renderMarkdown, lines 163-167:
out.push(`## Makespan — memory transport (${label})`, '')
out.push(sweepTable(report.memoryTransport))
out.push(`## Makespan — real transport (${label})`, '')
out.push(sweepTable(report.realTransport))
```

`packages/bench/src/harness.test.ts` — the two literals that need the new fields:

```ts
const config: RunConfig = { nodes: 4, shards: 8, redundancy: 2, transport: 'memory', skew: 'uniform' }
const point = (nodes: number, p50: number): SweepResult => ({ config: { ...config, nodes }, … })
// and, at :55, sweepNodeCount(…, { shards: 4, redundancy: 1, transport: 'memory', skew: 'uniform' }, …)
```

`packages/bench/src/index.ts` — the barrel block to extend (do not restructure the rest):

```ts
export { NODE_LADDER, RUNS_PER_CONFIG, connectivityTax, costCrossover, measure, sweepNodeCount } from './harness.ts'
export { hostCount, isSameMachine, machineLabel, renderMarkdown } from './report.ts'
```

**Purity, non-negotiable.** `purity.node.test.ts:28` lists `bench` among the portable
packages: no `node:*` import, no platform global, no libp2p. Every function added here is
pure over plain data, which is also what lets `integrity.test.ts` run under both the `node`
and the `browser` vitest projects without a suffix.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: the amendment, dated, before any number produced under it exists</name>
  <files>.planning/BENCHMARK-METHODOLOGY.md</files>
  <read_first>
    .planning/BENCHMARK-METHODOLOGY.md lines 1-16 (the preamble that makes appending the only permitted edit) and its `## Amendments` section at the end, which currently reads "*None. Entries appended here must be dated and must state what changed and why.*"
    .planning/BENCHMARK-METHODOLOGY.md §1.1 (the two axes, swept separately — the amendment must say plainly that this phase sweeps the node-count axis only) and §6.3 (the declared fixture bias this phase's second fixture is a response to)
    .planning/phases/phase-23-multi-process-benchmark-driver/23-CONTEXT.md decisions 1, 7, 9 and 11 (the three changes, with the measured numbers that justify the fixture parameters)
    packages/node/src/vocabulary.node.test.ts lines 47-73 (the BANNED array — this file is git-tracked and inside the scanned tree; read the array before writing prose, and do not restate the prohibition, because a document quoting it violates it)
  </read_first>
  <action>
    Append to `## Amendments`, replacing the "*None…*" placeholder line with three entries,
    each dated **2026-07-28** and each stating what changed and why. Change nothing above
    that heading — not a word, not a table cell. The value of this file is entirely in the
    order its edits appear in `git log`.

    **Entry 1 — a second fixture.** §6.3 declared as a known bias that the WASM fixtures do
    almost no work, so per-task overhead dominates and the COST crossover is worse than a
    realistic workload's. This phase adds a fixture that is not tiny: `@o2/demo`'s colouring
    kernel at `n = 700`, `budget = DEFAULT_BUDGET` (5,000,000), 8 partitions. State the
    measured per-shard cost on this host — 8 shards of 80-118 ms, serial total 740 ms, every
    shard returning `budget` — and state why those parameters rather than rounder ones:
    `n = 500` lets one cube finish early, so shard cost stops being uniform and a ratio
    across the ladder would be measuring the search rather than the fabric; `n = 1000`
    pushes the input past `WIRE_CHUNK_BYTES = 16_384` and changes the transport regime
    between the two fixtures for no gain. State that the trivial fixture is **kept**, that
    the COST crossover and the single-threaded baseline stay defined against it, and that
    the new fixture is added rather than substituted.

    **Entry 2 — a third driver, and two declared dimensions.** Both curves published under
    Phase 8 built every node inside the driver's own process, so no parallel speedup was
    measurable at any node count. This phase adds a `process-per-node` driver that spawns
    `bin/agent.ts` as N operating-system processes. `RunConfig` gains required `driver` and
    `fixture` fields so every observation in `.planning/bench/raw.json` carries its own
    provenance, and both are rendered as columns. Say plainly what this does **not** buy, in
    these words: a same-host run **cannot** detect divergence between machines, because it
    has one CPU, one V8 and one libc; the distinct-machine claim is **descoped and
    unmeasured, and unmeasured is not met**. Also declare the integrity thresholds this
    phase will judge its own runs against, so they are fixed before any data exists: a rung
    is rejected when the observed process identity is wrong (Task 3's first check), when the
    driver's own CPU time exceeds **half** the shard compute time it dispatched
    (`MAX_DRIVER_CPU_SHARE = 0.5`, chosen because work that stayed in the driver would put
    that ratio at or above 1.0 while a driver that only coordinates sits far below it), or
    when the fixture's shard status vector is anything other than eight `budget` results.

    **Entry 3 — a fixed-redundancy speedup sweep.** The existing ladders run at
    `redundancy = min(2, nodes)`, so the 1-node rung is R=1 and every other rung is R=2; a
    ratio taken between them has an ideal value of 4×, not 8×, before anything real happens.
    The published N=1 ↔ N=8 ratio therefore comes from a dedicated sweep at **R = 1** across
    the full ladder, run under both drivers on the same fixture so the multi-process curve
    has an in-process control that varies in exactly one thing. Declare the ideal bound
    ahead of the observation: `serialTotal / maxShard` = 740 / 118 ≈ **6.3×**, not 8×,
    because makespan at 8 nodes is set by the slowest shard. Declare, in the same entry, how
    the run will re-derive that same quantity for itself, so the amendment and the driver
    cannot drift apart: the eight shards are dispatched **one at a time** — one call in
    flight at a time — to the N=1 process-per-node fabric after that rung's measured runs, and
    `sum / max` is taken over those eight measurements. State why the measured job's own
    per-call times cannot be used for it: `submitJob` dispatches every shard concurrently
    (`packages/core/src/job/submit.ts:206`), so at N=1 the eight recorded intervals overlap,
    and a sum of overlapping intervals is not a serial total. Declare that the existing
    ladders keep `min(2, nodes)` and keep their published numbers, and that the verification
    tax on the R=1 sweep is reported as the identity `1.00×` — BENCH-04 is satisfied by the
    figure appearing, not by it exceeding one.

    Write the entries as prose a reader will meet cold. Do not enumerate the banned
    vocabulary anywhere; `vocabulary.node.test.ts` scans this file and a document quoting
    the prohibition is itself a violation.
  </action>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/o2.services && grep -q '^## Amendments' .planning/BENCHMARK-METHODOLOGY.md && ! grep -q 'None. Entries appended here' .planning/BENCHMARK-METHODOLOGY.md && grep -c '2026-07-28' .planning/BENCHMARK-METHODOLOGY.md && npx vitest run packages/node/src/vocabulary.node.test.ts</automated>
  </verify>
  <done>
    `## Amendments` carries three dated entries; the placeholder line is gone; nothing above
    the heading changed (`git diff` on this file shows additions below `## Amendments`
    only). The vocabulary guard passes over the new prose.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: driver and fixture as required dimensions, and a connectivity tax that cannot silently collapse</name>
  <files>packages/bench/src/harness.ts, packages/bench/src/report.ts, packages/bench/src/harness.test.ts</files>
  <read_first>
    packages/bench/src/harness.ts:45-56 (TransportKind, Skew, RunConfig — the exact idiom the two new dimensions copy) and :234-268 (connectivityTax, whose `new Map(real.map(…))` is the silent collapse decision 8 names)
    packages/bench/src/report.ts:77-98 (Report), :117-129 (sweepTable's 12 columns), :137-204 (renderMarkdown, and specifically :163-167 where the two makespan headings are emitted with the derived label)
    packages/bench/src/harness.test.ts:17-23 (the `config` literal), :141-154 (`point`, which spreads `config` so it inherits the new fields for free), :55-59 (the `sweepNodeCount` base literal, which does not), :240-263 (the label-occurrence assertion that must keep passing)
    .planning/phases/phase-23-multi-process-benchmark-driver/23-CONTEXT.md decisions 7 and 8 (why required rather than optional, and why the multi-process curve is a separate field rather than appended rungs)
  </read_first>
  <behavior>
    - A RunConfig literal missing `driver` or `fixture` does not compile. Proven by the
      whole package type-checking only after every existing literal in harness.test.ts has
      been given both fields — there is no runtime assertion for this, and none is wanted.
    - renderMarkdown over a report with all three curves emits three makespan headings, each
      naming its driver in words a reader can grep: memory transport and real transport
      under the in-process driver, and a third section for the process-per-node curve. The
      derived same-machine label still appears in every one of them, so the existing
      "at least three occurrences" assertion passes with room to spare rather than by luck.
    - Every data row in every makespan table carries a driver cell and a fixture cell.
      Asserted against the rendered string, not against the model: a column present in the
      header and absent from the rows is exactly the failure this criterion is about.
    - renderMarkdown over a report whose `multiProcess` is undefined emits no third section
      and no empty table — a report from before this driver existed still renders.
    - The connectivity-tax heading and the COST-crossover heading each name the driver and
      the fixture their numbers are defined against, **derived from the curve they are
      computed from** rather than written into the string: a report whose memory curve is
      `in-process` / `trivial` renders `## Connectivity tax — in-process driver, trivial
      fixture`, and the same report built from a `saturating` memory curve renders
      `saturating` in that heading. Asserted by rendering two reports that differ only in
      those config fields and comparing the two headings. These are the two published figure
      families — the tax table and the headline crossover factor — that carry no provenance
      today, and a heading interpolated from a literal would be a declaration rather than a
      derivation.
    - The excluded-configurations heading carries the same suffix, derived from
      `report.realTransport` — the curve whose sweep produced every row in that table. It is
      the third heading in this file with no provenance today, and Plan 23-05's section scan
      holds every figure-carrying section to a driver name in its heading, so a table of
      excluded rungs with an anonymous heading is a red test rather than an oversight.
    - When the curve a heading is derived from is empty, the heading says so
      (`— no runs`) rather than interpolating `undefined`.
    - connectivityTax throws a RangeError naming the duplicated node count when either curve
      contains two results at the same node count, rather than keeping whichever was
      appended last. The message names the count and the curve, because "duplicate rung" on
      its own does not tell anyone which figure was about to be silently overwritten.
    - connectivityTax over two curves with distinct node counts behaves exactly as it does
      today, including skipping node counts present in only one curve.
  </behavior>
  <action>
    In `packages/bench/src/harness.ts`, add `export type DriverKind = 'in-process' |
    'process-per-node'` and `export type FixtureKind = 'trivial' | 'saturating'`, each with
    a doc comment saying what the value means as a *measurement* claim rather than as an
    implementation note: `in-process` means every node shared the driver's event loop, so a
    flat curve is a property of the harness; `process-per-node` means each node was its own
    operating-system process on the same host, which is where a speedup can exist and where
    a same-host run still cannot say anything about divergence between machines. For
    `FixtureKind`, say that `trivial` is the fixed-output partition module the COST crossover
    and the single-threaded baseline are defined against, and `saturating` is the colouring
    kernel whose shards each spend a declared budget.

    Add both to `RunConfig` as **required** readonly fields, with a comment stating the
    reason they are not optional: an optional provenance field is one that gets omitted at
    the one call site nobody re-reads, and `.planning/bench/raw.json` has no headings to
    fall back on.

    Change `connectivityTax` to reject duplicate node counts in either argument before it
    builds its map. Write a small local helper that scans a curve for a repeated
    `config.nodes` and throws a `RangeError` naming the curve ('memory' or 'real') and the
    repeated count. Document why this is an error rather than a merge: the tax is defined in
    the methodology as the same job over two transports with everything else held constant,
    so two entries at one node count means something else varied, and picking either one is
    a choice the function is not entitled to make.

    In `packages/bench/src/report.ts`, add `readonly multiProcess?: readonly SweepResult[]`
    to `Report`, documented as the process-per-node curve kept deliberately apart from
    `realTransport` — with the reason: appending it there and letting a column tell them
    apart is exactly what `connectivityTax`'s map would have collapsed. Extend `sweepTable`
    to render `point.config.driver` and `point.config.fixture` as two further columns,
    placed immediately after `nodes` so provenance is read before any number is. Update the
    header row and the alignment row to match — a table whose header and body disagree on
    column count renders as garbage in every Markdown viewer, and that is the failure this
    task's row assertion is watching for.

    In `renderMarkdown`, change the two existing headings to name their driver
    (`## Makespan — memory transport, in-process driver (${label})` and the real-transport
    equivalent), and emit a third section `## Makespan — real transport, process-per-node
    driver (${label})` followed by `sweepTable(report.multiProcess)` only when
    `report.multiProcess` is defined. Keep the label in all three, which is what
    `harness.test.ts`'s occurrence count is guarding.

    Then close the two published figure families that carry no provenance at all today.
    `## Connectivity tax` (`report.ts:177`) and `## COST crossover` (`report.ts:189`) are
    the tax table and the paragraph carrying the headline crossover factor — the largest
    number in the file — and neither says which driver or which fixture defines it. Add a
    small local helper `provenanceOf(curve: readonly SweepResult[]): string` returning
    `` `${c.driver} driver, ${c.fixture} fixture` `` from the **first result's config**, or
    `'no runs'` when the curve is empty, and suffix both headings with it. Derived from the
    data, never interpolated from a literal: a heading that names a driver the numbers did
    not come from is worse than a heading that names none, and the only defence against that
    is reading the value off the curve being rendered. The tax is derived from
    `report.memoryTransport` (the same curve `connectivityTax` was fed) and the crossover
    from `report.memoryTransport` as well, since `costCrossover(baseline, memoryResults)` is
    what `bin/bench.ts` computes.

    Suffix `## Configurations excluded, and why` (`report.ts:170`) the same way, from
    `provenanceOf(report.realTransport)`: every row in that table is a rung of the
    real-transport sweep that could not be measured, so that curve is where its provenance
    comes from. Three headings gain a suffix in this task, not two. The reason to do it here
    rather than leave it: Plan 23-05's section scan requires a driver name in the heading of
    **every** section carrying a figure, and an exclusion is a published figure — a fact this
    repository already states at `report.ts:90-96`.

    One case needs a fallback and it is the case exclusions actually arise in. A rung reaches
    `excluded` precisely by *failing*, so a run in which every real rung failed has a
    non-empty `excluded` and an **empty** `realTransport` — and `provenanceOf` on an empty
    curve returns `'no runs'`, which names no driver. Derive this one heading from
    `report.realTransport` when it has runs and from `report.memoryTransport` otherwise, with
    a comment saying why that is still a derivation and not a guess: both trivial ladders are
    swept by the same driver on the same fixture, so the memory curve carries the same two
    values the real curve would have. Only when **both** are empty does the heading say
    `— no runs`, and a report with no measured rung at all failing the section scan is the
    right outcome. The tax and crossover headings keep the plain `— no runs` behaviour, and
    neither needs a fallback: when `report.connectivity` is empty the tax section renders
    `_not computed …_` and carries no figure at all, so the section scan does not hold it; and
    `report.memoryTransport` cannot be empty in a completed run, because the memory sweep in
    `bin/bench.ts:387-396` has no per-rung `try`/`catch` — it either produces rungs or throws
    out of `main()`.

    In `packages/bench/src/harness.test.ts`, add `driver: 'in-process'` and
    `fixture: 'trivial'` to the module-level `config` literal (which `point` spreads, so
    every existing crossover and tax test is covered by that one edit) and to the
    `sweepNodeCount` base literal at :55. Then add the new assertions from the behavior
    list above: three headings with their drivers; a driver cell and a fixture cell in a
    rendered data row; no third section when `multiProcess` is undefined; the three derived
    headings, asserted by rendering the same report twice with the memory curve's `fixture`
    changed and comparing the connectivity-tax and COST-crossover headings across the two
    renders, and by rendering it twice with the **real** curve's `driver` changed and
    comparing the excluded-configurations heading — that report needs a non-empty `excluded`
    array or `renderMarkdown` emits no such section at all (`report.ts:169`) — (equal strings
    would mean the provenance is a literal, which is the failure this assertion exists for,
    and comparing only one of the three headings would leave the other two able to be
    literals); the empty-curve
    heading; and the two `connectivityTax` cases, one throwing on a duplicated rung and one
    unchanged. For the duplicate case, build the two colliding points with different p50s and
    assert on the thrown message rather than merely on the throw, so a future refactor cannot
    satisfy it by throwing for some other reason.
  </action>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/o2.services && npx tsc --noEmit && npx vitest run packages/bench</automated>
  </verify>
  <done>
    `RunConfig` carries required `driver` and `fixture`; `Report` carries optional
    `multiProcess`; `sweepTable` renders both new columns on every row; `renderMarkdown`
    emits three driver-named makespan headings and keeps the derived label in each, and
    suffixes the connectivity-tax, COST-crossover and excluded-configurations headings from
    the curve each is derived from; `connectivityTax` throws on a duplicated node count with
    a message naming it.
    `npx tsc --noEmit` exits 0 for the whole repository and `packages/bench` is green,
    including the pre-existing label-occurrence assertion.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the integrity checker — four readings and one gate, each watched failing</name>
  <files>packages/bench/src/integrity.ts, packages/bench/src/integrity.test.ts, packages/bench/src/index.ts</files>
  <read_first>
    .planning/phases/phase-23-multi-process-benchmark-driver/23-CONTEXT.md, the "Criterion 1" section of `<specifics>` (its three checks, and the reason the CPU one is the falsifier that does not depend on the harness telling the truth about itself — this task adds a fourth, fixture provenance, and two cardinality readings the context section does not name) and decision 6 (why an integrity failure aborts the run while a measurement failure becomes an excluded row)
    packages/node/src/vocabulary.node.test.ts:518-590 (the plant-and-watch idiom this test file copies: synthetic inputs, one planted defect at a time, `toEqual` rather than `toContain` so each planted case doubles as the control for the rest)
    packages/net/src/egress.ts (any exported error class in this repository — the shape to copy: a typed detail on readonly fields, `this.name` set, and a doc saying callers branch on the fields rather than on the message string)
    packages/bench/src/index.ts (the barrel; this task adds one export block, it does not restructure the existing ones)
  </read_first>
  <behavior>
    - processIdentityViolations over a clean observation — N distinct child pids, each equal
      to the pid the child announced, none equal to the driver's own, N executor node ids
      each matching an announced peer id, and the submitting node's peer id absent from the
      executor set — returns an empty array.
    - Each of **eight** planted defects is reported, and reported alone, against an otherwise
      clean observation: a short pid list; a duplicated pid; the driver's own pid among the
      children; an announced pid that does not equal its child's pid; an executor node id
      that no agent announced; the submitting node's own peer id among the executors; **an
      executor count that is not `expected`**; and **an empty `agents` array**. Every message
      names the offending value, because a violation a reader cannot act on is a failed run
      with extra steps.
    - The two cardinality readings are the ones that stop an absent instrument reading as a
      clean rung. `executorNodeIds: []` satisfies "no executor id that an agent did not
      announce" and "the submitter is not among the executors" vacuously — a fabric that
      built no executors at all would otherwise return `[]` and be published as a measured
      rung. The message must name both numbers, observed and expected, because "wrong
      executor count" without them sends the reader back to the source.
    - cpuAttributionViolations returns empty when the driver's own CPU time is a small
      fraction of the shard compute time it dispatched, and reports when the ratio reaches
      MAX_DRIVER_CPU_SHARE — the reading that catches a fallback which faked its pids.
    - cpuAttributionViolations reports when the shard compute time is zero or negative, and
      **separately** when the driver's own CPU delta is zero or negative, each with a message
      saying which instrument read nothing. Two readings rather than one, because a zero
      numerator and a zero denominator are different failures: the first says the CPU clock
      was never sampled, the second says no shard work was attributed. An instrument that
      never read anything must not be mistakable for a clean reading — the same reasoning
      egress.test.ts's delivery counter uses. Note in the doc that these two are what make
      the ratio reading falsifiable at all: without them a run in which both numbers are zero
      passes the ceiling check.
    - fixtureUniformityViolations returns empty for eight statuses all equal to the expected
      one, and reports when any status differs or when the count differs from the expected
      shard count, naming the observed vector. It also reports an **empty** status vector,
      naming it as an unread instrument rather than as uniformity. A fixture whose shards did
      not all spend their budget makes the N=1 ↔ N=8 ratio a measurement of the search rather
      than of the fabric.
    - fixtureProvenanceViolations reports when the module CID a rung actually dispatched is
      not the CID registered for the fixture that rung declares, naming both CIDs and the
      declared name, and when the declared name has no registered CID at all. This is the
      reading that makes `fixture: 'saturating'` a fact about the bytes rather than a field
      the call site filled in: a rung wired to the wrong module produces a different CID and
      cannot pass, whatever its config says.
    - HarnessIntegrityError carries the violation list and the rung it was raised at as
      readonly fields, and its message names both, so a run that aborts prints something
      actionable to stdout without a debugger.
    - assertIntegrity(at, violations) throws that error when the list is non-empty and
      returns silently when it is empty. It exists so the driver has **one** gate call per
      rung instead of four `if (list.length > 0)` branches, three of which a later edit can
      quietly stop reading — a check whose result is discarded is not a check, and the only
      defence that does not depend on a reader noticing is having one place where the result
      is consumed.
  </behavior>
  <action>
    Create `packages/bench/src/integrity.ts` as a pure module — no `node:*` import, no
    platform global, nothing from `@o2/core`. Its module comment states what this file is
    for and what it deliberately is not: these are checks on whether the harness measured
    what it claims, kept separate from the measurement itself so they can be exercised
    without running a benchmark. Say plainly that nothing in CI runs `bin/bench.ts` (its
    `main()` executes on import), so these functions fire when a human runs the benchmark,
    not when a change breaks it — and that this is precisely why they are pure functions
    with planted-input tests rather than assertions buried in the driver.

    Export `MAX_DRIVER_CPU_SHARE = 0.5` with a doc naming the reasoning declared in Task 1's
    amendment: work that stayed in the driver would put the ratio at or above 1.0, a driver
    that only coordinates sits far below it, and the value is fixed before any data exists
    so it cannot be tuned to whatever a first run happened to produce.

    Export `MIN_SHARD_COMPUTE_MS = 0` and `MIN_DRIVER_CPU_MS = 0` implicitly through the two
    zero readings below rather than as constants — the point is that a non-positive reading
    is never a measurement, and a named threshold would invite someone to lower it.

    Export `class HarnessIntegrityError extends Error`, constructed from one detail object
    with readonly `at` (string — the rung, e.g. 'process-per-node, 8 nodes') and
    `violations` (readonly string[]). Assign both to readonly fields, set `this.name`, and
    build a `super` message that names the rung and joins the violations. Document that
    callers branch on `instanceof`, never on the message, and — the part that matters —
    that this error is the one class of failure `bin/bench.ts` must **not** convert into an
    excluded row: an excluded row says "this configuration could not be measured", and this
    error says "the measurement that came back is not of the thing it claims", which is a
    different statement and belongs in a different place.

    Export `assertIntegrity(at: string, violations: readonly string[]): void` — throws
    `new HarnessIntegrityError({at, violations})` when the list is non-empty, returns
    otherwise. Document it as **the** gate: the driver calls it once per rung with every
    violation list concatenated, so there is exactly one place where a check's result is
    consumed and exactly one line whose deletion turns the gate off. Say plainly that this
    shape was chosen over four inline `if` branches because a source-text guard cannot tell a
    called-and-read checker from a called-and-discarded one — it matches identifiers, not
    data flow — so the property has to come from there being only one consumer.

    Export `interface AgentIdentity { readonly childPid: number; readonly announcedPid:
    number; readonly peerId: string }` and `interface ProcessIdentity { readonly expected:
    number; readonly driverPid: number; readonly agents: readonly AgentIdentity[]; readonly
    executorNodeIds: readonly string[]; readonly submitterPeerId: string }`, then
    `processIdentityViolations(observed: ProcessIdentity): readonly string[]` implementing
    the eight checks in the behavior list — including the two cardinality readings,
    `agents.length === 0` and `executorNodeIds.length !== expected`. Return messages, not
    booleans, and put the observed value in each message; the executor-count message names
    the observed count *and* `expected`.

    Export `interface CpuAttribution { readonly driverCpuMs: number; readonly
    shardComputeMs: number; readonly maxShare?: number }` and
    `cpuAttributionViolations(observed: CpuAttribution): readonly string[]`, defaulting
    `maxShare` to `MAX_DRIVER_CPU_SHARE`. Report the two non-positive readings first and
    separately from the ratio case, so a silent instrument and a busy driver are different
    findings rather than one.

    Export `interface FixtureUniformity { readonly statuses: readonly string[]; readonly
    expected: string; readonly shards: number }` and
    `fixtureUniformityViolations(observed: FixtureUniformity): readonly string[]`. Take
    statuses as plain strings rather than importing `@o2/demo`'s `PartialStatus`: this
    package is portable and must not acquire a dependency on the demo package to check a
    string. An empty vector reports as an unread instrument, not as uniformity.

    Export `interface FixtureProvenance { readonly declared: string; readonly moduleCid:
    string; readonly expected: Readonly<Record<string, string>> }` and
    `fixtureProvenanceViolations(observed: FixtureProvenance): readonly string[]`. `expected`
    maps a fixture name to the CID string of the module that fixture *is*, built once by the
    driver from the bytes themselves. Two readings: the declared name has no entry in
    `expected`, and the entry does not equal `moduleCid`. Document what this closes, because
    it is not obvious from the signature: `RunConfig.fixture` is filled in by the call site,
    so every downstream heading, column and ratio inherits whatever the caller typed. A rung
    that declares `saturating` while its fabric was handed the trivial module is a wiring
    slip that produces a fast, flat curve and a large apparent speedup, with the word
    `saturating` printed beside it. Comparing CIDs is the only reading that is a fact about
    the bytes that were dispatched.

    Write `packages/bench/src/integrity.test.ts` in the plant-and-watch idiom. Build one
    clean `ProcessIdentity` fixture and derive each planted case from it by changing exactly
    one thing, asserting with `toEqual` on the resulting violation list length and on a
    substring of the message that names the planted value — so one planted case is the
    control for the other seven. Do the same for the three CPU cases, the three fixture
    uniformity cases and the two provenance cases: **sixteen** planted defects in all
    (8 + 3 + 3 + 2 — count them against the behavior list above, which is the authority if
    this number and that list ever disagree), each watched reported alone. Add one test constructing a `HarnessIntegrityError` and asserting
    its message names both the rung and at least one violation, and two tests on the gate:
    `assertIntegrity('x', [])` returns without throwing, and `assertIntegrity('process-per-node,
    8 nodes', ['…'])` throws a `HarnessIntegrityError` whose `at` and `violations` round-trip.
    No `.node.` suffix: every function here is pure, so the file runs under the browser
    project too, which is the standing rule for this package.

    In `packages/bench/src/index.ts`, add one export block after the existing `report.ts`
    block: the value exports `HarnessIntegrityError`, `MAX_DRIVER_CPU_SHARE`,
    `assertIntegrity`, `cpuAttributionViolations`, `fixtureProvenanceViolations`,
    `fixtureUniformityViolations`, `processIdentityViolations`, and the type exports
    `AgentIdentity`, `CpuAttribution`, `FixtureProvenance`, `FixtureUniformity`,
    `ProcessIdentity`. Note in a comment that Plan 23-03 supplies the production caller in
    `bin/bench.ts`, and that an export here without one joins `sweepNodeCount` on the
    built-not-wired list this project tracks.
  </action>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/o2.services && npx tsc --noEmit && npx vitest run packages/bench && npx vitest run packages/node/src/purity.node.test.ts</automated>
  </verify>
  <done>
    `integrity.ts` exports the error class, the gate, the declared threshold and four
    violation functions, all pure. `integrity.test.ts` plants sixteen defects — eight
    identity, three CPU, three uniformity, two provenance — and watches each reported alone,
    and both gate cases are asserted. The barrel re-exports every one of
    them. `purity.node.test.ts` still passes, so `@o2/bench` is still portable.
    `npx tsc --noEmit` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a measured run → the published file | Everything downstream of `renderMarkdown` is read by someone who was not there. Provenance that is not carried in the data is provenance that will be separated from it |
| the driver's claims about itself → the reader | Criterion 1 exists because a harness reporting its own process count is the least trustworthy witness available. `cpuAttributionViolations` is the reading that does not take the harness's word |
| `.planning/BENCHMARK-METHODOLOGY.md` before the run → the same file after | The pre-registration property is a fact about commit order, and nothing enforces it except that this plan is wave 1 and the run is wave 4 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-23-01 | Repudiation | A figure reaches `.planning/bench/raw.json` with no record of which driver or fixture produced it, and is later compared against a figure from the other | mitigate | `driver` and `fixture` are required `RunConfig` fields, so they travel into every `SweepResult` and every raw observation without anyone remembering a heading; Task 2 renders both as columns as well |
| T-23-02 | Tampering | Two curves at the same node count collapse inside `connectivityTax`, and the published tax is computed against an arbitrary one of two drivers with no error | mitigate | Task 2 makes a duplicated node count a `RangeError` naming the count and the curve; the test asserts the message, not merely the throw |
| T-23-03 | Spoofing | A run reports N processes it did not spawn — pids invented, or reused from a previous rung | mitigate | `processIdentityViolations` checks distinctness, self-exclusion, announced-equals-actual and **both cardinalities**, so an empty observation reports rather than passing; `cpuAttributionViolations` is the independent reading that does not rely on any pid being honest, and its two non-positive readings mean an unsampled clock cannot pass as a low ratio |
| T-23-03b | Tampering | A rung declares `fixture: 'saturating'` while its fabric was handed the trivial module, producing a fast flat curve and a large apparent speedup with the right word printed beside it | mitigate | `fixtureProvenanceViolations` compares the CID the rung actually dispatched against the CID of the fixture bytes, so the declared name has to agree with the bytes; Plan 23-03 runs it on every rung of **both** speedup sweeps, control included |
| T-23-04 | Information Disclosure | The amendment or the rendered report carries vocabulary that a reviewer greps for and reacts to before reading intent | mitigate | Task 1's verify block runs `vocabulary.node.test.ts`, which scans this file; the same guard runs over the generated report in Plan 23-05 |
| T-23-05 | Denial of Service | `MAX_DRIVER_CPU_SHARE` is set too tight and aborts legitimate runs, so the threshold gets raised after seeing data — which is the pre-registration failure this project exists to avoid | accept | The value is declared in Task 1's amendment before any run, and it is a constructor-overridable default rather than a hard-coded literal, so a *documented* change is possible and an undocumented one is visible in `git log` on a pre-registration file |
</threat_model>

## How this is proved

| Claim | Test file | The single-line deletion that turns it red |
|---|---|---|
| Provenance cannot be omitted | `packages/bench/src/harness.test.ts` | The `config` literal at :17 no longer type-checks without `driver` and `fixture`; `npx tsc --noEmit` is the assertion, and there is deliberately no runtime one. Deleting `readonly driver: DriverKind` from `RunConfig` turns the whole package red |
| Every figure names its driver | `packages/bench/src/harness.test.ts` | `expect(markdown).toContain('## Makespan — real transport, process-per-node driver')` and a row assertion matching `| 1 | process-per-node | saturating |` against the rendered string. Deleting the `driver` cell from `sweepTable`'s row template fails the row assertion |
| The tax, the crossover and the exclusion table name their driver and fixture, derived | `packages/bench/src/harness.test.ts` | Two renders differing only in the memory curve's `fixture`, asserted to produce different connectivity-tax headings **and** different COST-crossover headings; plus two renders differing only in the real curve's `driver`, asserted to produce different excluded-configurations headings. Deleting the `provenanceOf(...)` suffix from **any** of the three makes that pair of headings equal and fails it — all three are compared, because a row that describes only the tax heading leaves the other two able to be literals |
| The label survives the third section | `packages/bench/src/harness.test.ts:256-257` | The existing `expect(occurrences).toBeGreaterThanOrEqual(3)` on `SAME-MACHINE`, which must keep passing unchanged |
| Two drivers cannot collapse into one tax row | `packages/bench/src/harness.test.ts` | `expect(() => connectivityTax(memory, [point(4, 100), point(4, 900)])).toThrow(/real curve .* 4/)` |
| A faked process count is detectable | `packages/bench/src/integrity.test.ts` | `expect(processIdentityViolations({ …clean, agents: [a, { ...a }] })).toEqual([expect.stringContaining(String(a.childPid))])` — the duplicated-pid plant. Deleting the distinctness check turns it red |
| A fabric that built no executors is not a clean rung | `packages/bench/src/integrity.test.ts` | `expect(processIdentityViolations({ …clean, executorNodeIds: [] })).toEqual([expect.stringContaining('0')])` — the reading that stops an empty set satisfying every membership check vacuously. Deleting the `executorNodeIds.length !== expected` line turns it red |
| A fabric that observed no agents is not a clean rung | `packages/bench/src/integrity.test.ts` | `expect(processIdentityViolations({ …clean, agents: [] })).not.toEqual([])` — with the message naming that the instrument observed no agents |
| A silent in-process fallback is detectable without trusting pids | `packages/bench/src/integrity.test.ts` | `expect(cpuAttributionViolations({ driverCpuMs: 700, shardComputeMs: 740 })).toHaveLength(1)` against the clean control `{ driverCpuMs: 40, shardComputeMs: 740 }` returning `[]` |
| An instrument reading nothing is not a clean reading | `packages/bench/src/integrity.test.ts` | `expect(cpuAttributionViolations({ driverCpuMs: 0, shardComputeMs: 740 })).toEqual([expect.stringContaining('read nothing')])` and its sibling with `shardComputeMs: 0` — the two zeros report separately, and without them a run whose clock was never sampled passes the ratio check |
| A non-uniform fixture cannot pass as a parallelism measurement | `packages/bench/src/integrity.test.ts` | `expect(fixtureUniformityViolations({ statuses: ['budget','budget','budget','budget','budget','budget','budget','found'], expected: 'budget', shards: 8 })).toHaveLength(1)`, and the empty-vector case reporting rather than passing |
| A rung cannot declare a fixture it did not dispatch | `packages/bench/src/integrity.test.ts` | `expect(fixtureProvenanceViolations({ declared: 'saturating', moduleCid: TRIVIAL_CID, expected: { saturating: SATURATING_CID, trivial: TRIVIAL_CID } })).toEqual([expect.stringContaining(TRIVIAL_CID)])` — the wiring slip flaw 10 is about |
| The gate throws on a violation and only on a violation | `packages/bench/src/integrity.test.ts` | `expect(() => assertIntegrity('at', ['v'])).toThrow(HarnessIntegrityError)` together with `expect(() => assertIntegrity('at', [])).not.toThrow()`. Deleting the `throw` in `assertIntegrity` turns the first red — one line, one place, which is the reason the gate is a function |
| `@o2/bench` is still portable | `packages/node/src/purity.node.test.ts` | The portable-package scan over `bench`, which fails on any `node:` specifier in `integrity.ts` |
| The pre-registration prose is publishable | `packages/node/src/vocabulary.node.test.ts` | The per-term `expect(REPO.violations.filter(v => v.term === term).map(render)).toEqual([])` assertions, over a tree that includes `.planning/BENCHMARK-METHODOLOGY.md` |

## Files touched

- `.planning/BENCHMARK-METHODOLOGY.md`
- `packages/bench/src/harness.ts`
- `packages/bench/src/report.ts`
- `packages/bench/src/integrity.ts`
- `packages/bench/src/index.ts`
- `packages/bench/src/harness.test.ts`
- `packages/bench/src/integrity.test.ts`

## Out of scope

- **`sweepNodeCount` stays unwired.** It is a real built-not-wired export (`harness.ts:171`,
  barrel-exported, called only from `harness.test.ts:55`). The choice here is explicit:
  **leave it, do not delete it, do not wire it.** `bin/bench.ts`'s two hand-written loops
  carry a per-rung `try`/`catch` and, from Plan 23-03, per-rung disposal — neither of which
  its current signature has room for, so wiring it would mean changing its shape to suit one
  caller. Deleting it is defensible too, but that is a call for Phase 22's reachability
  work, which owns the built-not-wired burn-down and can see every such export at once.
- **`RUNS_PER_CONFIG` is wired in Plan 23-03**, not here — the call site is in `bin/bench.ts`,
  which this plan does not touch.
- **The user-count axis** (methodology §1.1's second axis, where a super-linear claim could
  live) is not swept by this phase and no amendment entry claims otherwise. It becomes
  buildable for the first time once the process driver exists; naming it as the next
  benchmark phase belongs in the roadmap, not in this plan.
- **BENCH-06's distinct-machine half** is descoped and unmeasured — not met, not transferred
  here, and nothing this plan renders may be read as closing it.

<verification>
`npx tsc --noEmit` exits 0 for the whole repository. `npx vitest run packages/bench` passes,
including every pre-existing assertion in `harness.test.ts`.
`npx vitest run packages/node/src/purity.node.test.ts` passes, so `@o2/bench` acquired no
platform import. `npx vitest run packages/node/src/vocabulary.node.test.ts` passes over the
amended methodology. `git diff .planning/BENCHMARK-METHODOLOGY.md` shows additions below
`## Amendments` and nowhere else.
</verification>

<success_criteria>
The pre-registered plan carries three dated amendment entries, committed in wave 1 with the
run three waves away. `RunConfig` cannot be built without stating its driver and its
fixture, and both are rendered as columns beside every number and named in every makespan
heading. The connectivity tax, the COST crossover and the excluded-configurations table each
name, in their headings, the driver and fixture they are derived from. `Report` can carry a process-per-node curve without the
connectivity tax quietly computing itself against the wrong driver. `@o2/bench` exports a
harness-integrity checker whose four readings have each been watched reporting a planted
defect — including a rung with no executors, a rung with no agents, an unsampled CPU clock
and a rung whose dispatched module is not the fixture it declares — behind one gate function
with one call site per rung, ready for Plan 23-03 to call from `bin/bench.ts`.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-23-multi-process-benchmark-driver/23-01-SUMMARY.md`
</output>
