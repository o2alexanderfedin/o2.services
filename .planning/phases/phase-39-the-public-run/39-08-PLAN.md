---
phase: 39-the-public-run
plan: 08
type: execute
wave: 4
depends_on: ["39-01", "39-02", "39-03", "39-04", "39-05", "39-06", "39-07"]
files_modified:
  - .planning/OWNER-ACTIONS.md
  - .planning/phases/phase-39-the-public-run/39-PRE-INVITE-READING.md
autonomous: false
requirements: [RUN-01, BENCH-06]
plant_target: none — this plan writes documents and pauses for the owner
must_haves:
  truths:
    - "Every act the run needs from the owner is on ONE list, with what to run, what to read back, and what number means stop"
    - "The owner has ruled on what a peer may announce about its machine, and the ruling is recorded before the release is cut"
    - "The published page carries the disclosure the tree carries"
    - "A funnel reading with a timestamp preceding the first invite exists in the tree, with its schema digest"
  artifacts:
    - path: ".planning/OWNER-ACTIONS.md"
      provides: "The extended list — never a second file"
      contains: "Phase 39"
    - path: ".planning/phases/phase-39-the-public-run/39-PRE-INVITE-READING.md"
      provides: "Criterion 2's timestamped evidence, taken before the first invite"
      contains: "schemaDigest"
  key_links:
    - from: ".planning/OWNER-ACTIONS.md"
      to: ".planning/phases/phase-39-the-public-run/39-RUNBOOK.md"
      via: "the run rows point at the runbook rather than restating it"
      pattern: "39-RUNBOOK"
---

## Phase Goal

**As a** project owner, **I want to** send the first invite only after seven conditions hold on a dated
checklist with named evidence for each, and to send invites in stages by region and by cohort slice with
the funnel read between them, **so that** the fabric's headline claim is finally measured on hundreds of
independently-owned devices rather than asserted.

<objective>
Everything an agent can do without money and without disclosure has landed in plans 39-01 to 39-07. This
plan is the separated tail: the acts that are the owner's, in the order they must happen, each with an exact
script.

The ordering is not a preference. **The published page is behind the tree's disclosure** — measured
2026-09-04 against `https://o2alexanderfedin.github.io/o2.services/assets/index-jXJKXCh1.js`, which carries
version 5's *"What does this page count about my visit"* line and **not** version 6's ground sentence; the
positive control for that grep is version 5's own headline, found once in the same bundle. Cutting the
release re-asks every returning visitor once, which is the version mechanism working. **So a disclosure
decision taken before the cut rides a re-ask that is already owed, and one taken after it costs a second
re-ask of a cohort that is spendable exactly once.** That is why the `BENCH-06` ruling is the first row and
the release cut is the second.

Purpose: put every irreversible act on one list with a stated stop condition, and produce criterion 2's
timestamped pre-invite evidence.
Output: extended `.planning/OWNER-ACTIONS.md`, and `39-PRE-INVITE-READING.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/OWNER-ACTIONS.md
@.planning/phases/phase-39-the-public-run/39-RUNBOOK.md
@.planning/phases/phase-39-the-public-run/39-GO-NO-GO.md
@.planning/phases/phase-37-the-six-stage-funnel-and-a-frozen-telemetry-schema/37-RUNBOOK.md

**SCOPE FENCE — this plan is where it bites hardest.** No agent sends an invitation, posts to Telegram,
publishes a figure, creates a Cloudflare resource, deploys, or cuts a release. This plan writes documents and
then **stops**. Every checkpoint below is a pause, not an instruction to an agent.

**Do not rewrite `.planning/STATE.md`.** Its YAML frontmatter is hand-written and GSD tooling has wiped it
twice, on 2026-08-25 and 2026-09-03. No task in this phase touches it.

**Extend `.planning/OWNER-ACTIONS.md`; never create a second one.** It already holds seven rows.
</context>

<interfaces>
<!-- The existing seven rows, and where the new ones attach. -->

`.planning/OWNER-ACTIONS.md` rows today:
1. the spending alert (**permanently unsatisfiable** — the ordering `HOST-10` names was lost and no later
   alert makes it true; the row is kept, not quietly dropped)
2. three Durable Objects, Phase 33, ≈$15/month — blocks the regional arms of RUN-07
3. a Cloudflare TURN key, Phase 34 — blocks Phase 34 criterion 1, which says both fallback rungs are
   verified *"before Phase 39 invites anyone"*
4. the telemetry's legal basis — **settled 2026-09-04, consent (reading A)**; the row is history now
5. Telegram on two real devices, Phase 38 — **half done**; `38-DEVICE-OBSERVATIONS.md` records the three
   devices, and what is missing is backgrounding while the phone stays **awake**
6. the public run — send the first invite. **This row exists; amend it, do not duplicate it.**
7. the cross-host lift, Phase 41

Each row's format: a two-column table with **Act**, **Cost**, **Why not an agent**, **Unblocks** (and
sometimes **Waits on**), then prose, and a closing **What to say back:** line naming exactly what the owner
reports.

The disclosure sentence at stake for row 8, from `packages/browser/src/disclosure.ts:241`:
*"No files, no browsing history, no identifiers beyond the key named below."*
And `packages/browser/src/browser-id.ts` refuses `platform`, the full user-agent string and
`hardwareConcurrency` by name, for that promise's sake.

The pre-invite reading's expected shape, read live 2026-09-04 from
`https://o2-bootstrap.af-4a0.workers.dev/funnel`:
```
{"entered":{"page-load":0,"consent":0,"wss-bootstrap":0,"ice-gathering":0,
"connection-classified":0,"first-task":0},"stalledAt":{...all zero...},"byStageHour":{},
"webrtcAttempts":{},"webrtcOutcomes":{},"population":"opted-in-only",
"schemaDigest":"3911527f1a04abee"}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend the owner list with the run's rows, in the order they must happen</name>
  <files>.planning/OWNER-ACTIONS.md</files>
  <read_first>
    - `.planning/OWNER-ACTIONS.md` in full — the row format, and row 6 which is amended rather than duplicated
    - `.planning/phases/phase-39-the-public-run/39-RUNBOOK.md` — the staged procedure the run rows point at
    - `.planning/phases/phase-39-the-public-run/39-KILL-SWITCH-DURING-RUN.md` — the mid-run script
    - `.planning/phases/phase-39-the-public-run/39-PARTICIPANT-COUNT.md` — the three routes row 8 rules on
    - `.planning/phases/phase-38-reaching-the-cohort/38-DEVICE-OBSERVATIONS.md` — what row 5 still owes
  </read_first>
  <action>
    Append to `.planning/OWNER-ACTIONS.md`, in this order, each in the file's existing row format with a
    closing **What to say back:** line. Do not renumber the existing seven.

    **Row 8 — what a peer may announce about its machine (`BENCH-06`).** Act: choose one of the three routes
    in `39-PARTICIPANT-COUNT.md` § 3. Cost: route (a) costs a disclosure version bump and a re-ask; routes
    (b) and (c) cost nothing and leave the distinct-machine half unmeasured. Why not an agent: it changes
    what the page promises a visitor, and `disclosure.ts:241`'s promise is the thing being widened. Unblocks:
    criterion 4's distinct-machine half — partially; the rest waits on the run. **Say in the row that this
    must be decided BEFORE row 9**, because the release cut re-asks everyone once regardless and a decision
    taken after it costs a second re-ask.

    **Row 9 — cut the release.** Act: `scripts/deploy-hosted.sh`, then `scripts/deploy-pages.sh --live`.
    Cost: this is the disclosure gate — public hosting is public disclosure, and EPO and China have no
    patent grace period. Why not an agent: `DEMO-04`'s ruling makes deployment a separately-triggered gate.
    Read back: `GET /self` answers the new `version`, and the published bundle contains version 6's ground
    sentence. **Give the exact check**, with its positive control, because an absence needs one:
    `curl -s <pages-url> | grep -o 'assets/[a-zA-Z0-9._-]*\.js'` to find the bundle, then on that bundle
    `grep -c "your permission, and nothing else"` (expect `1`) **and**
    `grep -c "This page can use your processor"` (expect `1`, the control that proves the grep can see the
    bundle at all). Today those read `0` and `1`.

    **Row 10 — the Telegram remainder (`RUN-06`).** Amend nothing in row 5; add this as its completion.
    Act, per device (one iOS, one Android): open the real link from a real Telegram message; let the node
    start; **switch away from Telegram while the phone stays awake and screen-on**, wait one minute, come
    back. Record four answers: (1) is the node still connected; (2) if not, does it reconnect without a
    reload; (3) did the tab's own indicator survive; (4) did anything the page stored survive — reload once
    and see whether consent is remembered. Cost: none, not a disclosure event, the link is already public.
    Why not an agent: criterion 1 rejects a green from a spoofed user-agent by name — the check is the engine.
    **What means no-go:** a device on which the node neither survives nor reconnects, because that is a
    cohort that participates once.

    **Row 11 — the pre-invite funnel reading.** Act: `curl https://<origin>/funnel` and record the whole
    response with a UTC timestamp into `39-PRE-INVITE-READING.md`. Why not an agent: it must be taken after
    row 9 and before row 12, and only the owner controls that ordering. **This is criterion 2's evidence**,
    and `37-RUNBOOK.md` step 2 is what it discharges.

    **Row 12 — amend row 6, the first invite.** Point it at `39-RUNBOOK.md` for the stages and the stop rule,
    and at `39-GO-NO-GO.md` for the gate. Add: no invite until every one of the seven reads `GO` **and** the
    preconditions section's rows are either `GO` or consciously accepted with the acceptance recorded in the
    row. Add the between-stage readings as the row's repeating step, naming the four readings from
    `39-RUNBOOK.md` § *what is read between stages*.

    **Row 13 — the mid-run kill-switch exercise (criterion 5).** Act: the six steps in
    `39-KILL-SWITCH-DURING-RUN.md` § 3, at a stage boundary. Cost: a deliberate, brief, cohort-global halt —
    production has one region today. Why not an agent: it is an operator-key write to production. **Say that
    the un-halt is part of the act**, and give the stop condition: no observed transition inside three poll
    intervals, or the status page and `/self` disagreeing, or a keyless write succeeding.

    Then add a short section at the top of the new rows stating the **order** and what each row blocks, so
    the list reads as a sequence rather than a menu.
  </action>
  <acceptance_criteria>
    - `.planning/OWNER-ACTIONS.md` gained six rows numbered 8-13 and row 6 was amended in place, not
      duplicated. Verified by `grep -c '^## ' .planning/OWNER-ACTIONS.md` returning 13 or more, and by
      `grep -c '^## 6\.' ` returning exactly 1.
    - Every new row has an **Act**, a **Cost**, a **Why not an agent**, and a closing
      **What to say back:** line — verified by grep per row.
    - Row 9 carries both grep commands including the positive control, and states the values they read today
      (`0` and `1`).
    - No second owner-actions file was created: `git ls-files | grep -ci 'owner-action'` returns exactly 1.
    - `git show --stat` on the commit lists exactly `.planning/OWNER-ACTIONS.md`.
  </acceptance_criteria>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: What may a peer announce about the machine it runs on?</name>
  <!--
    ANSWERED IN SUBSTANCE 2026-09-04, BEFORE EXECUTION, BY OWNER INSTRUCTION — kept as a checkpoint
    because the owner has not seen the corrected option text, and the correction is what makes the
    chosen route affordable rather than merely cheap.

    The instruction, verbatim: *"постарайся избежать лишних обменов данных по сети, потому что сеть —
    самое медленное"* — avoid unnecessary network exchanges, because the network is the slowest thing.
    Of the three routes only `peers-only` honours it: it adds no field and no message. `announce-coarse`
    adds a field to the job path; `defer` postpones without avoiding.

    So the executor's job here is NOT to re-ask which route, but to present the CORRECTED cost of
    `peers-only` — which is materially better than this plan first stated — and confirm the reading. If
    the owner wants a true machine count anyway, that is `announce-coarse` and it needs a new plan.
  -->
  <files>none — this task pauses for the owner and writes nothing</files>
  <action>Present the decision block below to the owner and STOP. No agent chooses. On resume, record
  the ruling and its date in `.planning/phases/phase-39-the-public-run/39-PARTICIPANT-COUNT.md` § 3,
  verbatim, beside the three routes it chose between — and change nothing else.</action>
  <decision>
    Whether a participating tab announces anything machine-scoped on the job path, so that `BENCH-06`'s
    distinct-machine half becomes measurable from the run.
  </decision>
  <context>
    `ReduceOutcome.executedBy` is peer ids. Nothing machine-scoped is announced anywhere on the browser
    job path today.

    **CORRECTED 2026-09-04 — this block said "Two tabs on one laptop are two peer ids", and that is FALSE
    on the browser tier.** `packages/node/src/cold-start-seed-race.e2e.test.ts` is titled, in its own
    docblock, *"N tabs opening one cold origin at once hold ONE identity"*, and
    `packages/node/src/gated-seed.e2e.test.ts:60` states *"same origin, same IndexedDB, therefore **the
    same peer id**"*. A browser peer id is per-ORIGIN, not per-tab, so ten tabs on one laptop are one peer
    id. The confusion `BENCH-06`'s wording guards against — sixteen nodes on one laptop reported as sixteen
    machines — is a **node-tier** hazard (N processes, N `--dir`s, N seed files), and the cohort is on the
    browser tier. **This correction materially changes what `peers-only` costs**, and the option below is
    rewritten with it.

    Adding one widens `DISCLOSURE`'s promise at `packages/browser/src/disclosure.ts:241` — *"no identifiers
    beyond the key named below"* — which means `DISCLOSURE_VERSION` `'6'` → `'7'` and a re-ask of every
    returning visitor.

    **The published page is already behind the tree**, measured 2026-09-04: it carries version 5's count line
    and not version 6's ground sentence. So the release cut re-asks everyone **once regardless**. A decision
    taken now rides a re-ask that is already owed; a decision taken after the cut costs a second one, from a
    cohort of a few hundred that is spendable exactly once.

    Full working, including what each route costs:
    `.planning/phases/phase-39-the-public-run/39-PARTICIPANT-COUNT.md` § 3.
  </context>
  <options>
    <option id="announce-coarse">
      <name>A per-visit coarse machine descriptor on the job path</name>
      <pros>Makes a distinct-machine count possible from the run. Two tabs on one device merge, which is the
      direction `BENCH-06` requires. Rides the re-ask already owed if decided before the release cut.</pros>
      <cons>Widens the disclosure promise; needs `DISCLOSURE_VERSION` `'7'`, a mirrored `demo/policy.html`,
      and new guard cases. A coarse descriptor merges two identical devices, so the count is a **lower
      bound** and must be published as one.</cons>
    </option>
    <option id="peers-only">
      <name>Announce nothing; publish distinct browser profiles and distinct countries, as bounds</name>
      <pros>No disclosure change, no re-ask, no new identifier, **and no extra byte on the wire** — the
      peer id is already announced in the handshake and already disclosed, and the two-letter country is
      already collected by the funnel. **Corrected 2026-09-04**: because a browser peer id is per-origin,
      distinct peer ids already count distinct browser PROFILES rather than tabs, so the count is far
      closer to a machine count than this block previously said. Publishable honestly as a pair of bounds:
      distinct profiles **over**-counts machines (two browsers on one laptop are two profiles), distinct
      countries **under**-counts them, so the machine count is bracketed with each bound's direction
      stated.</pros>
      <cons>Does not satisfy `BENCH-06`'s distinct-machine half **as literally worded** — the row asks for
      a machine read off its own announced line, and a profile is not a machine. The half stays descoped
      and unmeasured unless the row is amended, which is a separate owner act and must not be done by
      widening what counts as passing.</cons>
    </option>
    <option id="defer">
      <name>Defer the decision past this run</name>
      <pros>Costs nothing now.</pros>
      <cons>The cohort is spendable once. A later decision needs a second re-ask and a second run to measure
      against, and there is no second cohort.</cons>
    </option>
  </options>
  <resume-signal>Reply `announce-coarse`, `peers-only`, `defer`, or a sentence to use verbatim. The ruling is
  recorded in `39-PARTICIPANT-COUNT.md` § 3 with its date, and — under `announce-coarse` only — a follow-up
  plan implements it before row 9.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Cut the release, take the pre-invite reading, and record it</name>
  <files>.planning/phases/phase-39-the-public-run/39-PRE-INVITE-READING.md</files>
  <action>Present the block below to the owner and STOP. No agent deploys, publishes or cuts a release.
  On resume, transcribe what was pasted back into
  `.planning/phases/phase-39-the-public-run/39-PRE-INVITE-READING.md` verbatim — the whole funnel
  response, the UTC timestamp, the `schemaDigest`, the `population`, and one sentence stating that this
  reading precedes the first invite. Paraphrase nothing and retype no number from memory.</action>
  <what-built>
    Plans 39-01 to 39-07 landed the gate document and its guard, the funnel's probe-before-targeting fix, the
    counter reading, the participant-count path and its publication guard, the staged runbook and its budget
    arithmetic, the kill-switch observation instrument, and the node lane's re-derived file count. Nothing
    below can be automated: each act is a deployment, a public act, or a reading only the owner's account can
    take.
  </what-built>
  <how-to-verify>
    1. **Cut the hosted release.** `scripts/deploy-hosted.sh`. Read back `GET /self` and record the
       `version` and the date. Phase 29 criterion 5 — no preview deployments, ever — is unchanged and
       enforced by `wrangler.jsonc`. This discharges `37-RUNBOOK.md` step 1.
    2. **Publish the page.** `scripts/deploy-pages.sh` first (dry run, publishes nothing), then
       `scripts/deploy-pages.sh --live`.
    3. **Verify the disclosure went with it, with its positive control.** Find the bundle:
       `curl -s https://o2alexanderfedin.github.io/o2.services/ | grep -o 'assets/[a-zA-Z0-9._-]*\.js'`.
       On that bundle: `grep -c "your permission, and nothing else"` must be **1** — it is `0` today — and
       `grep -c "This page can use your processor"` must be **1**, which is the control proving the grep can
       see the bundle. Read `EXIT=$?` on the line immediately after each command, never after a pipe.
    4. **One smoke visit, your own.** Open the published link, consent, and let the node start. Then
       `curl https://<origin>/funnel` and confirm `entered['page-load']` and `entered['consent']` moved from
       0 to 1. **Record that this visit is one row of opted-in data** — it is, and the population is
       `opted-in-only`. This discharges `37-RUNBOOK.md` step 3.
    5. **Take the pre-invite reading.** `curl https://<origin>/funnel` and paste the **whole** response, with
       the UTC timestamp you took it at. This is criterion 2's evidence and `37-RUNBOOK.md` step 2.
    **What means stop:** the version on `/self` is not the one you deployed; or the published bundle's
    version-6 grep reads `0` after step 2; or the control grep reads `0`, which means the check itself is
    blind and nothing has been verified either way.
  </how-to-verify>
  <resume-signal>
    Paste back: the deployed `version` and its date; the two grep counts from step 3; the funnel counts from
    step 4; and the full pre-invite JSON with its UTC timestamp from step 5. On resume, the executor writes
    `.planning/phases/phase-39-the-public-run/39-PRE-INVITE-READING.md` — the whole response verbatim, the
    UTC timestamp, the `schemaDigest`, the `population`, and one sentence stating that this reading precedes
    the first invite and is what makes criterion 2 checkable rather than asserted. Nothing is paraphrased and
    no number is retyped from memory.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tree → public internet | The release cut is the disclosure event; it is irreversible |
| owner's account → production | Deploy credentials and the operator key |
| pasted reading → committed evidence | A number crosses from a terminal into criterion 2's evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-38 | Repudiation | the pre-invite reading | mitigate | Recorded verbatim with a UTC timestamp and the schema digest, before the first invite, in the tree — not reconstructed afterwards from stored events, which criterion 2 forbids by name |
| T-39-39 | Tampering | the published bundle | mitigate | The version-6 grep is paired with a positive-control grep, so a blind check is distinguishable from a failed deploy |
| T-39-40 | Information disclosure | the release cut | transfer | Public hosting is public disclosure and forfeits EPO and China patent rights permanently; the act is the owner's by ruling and the cost is stated in the row |
| T-39-41 | Elevation of privilege | deploy credentials | mitigate | No credential enters the repository; the scripts read the owner's ambient environment and the plan names variables, never values |
| T-39-42 | Denial of service | the smoke visit's own cost | accept | One visit, a handful of requests, negligible against the 10 000 000 included |
| T-39-43 | Spoofing | a second owner-actions file | mitigate | Asserted: exactly one file matching `owner-action` is tracked |
</threat_model>

<verification>
Task 1's document checks run as greps with their counts recorded. Tasks 2 and 3 are blocking checkpoints and
produce no automated verification of their own; what they produce is pasted back and transcribed verbatim
into the tree.
</verification>

<success_criteria>
`.planning/OWNER-ACTIONS.md` carries six new rows in execution order with an exact script and a stop
condition each, and no second such file exists. The owner has ruled on what a peer may announce. The
published page carries the disclosure the tree carries, verified with a positive control. A funnel reading
with a UTC timestamp preceding the first invite exists in the tree with its schema digest.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-39-the-public-run/39-08-SUMMARY.md`.
</output>
