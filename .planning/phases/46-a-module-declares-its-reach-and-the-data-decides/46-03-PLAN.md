---
phase: 46-a-module-declares-its-reach-and-the-data-decides
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - packages/core/src/executor/network-reach-guard.ts
  - packages/core/src/executor/network-reach-guard.test.ts
  - vitest.config.ts
autonomous: true
requirements: [CAP-01]

must_haves:
  truths:
    - "A task whose module declares network reach, dispatched against sovereign data, is refused before inner.execute is ever called"
    - "The same declaring module against public data reaches inner.execute unchanged — the positive control"
    - "A module that declares nothing reaches inner.execute unchanged on both labels — public and sovereign alike"
    - "The refusal names the module and the label, and its text is distinguishable from guardSovereignty's and guardModuleProvenance's own refusal text"
  artifacts:
    - path: "packages/core/src/executor/network-reach-guard.ts"
      provides: "guardNetworkReach, NetworkReachRefusal, describeNetworkReachRefusal — the refusal-union pattern copied from module-provenance.ts"
    - path: "packages/core/src/executor/network-reach-guard.test.ts"
      provides: "the watched()-counter proof, copied from sovereignty-guard.test.ts, for all four cases above"
  key_links:
    - from: "packages/core/src/executor/network-reach-guard.ts guardNetworkReach"
      to: "packages/core/src/ports.ts Task.label, Task.moduleRecord.wantsNetworkReach"
      via: "direct field read, unverified — safe only because this guard refuses and never grants"
      pattern: "task.label === 'sovereign' && task.moduleRecord?.wantsNetworkReach === true"
---

<objective>
CAP-01, half 3: the guard itself. `guardNetworkReach` wraps an `Executor` so a task whose
module declares network reach is refused whole — before `inner.execute` runs — when the task
is labelled sovereign, and is a no-op in every other case: public label, absent label, or a
module that declares nothing at all.

Purpose: this is the enforcement point 46-CONTEXT.md's roadmap goal names — "a module that
wants the internet says so in its own signature, and a node refuses the whole task ... rather
than run it half-privileged." It composes as a third adapter between `guardSovereignty`
(outermost) and `guardModuleProvenance` (innermost) — Plan 05 wires it into both node
factories; this plan proves the guard's own logic in isolation, the way `sovereignty-guard.ts`
and `module-provenance.ts` are each proven before anything composes them.
Output: a pure `@o2/core` module and its test file, both new; `vitest.config.ts`'s four
counts updated for the one new node-project spec file this plan adds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/46-a-module-declares-its-reach-and-the-data-decides/46-CONTEXT.md
@.planning/phases/46-a-module-declares-its-reach-and-the-data-decides/46-01-SUMMARY.md
</context>

<interfaces>
From packages/core/src/executor/module-provenance.ts — the refusal-union shape to copy
exactly (discriminated union, no `default` arm in the `describe*` switch, a `describe*`
function that renders each variant to a string, the wrapper's refuse() helper building the
`ExecutionOutcome` from it):
```
export type ModuleRefusal = { readonly kind: 'no-record'; ... } | ...
export function describeModuleRefusal(refusal: ModuleRefusal): string { switch (refusal.kind) { ... } }
export function guardModuleProvenance(inner: Executor, provenance: ModuleProvenance): Executor
```

From packages/core/src/executor/sovereignty-guard.ts — the composition-order docblock
argument to restate for this guard's own subject, and the ordering claim:
```
export function guardSovereignty(inner: Executor, node: NodeSovereignty): Executor
```
"`inner.execute` for a real `WasmExecutor` is what reaches `WebAssembly.instantiate`, and this
function never reaches that line when it refuses" — the identical claim this plan's guard
must make and prove.

From packages/core/src/executor/sovereignty-guard.test.ts — the `watched()` helper to copy
verbatim (an `Executor` that increments a `calls` counter and returns a canned success), and
the `baseTask`/case structure built around it.

From packages/core/src/ports.ts — `Task.label?: Sovereignty`, `Task.moduleRecord?: NameRecord`
(both already exist, unchanged by this plan), `NameRecord.wantsNetworkReach?: true` (added by
Plan 01, already landed).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: guardNetworkReach — the refusal, and why reading an unverified record here is safe</name>
  <files>packages/core/src/executor/network-reach-guard.ts</files>
  <read_first>
    packages/core/src/executor/module-provenance.ts (whole file — refusal-union shape, the
    `refuse()` closure pattern, the docblock density this project expects of an executor guard)
    packages/core/src/executor/sovereignty-guard.ts (whole file — the ordering claim's exact
    wording, and `guardSovereignty`'s "no exempt case" vs "label qualifier" framing)
    packages/core/src/ports.ts (Task, ExecutionOutcome, Executor)
    .planning/phases/46-a-module-declares-its-reach-and-the-data-decides/46-CONTEXT.md §3 (the
    reasoning that must ship in this file's own docblock, not only in the plan)
  </read_first>
  <behavior>
    - `guardNetworkReach(inner)` returns an `Executor` whose `execute` refuses (does not call
      `inner.execute`) exactly when `task.label === 'sovereign'` AND
      `task.moduleRecord?.wantsNetworkReach === true`.
    - Every other combination — public label with a declaring module, sovereign label with a
      non-declaring or absent module record, absent label — calls `inner.execute` unchanged
      and returns its outcome unmodified.
    - The refusal's `reason` string names `inner.nodeId`, the module's CID, and the word
      "sovereign"; it does not equal and does not contain `guardSovereignty`'s
      `'sovereignty violation'` prefix or `guardModuleProvenance`'s `'module provenance
      refused'` prefix.
  </behavior>
  <action>
    Create `packages/core/src/executor/network-reach-guard.ts`. Import only `type {
    ExecutionOutcome, Executor, Task } from '../ports.ts'` — a pure module, no platform
    imports, matching `sovereignty-guard.ts`'s own closing line.

    Write a docblock at the top of the file, matching the density of `sovereignty-guard.ts`'s
    own, that states: (a) this is CAP-01's serving-side gate; (b) it composes as a third
    adapter between `guardSovereignty` (outermost) and `guardModuleProvenance` (innermost) at
    both `fabric-node.ts:2929` and `browser-node.ts:2532` — `guardSovereignty(guardNetworkReach
    (provenance(abi)), sovereignty)`, where `provenance` is each factory's own closure over
    `guardModuleProvenance` (or the identity function on a `runs-unsigned-artifacts` node) —
    and state WHY it sits inside `guardSovereignty`: a refusal about the (module, data) pair is
    only meaningful on the node
    that is the right node for that data, which `guardSovereignty` has already established by
    the time this guard runs; (c) WHY it is a separate adapter and not a branch inside
    `guardModuleProvenance` after `accept()` succeeds: it must still apply when `provenance` is
    the identity wrapper a `runs-unsigned-artifacts` node substitutes for
    `guardModuleProvenance` — a branch living inside that guard would vanish along with it on
    exactly the node shape Plan 05's composition tests exercise; (d) the safety argument for
    reading `task.moduleRecord.wantsNetworkReach` before it is cryptographically verified,
    stated close to verbatim from 46-CONTEXT.md §3: reading an unverified declaration is safe
    only because this phase refuses and never grants — a forged declaration refuses its own
    forger and lying in it wins nothing — and the moment a grant decision exists that reads
    this field, this ordering argument stops holding, and a grant must never be made at a
    point that reads an unverified record.

    Define `export type NetworkReachRefusal = { readonly kind: 'declared-against-sovereign';
    readonly moduleCid: string; readonly label: 'sovereign' }` — a discriminated union with one
    variant today, shaped like `ModuleRefusal` so a second variant is a natural extension
    later.

    Define `export function describeNetworkReachRefusal(refusal: NetworkReachRefusal): string`
    with an exhaustive `switch (refusal.kind)` and no `default` arm, mirroring
    `describeModuleRefusal`'s shape. The `'declared-against-sovereign'` case returns a string
    containing the module CID, the word "declares network reach", the field name
    `wantsNetworkReach`, and the word "sovereign" — e.g. built as a template literal naming all
    four.

    Define `export function guardNetworkReach(inner: Executor): Executor` returning an object
    with `nodeId: inner.nodeId` and an `async execute(task: Task): Promise<ExecutionOutcome>`
    whose ENTIRE body is: one `if` whose condition is written EXACTLY as the literal source
    text `if (task.label === 'sovereign' && task.moduleRecord?.wantsNetworkReach === true) {`
    (this exact text is load-bearing — Plan 04's mutation-ledger entry targets it verbatim),
    inside which it builds a `NetworkReachRefusal` and returns `{ ok: false, reason:
    \`network reach refused on ${inner.nodeId}: ${describeNetworkReachRefusal(refusal)}\` }`;
    after the `if` block (unconditionally reached when the condition is false), `return
    inner.execute(task)`. No other branch, no `NodeSovereignty`-style config parameter — the
    rule reads only fields the dispatcher supplied on `task` itself.
  </action>
  <acceptance_criteria>
    - `grep -n "if (task.label === 'sovereign' && task.moduleRecord?.wantsNetworkReach === true) {" packages/core/src/executor/network-reach-guard.ts` matches exactly once
    - `grep -c "safe only because this phase refuses and never grants" packages/core/src/executor/network-reach-guard.ts` is exactly 1
    - `grep -c "must never be made at a point that reads an unverified record" packages/core/src/executor/network-reach-guard.ts` is exactly 1
    - `grep -c "runs-unsigned-artifacts" packages/core/src/executor/network-reach-guard.ts` is at least 1 (the "why a separate adapter" reasoning names this node shape)
    - none of the five `packages/node/src/banned-vocabulary.ts` patterns match this file: `npx vitest run --project node packages/node/src/vocabulary.node.test.ts` exits 0
    - `npx tsc --noEmit -p .` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit -p .; echo "EXIT=$?"</automated>
  </verify>
  <done>network-reach-guard.ts exists, exports guardNetworkReach/NetworkReachRefusal/describeNetworkReachRefusal, carries the required reasoning in its docblock, and typechecks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: network-reach-guard.test.ts — the watched()-counter proof, and vitest.config.ts's count</name>
  <files>
    packages/core/src/executor/network-reach-guard.test.ts
    vitest.config.ts
  </files>
  <read_first>
    packages/core/src/executor/sovereignty-guard.test.ts (whole file — copy `watched()` and
    `baseTask` verbatim in shape; copy the case structure: `count()` asserted before `outcome`)
    packages/core/src/executor/network-reach-guard.ts (this plan's Task 1 output)
    vitest.config.ts (lines ~860-880 for the `files`/`tests`/`unitFiles`/`unitTests` block, and
    at least one full dated derivation note above it — e.g. the "232 -> 234" or "257 -> 263"
    entries — as the format to copy for the new note this task adds)
  </read_first>
  <behavior>
    - Case A: sovereign task, module declares network reach → `count() === 0`, `outcome.ok ===
      false`, reason contains the guard's `nodeId`, the module CID, "sovereign", and "network
      reach"; reason does NOT contain `'sovereignty violation'` or `'module provenance
      refused'`.
    - Case B (criterion 3, the positive control): public task, same declaring module →
      `count() === 1`, outcome is the canned success, unchanged.
    - Case C (criterion 4, fixture 1): sovereign task, module record present but carries no
      `wantsNetworkReach` → `count() === 1`, unchanged.
    - Case D (criterion 4, fixture 2): sovereign task, `moduleRecord` entirely absent →
      `count() === 1`, unchanged (this is the `guardModuleProvenance`-shaped refusal's
      territory, not this guard's — confirm this guard does not manufacture a `no-record`
      refusal of its own).
    - Case E: public task, `moduleRecord` absent → `count() === 1`, unchanged (both axes at
      their default).
  </behavior>
  <action>
    Create `packages/core/src/executor/network-reach-guard.test.ts`. Copy
    `sovereignty-guard.test.ts`'s `watched()` helper and `baseTask` fixture verbatim in shape
    (same `Executor`-with-`calls`-counter pattern, same canned `{ ok: true, output: null,
    fuelUsed: 1, attestation: 'signed-by-nobody' }` success). Build a minimal `NameRecord`-
    shaped fixture object satisfying the type (arbitrary but type-valid `name`, `cid` — reuse
    the same `CID.parse('bafyrei...')` constant `sovereignty-guard.test.ts` uses — `version`,
    `expiresAt`, `signer`, `signature`; this guard never verifies the record, so the
    signature/signer values do not need to be cryptographically real) for the declaring and
    non-declaring variants (the second omits `wantsNetworkReach` entirely, not set to
    `undefined`).

    Write a `describe('guardNetworkReach — CAP-01 refusal before instantiation', ...)` block
    with the five `it` cases from `<behavior>` above, each following
    `sovereignty-guard.test.ts`'s own pattern: build the guarded executor, dispatch a `Task`,
    assert `count()` FIRST (the ordering claim), then assert on `outcome`.

    Then update ONLY the `files`/`unitFiles` half of `vitest.config.ts`'s four counts — NOT
    `tests`/`unitTests`, deferred below. This is a new bare `packages/core/src/executor/
    network-reach-guard.test.ts` file with no `.node.`/`.browser.`/`.e2e.` suffix, so — per
    the `node` project's `include: ['packages/*/src/**/*.test.ts']` with no matching
    `exclude` entry — it runs in BOTH the `node` project and the `browser` project, exactly
    the `kill-switch.test.ts` precedent recorded in the "231 -> 232" derivation note: it counts
    once in `files`/`unitFiles`. Increment `files` and `unitFiles` (currently 270/187) by
    exactly 1 each. Verify the increment with `npx vitest list --project node --filesOnly |
    wc -l` — collection only, no test execution — and confirm the printed count equals the new
    `files` value; this is safe to run while Plan 02's agent has `protocol.ts` mid-edit in the
    same wave, because it never executes any spec file's body, only lists the population.
    **Do not run `npx vitest run --project node` (the full lane) in this task** — Wave 2 runs
    this plan alongside Plan 02 in the same shared working tree, and a full-lane execution
    would both read Plan 02's uncommitted file and risk the "two specs snapshot `git status
    --porcelain` around themselves" hazard CLAUDE.md's Conventions section names.

    Leave `tests`/`unitTests` at their current values (3907/3138) and append a dated note
    directly above the `files: 270,`/`tests: 3907,` block, in the exact format the prior
    entries use: a bold `**270 -> 271 on <today's date> (Phase 46, CAP-01)**` heading, naming
    `packages/core/src/executor/network-reach-guard.test.ts` as the arriving file, stating it
    runs in both `node` and `browser` lanes as a bare `*.test.ts`, stating the derivation route
    used (`vitest list --project node --filesOnly`, collection only) and its count, and closing
    with a sentence on the precedent this file already carries for leaving `tests`/`unitTests`
    for a later pass: the "236 -> 242" and "242 -> 248" entries both left those two fields "at
    their run's figures on the rule that inventing a test total nobody counted is the defect
    this table exists to prevent" — state plainly that `tests`/`unitTests` are deferred to
    46-06-PLAN.md's full-lane sweep, which is the first point in this phase where a full
    `node`-project run against the complete, merged tree is safe to take.
  </action>
  <acceptance_criteria>
    - `npx vitest run --project node packages/core/src/executor/network-reach-guard.test.ts` exits 0, all five case titles from `<behavior>` present in output
    - `npx vitest list --project node --filesOnly | wc -l` equals the new `files` value (271)
    - `O2_UNIT_ONLY=1 npx vitest list --project node --filesOnly | wc -l` equals the new `unitFiles` value (188) — measured on its own and not inferred from the `unitFiles === files - excludedInNode` identity, because `vitest.config.ts`'s own note says a number satisfying its own check is not a reading. Both forms were confirmed to work on 2026-09-23 against the pre-phase tree: 270 and 187, matching the values recorded there
    - `grep -c "declared-against-sovereign\|network reach" packages/core/src/executor/network-reach-guard.test.ts` is at least 5
    - `vitest.config.ts`'s `files`/`unitFiles` read 271/188; `tests`/`unitTests` are UNCHANGED at 3907/3138; the new dated note names `network-reach-guard.test.ts` explicitly and states `tests`/`unitTests` are deferred to 46-06
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --project node packages/core/src/executor/network-reach-guard.test.ts; echo "EXIT=$?"</automated>
  </verify>
  <done>All five cases pass; vitest.config.ts's files/unitFiles are updated from a collection-only listing (never a full-lane run, to avoid reading Plan 02's mid-edit file in this shared wave); tests/unitTests are left for 46-06's full-lane sweep, per this file's own established precedent for splitting a derivation across passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Dispatcher-supplied Task → guardNetworkReach | Every field this guard reads (`label`, `moduleRecord.wantsNetworkReach`) arrives from whoever dispatched the task, unverified at this point in the chain |
| guardNetworkReach → inner.execute | The one line this guard must never reach when it has decided to refuse — the ordering claim Task 1 and Task 2 both prove |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-46-06 | Spoofing | `guardNetworkReach` reading an unverified `moduleRecord` | mitigate | the guard only ever refuses on what it reads here — it never grants — so a forged declaration can only cause an extra refusal of the forger's own task, never a bypass; stated in the file's own docblock per Task 1 |
| T-46-07 | Elevation of Privilege | the guard's single conditional branch | mitigate | exactly one `if` with no `else`-grants-anything path; every non-matching case falls through unconditionally to `inner.execute` unchanged, never to a synthesized success |
| T-46-08 | Information Disclosure | the refusal `reason` string | accept | every field it names (`nodeId`, `moduleCid`, `label`) is either the node's own id or something the dispatcher itself supplied — the owner ruling 46-CONTEXT.md §4 records, unchanged from `guardSovereignty`'s own precedent |
</threat_model>

<verification>
`npx tsc --noEmit -p .` exits 0. `npx vitest run --project node packages/core/src/executor/network-reach-guard.test.ts packages/node/src/slow-specs.node.test.ts` exits 0. `git show --stat` after commit lists `packages/core/src/executor/network-reach-guard.ts`, `packages/core/src/executor/network-reach-guard.test.ts`, and `vitest.config.ts` only.
</verification>

<success_criteria>
Roadmap criteria 2, 3, 4 and 6 are proven at the guard-unit level: refusal before
`inner.execute`, the public-shard positive control, the declares-nothing double control, and a
named, distinguishable refusal reason. Plan 05 extends this proof through both production node
factories.
</success_criteria>

<output>
After completion, create `.planning/phases/46-a-module-declares-its-reach-and-the-data-decides/46-03-SUMMARY.md`
</output>
