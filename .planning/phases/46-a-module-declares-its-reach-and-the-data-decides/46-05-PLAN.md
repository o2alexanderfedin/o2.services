---
phase: 46-a-module-declares-its-reach-and-the-data-decides
plan: 05
type: execute
wave: 3
depends_on: ["03"]
files_modified:
  - packages/node/src/fabric-node.ts
  - packages/browser/src/browser-node.ts
  - packages/core/src/index.ts
  - packages/node/src/fabric-node.node.test.ts
  - packages/browser/src/network-reach-composition.browser.test.ts
autonomous: true
requirements: [CAP-01]

must_haves:
  truths:
    - "A real, production-constructed FabricNode refuses a sovereign task whose module declares network reach, over real RPC, before instantiation"
    - "A real, production-constructed BrowserNode refuses the identical shape of task, reached without going through the wire"
    - "The same declaring module runs on both tiers against public data, and a non-declaring module is unaffected on both labels on both tiers"
    - "The two node factories compose guardNetworkReach identically — if one moves, the other moves in the same plan"
  artifacts:
    - path: "packages/node/src/fabric-node.ts"
      provides: "guardSovereignty(guardNetworkReach(provenance(abi)), sovereignty) at the existing composition site"
    - path: "packages/browser/src/browser-node.ts"
      provides: "the byte-identical composed expression at its own site"
    - path: "packages/core/src/index.ts"
      provides: "barrel export of guardNetworkReach, NetworkReachRefusal, describeNetworkReachRefusal"
    - path: "packages/node/src/fabric-node.node.test.ts"
      provides: "a CAP-01 describe block: the refusal proven over real RPC via RemoteExecutor (the requestor's view), the two accepted controls proven through node.executor directly (sidestepping the egress tap)"
    - path: "packages/browser/src/network-reach-composition.browser.test.ts"
      provides: "a CAP-01 describe block against a real BrowserNode, read through node.executor directly — the operator's view"
  key_links:
    - from: "packages/node/src/fabric-node.ts:2929"
      to: "packages/browser/src/browser-node.ts:2532"
      via: "byte-identical composed expression"
      pattern: "guardSovereignty(guardNetworkReach(provenance(abi)), sovereignty)"
    - from: "packages/node/src/mutation-ledger.ts M27/M28"
      to: "packages/node/src/fabric-node.ts / packages/browser/src/browser-node.ts"
      via: "the substring provenance(abi) must still occur exactly once in each file after this plan's edit"
      pattern: "provenance(abi)"
---

<objective>
Wire `guardNetworkReach` into both production node factories, at the one composition site each
already has, and prove the wiring is real — not merely present as text — with a genuine
dispatch on each tier: `fabric-node.ts`'s refusal proven over real RPC (the requestor's
view, using the same `RemoteExecutor`/dial/capability-chain infrastructure the existing
DATA-09 block already proves DET-03 with) with its two accepted controls read through
`node.executor` directly to avoid DATA-06's egress tap, and `browser-node.ts` through a
real, constructed `BrowserNode`'s `.executor` throughout (the operator's view, no relay
needed for a single-tab check).

Purpose: this repository has an on-point precedent (mutation entry `M28`) for why textual
identity between the two factories is not itself proof of behaviour — "a census that counts
call sites cannot tell a composed guard from a decorative one; only a job dispatched through a
real [node] can." This plan does not rely on the two composition lines merely matching; each
tier gets its own real dispatch.
Output: both factories updated at their existing composition site; a new barrel export;
`fabric-node.node.test.ts` gains a CAP-01 block; a new `.browser.test.ts` file proves the
browser tier independently. `packages/node/src/mutation-ledger.ts`'s existing `M27`/`M28`
entries (which plant on the `provenance(abi)` substring inside this same composition line)
are confirmed still well-defined after the edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/46-a-module-declares-its-reach-and-the-data-decides/46-CONTEXT.md
@.planning/phases/46-a-module-declares-its-reach-and-the-data-decides/46-03-SUMMARY.md
</context>

<interfaces>
From packages/core/src/executor/network-reach-guard.ts (this phase's Plan 03, landed):
```
export function guardNetworkReach(inner: Executor): Executor
export type NetworkReachRefusal = { readonly kind: 'declared-against-sovereign'; ... }
export function describeNetworkReachRefusal(refusal: NetworkReachRefusal): string
```
Not yet exported from `@o2/core`'s barrel — this plan's Task 1 is the first thing that adds
that export, immediately before the two factories that consume it.

From packages/core/src/naming.ts (Plan 01, landed): `NameRecord.wantsNetworkReach?: true`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: compose guardNetworkReach into both node factories, and export it</name>
  <files>
    packages/node/src/fabric-node.ts
    packages/browser/src/browser-node.ts
    packages/core/src/index.ts
  </files>
  <read_first>
    packages/core/src/index.ts (lines ~155-162 — the `guardSovereignty`/`guardModuleProvenance`
    export block, the exact pattern to copy)
    packages/node/src/fabric-node.ts (the import list containing `guardModuleProvenance,
    guardSovereignty,` ~lines 113-114; the composition line at line 2929,
    `const counter = new CountingExecutor(guardSovereignty(provenance(abi), sovereignty))`, and
    its surrounding comment block explaining the sovereignty-outside/provenance-inside order)
    packages/browser/src/browser-node.ts (the identical import list ~lines 52-53; the
    composition line at line 2532, byte-identical to fabric-node.ts's)
    packages/node/src/mutation-ledger.ts (entries `M27` — `file:
    'packages/node/src/fabric-node.ts'`, `find: 'provenance(abi)'` — and `M28` — `file:
    'packages/browser/src/browser-node.ts'`, `find: 'provenance(abi)'` — both around lines
    755-800; read their `why` text so the edit below is made with their invariant in view: the
    substring `provenance(abi)` must remain present exactly once in each file)
  </read_first>
  <action>
    In `packages/core/src/index.ts`, add a new export block immediately after the
    `guardModuleProvenance` block (after line 161's `export type { ModuleProvenance,
    ModuleRefusal } from './executor/module-provenance.ts'`), following the same two-line
    shape with a one-line comment above it naming CAP-01:
    `export { describeNetworkReachRefusal, guardNetworkReach } from
    './executor/network-reach-guard.ts'` and `export type { NetworkReachRefusal } from
    './executor/network-reach-guard.ts'`.

    In `packages/node/src/fabric-node.ts`, add `guardNetworkReach,` to the `@o2/core` import
    list, alphabetically placed the way the existing list already is (between
    `guardModuleProvenance,` and `guardSovereignty,` — `guardModuleProvenance` < `guardNetworkReach`
    < `guardSovereignty` in lexical order, matching the list's own ordering discipline). Change
    the composition line (2929) from `const counter = new CountingExecutor(guardSovereignty(
    provenance(abi), sovereignty))` to `const counter = new CountingExecutor(guardSovereignty(
    guardNetworkReach(provenance(abi)), sovereignty))` — wrapping `provenance(abi)`, not
    replacing it, so the substring `provenance(abi)` remains textually present exactly once (M27
    depends on this). Extend the comment immediately above the composition line (the one
    explaining "sovereignty outside provenance, provenance innermost") to name the third layer:
    state that `guardNetworkReach` now sits between the two, reading `task.moduleRecord.
    wantsNetworkReach` before `guardModuleProvenance` has verified anything, and that this is
    safe only because CAP-01 refuses and never grants — cross-reference
    `network-reach-guard.ts`'s own docblock rather than restating its full argument here.

    Make the byte-identical edit in `packages/browser/src/browser-node.ts`: add
    `guardNetworkReach,` to its own `@o2/core` import list at the same alphabetical position
    (~line 52-53), and change its composition line (2532) from `const counter = new
    CountingExecutor(guardSovereignty(provenance(abi), sovereignty))` to the identical `const
    counter = new CountingExecutor(guardSovereignty(guardNetworkReach(provenance(abi)),
    sovereignty))`. Extend its own composition comment the same way `fabric-node.ts`'s was
    extended — same reasoning, own file's voice, matching this codebase's established pattern
    of each tier carrying the argument in its own words rather than a cross-file "see the other
    file".
  </action>
  <acceptance_criteria>
    - `grep -c "guardSovereignty(guardNetworkReach(provenance(abi)), sovereignty)" packages/node/src/fabric-node.ts packages/browser/src/browser-node.ts` reports exactly 1 for each file
    - `grep -c "provenance(abi)" packages/node/src/fabric-node.ts` is exactly 1; the identical check on `packages/browser/src/browser-node.ts` is exactly 1 (M27/M28's `find` string stays well-defined)
    - `npx tsc --noEmit -p packages/core -p packages/node -p packages/browser` exits 0
    - `npx vitest run --project node packages/node/src/mutation-guard.node.test.ts` exits 0 (confirms M27/M28, among every other ledger entry, still describe real source)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit -p packages/node -p packages/browser; echo "EXIT=$?"</automated>
  </verify>
  <done>Both factories compose guardNetworkReach identically at their existing site; the barrel exports it; M27/M28 remain well-defined; typecheck passes on all three packages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fabric-node.node.test.ts — the requestor's view for the refusal, node.executor for the controls</name>
  <files>packages/node/src/fabric-node.node.test.ts</files>
  <read_first>
    packages/node/src/fabric-node.node.test.ts (the `DET-03` describe block's `fixture()`
    helper and its first `it` case, ~lines 465-497 — the exact `taskFor`/`recordFor`/
    `node.executor.execute` shape to copy for the two accepted cases; the file's top-level
    `keypair`, `recordFor`, `taskFor`, `publisher` fixtures already in scope; the earlier
    `DATA-09 and AUTH-03's serving half` block, ~lines 286-300, for the `sovereignty:
    { ownerId, ownerKey, canExecuteSovereign: true }` node-construction shape and the
    `RemoteExecutor`/`chainSupplierFor`/dial pattern for the refusal case; that same block's
    case 3b/3c commentary on why a WASI echo module's reply egresses refused over RPC — the
    reason this task does NOT read the two accepted cases over RPC)
    packages/core/src/executor/sovereignty-guard.ts (the ordering the composed chain now
    enforces: sovereignty first, then this guard)
  </read_first>
  <behavior>
    - A `FabricNode` started with a pinned owner key, a valid capability chain, and
      `canExecuteSovereign: true` — cleared past both `authorize` and `guardSovereignty` —
      still refuses, over `RemoteExecutor`, a sovereign task whose module record declares
      `wantsNetworkReach: true`; the reason string is readable at the RPC boundary and names
      "network reach". Nothing executes, so DATA-06's egress tap has no reply body to act on
      and cannot confound this case.
    - The identical declaring module, dispatched as a public task through `node.executor`
      directly, succeeds.
    - The identical sovereign task with a module record that declares nothing, dispatched
      through `node.executor` directly, succeeds.
  </behavior>
  <action>
    **Deliberately mixed dispatch shape, stated here rather than left implicit.** Case 1 (the
    refusal) goes over real RPC via `RemoteExecutor`, because nothing executes when a task is
    refused — there is no reply body, so DATA-06's egress tap (which the DATA-09 block above
    measures refusing a WASI echo module's reply because it literally carries the sovereign
    input back out) cannot apply, and this is exactly where "readable by the requestor" needs
    proving at the real RPC boundary. Cases 2 and 3 (the accepted controls) read through
    `node.executor.execute` directly, the same way the `DET-03` block below reads its own
    `MODULE_WRITES_PARTITION` dispatches — this sidesteps asking whether that module's output
    happens to trip the egress tap on a sovereign-labelled reply, a question this plan does not
    need to answer to prove CAP-01's criteria 3 and 4.

    Add a new `describe('CAP-01 — a real FabricNode refuses a declared module against
    sovereign data', ...)` block, placed after the existing `DATA-09 and AUTH-03's serving
    half` block (after its closing, before the `DET-03` block begins). Start one submitter and
    one node via `startNode`, the node with `sovereignty: { ownerId: 'alice', ownerKey:
    OWNER_KEY, canExecuteSovereign: true }` (the `clearedNode` shape from the block above),
    dial the submitter to it for case 1. Use `node.store.put(MODULE_WRITES_PARTITION)` for
    `moduleCid` and `node.store.put(new Uint8Array([0x80]))` for `inputCid` (the `DET-03`
    fixture's own values — a module that actually runs, since this node's default
    `trustAnchors: 'runs-unsigned-artifacts'` means provenance never blocks it). Build a
    declaring record with `signName(publisher.priv, { name: 'cap01-reaches', cid: moduleCid,
    version: 1, expiresAt: Date.now() + 3_600_000, wantsNetworkReach: true })` and a
    non-declaring one with the same fields minus `wantsNetworkReach`.

    Three `it` cases:
    1. Over `new RemoteExecutor(node.peerId, submitter.rpc, chainSupplierFor(node.peerId))
       .execute(task)`, sovereign task (`label: 'sovereign', ownerId: 'alice'`) with the
       declaring record → `outcome.ok === false`; `outcome.reason` contains `node.peerId`, the
       module CID's string, `'sovereign'`, and `'network reach'`; does NOT contain
       `'sovereignty violation'` (proves the refusal came from the new guard, not the
       clearance check the node was deliberately given).
    2. Over `node.executor.execute(task)` directly, public task (`label: 'public'`) with the
       same declaring record → `outcome.ok === true` (criterion 3, the positive control).
    3. Over `node.executor.execute(task)` directly, sovereign task with the non-declaring
       record → `outcome.ok === true` (criterion 4).
  </action>
  <acceptance_criteria>
    - `npx vitest run --project node packages/node/src/fabric-node.node.test.ts` exits 0
    - the three new case titles are present in the run's output
    - case 1's assertions include a check that `outcome.reason` does NOT contain `'sovereignty violation'`
    - cases 2 and 3 call `node.executor.execute`, not `RemoteExecutor` (confirm by reading the diff — this is a source-shape check, not a runtime one)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --project node packages/node/src/fabric-node.node.test.ts; echo "EXIT=$?"</automated>
  </verify>
  <done>All three cases pass against a production-constructed FabricNode; the refusal is proven at the real RPC boundary and the two accepted controls are proven without the egress-tap confound; no pre-existing case in this file regressed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: a real BrowserNode — the operator's view</name>
  <files>packages/browser/src/network-reach-composition.browser.test.ts</files>
  <read_first>
    packages/browser/src/paused-local-admission.browser.test.ts (the `startTab` helper and its
    required `BrowserNode.start` options — `relayAddrs: []`, `createWorker`, `blockstoreName`,
    `trustAnchors`, `whenSeedIsGone`, `identityProtection`, `startReporting` — and this file's
    own docblock explaining why a real node is started rather than a hand-built executor)
    packages/browser/src/worker-executor.browser.test.ts (imports `PROBE_EMITS_TEN` from
    `./wasm-probes.ts` and `TaskExecutorWorker` from `./task-executor.worker.ts?worker` — the
    module fixture and real-Worker pattern to reuse for an actual execution, which
    `paused-local-admission.browser.test.ts` does not need since it never dispatches a task)
    packages/browser/src/browser-node.ts (`BrowserNode.executor: GovernedExecutor`, `.store`
    exposed publicly at ~lines 1382-1383/1649-1650; `BrowserNodeOptions.sovereignty`)
  </read_first>
  <behavior>
    - A real `BrowserNode`, started with `sovereignty: { ownerId: 'alice',
      canExecuteSovereign: true }` and `trustAnchors: 'runs-unsigned-artifacts'`, refuses
      (through `node.executor.execute`, not over RPC — a single tab, nothing to dial) a
      sovereign task whose module record declares `wantsNetworkReach: true`.
    - The same declaring module succeeds as a public task; a non-declaring module succeeds on
      the sovereign label too.
  </behavior>
  <action>
    Create `packages/browser/src/network-reach-composition.browser.test.ts`, browser-project
    only (`.browser.test.ts` suffix — this file is deliberately excluded from the `node`
    project's glob, so it needs no `vitest.config.ts` count change). Open with a docblock
    stating the subject — CAP-01's browser-tier composition, proven the way `M28`'s own
    reasoning requires: a real dispatch through a real tab, not an argument by analogy from
    the Node tier — and citing `fabric-node.node.test.ts`'s CAP-01 block (this phase's Task 2)
    as the requestor-side counterpart to this file's operator-side one.

    Copy `paused-local-admission.browser.test.ts`'s `startTab`-shaped helper, adding
    `sovereignty: { ownerId: 'alice', canExecuteSovereign: true }` to the options object and
    keeping `trustAnchors: 'runs-unsigned-artifacts'` (not the empty-anchor-set opt-in that
    file uses — this test's subject needs a module that actually runs, and the opt-out is what
    lets `network-reach-guard.ts`'s own docblock claim — "must still apply when provenance is
    the identity wrapper" — be exercised for real rather than only argued). Use
    `TaskExecutorWorker` (from `./task-executor.worker.ts?worker`) as `createWorker`.

    Put `PROBE_EMITS_TEN` (from `./wasm-probes.ts`) into the tab's store as `moduleCid`, and an
    empty/minimal input block as `inputCid`, mirroring `worker-executor.browser.test.ts`'s own
    use of that fixture. Sign a declaring record and a non-declaring one exactly as Task 2's
    fabric-node test does (same `signName` call shape, a local ed25519 keypair — this file
    does not need `guardModuleProvenance` to verify anything, since the tab opts out of
    provenance, so the record's signer/signature values do not need to trace to any pinned
    anchor).

    **No existing browser-project file combines a real `BrowserNode` with a real Worker
    execution — say so in this file's own docblock rather than leaving it to be discovered.**
    `paused-local-admission.browser.test.ts` starts a real node but never executes a task;
    `worker-executor.browser.test.ts` executes `PROBE_EMITS_TEN` for real but against a bare
    `browserWorkerExecutor`, not a node. This file is the first to combine the two halves, each
    independently proven elsewhere. If the combination does not reach `outcome.ok === true`
    within the browser project's own default timeout — a genuinely open question this plan
    does not resolve in advance — cases 2 and 3 fall back to the weaker but still
    discriminating assertion `!outcome.ok || !outcome.reason.includes('network reach')`
    together with an explicit code comment naming the fallback and why (case 2: a public task
    must never be refused for network reach, whatever else it does or does not do; case 3: a
    non-declaring module must never be refused for network reach either). **Do not fall back on
    case 1** — the refusal case must assert `outcome.ok === false` and the full reason-content
    checks unconditionally, or this file degenerates into "refuses everything", the exact
    failure §5 of 46-CONTEXT.md names as the reason criterion 3 exists as a control. If case 1
    alone cannot be made to pass because the module never finishes compiling/instantiating in
    this environment, that is a blocking finding to report in this plan's SUMMARY, not a
    silent scope reduction.

    Three `it` cases, each calling `node.executor.execute(task)` directly:
    1. sovereign + declaring → `outcome.ok === false`; reason contains the tab's own node id,
       the module CID, `'sovereign'`, `'network reach'`. No fallback.
    2. public + declaring → `outcome.ok === true`, or the fallback assertion above if the real
       execution does not complete within the project's default timeout.
    3. sovereign + non-declaring → `outcome.ok === true`, or the identical fallback.

    Register an `afterEach` that stops every started node, matching
    `paused-local-admission.browser.test.ts`'s own teardown pattern (`started.splice(0)`,
    `Promise.all`, swallow a teardown error from an already-failed node).
  </action>
  <acceptance_criteria>
    - `npx vitest run --project browser packages/browser/src/network-reach-composition.browser.test.ts` exits 0
    - the three case titles are present in the run's output
    - case 1's assertions are the full, non-fallback set (`outcome.ok === false` plus the four reason-content checks) — no exceptions
    - cases 2/3 either assert `outcome.ok === true` outright, or use the documented fallback with an inline comment naming it; either way each asserts the outcome is NOT a network-reach refusal
    - `grep -c "\.browser\.test\.ts" vitest.config.ts` context confirms this file's suffix keeps it out of the `node` project's `include` glob — no `vitest.config.ts` edit is made by this task
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --project browser packages/browser/src/network-reach-composition.browser.test.ts; echo "EXIT=$?"</automated>
  </verify>
  <done>A real, production-constructed BrowserNode proves case 1 in full; cases 2 and 3 pass either at full strength or at the documented fallback strength, with the choice stated in the SUMMARY; the file is browser-project-only and vitest.config.ts is untouched by this task.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Real peer over RPC → FabricNode's composed executor | Task 2 proves the refusal at the actual boundary a hostile requestor would cross, not only in-process |
| A tab's own dispatcher call → BrowserNode's composed executor | Task 3 proves the identical property on the tier with no peer-to-peer path in this test, matching this project's "equal functionality across tiers" standard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-46-11 | Tampering | the two composition lines drifting apart over time | mitigate | Task 1 makes the edit byte-identical in both files in the same task; `M27`/`M28`'s pre-existing ledger entries continue to pin the shared substring on both files independently |
| T-46-12 | Elevation of Privilege | a future edit that reorders the three guards | accept, flagged | this plan does not add a structural order-assertion beyond the existing composition comment and the two behavioural test suites; reordering `guardNetworkReach` outside `guardSovereignty` would still refuse correctly (both are refuse-only), so no security property is lost, only the "meaningful only on the right node" argument documented in `network-reach-guard.ts`'s own docblock |
</threat_model>

<verification>
`npx tsc --noEmit -p packages/core -p packages/node -p packages/browser` exits 0.
`npx vitest run --project node packages/node/src/fabric-node.node.test.ts
packages/node/src/mutation-guard.node.test.ts` exits 0.
`npx vitest run --project browser packages/browser/src/network-reach-composition.browser.test.ts`
exits 0. `git show --stat` after commit lists exactly the five files in this plan's
frontmatter.
</verification>

<success_criteria>
Roadmap criteria 2, 3, 4 and 6 are proven a second time, this time through both real,
production-constructed node factories rather than only the guard in isolation — the requestor's
view on the Node tier, the operator's view on the browser tier — closing the gap `M28`'s own
precedent says an isolated unit test and a textual match cannot close on their own.
</success_criteria>

<output>
After completion, create `.planning/phases/46-a-module-declares-its-reach-and-the-data-decides/46-05-SUMMARY.md`
</output>
