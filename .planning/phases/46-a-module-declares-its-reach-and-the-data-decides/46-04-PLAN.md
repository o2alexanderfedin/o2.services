---
phase: 46-a-module-declares-its-reach-and-the-data-decides
plan: 04
type: execute
wave: 3
depends_on: ["02", "03"]
files_modified:
  - packages/node/src/mutation-ledger.ts
autonomous: true
requirements: [CAP-01]

must_haves:
  truths:
    - "A regression that drops wantsNetworkReach from the wire codec is caught, watched red, and the catch is now re-checkable on demand rather than a one-time observation"
    - "A regression that removes the sovereign-check condition from guardNetworkReach is caught, watched red, and the catch is now re-checkable on demand"
  artifacts:
    - path: "packages/node/src/mutation-ledger.ts"
      provides: "two new entries — the codec-drop plant (mirrors :3715's XW4 template) and the sovereign-check-removal plant (criterion 5)"
  key_links:
    - from: "packages/node/src/mutation-ledger.ts NR1 entry"
      to: "packages/net/src/protocol.test.ts"
      via: "caughtBy names the round-trip case Plan 02 added"
      pattern: "NR1"
    - from: "packages/node/src/mutation-ledger.ts NR2 entry"
      to: "packages/core/src/executor/network-reach-guard.test.ts"
      via: "caughtBy names the guard's own refusal case"
      pattern: "NR2"
---

<objective>
CAP-01's criterion 5, plus the codec regression 46-CONTEXT.md §6 requires a template-matched
entry for: two deliberate defects, each planted for real, watched failing, restored by the
exact-inverse edit, and verified restored with `cmp` — then recorded as data in
`mutation-ledger.ts` so a future edit that reopens either hole is caught automatically by
`npm run test:mutations` rather than rediscovered by hand.

Purpose: "a proof that cannot fail is not a proof." Both plants here target a DECISION, not a
write reachable only after one — the codec plant removes the field from the encoder's output
(a decision about what bytes leave the function), and the guard plant disables the sovereign
check's condition (the decision the whole phase exists to make), so neither plant is the
unreachable-with-a-wrong-value pattern this repository's own conventions warn is a blind
instrument.
Output: two new `Mutation` entries in `mutation-ledger.ts`, `NR1` and `NR2`, each carrying a
`signature` that was read off a real failing run, not predicted.

**Both plants target files this plan does not otherwise modify** (`protocol.ts`,
`network-reach-guard.ts`). Every plant in this plan is applied, watched, and restored to a
byte-identical state before the plan's own commit — `git show --stat` after this plan's commit
must show ONLY `mutation-ledger.ts` changed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/46-a-module-declares-its-reach-and-the-data-decides/46-CONTEXT.md
@.planning/phases/46-a-module-declares-its-reach-and-the-data-decides/46-02-SUMMARY.md
@.planning/phases/46-a-module-declares-its-reach-and-the-data-decides/46-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: NR1 — the wire codec's wantsNetworkReach drop, mirroring XW4</name>
  <files>packages/node/src/mutation-ledger.ts</files>
  <read_first>
    packages/node/src/mutation-ledger.ts (the `Mutation` interface at the top for field
    meanings; the `XW4` entry, ~lines 3708-3722, as the exact template — same file, same
    spread-omit idiom, same "plants a dropped field, caught by the round-trip spec" shape)
    packages/net/src/protocol.ts (this phase's Plan 02 output — the exact current text of
    `nameRecordToValue`'s `wantsNetworkReach` spread line)
    packages/net/src/protocol.test.ts (this phase's Plan 02 output — the NETWORK-REACH
    round-trip case, the sole `caughtBy` target)
  </read_first>
  <action>
    Snapshot `packages/net/src/protocol.ts` (`cp packages/net/src/protocol.ts
    /tmp/protocol.ts.before` or equivalent in the session scratchpad — never the project
    directory) before planting.

    Plant, in `packages/net/src/protocol.ts`, the `wantsNetworkReach` spread line inside
    `nameRecordToValue` (Plan 02's Task 1 output): change
    `...(record.wantsNetworkReach === undefined ? {} : { wantsNetworkReach:
    record.wantsNetworkReach }),` to `...(record.wantsNetworkReach === undefined || true ? {}
    : { wantsNetworkReach: record.wantsNetworkReach }),` — the identical `|| true` idiom XW4
    uses on `certificate.x509`, which makes the spread always resolve to `{}` regardless of
    the record's actual value, i.e. the field is silently dropped from the wire output.

    Run `npx vitest run --project node packages/net/src/protocol.test.ts`. Read `EXIT=$?` on
    the line immediately after — no pipe, no trailing command. Confirm it is non-zero. Record
    the exact failing assertion text vitest prints for the NETWORK-REACH round-trip case (the
    `expected ... to be ...` line, or the `bad-signature`-shaped failure if the round trip
    fails at `.accept(...).ok`) — this is the `signature`, observed rather than predicted.

    Restore `packages/net/src/protocol.ts` by the surgical inverse: remove exactly the ` ||
    true` text you added, restoring
    `...(record.wantsNetworkReach === undefined ? {} : { wantsNetworkReach:
    record.wantsNetworkReach }),` — do not `cp` the whole file back (this repository shares
    its working tree with other concurrent agents; a whole-file restore risks reverting a
    concurrent edit elsewhere in the file). `cmp` the restored file against the snapshot taken
    before planting; confirm exit 0.

    Re-run `npx vitest run --project node packages/net/src/protocol.test.ts`; confirm `EXIT=0`.

    Add a new entry to the `MUTATIONS` array in `mutation-ledger.ts`, placed immediately after
    the `XW4` entry (after its closing `},`), with `id: 'NR1'`, a `why` stating that dropping
    `wantsNetworkReach` from the wire is not a lost optional extra — a publisher's declaration
    that a module wants network reach silently arrives as "declares nothing", which is the one
    field this whole phase exists to make load-bearing, and CAP-01's guard would then pass a
    sovereign task straight through with no refusal at all; `file:
    'packages/net/src/protocol.ts'`; `find` and `replace` exactly the strings used above;
    `caughtBy: ['packages/net/src/protocol.test.ts']`; `signature` set to the exact text
    observed in this task's run (a comment above it stating the date and the raw `Test Files`/
    `Tests` line, matching XW4's own comment style); `signatureSource` set to whichever of
    `'test-title'`/`'rendered-at-runtime'` matches what was actually observed (if the observed
    text is the `it(...)` title vitest echoes on the FAIL line, it is `'test-title'`; if it is
    assertion output assembled at run time, it is `'rendered-at-runtime'`).
  </action>
  <acceptance_criteria>
    - `git status --porcelain packages/net/src/protocol.ts` shows no diff after this task completes (the plant was fully restored)
    - `cmp` against the pre-plant snapshot exited 0, and the plan's own execution notes/summary record that exit code
    - the new `NR1` entry's `signature` field is verbatim text this task's own run printed, not authored prose
    - `npx vitest run --project node packages/node/src/mutation-guard.node.test.ts` exits 0 (the cheap layer confirms `NR1`'s `find` text occurs exactly once in `protocol.ts` and every `caughtBy` file exists)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --project node packages/node/src/mutation-guard.node.test.ts; echo "EXIT=$?"</automated>
  </verify>
  <done>protocol.ts is byte-identical to before this task ran; mutation-ledger.ts carries a new NR1 entry whose signature was observed on a real red run.</done>
</task>

<task type="auto">
  <name>Task 2: NR2 — criterion 5's plant, the sovereign check itself</name>
  <files>packages/node/src/mutation-ledger.ts</files>
  <read_first>
    packages/node/src/mutation-ledger.ts (this task's own Task 1 edit, now landed, for the
    array's current shape and the next insertion point)
    packages/core/src/executor/network-reach-guard.ts (this phase's Plan 03 output — the exact
    current text of the guard's one `if` condition)
    packages/core/src/executor/network-reach-guard.test.ts (this phase's Plan 03 output — Case
    A, the sovereign+declares refusal case, the `caughtBy` target)
  </read_first>
  <action>
    Snapshot `packages/core/src/executor/network-reach-guard.ts` before planting.

    Plant: change the guard's condition from `if (task.label === 'sovereign' &&
    task.moduleRecord?.wantsNetworkReach === true) {` to `if (false && task.label ===
    'sovereign' && task.moduleRecord?.wantsNetworkReach === true) {` — this repository's
    standing idiom for disabling a branch without deleting it (matches `XW1`-`XW3`'s `if
    (false && ...)` pattern exactly). This targets the DECISION the guard makes, not a write
    downstream of it — with the condition permanently false, the refusal branch is dead code
    and every task reaches `inner.execute` unconditionally, which is exactly the regression
    criterion 5 must catch.

    Run `npx vitest run --project node packages/core/src/executor/network-reach-guard.test.ts`.
    Read `EXIT=$?` immediately, no pipe. Confirm non-zero. Record the exact failing text —
    Case A's `it` title is the expected signature source if it is the FAIL line vitest prints;
    confirm which cases actually reddened (Case A should; confirm B-E, which assert
    pass-through, do NOT also flip in a way that changes their own expected outcome — they
    should stay green, since they already expect `count() === 1`/pass-through and the plant
    only ever removes a refusal, never adds one).

    Restore by the surgical inverse: remove exactly the `false && ` text, restoring `if
    (task.label === 'sovereign' && task.moduleRecord?.wantsNetworkReach === true) {`. `cmp`
    against the pre-plant snapshot; confirm exit 0. Re-run the guard's test file; confirm
    `EXIT=0`.

    Add a new entry to `MUTATIONS`, `id: 'NR2'`, immediately after `NR1`, with `why` stating
    this is CAP-01's central claim: removing the sovereign check does not merely weaken a
    check, it turns the whole guard into an always-pass-through adapter that no longer refuses
    anything, and it is the one line the roadmap's criterion 2 depends on entirely; `file:
    'packages/core/src/executor/network-reach-guard.ts'`; `find`/`replace` exactly the strings
    used above; `caughtBy: ['packages/core/src/executor/network-reach-guard.test.ts']`;
    `signature` and `signatureSource` set from what this task's run actually printed.
  </action>
  <acceptance_criteria>
    - `git status --porcelain packages/core/src/executor/network-reach-guard.ts` shows no diff after this task completes
    - `cmp` against the pre-plant snapshot exited 0
    - the new `NR2` entry's `signature` field is verbatim observed text
    - `npx vitest run --project node packages/node/src/mutation-guard.node.test.ts` exits 0 with both `NR1` and `NR2` present and well-formed
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --project node packages/node/src/mutation-guard.node.test.ts packages/core/src/executor/network-reach-guard.test.ts; echo "EXIT=$?"</automated>
  </verify>
  <done>network-reach-guard.ts is byte-identical to before this task ran; mutation-ledger.ts carries NR1 and NR2, both with observed signatures; the cheap layer passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| mutation-ledger.ts entry → real source line | A `find` string that no longer matches is a guard that has silently stopped guarding — this plan's own `read_first` cites the disclosure-gate precedent for exactly this failure mode |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-46-09 | Tampering (of the regression test itself) | `NR1`/`NR2` `find`/`replace` strings | mitigate | both plants are applied for real in this plan and restored with `cmp`-verified byte identity; `mutation-guard.node.test.ts`'s cheap layer additionally re-checks on every ordinary run that the `find` text still occurs exactly once |
| T-46-10 | Repudiation | a `signature` field that was authored rather than observed | mitigate | this plan's own acceptance criteria require the signature to be the literal text a real run printed, per the ledger's own stated rule that "a signature is output that was observed, never predicted" |
</threat_model>

<verification>
`npx vitest run --project node packages/node/src/mutation-guard.node.test.ts` exits 0. Both
`git status --porcelain` checks in Tasks 1 and 2 show no diff on the planted-and-restored
files. `git show --stat` after this plan's commit lists only `packages/node/src/
mutation-ledger.ts`.
</verification>

<success_criteria>
Roadmap criterion 5 is met: a mutation removing the sovereign check turns criterion 2 red,
watched failing, restored by the surgical inverse, `cmp`-verified. 46-CONTEXT.md §6's "the
ledger gains entries; none go stale" is satisfied for both new entries with observed
signatures.
</success_criteria>

<output>
After completion, create `.planning/phases/46-a-module-declares-its-reach-and-the-data-decides/46-04-SUMMARY.md`
</output>
