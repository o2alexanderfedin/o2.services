---
phase: 46-a-module-declares-its-reach-and-the-data-decides
plan: 06
type: execute
wave: 5
depends_on: ["04", "05"]
files_modified:
  - vitest.config.ts
autonomous: true
requirements: [CAP-01]

must_haves:
  truths:
    - "The complete node-project suite is green with every one of this phase's edits landed together, not only file-by-file"
    - "vitest.config.ts's tests and unitTests counts are read off this phase's first safe full-lane run, completing the derivation 46-03 deliberately deferred"
    - "All six roadmap success criteria have a named, passing test carrying each"
  artifacts:
    - path: "vitest.config.ts"
      provides: "tests/unitTests updated from this plan's own full-lane run output, completing the files/unitFiles half 46-03 already landed"
  key_links: []
---

<objective>
Every prior plan in this phase (46-01 through 46-05) verified only the files it touched, by
design — Wave 2's two plans ran in parallel and each was scoped to its own spec, per this
project's standing rule that a whole-tree check during a concurrent edit reports another
agent's mid-edit file rather than a real defect. Nothing in this phase has yet run the complete
`node` project in one sweep with every edit landed together. This plan is that sweep: it
completes the `vitest.config.ts` derivation 46-03 deliberately left half-done (`files`/
`unitFiles` only, `tests`/`unitTests` deferred), and closes with a criteria-to-test accounting.

Purpose: prove the five prior plans compose — that `naming.ts`'s field, `protocol.ts`'s wire
codec, `network-reach-guard.ts`'s refusal, `mutation-ledger.ts`'s two new entries, and both
node factories' wiring do not conflict with each other or with anything that existed before
this phase — and read `tests`/`unitTests` off the first tree state in this phase where a
full-lane run is safe to take (no concurrent agent mid-edit).
Output: a single passing full-suite run; `vitest.config.ts`'s `tests`/`unitTests` updated from
that run's own output; a criteria-to-test mapping recorded in this plan's SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/46-a-module-declares-its-reach-and-the-data-decides/46-CONTEXT.md
@.planning/phases/46-a-module-declares-its-reach-and-the-data-decides/46-01-SUMMARY.md
@.planning/phases/46-a-module-declares-its-reach-and-the-data-decides/46-02-SUMMARY.md
@.planning/phases/46-a-module-declares-its-reach-and-the-data-decides/46-03-SUMMARY.md
@.planning/phases/46-a-module-declares-its-reach-and-the-data-decides/46-04-SUMMARY.md
@.planning/phases/46-a-module-declares-its-reach-and-the-data-decides/46-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: the full node lane, in one sweep, vitest.config.ts's remaining two counts, and the criteria-to-test map</name>
  <files>vitest.config.ts</files>
  <read_first>
    .planning/ROADMAP.md (Phase 46's six success criteria, verbatim)
    packages/node/src/slow-specs.node.test.ts (the drift assertion this sweep must also satisfy)
    vitest.config.ts (the dated note 46-03 added for the `files`/`unitFiles` half of this same
    derivation — read it to see exactly which two fields it left unset and why, and match this
    task's own note to the same heading date/phase reference)
  </read_first>
  <action>
    Confirm `git status --porcelain` is clean before starting (no plant left mid-restore by an
    earlier plan — Plan 04's two tasks each restore-and-`cmp` their own plant, so this should
    already be true). Run `npx vitest run --project node`. Read `EXIT=$?` on the line
    immediately after — no pipe, no trailing `tail`/`echo` between the command and the read.
    Confirm 0. Read `Test Files N passed (N)` and `Tests M passed | S skipped (T)` off this
    same run's own output.

    Run `O2_UNIT_ONLY=1 npx vitest run --project node`. Read `EXIT=$?` immediately; confirm 0.
    Read `Test Files N2 passed (N2)` and `Tests M2 passed | S2 skipped (T2)` off this run.

    Separately run `npx vitest run --project browser packages/browser/src/
    network-reach-composition.browser.test.ts packages/core/src/executor/
    network-reach-guard.test.ts` (the two files this phase added or touched that also run in
    the browser lane) and confirm `EXIT=0` on that too.

    Read `[host conditions]` off all three runs' own banners (none of the three commands above
    pass a CLI `--reporter=` flag, so all three print one) and record the load/core readings in
    this plan's SUMMARY rather than asserting anything about duration from a run that ran under
    contention.

    Update `vitest.config.ts`: `tests` becomes `T` (the collected total from the plain
    `--project node` run) and `unitTests` becomes `T2` (the collected total from the
    `O2_UNIT_ONLY=1` run). Confirm `N` matches the `files` value 46-03 already set (271) and
    `N2` matches `unitFiles` (188) — this is the derivation's own self-check, stated in every
    prior dated note in this file; if either mismatches, that is a real drift to investigate,
    not a number to paper over. Append a short dated note immediately below 46-03's own note
    (same heading style, dated today, `Phase 46, CAP-01 — completing the derivation`), stating
    that `tests`/`unitTests` are now read off this plan's own full-lane run rather than left
    deferred, and quoting both runs' literal `Test Files`/`Tests` lines.

    Write, in this plan's SUMMARY, a table mapping each of the six roadmap success criteria to
    the specific test file and case title(s) that carry it, per this phase's own plans:
    criterion 1 → `naming.test.ts`'s three CAP-01 cases (46-01) and `protocol.test.ts`'s
    NETWORK-REACH round trip (46-02); criterion 2 → `network-reach-guard.test.ts` Case A
    (46-03) and `fabric-node.node.test.ts`'s case 1 / `network-reach-composition.browser.
    test.ts`'s case 1 (46-05); criterion 3 → Case B and each file's case 2; criterion 4 → Cases
    C and D and each file's case 3; criterion 5 → `mutation-ledger.ts`'s `NR2` entry (46-04);
    criterion 6 → `network-reach-guard.test.ts`'s distinguishability assertions and
    `fabric-node.node.test.ts`'s "does not contain 'sovereignty violation'" assertion.
  </action>
  <acceptance_criteria>
    - `npx vitest run --project node` exits 0 with every file in the run, not a subset
    - `O2_UNIT_ONLY=1 npx vitest run --project node` exits 0
    - `npx vitest run --project browser packages/browser/src/network-reach-composition.browser.test.ts packages/core/src/executor/network-reach-guard.test.ts` exits 0
    - `vitest.config.ts`'s `tests`/`unitTests` are read off these two runs' real output, not guessed, and the new dated note quotes both runs' literal `Test Files`/`Tests` lines
    - the collected `Test Files` counts from both runs equal `vitest.config.ts`'s `files`/`unitFiles` (271/188) — a mismatch is reported, not silently reconciled
    - the SUMMARY's criteria-to-test table names all six roadmap criteria and at least one concrete test file/case per row
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --project node; echo "EXIT=$?"</automated>
  </verify>
  <done>The full node lane, the unit-only lane, and the two browser-lane files this phase touches are all green together; vitest.config.ts's tests/unitTests are completed from real run output; the SUMMARY records which test carries which criterion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new — this plan runs existing tests and completes a documentation-only field pair; it
writes no behavioural source code.

## STRIDE Threat Register

None new — this plan is a verification checkpoint over code already covered by Plans 01-05's
own threat registers.
</threat_model>

<verification>
`npx vitest run --project node` exits 0. `O2_UNIT_ONLY=1 npx vitest run --project node` exits
0. The browser-lane command above exits 0. `git show --stat` after commit lists only
`vitest.config.ts`.
</verification>

<success_criteria>
All six roadmap success criteria are demonstrated together, in one tree state, with a named
test carrying each. vitest.config.ts's four counts are complete and self-consistent — closing
this phase.
</success_criteria>

<output>
After completion, create `.planning/phases/46-a-module-declares-its-reach-and-the-data-decides/46-06-SUMMARY.md`
</output>
