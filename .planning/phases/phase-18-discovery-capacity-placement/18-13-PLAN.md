---
phase: phase-18-discovery-capacity-placement
plan: 13
type: execute
wave: 6
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - packages/node/src/requirements-ledger.node.test.ts
  - packages/node/src/discover-arm.node.test.ts
  - packages/node/src/fabric-node.ts
  - packages/browser/src/browser-node.ts
autonomous: true
requirements: [SCHED-03, SCHED-04, SCHED-05]
gap_closure: true

must_haves:
  truths:
    - "SCHED-03, SCHED-04 and SCHED-05 state what the tree does, and each was UNDER-reporting shipped work rather than overstating it — the rarer direction, and the one no guard was watching"
    - "The ledger guard's blind spot is DECLARED rather than silent: a row whose claim the guard cannot bind is either rephrased so it can, or named in `WITHOUT_A_CHECKABLE_CLAIM` where a reader can see the exemption"
    - "`bin/bench.ts --discover` is executed by an automated test rather than held by a source-text count, so removing the arm fails a run and not only a grep"
    - "The two tiers' `start` contracts differ on relay-dial failure BY DESIGN, and both files say so — a divergence documented on one side only is a trap for whoever reads the other"
    - "ROADMAP.md's Phase 18 plan list matches the eleven summaries on disk"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "SCHED-03/04/05 rows restated against the shipped tree"
    - path: "packages/node/src/requirements-ledger.node.test.ts"
      provides: "the exemption list, updated so no row's claim is silently unbindable"
    - path: "packages/node/src/discover-arm.node.test.ts"
      provides: "W-1 — the --discover arm executed, not counted"
  key_links:
    - from: "packages/node/src/discover-arm.node.test.ts"
      to: "packages/node/src/bin/bench.ts"
      via: "runs the driver with cwd in a temp directory and reads real qualification counts"
      pattern: "--discover"
---

<objective>
Close `18-VERIFICATION.md`'s non-critical findings. No behaviour changes anywhere; this plan
edits documents, adds one test, and adds two comments.

**The finding worth understanding before starting is not the stale rows — it is why nothing
caught them.** SCHED-03, SCHED-04 and SCHED-05 are all stale in the **under-reporting**
direction: each claims less is wired than actually is. SCHED-04 says *"FabricNode composes a
WorkerExecutor and no governor, and the duty cycle is readonly on both tiers"* — contradicted
by `fabric-node.ts:1460` (`new GovernedExecutor(counter, governor)`), `fabric-node.ts:985`
and `browser-node.ts:677`. SCHED-03 says `planWithOffers` and `runResilient` have no
production caller — `submit.ts:340` calls `planWithOffers` and `bin/bench.ts:723` supplies
`rpcAdmission`.

`requirements-ledger.node.test.ts` exists to catch exactly this and **cannot**. Its
extraction binds only to **exported symbols**, and these three rows are written in concepts
and pronouns — *"the sovereignty gate"*, *"the requestor does not re-pick"*. SCHED-05 is
named in `WITHOUT_A_CHECKABLE_CLAIM:279`; SCHED-03 and SCHED-04 are not. So the guard reports
green over two rows it was never able to read, and the blind spot is **silent**.

A silent blind spot in the guard whose job is to stop the ledger overstating itself is worse
than the stale rows, because it is what let them go stale. The rows are a day's drift; the
exemption that does not declare itself is structural.

Output: three true rows, a guard whose blind spot is visible, one uncovered call path
covered, and two comments that stop a documented divergence from being a trap.
</objective>

<execution_context>
Branch `feature/phase-18-discovery-capacity-placement`. Run vitest **by project**. Note that
`requirements-ledger.node.test.ts` and `vocabulary.node.test.ts` read the **git index** — `git
add` before running them, or they measure the last commit rather than your edit.
</execution_context>

<context>
`18-VERIFICATION.md`'s "Non-Critical Gaps" section (W-1, W-2) and "Stale documents (I-1, I-2,
and three ledger rows)" are the source.

**Ticking a requirement is more than one edit.** `.planning/STATE.md` records the rule: the
checkbox, the traceability row's marker, and the section header's own count all move together,
and `acceptance-traceability.node.test.ts` pins specific ids in specific states — closing one
while that spot-check still asserts it open turned `develop` red once already. Run that file
after any ledger edit.

**These rows are being made accurate, not being ticked.** Whether SCHED-03/04/05 move status
at all is a question for their own evidence; do not tick anything this plan has not measured.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: three rows that describe the tree, and a guard that admits what it cannot read</name>
  <files>.planning/REQUIREMENTS.md, packages/node/src/requirements-ledger.node.test.ts</files>
  <read_first>
    .planning/REQUIREMENTS.md:616-618 (the three rows, verbatim)
    packages/node/src/requirements-ledger.node.test.ts:270-300 (WITHOUT_A_CHECKABLE_CLAIM and the hookSuppliers extraction — see for yourself that it binds on exported symbols and quoted sentinels, and therefore cannot bind a pronoun)
    packages/node/src/requirements-ledger.node.test.ts:490-512 (the two assertions that make the exemption list load-bearing)
    packages/node/src/fabric-node.ts:1455-1462 (GovernedExecutor composed — what SCHED-04 denies)
    packages/node/src/fabric-node.ts:985 and packages/browser/src/browser-node.ts:677 (setDutyCycle on both tiers — what "readonly on both tiers" denies)
    packages/core/src/job/submit.ts:340 and packages/node/src/bin/bench.ts:723 (planWithOffers and rpcAdmission in production — what SCHED-03 denies)
  </read_first>
  <behavior>
    - Each row states what is true after Phase 18, with its evidence as `file:line`.
    - Where a clause is still genuinely open, it says so in those words. Under-reporting and
      over-reporting are both wrong; the fix is accuracy, not optimism.
    - **Every row is either bindable or declared.** For each of SCHED-03 and SCHED-04, either
      rephrase the claim around an exported symbol the guard can bind, or add the id to
      `WITHOUT_A_CHECKABLE_CLAIM` with a comment saying which clause is unbindable and why.
      Prefer rephrasing — an exemption is a promise to re-read by hand.
  </behavior>
  <action>
    Rewrite the three rows against the evidence above. Then decide, per row, bindable or
    exempt, and make the guard say so.

    Extend `WITHOUT_A_CHECKABLE_CLAIM`'s comment to state the rule the list encodes: a row
    naming no exported symbol cannot be checked by this file, and the list is the set of rows
    that have accepted that. Today the list reads as two arbitrary ids.
  </action>
  <proof>
    - the guard binds what it claims to bind — after rephrasing, plant a false clause in a
      rephrased row (name a symbol that is not supplied) and confirm the guard goes RED.
      Restore by `cp` + `cmp`.
    - the exemption is real — for any row left exempt, confirm the guard is green both with
      the true row and with a deliberately false one. That is the point: it cannot tell them
      apart, which is what the exemption declares.
    - the ledger's own counts still reconcile — run `acceptance-traceability.node.test.ts`.
  </proof>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/o2.services && git add -u && npx vitest run --project node requirements-ledger.node.test.ts acceptance-traceability.node.test.ts vocabulary.node.test.ts</automated>
  </verify>
  <done>
    The three rows match the tree, and no row's claim is unreadable by the guard without the
    guard saying so.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the --discover arm, executed rather than counted</name>
  <files>packages/node/src/discover-arm.node.test.ts</files>
  <read_first>
    packages/node/src/bin/bench.ts:700-790 (the --discover arm, `rpcAdmission` at :723, and the comment at :776 about what planWithOffers gives)
    .planning/phases/phase-18-discovery-capacity-placement/18-VERIFICATION.md (W-1 — the verifier ran this successfully with `cwd` in a temp directory and confirmed real qualification counts; reproduce that shape)
    packages/node/src/discovery-agents.node.test.ts (the existing spawned-process idiom in this package — process lifecycle, orphan leash, timeouts)
  </read_first>
  <behavior>
    - The driver runs with `--discover` and its `cwd` set to a temporary directory, so it
      writes nothing into the repository and cannot touch the committed measurements.
    - The test reads real qualification counts out of the run, not merely a zero exit.
    - It fails if the `--discover` arm is removed — which a source-text count does not.
  </behavior>
  <action>
    Add the file. Keep it to the one thing W-1 asks for: proof the arm executes and produces
    the counts it claims. Do not turn it into a benchmark — this project records that a
    reading taken under contention is worthless, and this test must be safe to run beside the
    rest of the suite.

    If its measured span comes in at or above 1 s, note that in the summary against
    `deferred-items.md` item 1, which already records `SLOW_NODE_SPECS` as broadly unenforced.
    Do not add a single entry to that list — the deferred item explains why a partial
    application makes the list look maintained without making it honest.
  </action>
  <proof>
    - it inverts — delete the `admit:` line at `bin/bench.ts:723` and confirm the test goes
      RED. Restore by `cp` + `cmp`. This is the exact mutation the source-text count could
      not distinguish from a working arm.
    - it does not write into the repo — assert the temp `cwd` and confirm `git status
      --porcelain` is unchanged by the run.
  </proof>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/o2.services && npx tsc --noEmit && npx vitest run --project node discover-arm.node.test.ts</automated>
  </verify>
  <done>
    Removing the `--discover` arm fails a test run rather than only a grep.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: a divergence documented on both sides, and a roadmap that matches the disk</name>
  <files>packages/node/src/fabric-node.ts, packages/browser/src/browser-node.ts, .planning/ROADMAP.md</files>
  <read_first>
    packages/node/src/fabric-node.ts (the relay-dial loop — non-fatal, collecting RelayDialFailure)
    packages/browser/src/browser-node.ts (its relay handling — the OPPOSITE disposition, and correct: a tab cannot bind a socket, so a tab that cannot reserve has no other way to be reached)
    packages/node/src/start-unwind.browser.test.ts (the browser disposition, measured in three engines)
    packages/node/src/reservation-exhaustion.node.test.ts (case C — the Node disposition, driven cross-process through bin/agent.ts)
    .planning/ROADMAP.md:584 (the "3/11 plans executed" line and the 18-04…18-11 checkboxes)
  </read_first>
  <behavior>
    - Each tier's relay-dial comment names the other tier's disposition and the platform
      reason for the difference. A reader of either file learns that the contracts differ
      deliberately, and does not "fix" one to match the other.
    - `ROADMAP.md`'s Phase 18 plan list matches disk: eleven plans, eleven summaries.
  </behavior>
  <action>
    Add one sentence to each relay-dial comment. The Node side: a failed dial is recorded on
    `relayFailures` and the node keeps running, because a node that could not enter one relay
    can still work, and killing it would be a worse answer than the one NET-05 replaced. The
    browser side: the opposite, because a tab that cannot reserve has no other way to be
    reached at all. Each names the other file and the test measuring it.

    Correct `ROADMAP.md:584` to `11/11` and tick 18-04 through 18-11. Append 18-12 and 18-13
    to that list as the gap-closure plans, so the block keeps matching disk.

    **Do not touch `.planning/STATE.md`.** Its prose body is stale by one phase and its
    frontmatter is correct; it is hand-maintained and three separate tools have corrupted it.
  </action>
  <proof>
    - the comments are accurate — confirm each named test exists and measures the disposition
      claimed, by running both. A comment asserting a fact about another file is a claim with
      an expiry date; this project has been bitten by exactly that (Phase 16's gate comment).
  </proof>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/o2.services && npx tsc --noEmit && git add -u && npx vitest run --project node reservation-exhaustion.node.test.ts start-unwind.node.test.ts vocabulary.node.test.ts</automated>
  </verify>
  <done>
    Both tiers state the divergence, each pointing at the other and at the test that measures
    it. The roadmap's plan list matches the directory.
  </done>
</task>

</tasks>

## Out of scope

- **`.planning/STATE.md` prose.** Hand-maintained; the Phase 18 verification updates
  `Current Position` as its own output.
- **`SLOW_NODE_SPECS`.** `deferred-items.md` item 1 explains why a whole re-derivation is the
  only honest fix and why adding one entry is not it.
- **`tools/aot/lift.node.test.ts`.** `deferred-items.md` item 2; the reasoning still holds.
- Both criteria gaps — 18-12.

<verification>
No criterion changes status from this plan. It closes W-1, W-2, I-1, I-2 and the three ledger
rows. Re-verification is 18-12's business.
</verification>

<success_criteria>
- [ ] SCHED-03, SCHED-04, SCHED-05 state what the tree does, with `file:line` evidence
- [ ] No row's claim is unbindable by the ledger guard without the guard declaring it
- [ ] A planted false clause in a rephrased row turns the guard RED, observed
- [ ] Deleting `bin/bench.ts:723`'s `admit:` turns the new test RED, observed
- [ ] The `--discover` test writes nothing into the repository
- [ ] Both tiers' relay-dial comments name the other's disposition and the measuring test
- [ ] `ROADMAP.md` Phase 18 plan list matches the thirteen plans on disk
- [ ] `acceptance-traceability.node.test.ts` green after the ledger edits
- [ ] `tsc --noEmit` clean; `--project node` green
</success_criteria>

<output>
Three accurate rows, a declared blind spot, one covered call path, two comments, one corrected
roadmap block. No behaviour changed.
</output>
